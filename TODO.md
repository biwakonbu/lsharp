# L# セルフホスティング & エコシステム TODO

> 凡例: `[x]` 完了 / `[ ]` 未着手 / `[~]` 部分実装 / `[BLOCKED: ...]` 依存待ち / `[DEFERRED]` 後続トラック送り
>
> **完了済みフェーズ**: Phase 0-7, P8, P9-1/2/3/4/6, P10, P12, P13, P14。
> **Phase 11**: ADR-152〜ADR-157 で仕様固定済みだが、実装完了ではない。完了判定は `docs/development/planning/completion-criteria.md`, `docs/development/validation/verification-spec.md`, `docs/development/planning/compatibility-matrix.md` を優先する。
>
> P8-9 (T4-4/T4-5) → ADR-148, P9-6b → ADR-149, P9-6c → ADR-150, P9-6d → ADR-151
> P12 → ADR-166, P13 → ADR-167, STR-01~03 → ADR-168, DOC-02 → ADR-169, V2-01~07+P11-2e-2 → ADR-170

---

## 現在の残タスク一覧（正本）

> **この節を残タスクの単一正本とする。**
> 下位セクションの `[~]` / `[ ]` は背景説明・証跡・再開メモを含む詳細であり、実際の残件カウントはこの節を基準に読む。
> なお `docs/development/planning/phase12-package-ai-ecosystem-roadmap.md` の P12-0 / P12-A / P12-B / P12-C は現時点で TODO 反映済みで、追加の未完了項目はない。

> **2026-04-08 再監査結果**:
>
> - `CP-05` / `BOOT-04` / 作業ツリー WIP / clippy 残 warning / e2e full-run の各メモは、再開用の stale 課題または解消済みメモだった。正本の current 残件としては数えない。
> - 再確認証跡: `cargo test -q -p lsharp-wasm --test e2e test_e2e_runtime_collector_ignores_legacy_zero_root_slot_sentinel`, `cargo test -q -p lsharp-wasm --test e2e test_e2e_alloc_metrics_ci_artifact_payload`, `cargo test -q -p lsharp-wasm --test e2e test_e2e_gc_lsp_actual_stdio_repeated_sequence_in_session_collector_telemetry`, `cargo test -q -p lsharp-wasm --test e2e test_e2e_bootstrap_fixed_input_set_stage_chain_match`, `cargo clippy -q -- -D warnings`。
- `test_e2e_selfhost_cli_compile_functions_data_with_cache_matches_fresh_compile_after_change` は green になり、dirty module change 後の cached compile が fresh compile と一致することは固定できた。一方で clean payload parity を詰める途中で、同一 source graph の direct selfhost compile が allocation 履歴で別 code-section fingerprint を返す兆候が見つかったため、cache helper 以前に selfhost compiler determinism を修正する必要がある。
- [x] `V2-08` Native backend self-regeneration — Deferred / v2 セクションの正本を参照。代表入力の actual `stage1-native -> stage2-native -> stage3-native` 再生成・実行比較は `test_e2e_stage23_actual_native_self_regeneration_harness_stage2_stage3_match` で固定済み（Darwin arm64 actual native execution）。native-only 配布化は `V2-10` の experimental scope として workflow まで接続済み。
- [x] `V2-09` Wasm/native differential zero — actual self-regenerated stage artifacts を differential input に組み込み、5 観測点・empty allowlist・aarch64 selfhost opcode gap 0 を固定済み。Evidence: `test_e2e_wasm_native_differential_five_observation_points`, `test_e2e_differential_allowlist_empty`, `test_e2e_wasm_native_differential_uses_actual_self_regenerated_stage_artifacts`, `test_e2e_native_actual_stage23_gap_report_has_zero_aarch64_selfhost_blockers`。
- [x] `V2-10` Native-only RC distribution — official host launcher + embedded guest component を維持したまま、Darwin arm64 experimental RC artifact を release workflow / smoke / checksum / docs に接続済み。Evidence: `test_e2e_native_ops02_native_only_rc_contract`, `.github/workflows/release.yml`, `scripts/ci/native-only-rc-smoke.sh`。
- [ ] `V2-13` Native-only official replacement target matrix — native-only を公式配布へ完全置換するため、Tier1 (`aarch64-apple-darwin`, `x86_64-apple-darwin`, `x86_64-unknown-linux-gnu`, `x86_64-pc-windows-msvc`) の target descriptor / object format / actual execution support を正本化する。現状 blocker: actual native self-regeneration は Darwin arm64 のみ、Windows native artifact は native backend spec 上 BLOCKED。Evidence target: `test_e2e_native_ops03_official_native_only_replacement_backlog_contract`。
- [ ] `V2-13a` Linux x86_64 native server target — サーバー用途の優先 slice として `x86_64-unknown-linux-gnu` を full Tier1 置換から先に切り出し、Ubuntu x86_64 VM / runner 上で target descriptor、ELF object emitter、x86_64 codegen exact-byte smoke、artifact 証跡を required CI に固定する。local inner loop は `scripts/ci/native-linux-x86-local-vm-smoke.sh` で descriptor / ELF / runtime-link smoke を短時間検証し、host 側 selfhost `emit-native` 生成 code artifact の VM link/run は `scripts/ci/native-linux-x86-hostgen-vm-exec.sh` で分離して確認する。次の blocker: actual Linux native self-regeneration は Linux VM 上で段階実装する。Evidence target: `test_e2e_native_ops04_linux_x86_server_target_contract`, `scripts/ci/native-linux-x86-smoke.sh`, `scripts/ci/native-linux-x86-local-vm-smoke.sh`, `scripts/ci/native-linux-x86-hostgen-vm-exec.sh`。
  - [x] `V2-13a-1` x86 selfhost runtime helper parity — x86_64 backend は command-line-arg / read-file / string-length / string-char-at / substring / string-concat / print / vector / ref / map / file-exists helper を実装し、host 側 selfhost `emit-object` 由来 ELF を Lima Linux x86_64 VM で argv 実行（`seedling` length=8 / char-at[1]=101）、substring（exit 3）、string-concat（exit 8）、print stdout（`42\n`）、vector push/get（exit 42）、ref set/get（exit 99）、map insert/get（exit 42）、map-size（exit 1）、file-exists（exit 1）まで確認済み。代表 gap report の `selfhost_unsupported_x86_64` は空。Evidence: `test_native_codegen_x86_command_line_arg_and_read_file_call_sites_resolve_helper_offsets`, `test_native_codegen_x86_runtime_helper_emitters_return_executable_byte_vectors`, `test_native_codegen_x86_string_slice_concat_helper_call_sites_resolve_offsets`, `test_native_codegen_x86_string_slice_concat_helper_emitters_return_executable_byte_vectors`, `test_native_codegen_x86_vector_and_ref_helper_call_sites_resolve_offsets`, `test_native_codegen_x86_vector_and_ref_helper_emitters_return_executable_byte_vectors`, `test_native_codegen_x86_map_and_file_helper_call_sites_resolve_offsets`, `test_native_codegen_x86_map_and_file_helper_emitters_return_executable_byte_vectors`, `test_e2e_native_linux_x86_host_generates_argv_string_length_elf_object_artifact`, `test_e2e_native_linux_x86_host_generates_argv_string_char_at_elf_object_artifact`, `test_e2e_native_linux_x86_host_generates_substring_elf_object_artifact`, `test_e2e_native_linux_x86_host_generates_string_concat_elf_object_artifact`, `test_e2e_native_linux_x86_host_generates_print_elf_object_artifact`, `test_e2e_native_linux_x86_host_generates_vector_elf_object_artifact`, `test_e2e_native_linux_x86_host_generates_ref_elf_object_artifact`, `test_e2e_native_linux_x86_host_generates_map_insert_get_elf_object_artifact`, `test_e2e_native_linux_x86_host_generates_map_size_elf_object_artifact`, `test_e2e_native_linux_x86_host_generates_file_exists_elf_object_artifact`, `scripts/ci/native-linux-x86-hostgen-vm-exec.sh`, `/tmp/linux-x86-helper-gap-after-map-file.json`。
  - [x] `V2-13a-2` Linux runtime trampoline — `object-runtime.s` で Linux x86_64 SysV trampoline を生成し、`argc` を `%r14`、tagged argv vector を `%r15` に seed して selfhost 生成 ELF `program.o` の `generated` を呼び出す導線を VM 上で確認済み。Evidence: `scripts/ci/native-linux-x86-hostgen-vm-exec.sh` (`object-runtime.s`, `object-program.native`, `actual_exit_code=42`)。
  - [x] `V2-13a-3` real ELF object/link artifact — selfhost ELF emitter は `.text` / `.symtab` / `.strtab` / `.shstrtab` / `.note.GNU-stack` を持つ linkable ELF64 relocatable object を生成し、host 側 selfhost `emit-object` 由来の `program.o` を Lima Linux x86_64 VM 内で `object-program.native` にリンクして `actual_exit_code=42` を確認済み。Evidence: `test_e2e_native_linux_x86_selfhost_elf_object_has_linkable_sections`, `test_e2e_native_linux_x86_host_generates_selfhost_elf_object_artifact`, `scripts/ci/native-linux-x86-hostgen-vm-exec.sh`。
  - [x] `V2-13a-4` actual Linux x86_64 selfregen stage1 artifact — host 側 selfhost から Linux x86_64 target の actual stage1 code/data bundle を生成し、巨大 code vector を避ける segmented packed transport (`9000000010`) を Rust harness / VM smoke decoder の双方に固定した。Evidence: `test_e2e_native_linux_x86_host_generates_actual_selfregen_stage1_bundle_artifact`, `test_extract_native_code_data_transport_output_supports_segmented_packed_payloads`, `test_e2e_native_x86_deep_direct_append_matches_static_size`, `test_e2e_selfhost_main_representative_x86_function_size_matches_generated_length_diagnostic`, `scripts/ci/native-linux-x86-hostgen-vm-exec.sh` (`decode_packed_payload`)。
  - [BLOCKED: stage3-run SIGSEGV / stack PC] `V2-13a-5` actual Linux x86_64 stage2/stage3 VM regeneration — `scripts/ci/native-linux-x86-hostgen-vm-exec.sh` で stage1 artifact を Lima Linux x86_64 VM に転送し、helper/object smokes / actual stage1 materialize / stage1 native compiler による stage2 transport 生成までは通過する。`ftable` を map helper から key/value flat vector へ分離して stage2-run の exit 137/OOM は解消済み。現 blocker は stage2 産物を materialize した `actual-stage2/program.native src/App/Seed.ls` が stage3-run で exit 139 (SIGSEGV) になり、GDB では `generated` entry の戻り先自体は保持されたまま `rip=0x7fffffffe4a4` の stack address に落ちること。stage2 entrypoint 近傍は stage1 と比較して call 命令が減り、深い value-window spill 列の途中で実行制御が崩れる。`native-value-window-spill-slot-count-x86` の conservative floor を 128 から 144 / 256 へ上げる案は host 側 `test_e2e_native_linux_x86_host_generates_actual_selfregen_stage1_bundle_artifact` までは通るが、VM 上の actual-stage1 chunked run が exit 139 で壊れるため不採用。次の実装対象は floor 増量ではなく、stage2-generated entrypoint の call 欠落と stack PC 遷移を、IR opcode / emitted bytes / rel32 target の同一 window で固定する診断を追加してから、該当 arity の x86 call bundle 生成だけを最小修正すること。Evidence: `test_selfhost_ftable_uses_flat_vector_storage_for_large_tables`, `test_e2e_native_linux_x86_host_generates_actual_selfregen_stage1_bundle_artifact`, `scripts/ci/native-linux-x86-hostgen-vm-exec.sh` (`actual-selfregen-summary.json` phase `stage3-run`, exit_code 139), local VM artifact `ci-artifacts/native-linux-x86-hostgen-vm/local-final-nowrapper-v2/stage2-debug/stage-code.bin`, `/tmp/lsharp-stage2-debug-nowrapper-v2/program.native` GDB observation。
    - 2026-05-19 追加進捗: stage2 entrypoint artifact 診断を追加し、既存 artifact で stage1 は `map-new/ref-new/ref-new` が entry prefix に並ぶ一方、stage2 は最初の `map-new` 後に深い spill shift へ流れることを固定した。Linux x86 segmented seed の `make-x86-code-segment-context` は stage1 native 実行中の vector-push GC で `functions/starts` context を壊さないよう root-safe 化し、`test_linux_x86_representative_seed_roots_code_segment_context_inputs` と `test_e2e_native_linux_x86_host_generates_actual_selfregen_stage1_bundle_artifact` (`ci-artifacts/native-linux-x86-hostgen-vm/local-segmentctx-root-verify/actual-stage1`) で検証済み。別案の `generate-native-function-x86-64-bundle-with-layout` offsets/depths 単一 snapshot 化は heavy host layout が wasm OOB になったため不採用。
    - 2026-05-19 追加進捗: stage2 entrypoint IR trace 照合を `opcode 40` 限定から `target_offset` 付き rel32 row 全体へ拡張し、runtime helper opcode `60(map-new)` / `56(ref-new)` も emitted bytes / rel32 target と同一 window で検証できるようにした。Evidence: `test_x86_ir_trace_rejects_runtime_helper_rel32_target_mismatch`, `test_e2e_linux_x86_actual_stage2_entrypoint_call_windows_diagnostic`。
    - 2026-05-19 追加進捗: `stage-entry-ir-trace.txt` sidecar が stage debug dir に存在する場合は hostgen VM artifact として回収し、stage2 entrypoint 診断は `LSHARP_NATIVE_LINUX_X86_STAGE2_ENTRY_IR_TRACE` 未指定でも stage1 debug dir の sidecar を自動照合できるようにした。seed へ sidecar trace 生成を直接埋め込む案は actual-stage1 の先頭 chunk で exit 139 を誘発したため不採用。Evidence: `test_e2e_native_ops04_linux_x86_server_target_contract`, `test_e2e_linux_x86_actual_stage2_entrypoint_call_windows_diagnostic` (`opcode 60` synthetic sidecar)。
    - 2026-05-19 追加進捗: stage2 entrypoint 診断に rel32 target が `48 83 c4` / `48 81 c4` の REX prefix を 1 byte 飛ばして stack restore へ着地するケースの検出を追加した。stale artifact `local-segment-padding-v2/stage2-debug` では `entrypoint+574` の call が `entrypoint+15495` (`83 c4 20`) に着地し、`add esp, 0x20` として実行される形を call window / target window 付きで固定できた。Evidence: `test_x86_call_target_diagnostic_rejects_rex_prefix_skip`, `test_e2e_linux_x86_actual_stage2_entrypoint_call_windows_diagnostic` (`local-segment-padding-v2` で expected diagnostic failure)。
    - 2026-05-19 追加進捗: manual VM rerun で actual-stage1 chunk `832-896` の split 後に `832-836` 成功から `836-900` へ飛ぶ範囲破壊を確認した。原因は `run_actual_stage_range` / `run_actual_stage_chunked` の再帰 split 状態が shell global 変数として親 loop に漏れることだったため、hostgen VM script の chunk/failure summary/debug copy 関数の状態変数を `local` 化した。加えて host 側 `LSHARP_NATIVE_LINUX_X86_ACTUAL_CHUNK_SIZE=64` が guest heredoc に渡らず VM 内 default 8 に戻る問題を `limactl shell ... env` で固定した。Evidence: `test_native_linux_x86_hostgen_vm_script_keeps_chunk_split_state_local`, `test_native_linux_x86_hostgen_vm_script_forwards_actual_chunk_env_to_guest`, `scripts/ci/native-linux-x86-hostgen-vm-exec.sh`。
    - 2026-05-19 追加進捗: `local-fixed-chunk64` の manual VM rerun は helper/object smokes、actual-stage1 materialize、actual-stage1 chunked stage2 transport 生成を完走し、stage2 artifact materialize 後の最小 chunk `actual-stage2/program.native src/App/Seed.ls 0 1 1 0` で SIGSEGV に絞れた。GDB では `generated+434` が `movabs $0,%rax` 直後の `00 00` を `add %al,(%rax)` として実行し、`rax=0` で null write している。現 artifact の stage2 entrypoint call-window 診断は REX target skip を再現せず、`Root x86 i64 const direct append` の `result` root 化案は host 側 actual-stage1 生成で Wasm OOB を起こしたため revert 済み。新しい env-driven function offsets 診断は `LSHARP_NATIVE_LINUX_X86_FUNCTION_INDEX=1518` / 相対 window `360..520` で IR opcode、slot-layout offset、stage2 emitted bytes、rel32 call site を同一 row に対応付け、`i64.const` 直後の `root_push` / shallow `drop` 相当 5 bytes が `00` 化していることを固定した。次は broad append helper / spill floor / ctx-state refactor ではなく、actual native stage1 の x86 direct append byte value liveness（特に opcode 74/44 近傍）を最小範囲で修正する。Evidence: `test_linux_x86_function_offsets_diagnostic_is_env_driven`, `test_e2e_selfhost_main_linux_x86_actual_seed_function_offsets_diagnostic` (`local-fixed-chunk64/stage2-debug`)。
    - 2026-05-20 追加進捗: `append-x86-byte` の current/next root 化は host 側 actual-stage1 artifact gate で Wasm `call stack exhausted` になり、root_push/drop の fixed byte-vector 化は full VM で stage2 `generated+434` の `00` window が残ったため、いずれも不採用。追加の ref/vector micro object は `i64.const 0` 後に `ref-get -> vector-push -> ref-set` を 2 回行って VM link/run の exit code 192 を返すため、generic runtime helper と単純な direct append value liveness ではなく、function 1518 の offset drift / direct branch continuation / segment materialization のいずれかへ絞る。Evidence: `test_e2e_native_linux_x86_host_generates_ref_vector_after_i64_zero_elf_object_artifact`, manual VM link/run `/tmp/lsharp-ref-vector-zero.o` (`exit_code=192`), `test_e2e_selfhost_main_linux_x86_actual_seed_function_offsets_diagnostic` (`local-rootdrop-fixedbytes-vm/stage2-debug`)。
    - 2026-05-21 追加進捗: stage2 生成 artifact の `lsharp_bundle+3038634` SIGSEGV を `stage-code-segments.tsv` と stage1 metadata で segment 997 / function 1007 / `idx=19 op=55(vector-push) depth=2 off=146 size=5 bytes=[0,0,0,0,0]` に対応付けた。`vector-push` runtime helper call は fallback の byte-vector 生成ではなく、既存の consume-two direct append 経路へ合流させ、RED/GREEN の静的 guard と fresh actual stage1 artifact gate で固定した。Evidence: `test_native_codegen_x86_vector_push_runtime_call_shares_consume_two_direct_append_in_stage1`, `test_e2e_native_linux_x86_host_generates_actual_selfregen_stage1_bundle_artifact` (`ci-artifacts/native-linux-x86-hostgen-vm/vector-push-direct-stage1/actual-stage1`)。
    - 2026-05-21 追加進捗: vector-push 修正後の full VM stage3-run SIGSEGV は stage1 metadata の segment 1046 / function 1056 / `idx=105 op=58(ref-set) depth=2 off=743 size=5 bytes=[0,0,0,0,0,72,137,200]` に対応することを、metadata parser + zeroed runtime helper call detector で固定した。新しい ignored gate は actual metadata file を env 指定で検査し、現 artifact では `opcode=58 offset=743` を名指しで失敗する。Evidence: `test_linux_x86_function_segment_metadata_detects_zeroed_ref_set_runtime_call`, `test_e2e_linux_x86_actual_function_segment_metadata_has_no_zeroed_runtime_helper_calls` (`LSHARP_NATIVE_LINUX_X86_FUNCTION_METADATA=ci-artifacts/native-linux-x86-hostgen-vm/vector-push-direct-full-v1/stage1-metadata-1046-prefix120.txt`)。
    - 2026-05-21 追加進捗: `ref-set` runtime helper call も fallback の byte-vector 生成を避け、control-loop の direct append 経路へ追加した。既存の `codegen-ir-instr-bundle-x86-with-import-count-and-base` は runtime opcode の長い重複分岐を `is-selfhost-runtime-opcode-x86` predicate へ畳み、selfhost compiler の parse/check complexity を増やさずに `op58` branch を通せる形にした。Evidence: `test_native_codegen_x86_ref_set_runtime_call_direct_appends_in_stage1`, `runtime_call_direct_appends_in_stage1`, `native_codegen_x86_control`, `cargo run -q --bin lsharp -- parse selfhost/src/Backend/Native/NativeCodegen.ls`, `cargo run -q --bin lsharp -- check selfhost/src/Backend/Native/NativeCodegen.ls`, `test_e2e_native_linux_x86_host_generates_actual_selfregen_stage1_bundle_artifact` (`ci-artifacts/native-linux-x86-hostgen-vm/ref-set-direct-stage1/actual-stage1`)。
    - 2026-05-21 追加進捗: `ref-set` 修正後の full VM stage2 SIGSEGV は function `1006` / segment `996` / relative offset `222` 付近に移動し、function offsets 診断では `idx=20 opcode=53(vector-get) depth=2` の helper call window 後に `00` window が残ることを確認した。`vector-get` runtime helper call も `string-char-at` / `vector-push` / `map-get` と同じ consume-two direct append 分岐へ合流させ、fallback の byte-vector 生成を避ける形で固定した。Evidence: `test_native_codegen_x86_vector_get_runtime_call_shares_consume_two_direct_append_in_stage1`, `test_linux_x86_function_segment_metadata_detects_zeroed_ref_set_runtime_call`, `runtime_call_direct_appends_in_stage1`, `native_codegen_x86_control`, `cargo run -q --bin lsharp -- parse selfhost/src/Backend/Native/NativeCodegen.ls`, `cargo run -q --bin lsharp -- check selfhost/src/Backend/Native/NativeCodegen.ls`, `test_e2e_native_linux_x86_host_generates_actual_selfregen_stage1_bundle_artifact` (`ci-artifacts/native-linux-x86-hostgen-vm/vector-get-direct-stage1/actual-stage1`)。
    - 2026-05-21 追加進捗: `vector-get` 修正後も full VM は同じ function `1006` / segment `996` 付近の stage2 SIGSEGV に残り、actual byte window には 12 byte 級の `00` window が残った。未 direct 化の consume-two runtime helper fallback を潰す次の narrow slice として、`string-concat` (`opcode=70`) も `string-char-at` / `vector-get` / `vector-push` / `map-get` と同じ consume-two direct append 分岐へ合流させた。Evidence: `test_native_codegen_x86_string_concat_runtime_call_shares_consume_two_direct_append_in_stage1`, `test_native_codegen_x86_vector_get_runtime_call_shares_consume_two_direct_append_in_stage1`, `runtime_call_direct_appends_in_stage1`, `native_codegen_x86_control`, `cargo run -q --bin lsharp -- parse selfhost/src/Backend/Native/NativeCodegen.ls`, `cargo run -q --bin lsharp -- check selfhost/src/Backend/Native/NativeCodegen.ls`, `test_e2e_native_linux_x86_host_generates_actual_selfregen_stage1_bundle_artifact` (`ci-artifacts/native-linux-x86-hostgen-vm/string-concat-direct-stage1/actual-stage1`)。
    - 2026-05-21 追加進捗: `string-concat` 修正後の full VM でも stage2 SIGSEGV は同じ relative `222` の `00` window に残ったが、actual segment `996` と同型の early segment `26` / function `36` を比較し、function offsets 診断で `idx=27 opcode=62(map-insert) depth=3 off=222 size=12 bytes=[0,0,0,0,0,0,0,0,...]` に対応することを固定した。`map-insert` runtime helper call は fallback の byte-vector 生成を避け、`mov rdx, spill0` + `call rel32` + consume-three restore を direct append する専用経路へ合流させた。Evidence: `test_native_codegen_x86_map_insert_runtime_call_direct_appends_in_stage1`, `test_linux_x86_function_segment_metadata_detects_zeroed_map_insert_runtime_call`, `runtime_call_direct_appends_in_stage1`, `native_codegen_x86_control`, `cargo run -q --bin lsharp -- parse selfhost/src/Backend/Native/NativeCodegen.ls`, `cargo run -q --bin lsharp -- check selfhost/src/Backend/Native/NativeCodegen.ls`, `test_e2e_native_linux_x86_host_generates_actual_selfregen_stage1_bundle_artifact` (`ci-artifacts/native-linux-x86-hostgen-vm/map-insert-direct-stage1/actual-stage1`)。
    - 2026-05-21 追加進捗: `map-insert` 修正後の full VM は helper/object smokes と actual stage1 artifact gate を通過し、stage2 実行の最小 chunk `0-1` で `lsharp_bundle+19836` SIGSEGV に進んだ。GDB と function offsets 診断で stage-code segment `8` / function `18` / relative `692` が `idx=77 opcode=69(substring) depth=3 off=692 size=12 bytes=[0,0,0,0,0,0,0,0,...]` に対応することを固定した。`substring` runtime helper call も `map-insert` と同じ consume-three helper direct append 経路へ合流させた。Evidence: full VM artifact `ci-artifacts/native-linux-x86-hostgen-vm/map-insert-direct-full-v1`, `test_native_codegen_x86_substring_runtime_call_direct_appends_in_stage1`, `test_linux_x86_function_segment_metadata_detects_zeroed_substring_runtime_call`, `runtime_call_direct_appends_in_stage1`, `native_codegen_x86_control`, `cargo run -q --bin lsharp -- parse selfhost/src/Backend/Native/NativeCodegen.ls`, `cargo run -q --bin lsharp -- check selfhost/src/Backend/Native/NativeCodegen.ls`, `test_e2e_native_linux_x86_host_generates_actual_selfregen_stage1_bundle_artifact` (`ci-artifacts/native-linux-x86-hostgen-vm/substring-direct-stage1/actual-stage1`)。
    - 2026-05-21 追加進捗: `substring` 修正後の full VM は helper/object smokes と actual stage1 artifact gate を通過し、stage2 実行の最小 chunk `0-1` で `lsharp_bundle+30583` SIGSEGV に進んだ。GDB と function offsets 診断で stage-code segment `12` / function `22` / relative `259` が `idx=35 opcode=73(file-exists?) depth=1 off=259 size=7 bytes=[0,0,0,0,0,0,0]` に対応することを固定した。`file-exists?` runtime helper call は同じ preserving-rcx helper call 形の `ref-get` branch に合流させ、専用 branch を増やさず direct append する形にした。Evidence: full VM artifact `ci-artifacts/native-linux-x86-hostgen-vm/substring-direct-full-v1`, `test_native_codegen_x86_file_exists_runtime_call_direct_appends_in_stage1`, `test_linux_x86_function_segment_metadata_detects_zeroed_file_exists_runtime_call`, `runtime_call_direct_appends_in_stage1`, `native_codegen_x86_control`, `cargo run -q --bin lsharp -- parse selfhost/src/Backend/Native/NativeCodegen.ls`, `cargo run -q --bin lsharp -- check selfhost/src/Backend/Native/NativeCodegen.ls`, `test_e2e_native_linux_x86_host_generates_actual_selfregen_stage1_bundle_artifact` (`ci-artifacts/native-linux-x86-hostgen-vm/file-exists-direct-stage1/actual-stage1`)。
    - 2026-05-22 追加進捗: `file-exists?` 修正後の full VM は helper/object smokes と actual stage1 artifact gate を通過し、stage2 実行の最小 chunk `0-1` で `lsharp_bundle+3315255` SIGSEGV に進んだ。`stage1-debug` の full `src/` 回収漏れにより手元 metadata 再実行が縮退していたため、debug artifact に selfhost source tree も回収するよう固定し、VM 側の実 work dir から取り直した function metadata で stage-code segment `1090` / function `1100` / relative `1182` が `idx=168 opcode=52(vector-length) depth=1 off=1182 size=7 bytes=[0,0,0,0,0,0,0]` に対応することを確定した。`vector-length` runtime helper call は `string-length` と同じ preserving-rcx direct append 分岐へ合流させた。Evidence: full VM artifact `ci-artifacts/native-linux-x86-hostgen-vm/file-exists-direct-full-v1`, `test_native_codegen_x86_vector_length_runtime_call_direct_appends_in_stage1`, `test_linux_x86_function_segment_metadata_detects_zeroed_vector_length_runtime_call`, `test_native_linux_x86_hostgen_vm_script_copies_stage_debug_source_tree`, `runtime_call_direct_appends_in_stage1`, `native_codegen_x86_control`, `vector_length`, `cargo run -q --bin lsharp -- parse selfhost/src/Backend/Native/NativeCodegen.ls`, `cargo run -q --bin lsharp -- check selfhost/src/Backend/Native/NativeCodegen.ls`, `test_e2e_native_linux_x86_host_generates_actual_selfregen_stage1_bundle_artifact` (`ci-artifacts/native-linux-x86-hostgen-vm/vector-length-direct-stage1/actual-stage1`)。
    - 2026-05-21 追加進捗: `vector-length` 修正後の full VM は helper/object smokes と actual stage1 artifact gate を通過し、stage2 実行の最小 chunk `0-1` で `generated+15738` に進んだ。`generated` symbol は `lsharp_bundle` 後の trailer 開始 (`entrypoint-offset.txt=4897868`) なので、実 offset は `4913606`、`stage-code-segments.tsv` 上は trailer segment `1523` の `vector-get` helper 内だった。GDB では caller return address が `lsharp_bundle+3219286`、call site が function `1072` / segment `1062` / `idx=52 opcode=53(vector-get) depth=2 off=427` に対応し、helper call 自体は `e8 rel32` として正しく出ていた。現 blocker は zeroed helper call ではなく、high-bit の非 canonical immediate を `vector-get` helper が tagged pointer と誤認して `[rcx+8]` を dereference する点に絞れたため、`btr rcx,63` 後に lower-half canonical pointer guard を入れ、helper size/trailer append/後続 helper offsets を同期した。あわせて stage debug `src/` 回収は `src/src` へネストしない destination に修正した。Evidence: full VM artifact `ci-artifacts/native-linux-x86-hostgen-vm/vector-length-direct-full-v1`, `test_native_codegen_x86_vector_get_helper_rejects_tagged_noncanonical_immediates`, `test_native_linux_x86_hostgen_vm_script_copies_stage_debug_source_tree`, `test_native_codegen_x86_runtime_helper_emitters_return_executable_byte_vectors`, `runtime_call_direct_appends_in_stage1`, `native_codegen_x86_control`, `vector_get`, `cargo run -q --bin lsharp -- parse selfhost/src/Backend/Native/NativeCodegen.ls`, `cargo run -q --bin lsharp -- check selfhost/src/Backend/Native/NativeCodegen.ls`, `test_e2e_native_linux_x86_host_generates_actual_selfregen_stage1_bundle_artifact` (`ci-artifacts/native-linux-x86-hostgen-vm/vector-get-guard-stage1/actual-stage1`)。
    - 2026-05-21 追加進捗: `vector-get` lower-half canonical guard 後の full VM は stage2 最小 chunk `0-1` で `generated+15758` / `vector-get` helper 内の `mov 0x10(%rcx,%rax,8),%rax` に進み、GDB では `rcx=0x7ffff7f92000` の object tag が `5`、内容が `"(module App.Seed)\n..."` の source/string object だった。つまり helper call / rel32 は正しく、残 blocker は string object を vector として dereference する型タグ検査不足に絞れたため、`vector-get` helper に short canonical pointer guard (`mov rdx, rcx; shr rdx, 47`) と vector tag guard (`cmp dword ptr [rcx], 2`) を追加し、helper size/trailer append/後続 helper offsets を 47 byte 版へ同期した。Evidence: full VM artifact `ci-artifacts/native-linux-x86-hostgen-vm/vector-get-guard-full-v1`, `test_native_codegen_x86_vector_get_helper_rejects_non_vector_objects`, `test_native_codegen_x86_runtime_helper_emitters_return_executable_byte_vectors`, `runtime_call_direct_appends_in_stage1`, `native_codegen_x86_control`, `vector_get`, `cargo run -q --bin lsharp -- parse selfhost/src/Backend/Native/NativeCodegen.ls`, `cargo run -q --bin lsharp -- check selfhost/src/Backend/Native/NativeCodegen.ls`, `test_e2e_native_linux_x86_host_generates_actual_selfregen_stage1_bundle_artifact` (`ci-artifacts/native-linux-x86-hostgen-vm/vector-get-tag-guard-short-stage1-tty/actual-stage1`)。
    - 2026-05-21 追加進捗: `vector-get` tag guard 後の full VM は helper/object smokes と actual stage1 artifact gate を通過し、以前の `vector-get` helper SIGBUS は再発しなくなった。一方で `actual-stage2/program.native src/App/Seed.ls 0 1 1 0` は exit 124 / stdout 0 / stderr 0 の timeout と高 RSS に変わり、GDB では `generated+15661` の `vector-new` helper fill loop (`mov %rdi,(%rdx)`, `rdi=0x9090909090909090`) に滞在していた。return address mapping では function `1072` / `1076` 周辺の再帰と、actual stage2 segment `1066` が host metadata 上の function size を越えている drift まで確認した。次の narrow diagnostic として、Linux x86 representative seed に `command-line-arg 8` の progress mode を追加し、payload 直前/直後、native-callable 正規化後、starts/user-total/code-len/entrypoint/segment context 直前直後の numeric marker を通常 transport と分離して出せるようにした。VM script は `LSHARP_NATIVE_LINUX_X86_STAGE3_PROGRESS` 指定時だけ materialized actual-stage2 から `actual-stage3-progress.txt` / stderr を回収する。Evidence: full VM artifact `ci-artifacts/native-linux-x86-hostgen-vm/vector-get-tag-guard-short-full-v1`, manual GDB diagnostics under `manual-diagnostics/`, `test_linux_x86_representative_seed_can_print_stage_progress_markers`, `test_native_linux_x86_hostgen_vm_script_can_collect_stage3_progress_markers`, `linux_x86_representative_seed`, `native_linux_x86_hostgen_vm_script`。
    - 2026-05-20 追加進捗: `append-i64-const-bundle-x86 -> append-root-push-bundle-x86 -> append-mov-rax-rcx-x86` だけを呼ぶ codegen append probe を Linux x86 native bundle 化し、VM 上で exit code 49 (`0x31`) を確認した。isolated native execution では x86 direct append codegen 関数が期待 byte を返すため、残る blocker は full stage1 の function 1518 生成時に限定される。次の診断は function 1518 の static row offset と actual generated segment offset の before/after、または `segment` / `padded-segment` / packed artifact の同一 window 比較。Evidence: `test_e2e_native_linux_x86_host_generates_codegen_append_probe_bundle_artifact`, manual VM materialization `/tmp/lsharp-codegen-append-probe-vm` (`exit_code=49`)。
    - 2026-05-20 追加進捗: 小さい function-meta codegen loop probe で `emit-native-function-meta-bundle` 自体を Linux x86 native 実行し、`31 c0 48 89 c8` window の検出結果として exit code 49 を確認した。x86 direct append 関数単体だけでなく、小規模な native codegen loop も正常なため、問題は large selfhost Seed の function 1518 生成ループ、またはその segment/padded/packed 出力文脈に残る。Evidence: `test_e2e_native_linux_x86_host_generates_codegen_loop_probe_bundle_artifact`, manual VM materialization `/tmp/lsharp-codegen-loop-probe-vm` (`exit_code=49`)。
    - 2026-05-19 追加進捗: Seed entry segment probe を Linux x86 native bundle 化し、VM 上で `src/App/Seed.ls` を argv 経由で読ませた実行が `functions=1509` / `entrypoint_func_idx=1518` / `ir_len=499` / `segment_len=10800` / segment offsets `432..436 = 00 00 00 00 00` を出力することを確認した。これにより `generated+434` の zero window は packed artifact/materialize 後ではなく、function 1518 の `generate-native-function-x86-64-bundle-with-layout` 生成 segment 内で既に発生している。Probe 内の 7 引数 helper 呼び出しは空 stdout を誘発したため、次は高 arity x86 call bundle を Linux native 実行で最小再現してから修正する。Evidence: `test_e2e_native_linux_x86_host_generates_seed_entry_segment_probe_bundle_artifact`, manual VM materialization `/tmp/lsharp-seed-entry-segment-probe-vm`。
    - 2026-05-19 追加進捗: 7 引数 source-level user call probe は Linux x86 native VM で stdout `28` / exit code 0 となり、単純な 7 引数 user call と SysV stack arg は blocker から外れた。また `append-i64-const-bundle-x86 result 0 16 1 -> append-root-push-bundle-x86 -> append-drop-bundle-x86` を Linux x86 native 実行し、result length 0 と事前 padding 424 の両方で expected tail byte `200` を返すことを確認した。残る blocker は generic direct append / 7 引数 call ではなく、large Seed entry function 1518 の生成文脈でだけ発生する IR row / emitted byte drift または runtime helper call row の欠落に絞る。Evidence: `test_e2e_native_linux_x86_host_generates_source_seven_arg_call_probe_bundle_artifact`, `test_e2e_native_linux_x86_host_generates_codegen_append_depth_one_probe_bundle_artifact`, `test_e2e_native_linux_x86_host_generates_codegen_append_padded_depth_one_probe_bundle_artifact`, manual VM materialization `/tmp/lsharp-source-seven-arg-probe-vm`, `/tmp/lsharp-codegen-append-depth-one-probe-vm`, `/tmp/lsharp-codegen-append-padded-depth-one-probe-vm`。
    - 2026-05-19 追加進捗: isolated opcode53 vector-get codegen probe は Linux x86 native VM で `exit_code=232` (`0xE8`) を返し、単体では rel32 call opcode を生成できることを確認した。一方、Seed entry IR/segment window probe は同じ Linux x86 native 実行内で `row57 opcode=53 operand=4 depth=2 offset=428 size=5 byte0=0 rel32=-1`、隣接 row58 `offset=433`、segment bytes `428..432 = 00 00 00 00 00` を固定した。これにより missing call は opcode53 helper call の rel32 計算ではなく、large Seed entry function 1518 の control-loop fallback が返す 5-byte bundle だけがゼロ化する文脈依存の問題に絞られた。`opcode53 depth<=2` direct append patch は selfhost compiler の Wasm 実行が `unreachable` で崩れたため不採用。Evidence: `test_e2e_native_linux_x86_host_generates_codegen_vector_get_probe_bundle_artifact`, `test_e2e_native_linux_x86_host_generates_seed_entry_ir_segment_window_probe_bundle_artifact`, manual VM materialization `/tmp/lsharp-codegen-vector-get-probe-vm` (`exit_code=232`), `/tmp/lsharp-seed-entry-ir-segment-probe-vm`。
    - 2026-05-19 追加進捗: Seed entry IR/segment window probe を expected bundle bytes / expected rel32 target / actual rel32 target / 実 body offset 付きに拡張した。current selfhost graph を VM で `program.native src/App/Main.ls` として実行すると `functions=1712` / `entrypoint=1721` / `ir_len=199` / `segment_len=1433` で、row52 `opcode=40` は expected bytes `e8 51 fe ff ff` / expected target `10` に対し、row offset `436` の actual は `00 00 00 e8 4e`、byte window `439..443 = e8 4e fe ff ff` だった。つまり旧 `offset428` zero 診断は body-offset/prologue 分の混入を含んでおり、call bundle 自体は生成されて実 byte stream へ現れるが、static offset row と actual append cursor の間に残る `+3` bias がある。次は call emitter 書き換えではなく、`collect-native-bundle-offsets-x86` の start offset と `generate-native-function-x86-64-bundle-with-layout` の actual emitted offset を同一 row で確定する。Evidence: `test_linux_x86_seed_entry_ir_segment_probe_reports_expected_rel32_targets`, `test_e2e_native_linux_x86_host_generates_seed_entry_ir_segment_window_probe_bundle_artifact`, manual VM materialization `/tmp/lsharp-seed-entry-ir-segment-probe-v4`。
    - 2026-05-19 追加進捗: Seed entry IR/segment probe に control-loop cursor 診断と lightweight row window を追加し、VM 上で `program.native src/App/Seed.ls` を実行して stage2 生成 entrypoint 相当の failure point を row41 に絞った。実測は `functions=1509` / `entrypoint=1518` / `ir_len=499` / `segment_len=10800` / prefix len `11`。row38〜40 は declared offset と actual append cursor の差が `+3` のまま size と emitted length が一致するが、row41 `opcode=40` / `operand=1102` / `depth=4` / `declared=318` / `before=321` は static size `25` に対して emitted length `20` で、actual bytes は four-arg setup (`48 89 ca ... 48 89 c1`) のみで `e8` rel32 call が無い。以後 row42 から cursor 差分が `-2` へ反転し、row57 `opcode=53` の zero window は下流症状と確定した。次は broad append helper / spill floor / body-offset fix ではなく、`target-param-count=4` の x86 user-call bundle が `call-rel-bytes` を落とす条件を最小再現して修正する。Evidence: `test_linux_x86_seed_entry_ir_segment_probe_reports_expected_rel32_targets`, `test_e2e_native_linux_x86_host_generates_seed_entry_ir_segment_window_probe_bundle_artifact`, manual VM materialization `/tmp/lsharp-seed-entry-ir-segment-probe-v10-run`。
    - 2026-05-19 追加進捗: `target-param-count=4` / `current-depth=4` の最小 codegen probe を追加し、修正前 VM では `codegen-x86-opcode-call-bundle` が 25 bytes ではなく setup のみ 20 bytes を返すことを exit code `20` で再現した。four-arg branch は precomputed `call-rel-bytes` vector を helper へ渡す経路だと native stage1 実行中に call bytes が落ちるため、`call-rel` を ref へ固定し、`emit-four-arg-call-x86-core-with-rel-ref` 内で setup 構築後に `emit-call-rel32` する最小修正へ変更。修正後の four-arg probe は VM exit code `232` (`0xE8`) となり、Seed probe でも row41 は `size=25` / `emitted_len=25`、actual call offset `341`、bytes `e8 a6 fe ff ff` まで回復した。row57 は cursor 上で `before=439` に call があり、残る `+3` は offset table 診断上のずれとして分離された。次はこの修正で full `native-linux-x86-hostgen-vm-exec.sh` の stage3-run SIGSEGV が解消するか確認する。Evidence: `test_e2e_native_linux_x86_host_generates_codegen_four_arg_call_probe_bundle_artifact`, `test_e2e_native_linux_x86_host_generates_seed_entry_ir_segment_window_probe_bundle_artifact`, manual VM materialization `/tmp/lsharp-codegen-four-arg-call-probe-v4-run`, `/tmp/lsharp-seed-entry-ir-segment-probe-v11-run`。
    - 2026-05-19 15:30 再開進捗: `local-fourarg-relref-v1` の full hostgen VM artifact では stage3 ではなく actual-stage1 chunked stage2 transport 生成の chunk `866-867` が exit 139 で落ちていることを確認した。GDB では `vector-length` helper (`generated+4172`) が `rax=0x7fffffffffffffff` を dereference し、戻り元 `lsharp_bundle+1181014` は function index `1472` の `concat-five-byte-vectors-rooted` 内で、caller `1742` は `emit-eight-arg-call-x86-core-with-call-bytes` と特定した。追加した env-driven 診断で function start window と owner decl を任意 index から引けるようにし、`1472 -> Backend.Native.NativeCodegen / concat-five-byte-vectors-rooted`, `1742 -> Backend.Native.NativeCodegen / emit-eight-arg-call-x86-core-with-call-bytes` を固定した。八引数 helper の `call-rel-bytes` helper-local root 化、`call-rel-ref` 化、direct core rel 化、`call-tail` 先行結合はいずれも host 側 selfhost artifact gate で Wasm OOB / `unreachable` を起こしたため不採用。次は八引数 helper を広げず、stage1 chunk `866-867` に近い call-bytes liveness を再現する最小 probe を切る。Evidence: `test_linux_x86_start_window_diagnostic_is_env_driven`, `test_linux_x86_owner_decl_diagnostic_is_env_driven`, `test_e2e_selfhost_main_linux_x86_actual_seed_start_window_diagnostic`, `test_e2e_selfhost_main_linux_x86_actual_seed_owner_decl_diagnostic`, rejected `test_e2e_native_linux_x86_host_generates_codegen_eight_arg_call_probe_bundle_artifact` attempts。
    - 2026-05-19 15:30 再開進捗: 八引数 codegen probe に bundle byte window 診断を追加し、Linux VM 実行で `codegen-x86-opcode-call-bundle` の戻り値が length `65`、offset `58..64 = 48 81 c4 10 00 00 00` になることを固定した。期待される `native-call-rel-next-offset-x86 8 8 = 63` に対し、本来 offset `58` にあるべき `e8 rel32` 5 bytes だけが抜けている。後続 mov/restore bytes の事前 bind/root 案は `lsharp check selfhost/src/Backend/Native/NativeCodegen.ls` で Wasm call stack exhausted、八引数だけ scalar `rel` 経路へ戻す案は ignored artifact gate で Wasm `unreachable` になったため不採用。次は八引数 helper 全体ではなく、`concat-five-byte-vectors-rooted setup2 mov-rsi mov-rdi call-rel-bytes restore` の第四引数だけが落ちる native evaluation/rooting の最小再現へ絞る。Evidence: `test_e2e_native_linux_x86_host_generates_codegen_eight_arg_call_probe_bundle_artifact`, manual VM materialization `/tmp/lsharp-codegen-eight-arg-window-v1-run` (`exit=0`, printed length/window), rejected prebind/scalar-rel attempts。
    - 2026-05-20 追加進捗: `concat-five-byte-vectors-rooted` 自体の第四引数 liveness を切り分ける Linux x86 native probe を追加し、`(byte-vector-3 1 2 3)`, `(byte-vector-2 4 5)`, `(byte-vector-2 6 7)`, `(emit-call-rel32 0)`, `(byte-vector-2 8 9)` の直接結合は VM 実行で length `14`、offset `7 = 232` (`e8`) となることを確認した。これにより generic concat-five / 5 引数 call ABI ではなく、`emit-eight-arg-call-x86-core-with-call-bytes` の setup2 + mov-rsi/mov-rdi + call-rel-bytes + restore 文脈に問題が残る。Evidence: `test_e2e_native_linux_x86_host_generates_concat_five_fourth_arg_probe_bundle_artifact`, manual VM materialization `/tmp/lsharp-concat-five-fourth-arg-probe-v1-run` (`exit=232`, printed length/window)。
    - 2026-05-20 追加進捗: `emit-eight-arg-call-x86-core-with-call-bytes` 単体を Linux x86 native bundle 化し、VM 実行で length `70`、offset `58 = 232` (`e8`)、offset `63..69 = 48 81 c4 10 00 00 00` を確認した。つまり core helper と direct concat-five は正常で、`codegen-x86-opcode-call-bundle` の長い opcode 40 分岐文脈でだけ `call-rel-bytes` が失われる。target-param-count=8 専用の early root-pop 分岐は `lsharp check selfhost/src/Backend/Native/NativeCodegen.ls` で Wasm call stack exhausted になったため不採用。Evidence: `test_e2e_native_linux_x86_host_generates_eight_arg_core_call_bytes_probe_bundle_artifact`, manual VM materialization `/tmp/lsharp-eight-arg-core-call-bytes-probe-v1-run` (`exit=232`, printed length/window), rejected early-pop attempt。
    - 2026-05-20 追加進捗: current HEAD の `codegen-x86-opcode-call-bundle` / target-param-count=8 probe を再採取し、VM 実行で bundle length `65`、offset `58..64 = 48 81 c4 10 00 00 00`、`e8` call byte 不在を確認した。つまり欠落は direct core ではなく opcode 40 branch から core helper へ渡す `call-rel-bytes` に限定される。修正候補として試した 3 案、(1) `emit-eight-arg-call-x86-core-with-rel-ref` 追加、(2) target-param-count=8 専用小 helper への委譲、(3) branch 内 `emit-call-rel32 call-rel` 再生成は、いずれも静的 contract は通るが `cargo run -q --bin lsharp -- check selfhost/src/Backend/Native/NativeCodegen.ls` が embedded component 実行失敗になるため不採用。次は新規 helper 追加や branch 内 rel32 再生成ではなく、source-level helper call の第一引数 liveness / value-window spill を診断する最小 probe を追加する。Evidence: `test_e2e_native_linux_x86_host_generates_codegen_eight_arg_call_probe_bundle_artifact`, manual VM materialization `/tmp/lsharp-codegen-eight-arg-window-current-run` (`length=65`, no `e8`), rejected rel-ref/helper-delegation/branch-regeneration attempts。
    - 2026-05-20 追加進捗: `codegen-x86-opcode-call-bundle` 全体ではなく、0..8 の nested branch と `emit-eight-arg-call-x86-core-with-call-bytes` 呼び出しだけを別関数化した Linux x86 native probe を追加した。VM 実行では length `70`、offset `58 = 232` (`e8`) となり、nested branch / core helper / first argument ABI は単体で正常。残る blocker は `codegen-x86-opcode-call-bundle` と同一関数内での context/ref/local liveness、または同一関数内 nested branch での `call-rel-bytes` local の保持に絞る。Evidence: `test_e2e_native_linux_x86_host_generates_eight_arg_branch_call_bytes_probe_bundle_artifact`, manual VM materialization `/tmp/lsharp-eight-arg-branch-call-bytes-probe-v1-run` (`exit=232`, printed length/window)。
    - 2026-05-20 追加進捗: 0..8 nested branch を別関数に出さず `main` 内へ inline した Linux x86 native probe でも、VM 実行は length `70`、offset `58 = 232` (`e8`) となった。したがって関数境界の有無や同一関数内 nested branch だけでは再現しない。残る差分は `codegen-x86-opcode-call-bundle` 固有の `operand-ref/current-offset-ref/function-starts-ref` rooting、context vector からの `function-metas/import-count/import-stub-offset/function-start-base/frame-base-slot-count/current-depth` 読み出し、`native-function-param-count` / target offset / call-rel 計算と nested branch の組み合わせ。Evidence: `test_e2e_native_linux_x86_host_generates_inline_eight_arg_branch_call_bytes_probe_bundle_artifact`, manual VM materialization `/tmp/lsharp-inline-eight-arg-branch-call-bytes-probe-v1-run` (`exit=232`, printed length/window)。
    - 2026-05-20 追加進捗: `function-metas/context/operand-ref/current-offset-ref/function-starts-ref/native-function-param-count/call-rel` まで含めた context/direct eight-arg probe でも、target-param-count=8 への直接分岐なら VM 実行は length `70`、offset `58 = 232` (`e8`) となった。したがって context/ref 計算単体では call byte は落ちず、失敗条件は full dispatch chain と context/ref local の組み合わせに残る。target-param-count=8 を zero-arg branch より前へ移動する案は静的テストだけ通ったが、`cargo run -q --bin lsharp -- check selfhost/src/Backend/Native/NativeCodegen.ls` が embedded component 実行失敗になったため不採用。Evidence: `test_e2e_native_linux_x86_host_generates_context_direct_eight_arg_call_bytes_probe_bundle_artifact`, manual VM materialization `/tmp/lsharp-context-direct-eight-arg-call-bytes-probe-v1-run` (`exit=232`, printed length/window), rejected eight-before-zero branch reorder。
    - 2026-05-20 追加進捗: control-loop opcode 40 fast path の 1 行だけを通す eight-arg probe を追加し、IR row / emitted bytes / rel32 target を同一 window で出力できるようにした。VM 実行では `idx=8 opcode=40 operand=10 depth=8 declared=316 size=70 before=0 emitted=65 expected-target=0 actual-call-offset=-1 actual-target=-1`、byte `58..64 = 48 81 c4 10 00 00 00`、exit code `65` となり、Seed の大きい関数文脈なしでも `generate-native-control-instr-bundle-loop-x86-with-context -> codegen-x86-opcode-call-bundle` の full dispatch chain だけで `e8 rel32` 欠落が再現する。Evidence: `test_linux_x86_control_loop_eight_arg_call_probe_reports_opcode40_bytes_and_targets`, `test_e2e_native_linux_x86_host_generates_control_loop_eight_arg_call_probe_bundle_artifact`, manual VM materialization `/tmp/lsharp-control-loop-eight-arg-call-probe-v1-run` (`exit=65`, printed row/window)。
    - 2026-05-20 追加進捗: control-loop eight-arg probe に同一 context の direct `codegen-x86-opcode-call-bundle` 診断を追加し、VM 実行で direct 側も `direct-len=74` / `direct-call-offset=-1` / `direct-target=-1`、control-loop append 側は従来通り `emitted=65` / `actual-call-offset=-1` となることを確認した。direct byte window では call slot の `58..65` に pointer-like 値が入り、`66..73 = 10 48 81 c4 10 00 00 00` へずれていたため、欠落は append 境界ではなく full dispatch chain の `call-rel-bytes` 生成文脈で発生する。`call-rel-bytes-ref` 化、opcode40 append 前 `native` root 化、control-loop 専用 direct helper、target=8 numeric `emit-eight-arg-call-x86` はいずれも静的テストだけ通り、`cargo run -q --bin lsharp -- check selfhost/src/Backend/Native/NativeCodegen.ls` が embedded component 実行失敗になったため不採用。Evidence: `test_linux_x86_control_loop_eight_arg_call_probe_reports_opcode40_bytes_and_targets`, `test_e2e_native_linux_x86_host_generates_control_loop_eight_arg_call_probe_bundle_artifact`, manual VM materialization `/tmp/lsharp-control-loop-eight-arg-call-probe-v4-run` (`exit=65`, printed direct/control row/window)。
    - 2026-05-21 追加進捗: full VM artifact `control-if-else-helper-full-vm-v1` の stage2 SIGSEGV を GDB と segment table で function `1032` / segment `1022` / relative offset `307` に絞り、`stage-code-segments.tsv` を読む env-driven function offset 診断で actual segment bytes へ map できるようにした。さらに metadata mode の prefix row 数を `command-line-arg 7` で拡張し、fresh stage1 metadata で failure row が `idx=31 opcode=50(string-char-at) depth=3 offset=307 size=12`、修正前 bytes `00 00 00 00 00 00 00 00` と確定。`opcode50` は control-loop fallback の byte-vector 経由を避け、`append-consume-two-helper-call-x86` で `e8 rel32` と depth>=3 restore を direct append する最小修正に変更し、fresh stage1 metadata では row31 bytes が `e8 ab a0 1b 00 48 8b 8d` まで回復した。Evidence: `test_linux_x86_stage_code_segments_tsv_maps_function_index_start`, `test_linux_x86_representative_seed_can_print_function_segment_metadata`, `test_native_codegen_x86_string_char_at_runtime_call_direct_appends_in_stage1`, `test_e2e_native_linux_x86_host_generates_actual_selfregen_stage1_bundle_artifact` (`ci-artifacts/native-linux-x86-hostgen-vm/opcode50-direct-stage1/actual-stage1`), manual VM metadata `stage1-metadata-1022-prefix50.txt`。
    - 2026-05-21 追加進捗: `opcode50` 修正後の full VM `opcode50-direct-full-v1` は stage2 chunk `0-1` でまだ exit 139 となったが、GDB で新しい crash は function `1004` / segment `994` / relative offset `35` に移動した。fresh stage1 metadata では `idx=1 opcode=57(ref-get) depth=1 offset=35 size=7` が `00` 化していたため、`opcode57` も control-loop fallback の byte-vector 経由を避け、`append-x86-helper-call-preserving-rcx` で direct append する最小修正に変更した。fresh stage1 metadata では row1 bytes が `51 e8 eb b8 1c 00 59` まで回復し、同じ function の次候補は `idx=11 opcode=63(map-get) offset=99` の `00` window に絞れている。Evidence: `test_native_codegen_x86_ref_get_runtime_call_direct_appends_in_stage1`, `test_e2e_native_linux_x86_host_generates_actual_selfregen_stage1_bundle_artifact` (`ci-artifacts/native-linux-x86-hostgen-vm/refget-direct-stage1/actual-stage1`), manual VM metadata `stage1-metadata-994-prefix50.txt`。
    - 2026-05-21 追加進捗: `opcode63(map-get)` は fallback 側に小さい helper を追加する案、および control-loop へ単独 branch を追加する案が `parse/check selfhost/src/Backend/Native/NativeCodegen.ls` の embedded component 実行失敗を誘発したため不採用。採用案は既存の `opcode50(string-char-at)` consume-two direct append 分岐へ `opcode63` を合流し、runtime dispatch 側でも同じ op50/op63 分岐へ統合して nested branch を増やさない形にした。fresh stage1 metadata では function `1004` / segment `994` / `idx=11` の bytes が `e8 bc ba 1c 00` へ戻り、`00` window は解消済み。Evidence: `test_native_codegen_x86_map_get_runtime_call_shares_consume_two_direct_append_in_stage1`, `test_native_codegen_x86_ref_get_runtime_call_direct_appends_in_stage1`, `test_native_codegen_x86_string_char_at_runtime_call_direct_appends_in_stage1`, `test_e2e_native_linux_x86_host_generates_actual_selfregen_stage1_bundle_artifact` (`ci-artifacts/native-linux-x86-hostgen-vm/mapget-direct-stage1/actual-stage1`), manual VM metadata `stage1-metadata-994-prefix50.txt`。
    - 2026-05-21 追加進捗: `mapget-direct-full-v1` の stage2 crash は function `1055` / segment `1045` / relative offset `97` に移動し、fresh stage1 metadata では `idx=11 opcode=44(drop) depth=3` が `00` 化していた。既存の shallow drop early branch を depth 条件なしの `append-drop-bundle-x86` direct append に広げ、遅い direct selector へ落ちる前に深い drop も処理する形へ変更。fresh metadata では `idx=11/33/35/46/48` の depth=3 drop bytes が `48 89 c8 48 8b 8d ...` へ戻った。Evidence: `test_native_codegen_x86_drop_direct_appends_before_late_selector`, `test_e2e_native_linux_x86_host_generates_actual_selfregen_stage1_bundle_artifact` (`ci-artifacts/native-linux-x86-hostgen-vm/drop-direct-stage1/actual-stage1`), manual VM metadata `stage1-metadata-1045-prefix80.txt`。
    - 2026-05-20 追加進捗: eight-arg dispatch matrix probe を追加し、同じ context/ref/call-rel 計算で低 arity 分岐 body の有無だけを比較した。Linux VM 実行では空の 0..7 分岐なら `label=1 len=70 call-offset=58` で `e8` が残る一方、低 arity の実 body を同一関数内に持つだけで `label=2 len=65 call-offset=-1` となり、consume wrapper 抜きでも `label=3 len=130 call-offset=-1` になった。これにより失敗条件は eight-arg core / context-ref / nested branch 単体ではなく、非実行の low-arity helper body が同一関数の x86 native codegen に与える branch pressure / value-window liveness に絞られた。次は low-arity body を 1..7 で二分探索し、どの helper shape が first argument の `call-rel-bytes` を壊すか固定してから最小修正する。Evidence: `test_linux_x86_eight_arg_dispatch_matrix_probe_compares_branch_pressure`, `test_e2e_native_linux_x86_host_generates_eight_arg_dispatch_matrix_probe_bundle_artifact`, manual VM materialization `/tmp/lsharp-eight-arg-dispatch-matrix-probe-v1` (`exit=65`, rows `1/70/58`, `2/65/-1`, `3/130/-1`)。
    - 2026-05-20 追加進捗: env-driven low-arity prefix probe を追加し、低 arity body を `0..N` で切り替えられるようにした。Linux VM 実行では `MAX=0` が `len=70 call-offset=58 exit=232` で正常、`MAX=1` が `len=65 call-offset=-1 exit=65` で失敗したため、非実行の one-arg branch body が同一関数内に入った時点で eight-arg 側の `call-rel-bytes` が落ちることを固定した。次の修正候補は broad helper や eight-arg rel-ref 再試行ではなく、one-arg branch の `call-rel-bytes` shadow / liveness pressure を減らして full dispatch の後続 branch に干渉させない最小変更。Evidence: `test_linux_x86_low_arity_prefix_probe_is_env_driven`, `test_e2e_native_linux_x86_host_generates_low_arity_prefix_probe_bundle_artifact`, manual VM materialization `/tmp/lsharp-low-arity-prefix-max0-v1` (`exit=232`), `/tmp/lsharp-low-arity-prefix-max1-v1` (`exit=65`)。
    - 2026-05-20 追加進捗: low-arity prefix probe に `ONE_ARG_SHAPE` を追加し、`MAX=1` の one-arg branch を `call-only` / `root-only` / `shadow-only` / `full` で切り替えられるようにした。Linux VM 実行では `call-only` と `root-only` は `len=70 call-offset=58 exit=232` で正常、`shadow-only` は `len=65 call-offset=-1 exit=65` で失敗したため、最小トリガは helper call や root 操作ではなく、非実行 branch 内の same-name shadow `(let [call-rel-bytes call-rel-bytes] ...)` に絞れた。Evidence: `test_linux_x86_low_arity_prefix_probe_is_env_driven`, `test_e2e_native_linux_x86_host_generates_low_arity_prefix_probe_bundle_artifact`, manual VM materialization `/tmp/lsharp-low-arity-prefix-max1-callonly-v1` (`exit=232`), `/tmp/lsharp-low-arity-prefix-max1-rootonly-v1` (`exit=232`), `/tmp/lsharp-low-arity-prefix-max1-shadowonly-v1` (`exit=65`)。
    - 2026-05-20 追加進捗: `codegen-x86-opcode-call-bundle` の 1〜3 / 5〜9 引数 branch から redundant な same-name shadow/root を除去し、outer root 済み `call-rel-bytes` を直接 helper へ渡す形に変更した。これにより synthetic low-arity prefix `MAX=7 full` は Linux VM 上で `len=70 call-offset=58 exit=232` となり、旧 `MAX=1` failure の call 欠落は解消した。helper trailer offset fixture は NativeCodegen text 変更に伴う新しい selfhost 実測値へ更新済み。Evidence: `test_native_codegen_x86_low_arity_call_branches_do_not_shadow_call_rel_bytes`, `test_linux_x86_low_arity_prefix_probe_is_env_driven`, `helper_call_sites_resolve_offsets`, `cargo run -q --bin lsharp -- check selfhost/src/Backend/Native/NativeCodegen.ls`, manual VM materialization `/tmp/lsharp-low-arity-prefix-max7-full-nosshadow-v1` (`exit=232`)。
    - 2026-05-20 追加進捗: current rerun 前に stage2 function offsets 診断を absolute byte mapping に直し、`LSHARP_NATIVE_LINUX_X86_START_NEAREST_OFFSET` と prologue window scan を追加した。既存 `local-paramspill-literal-v1/stage2-debug` では function 1179 (`compile-do-expr-with-source`) の host 再計算 start `3321733` に対して実 prologue は `3321717` (`drift=-16`) で、クラッシュ byte window は IR body row ではなく prologue/param-spill 直後にあることを固定した。一方、古い stage2 artifact は current source の entrypoint/start table と大きくずれる箇所もあるため、次は fresh `native-linux-x86-hostgen-vm-exec.sh` artifact で同じ prologue/row 診断を取り、call bundle 追加修正へ進む。未検証だった four-arg direct call-bytes 化と register param literal offset 化は full VM で同じ SIGSEGV が残ったため commit 対象から外した。Evidence: `test_linux_x86_start_window_diagnostic_is_env_driven`, `test_linux_x86_function_offsets_diagnostic_is_env_driven`, `test_nearest_x86_prologue_start_prefers_declared_window_candidate`, `test_e2e_selfhost_main_linux_x86_actual_seed_function_offsets_diagnostic` (`local-paramspill-literal-v1/stage2-debug`)。
    - 2026-05-20 追加進捗: fresh `local-prologue-drift-current-v1` / `local-r8r9-inline-v1` の full VM でも `actual-stage2 0..1` が `lsharp_bundle+3321766` で SIGSEGV し、GDB は同じ `movabs $0x8,%rax` 直後の zero window を示した。actual transport の segment table では offset `3321766` は segment index `1092` / function idx `1102` / `Backend.Wasm.Compiler.compile-let-chain-step-8-with-source` の relative `49` に対応する。r8/r9 spill を branch 内 direct append にする案は full VM で同じ SIGSEGV が残ったため不採用。追加した param spill 単体 probe は Linux VM で length `53`、r8/r9 window `4c 89 85 d0 ff ff ff / 4c 89 8d c8 ff ff ff` を返し、`spill-native-function-params-x86-loop` 単体は正常と確認した。次は Seed の該当 function segment 生成文脈で、`generate-native-function-x86-64-bundle-with-layout` の prefix emit 後に r8 spill が壊れる条件を切る。Evidence: `test_linux_x86_param_spill_probe_reports_high_register_bytes`, `test_e2e_native_linux_x86_host_generates_param_spill_probe_bundle_artifact`, manual VM materialization `/tmp/lsharp-param-spill-probe-v2` (`exit=76`, printed r8/r9 window), GDB `/tmp/lsharp-native-linux-x86-hostgen-vm-local-r8r9-inline-v1/actual-stage2/program.native`。
    - 2026-05-20 追加進捗: Seed function segment probe を追加し、`LSHARP_NATIVE_LINUX_X86_SEED_FUNCTION_SEGMENT_PROBE_INDEX=1102` で指定 function の prologue window を Linux native 実行から出せるようにした。manual VM materialization では App.Probe 内の native selfhost compile が target idx 1102 を param/local 0、segment length 20 の stub 相当にしており、full actual-stage2 transport の segment index 1092 (`compile-let-chain-step-8-with-source`, 7 params) とは一致しなかった。したがって次は probe 内の再コンパイルではなく、actual-stage1/actual-stage2 transport 自体に function metadata sidecar または segment table diagnostic を出して、stage1 native compiler が生成した `functions/starts` の実値を直接採る。Evidence: `test_linux_x86_seed_function_segment_probe_reports_prologue_bytes`, `test_e2e_native_linux_x86_host_generates_seed_function_segment_probe_bundle_artifact`, manual VM materialization `/tmp/lsharp-seed-func1102-segment-probe-v1`。
    - 2026-05-19 15:30 再開進捗: actual transport decoder が `stage-code-segments.tsv` を書き出すようにし、full VM artifact で segment index / function idx / absolute start / len / first 32 bytes を直接残せるようにした。既存 `local-prologue-drift-current-v1` の stdout を新 decoder と Rust diagnostic の双方で読み直し、target offset `3321766` は `segment_index=1092` / `function_idx=1102` / `start=3321717` / `len=3028` / `relative_offset=49` と再確認した。first bytes は prologue から rcx spill までは正常で、その直後に `48 b8 08 00 ...` の body 相当 byte stream へ入るため、次は host 再計算 starts ではなく actual transport の segment/function 座標を基準に、stage1 native compiler が該当 function を何 param/local/body offset として見ているかを採る。Evidence: `test_native_linux_x86_hostgen_vm_decoder_writes_stage_code_segment_table`, `test_linux_x86_actual_transport_segment_table_maps_absolute_offsets`, `test_e2e_linux_x86_actual_stage2_transport_segment_table_diagnostic`, extracted `/tmp/lsharp-stage2-decode-segment-table-check/stage-code-segments.tsv`。
    - 2026-05-19 15:30 再開進捗: actual stage1 native compiler から segment metadata range を直接出す診断 mode を追加し、fresh `paramcount-reread-v2` artifact の segment `1092` / function `1102` は `start=3321446` / `len=3028` / `param_count=4` / `local_count=10` / `frame_base=15` / `stack_bytes=1152` / `param_spill=28` / `body_offset=39` / `ir_len=134` と確認した。GDB では `actual-stage2/program.native src/App/Seed.ls 0 1 1 0` が `lsharp_bundle+3321495` (`start+49`) で SIGSEGV し、entrypoint 側 call site は function start `3321446` を指す一方、function body 先頭の `movabs $0x8,%rax` 後に余分な `00` byte が挿入され `add %al,(%rax)` として実行される。`compile-defn-function*` の param-count reread 案は fresh metadata が `4` のままで効果なしのため未採用。hostgen VM script は stale metadata/debug artifact を拾わないよう VM work dir を毎回削除し、split chunk 内の最小 range 失敗を親 range へ伝播するよう修正した。Evidence: `test_linux_x86_representative_seed_can_print_function_segment_metadata`, `test_native_linux_x86_hostgen_vm_script_can_collect_stage2_metadata_range`, `test_native_linux_x86_hostgen_vm_script_cleans_vm_work_dir_before_run`, `test_native_linux_x86_hostgen_vm_script_propagates_split_chunk_failures`, `test_e2e_native_linux_x86_host_generates_actual_selfregen_stage1_bundle_artifact` (`ci-artifacts/native-linux-x86-hostgen-vm/paramcount-reread-stage1-v2/actual-stage1`), `test_e2e_linux_x86_actual_stage2_transport_segment_table_diagnostic`, GDB `/tmp/lsharp-native-linux-x86-hostgen-vm-paramcount-reread-v2/actual-stage2/program.native`。
    - 2026-05-19 15:30 再開進捗: metadata mode を IR prefix / emitted byte prefix 付きに拡張し、actual stage1 native compiler 実体では function `1102` の row0 が `opcode=1 operand=8 depth=0 offset=39 size=10 bytes=48 b8 08 00 00 00 00 00...`、row1 が `opcode=54 operand=5 depth=1 offset=49 size=7 bytes=00 00 00 00 00 00 00 48...` と分かった。つまり host 再計算 row0 (`opcode=10 operand=1`) との不一致だけでなく、actual IR prefix 上でも row0 expected size 10 に対して append cursor が row1 を 6 byte 後ろへずらしている。`append-mov-rax-imm64-x86` の imm64 byte inline 化と opcode1 depth0 の rooted `emit-mov-imm64` 化は actual metadata が変わらず未採用。次は `append-x86-byte` 全体ではなく、opcode1 direct append 後に result length / current emitted offset が 6 進む条件を actual stage1 metadata mode で絞る。Evidence: `test_linux_x86_representative_seed_can_print_function_segment_metadata`, manual VM metadata `/tmp/lsharp-ir-prefix-stage1-v1/actual-stage1/program.native`, rejected `/tmp/lsharp-imm64-inline-stage1-v1/actual-stage1/program.native`, rejected `/tmp/lsharp-i64-depth0-rooted-stage1-v1/actual-stage1/program.native`。
    - 2026-05-20 追加進捗: fresh full VM `low-arity-shadowless-vm-v1` では stage1→stage2 transport 生成までは完走したが、stage2→stage3 の `actual-stage2 0..1` で引き続き SIGSEGV となり、metadata は `function_idx=1102` / row1 `opcode=54(vector-new)` / offset `49` / size `7` の emitted bytes が `00 00 00 00 00 00 00` であることを示した。`generate-native-control-instr-bundle-loop-x86-with-context` に opcode54 の direct helper-call append branch を追加した結果、fresh artifact `vector-new-direct-stage1-v2` の metadata では同 row が `81 e8 45 dd 12 00 89` となり、row1 のゼロ化は解消した。Evidence: `test_native_codegen_x86_vector_new_runtime_call_direct_appends_in_stage1`, `cargo run -q --bin lsharp -- parse selfhost/src/Backend/Native/NativeCodegen.ls`, `cargo run -q --bin lsharp -- check selfhost/src/Backend/Native/NativeCodegen.ls`, `test_e2e_native_linux_x86_host_generates_actual_selfregen_stage1_bundle_artifact`, manual VM materialization `/tmp/lsharp-vector-new-direct-stage1-v2` metadata `1092..1093`。
    - 2026-05-20 追加進捗: `vector-new` direct append 後の manual stage2 prefix rerun では新しい crash が `segment=1046` / `function_idx=1056` / relative `42` に移り、metadata は row1 `opcode=64(read-file)` / size `7` の emitted bytes が `00 00 00 00 00 00 00` であることを示した。`generate-native-control-instr-bundle-loop-x86-with-context` に opcode64 の direct helper-call append branch を追加した結果、fresh artifact `read-file-direct-stage1-v1` の metadata では同 row が `81 e8 bb 49 15 00 89` となり、read-file row1 のゼロ化も解消した。Evidence: `test_native_codegen_x86_read_file_runtime_call_direct_appends_in_stage1`, `cargo run -q --bin lsharp -- parse selfhost/src/Backend/Native/NativeCodegen.ls`, `cargo run -q --bin lsharp -- check selfhost/src/Backend/Native/NativeCodegen.ls`, `test_e2e_native_linux_x86_host_generates_actual_selfregen_stage1_bundle_artifact`, manual VM materialization `/tmp/lsharp-read-file-direct-stage1-v1` metadata `1046..1047` / `1092..1093`。
    - 2026-05-20 追加進捗: broad preserving-helper branch 追加は selfhost parse/check を壊したため不採用とし、fresh metadata の zero row に従って opcode51 `string-length` だけを narrow direct append した。fresh artifact `string-length-direct-stage1-v1` の VM metadata では segment `1` / function `11` の opcode51 rows が `81 e8 ... 89` へ変わり、全 segment scan でも preserving runtime helper opcode (`51/52/57/59/61/64/73`) の size 7 zero row は `0` 件になった。Evidence: `test_native_codegen_x86_string_length_runtime_call_direct_appends_in_stage1`, `cargo test -q -p lsharp-wasm --test e2e runtime_call_direct_appends_in_stage1 -- --nocapture`, `cargo test -q -p lsharp-wasm --test e2e x86_low_arity -- --nocapture`, `test_e2e_native_linux_x86_host_generates_actual_selfregen_stage1_bundle_artifact`, manual VM materialization `/tmp/lsharp-string-length-direct-stage1-v1` metadata `1..2` / all-segment scan。
    - 2026-05-20 追加進捗: `string-length-direct-stage1-v1` からの manual stage1→stage2 rerun では stage2 最小 chunk が `segment=1034` / `function_idx=1044` / row2 `opcode=35(i64.ge)` の size 9 zero row で停止した。metadata mode に `9000000022` expected emitted bytes row を追加し、fresh artifact `i64-ge-emitted-diagnostic-stage1-v1` / `generic-native-root-stage1-v1` / `plain-two-helper-root-stage1-v1` で row2 の actual segment bytes は `00 00 00 00 00 00 00 00` のまま、同じ row の `codegen-ir-instr` expected bytes は `72 57 193 15 157 192 15 182` と確認した。`emit-i64-ge-x86` fixed chunks、fallback `native` root、plain two-to-one helper root は artifact metadata が変わらず不採用。shallow direct append は静的 test のみ通り、`cargo run -q --bin lsharp -- check selfhost/src/Backend/Native/NativeCodegen.ls` が embedded component recursion failure になったため不採用。次は direct branch 追加ではなく、actual stage1 の control-loop fallback が produced vector を zero-filled segment として append する条件を、segment/window 診断でさらに分離する。Evidence: `test_linux_x86_metadata_correlates_i64_ge_expected_emitted_bytes`, `test_e2e_native_linux_x86_host_generates_actual_selfregen_stage1_bundle_artifact`, manual VM metadata `/tmp/lsharp-i64-ge-emitted-diagnostic-stage1-v1/actual-stage1`, `/tmp/lsharp-generic-native-root-stage1-v1/actual-stage1`, `/tmp/lsharp-plain-two-helper-root-stage1-v1/actual-stage1`。
    - 2026-05-20 追加進捗: metadata mode の `opcode=35(i64.ge)` 診断に `9000000023` scratch append row を追加し、fresh artifact `i64-ge-scratch-append-diagnostic-stage1-v1` で `codegen-ir-instr` expected bytes と fresh `append-native-bytes-loop` の scratch bytes がともに `72 57 193 15 157 192 15 182` になる一方、real control-loop 生成 segment の row2 bytes は `00 00 00 00 00 00 00 00` のままと確認した。これにより standalone emitter と standalone append は blocker から外れ、次の境界は同一 ctx/state で `generate-native-control-instr-bundle-loop-x86-with-context` を row2 だけ replay したときの fallback native / appended window / final segment の相関に絞る。Evidence: `test_linux_x86_metadata_correlates_i64_ge_expected_emitted_bytes`, manual VM metadata `/tmp/lsharp-i64-ge-scratch-append-diagnostic-stage1-v1/actual-stage1`。
    - 2026-05-20 追加進捗: metadata mode に `9000000024` exact fallback / `9000000025` scratch append / `9000000026` single-row control-loop replay を追加し、fresh artifact `i64-ge-control-replay-diagnostic-stage1-v1` の VM metadata で exact fallback と scratch append は `72 57 193 15 157 192 15 182`、fresh `generate-native-control-instr-bundle-loop-x86-with-context` replay だけが `00 00 00 00 00 00 00 00` になることを固定した。これにより `codegen-ir-instr` / `emit-consume-two-bundle-x86` / `append-native-bytes-loop` 単体ではなく、巨大 control-loop fallback 文脈の `native` 生成+append が zero 化する failure point と確定。次の修正候補は broad append helper や spill floor ではなく、`x86-plain-two-to-one-needs-window-restore` fallback の生成+append だけを小さい helper に切り出すこと。Evidence: `test_linux_x86_metadata_replays_i64_ge_control_loop_single_row`, `test_e2e_native_linux_x86_host_generates_actual_selfregen_stage1_bundle_artifact`, manual VM metadata `/tmp/lsharp-i64-ge-control-replay-diagnostic-stage1-v1/actual-stage1`。
    - 2026-05-20 追加進捗: `x86-plain-two-to-one-needs-window-restore` fallback の生成+append を `append-plain-two-to-one-codegen-bundle-x86` へ切り出し、巨大 control-loop `let [native ...]` から外した。fresh artifact `plain-two-helper-append-fix-stage1-v1` の VM metadata では segment `1034` / row2 `opcode=35(i64.ge)` の actual bytes が `72 57 193 15 157 192 15 182` へ回復し、single-row control replay `9000000026` も同じ bytes になった。次は full hostgen VM script で stage2/stage3 の次の停止点または完走を確認する。Evidence: `test_native_codegen_x86_plain_two_to_one_fallback_appends_in_helper`, `cargo run -q --bin lsharp -- parse selfhost/src/Backend/Native/NativeCodegen.ls`, `cargo run -q --bin lsharp -- check selfhost/src/Backend/Native/NativeCodegen.ls`, `test_e2e_native_linux_x86_host_generates_actual_selfregen_stage1_bundle_artifact`, manual VM metadata `/tmp/lsharp-plain-two-helper-append-fix-stage1-v1/actual-stage1`。
    - 2026-05-20 追加進捗: `plain-two-helper-append-fix` 後の full VM は stage2 生成物の `segment=1034` で次の zero row へ進み、row3 `opcode=41(if)` と row5 `opcode=79(else)` が zero-filled になることを確認した。metadata mode に `9000000027` exact control / `9000000028` scratch append / `9000000029` single-row replay を追加し、`emit-control-instr-x86` と scratch append は期待 bytes を返す一方、control-loop replay だけ zero になる failure point を固定した。control-loop fallback から到達不能な opcode40 branch を除去して parse/check の分岐圧を下げたうえで、opcode41/79 の生成+append を `append-control-if-instr-x86` / `append-control-else-instr-x86` へ切り出した。fresh artifact `control-if-else-helper-stage1-v1` の VM metadata では row3 が `85 c0 0f 84 0f 00 00 00`、row5 が `e9 cb 02 00 00` へ回復した。Evidence: `test_linux_x86_metadata_replays_control_if_control_loop_single_row`, `test_native_codegen_x86_control_loop_omits_unreachable_opcode40_fallback`, `test_native_codegen_x86_control_loop_fallback_native_uses_helper`, `test_native_codegen_x86_control_if_fallback_appends_in_helper`, `test_native_codegen_x86_control_else_fallback_appends_in_helper`, `cargo run -q --bin lsharp -- parse selfhost/src/Backend/Native/NativeCodegen.ls`, `cargo run -q --bin lsharp -- check selfhost/src/Backend/Native/NativeCodegen.ls`, `test_e2e_native_linux_x86_host_generates_actual_selfregen_stage1_bundle_artifact`, manual VM metadata `/tmp/lsharp-control-if-else-helper-stage1-v1/actual-stage1`。
- [ ] `V2-14` Native-only official release artifact layout — official release asset を native-only archive に切り替える package layout を定義し、host launcher + embedded guest component は rollback compatibility へ降格する。現状 blocker: `scripts/release.sh` / `scripts/ci/release-smoke.sh` は `lsharp.component.wasm` を必須化している。Evidence target: `test_e2e_native_ops03_official_native_only_replacement_backlog_contract`。
- [ ] `V2-15` Native-only official release smoke and rollback — native-only official archive の smoke / checksum / signing / rollback anchor を Tier1 へ拡張し、experimental RC ではなく stable 既定導線にする。現状 blocker: `.github/workflows/release.yml` は host launcher matrix + Darwin arm64 experimental RC の併用。Evidence target: `test_e2e_native_ops03_official_native_only_replacement_backlog_contract`。
- [x] `SH-DET-01` selfhost compile determinism — 同一 source graph の repeated direct compile determinism を固定し、direct determinism slice 44 件・focused rerun 6 件・snapshot 14 件・`cargo clippy -q -- -D warnings` で確認済み。Evidence: `test_e2e_selfhost_cli_direct_multifile_compile_is_deterministic`, `crates/lsharp-wasm/tests/snapshot.rs`, `selfhost/src/Backend/Wasm/Compiler.ls`, `selfhost/src/Backend/Wasm/CompilerBase.ls`

> **2026-04-01 進捗メモ**（上の `[ ]` 残件カウントは変えない）:
> - **マルチファイル型検査**: `crates/lsharp-ir/src/lib.rs` の `compile_multi_file` が `Tools.Text.FormatterExpr` / `FormatterDecl` / `Formatter` を import マージした疑似 1 プログラムとしてバッチ型推論（`try_infer_formatter_trio_batch`）。`FormatterExpr.ls` の再帰整形は `format-expr` / `format-expr-with-source`（`Formatter.ls` のディスパッチ）へ委譲し、単体モジュール順の `format-expr` 未束縛を避ける。
> - **LSP**: STR-02 分割で欠けていた `json-rpc-dispatch` / `server-loop-step` / `server-loop-sequence` / `server-loop` を `selfhost/src/Tools/Lsp/LspServer.ls` に復元。`LspServerNav.ls` に `Syntax.Parser` / `Tools.Text.FormatterDecl` / `JsonRpc` / `LspServerCore` を接続。E2E の `App.Main` harness は `LspServerCore` + `LspServerNav` + `LspServer` を結合（`crates/lsharp-wasm/tests/e2e/support.rs`）。
> - **E2E / マトリクス**: `test_e2e_selfhost_lsp_hier_fixture_shape_cross_document_definition`。`docs/development/planning/compatibility-matrix.md`（goto_definition）と `docs/development/planning/completion-criteria.md`（Formatter トリオ注記）を同期。
> - **CP-06 文書**: `docs/development/operations/branch-protection-checklist.md`（required checks / branch protection の人手用チェックリスト。GitHub 設定そのものはリポジトリ側作業）。
> - **Bootstrap / CI**: `scripts/ci/compile-phase11-inputs.sh` が selfhost/stdlib/examples の fixed input set compile gate と optional fixed-point verify を持ち、`RUN_BOOTSTRAP_FIXED_POINT=1` では `test_e2e_bootstrap_fixed_point_stage2_stage3`・`test_e2e_bootstrap_stage2_self_feed_fixed_input_set`・`test_e2e_bootstrap_fixed_input_set_stage_chain_match` を exact 実行する。`.github/workflows/ci.yml` bootstrap job は `RUN_BOOTSTRAP_FIXED_POINT: 1` と `bootstrap-diff-${{ github.sha }}` upload (`if: always()`) を接続し、ローカル再実行も exit 0 を確認した。
> - **Bootstrap fixed-point evidence**: historical `App/Cli.ls` compiler-mode memory.grow blocker は stale で、`test_e2e_bootstrap_cli_fixed_input_compile_gate` と import 同期後の `RUN_BOOTSTRAP_FIXED_POINT=1 bash scripts/ci/compile-phase11-inputs.sh` では再現しなかった。現在は `test_e2e_bootstrap_stage2_self_feed_fixed_input_set` が fixed input set 54 件（selfhost 40 / stdlib 11 / examples 3）の stage2 self-feed 決定性を、`test_e2e_bootstrap_fixed_input_set_stage_chain_match` が同じ 54 件の `stage1 -> stage2` 出力と `stage2 -> stage3` 出力の bit-identical compare を artifact (`fixed-input-set-self-feed-report.txt` / `fixed-input-set-self-feed.json` / `fixed-input-set-stage-chain-report.txt` / `fixed-input-set-stage-chain.json`) 付きで固定した。
> - **LSP stateful completion**: `selfhost/src/Tools/Lsp/LspServerNav.ls` が open 済み別 document の top-level `defn` も completion candidate に merge し、label 単位で dedupe するようになった。`crates/lsharp-wasm/tests/lsp_stateful_parity.rs` で stateful / actual `lsp --stdio` parity を固定。
> - **DocTools / HtmlDoc**: `selfhost/src/Tools/Doc/HtmlDoc.ls` は function 名 / type 名 / module title を escape して出力するようになり、`test_e2e_selfhost_htmldoc_render_function_signature_escapes_html` / `test_e2e_selfhost_htmldoc_render_type_definition_escapes_html` / `test_e2e_selfhost_htmldoc_render_module_page_escapes_title` を追加。
> - **Doc trailer parity**: `selfhost/src/Tools/Doc/DocTools.ls` は comment-form trailer (`; Doc-Review-Status:` / `; Doc-Reviewed-By:`) と `doc-check-trailer-valid?` を持つようになり、`selfhost/src/App/Cli.ls` も `doc-ack --trailer` / `doc-check --strict` を file handler / actual `main` まで接続。`test_e2e_selfhost_doctools_doc_check_trailer_validation_*`、`test_e2e_selfhost_cli_doc_ack_file_handler_trailer_only`、`test_e2e_selfhost_cli_doc_check_file_handler_strict_*`、`test_e2e_selfhost_cli_main_with_args_doc_ack_trailer_only`、`test_e2e_selfhost_cli_main_with_args_doc_check_strict_*` が green。
> - **README / book**: `README.md` と `book/ch15-selfhosting.md` を host launcher + embedded guest component / `wasi-component` + `web-wasm` 前提へ同期。
> - **Builtin/API drift cleanup**: stray `str` builtin は `crates/lsharp-types/src/infer.rs` から除去し、`book/ch10-traits.md` の例も `int-to-string` + `string-concat` に揃えた。これで `(str ...)` は lowering まで漏れず、type inference 段階で通常の未定義変数として止まる。
> - **CP-06 / GitHub 設定**: `gh api repos/biwakonbu/lsharp/branches/main/protection` で `CI Gate v2` required check / strict update / PR review / conversation resolution / admin enforcement を再確認し、`gh api repos/biwakonbu/lsharp/actions/workflows` で `CI` / `Release` workflow がともに active であることを確認した。実署名 credential 投入と実 tag / GitHub Release publish は repo 外運用として TODO blocker から切り離す。
> - **GC-06 contract**: `gc_metrics_contract.rs` に collector proof fixture を追加し、`scripts/ci/collect-gc-metrics.sh` は `s15_proof.gc_mode` / `s16_proof.gc_mode` を collector mode (`mark-sweep` / `generational`) に制限、さらに `s16_status = pass` では canonical workload set の complete declaration を必須化した。加えて `LSHARP_GC_PROOF_BUNDLE_INPUT=/path/to/collector-proof.json` で external `s15_status` / `s15_proof` / `s16_status` / `s16_proof` を `summary.json` へ merge して同一 validator に通せるようにし、future collector job が proof slot を artifact へ注入する repo-controlled 経路を追加した。`test_gc_metrics_script_rejects_fixture_payload_with_noncollector_s15_gc_mode` / `test_gc_metrics_script_rejects_fixture_payload_with_incomplete_s16_workload_set` / `test_gc_metrics_script_accepts_fixture_payload_with_valid_s15_s16_proofs` / `test_gc_metrics_script_merges_external_collector_proof_bundle_into_summary` / `test_gc_metrics_script_rejects_invalid_external_collector_proof_bundle` が green。
> - **GC-06 sidecar wiring**: `scripts/ci/collect-gc-metrics.sh` は `summary.json` と同じ directory の sibling `collector-proof.json` を env 指定なしでも自動検出して merge できるようになり、さらに validator 通過後は current `s15_*` / `s16_*` slot を持つ normalized `collector-proof.json` を常に書き戻すようになった。`.github/workflows/ci.yml` の `gc-metrics-artifact` job も `ci-artifacts/gc-metrics/{sha}/summary.json` 単体ではなく directory 全体を upload して stable proof sidecar を保持する。`test_gc_metrics_script_autoloads_adjacent_collector_proof_bundle`, `test_gc_metrics_script_writes_default_collector_proof_sidecar`, `test_gc_metrics_script_normalizes_collector_proof_sidecar_after_merge`, `test_e2e_gc06_ci_artifact_contract` が green。
> - **GC-06 blocked reasons / actual proofs**: `summary.json` は `s14_reason` / `s15_reason` / `s16_reason` を持つようになり、`blocked` / `n/a` の理由 (`collector_heap_series_missing` / `collector_fixed_point_artifact_missing` / `collector_workload_artifact_missing` / `allocator_mode_bump`) を machine-readable に保持する。今回 `test_e2e_alloc_metrics_ci_artifact_payload` は actual collector runtime payload に `s15_proof`（`stage2`/`stage3` fixed-point compare）と `s16_proof`（canonical workload set 完了）を実測で載せるようになり、`scripts/ci/collect-gc-metrics.sh` も fresh payload が既に持つ actual proof を stale な隣接 sidecar で上書きしない。`collector-proof.json` sidecar は current `s15_*` / `s16_*` slot を mirror する正本として維持され、`test_gc_metrics_script_rejects_fixture_payload_with_invalid_blocked_s14_reason`, `test_gc_metrics_script_rejects_fixture_payload_with_invalid_blocked_s15_reason`, `test_gc_metrics_script_adjacent_proof_bundle_does_not_override_actual_payload_proofs`, `test_e2e_alloc_metrics_ci_artifact_payload`, `bash scripts/ci/collect-gc-metrics.sh` が contract を固定する。
> - **Bootstrap / compiler-mode repair**: `selfhost/src/Backend/Wasm/Compiler.ls` は `file-exists?` builtin hash を opcode `73` として lowering し、`selfhost/src/Backend/Wasm/WasmEmit.ls` / `selfhost/src/App/CompilerMode.ls` は compiler-mode import model を 7-import (`alloc` / `print` / `read-file` / `command-line-arg` / `string-concat` / `substring` / `file-exists?`) に拡張、`register-all-pairs` の user function index 起点も `7` に同期した。これで `test_e2e_selfhost_compiler_file_exists_builtin_lowering` / `test_e2e_selfhost_wasmemit_file_exists_instr` / `test_e2e_boot04_self_hosted_stage2_compiles_main_again` / `test_e2e_bootstrap_fixed_point_stage2_stage3` / `test_e2e_bootstrap_fixed_input_set_stage_chain_match` が再 green になり、`ModuleResolver.ls` の package-root 経路で `file-exists?` を踏む stage2 self-compile も復旧した。
> - **Selfhost Runtime.GC executable slice**: `selfhost/src/Runtime/GC.ls` は mutable `gc-state` / `root-set` / `free-list` / live-object table を持つ最小 mark-sweep 実装へ更新し、fixture import できるよう `crates/lsharp-wasm/tests/e2e/support.rs` に `GC.ls` embedding を追加した。`test_e2e_selfhost_gc_collect_reclaims_unrooted_allocations` と `test_e2e_selfhost_gc_collect_preserves_rooted_allocations_and_reuses_freed_block` により、module 単体で unrooted allocation の回収・root 保持・free-list 再利用を実行確認できるようになった。
> - **Root runtime API / root stack slice**: `crates/lsharp-types/src/infer.rs` と `crates/lsharp-ir/src/lower/expr.rs` は `root_push` / `root_pop` / `root_set` を internal runtime helper として扱うようになり、Rust/WASI runtime (`crates/lsharp-wasm/src/wasi.rs`) は reserved root-stack region + top global を使う actual `root_push` / `root_pop` / `root_set` helper を持つようになった。`test_e2e_root_runtime_api_tracks_slots_and_values` と `test_e2e_root_runtime_api_preserves_argument_evaluation` により slot handle / LIFO pop / 引数評価順を実行確認し、今回さらに `selfhost/src/Backend/Wasm/WasmEmit.ls` / `selfhost/src/App/CompilerMode.ls` / `selfhost/src/App/Cli.ls` も opcode `74` / `75` / `76` を actual `env.root_push` / `env.root_pop` / `env.root_set` import call へ落とすように同期した。`crates/lsharp-wasm/tests/e2e/selfhost_bootstrap_four_layer.rs` の compiler-mode harness も root stack state を持つように更新され、`test_e2e_selfhost_compiler_root_*`, `test_e2e_selfhost_wasmemit_root_*`, `test_e2e_selfhost_compiler_mode_root_runtime_api_works`, `test_e2e_boot04_stage2_compiler_to_stage3_minimal`, `test_e2e_boot04_self_hosted_stage2_compiles_main_again`, `test_e2e_boot04_self_hosted_stage2_reports_main_again_build_progress`, `test_e2e_boot04_compiler_mode_import_resolution`, `test_e2e_selfhost_cli_compile_functions_data_with_cache_reuses_clean_hit` が green。
> - **Actual Wasm runtime telemetry slice**: `crates/lsharp-wasm/src/wasi.rs` は language surface を増やさず internal telemetry export (`__lsharp_heap_ptr` / `__lsharp_heap_start` / `__lsharp_alloc_count` / `__lsharp_root_stack_top`) を持つようになり、bump allocator と root stack の実際の進み方を実行後に読み出せるようになった。`crates/lsharp-wasm/tests/e2e/support.rs` に runtime telemetry harness を追加し、`test_e2e_runtime_telemetry_exports_heap_usage_and_alloc_count` と `test_e2e_runtime_telemetry_tracks_root_stack_depth`、さらに `cargo test -q -p lsharp-wasm --lib wasi::tests:: -- --nocapture` が green。これにより collector 未接続の現段階でも、future `collector-proof.json` / heap series 収集へ寄せる actual runtime observability の足場ができた。
> - **Actual collector runtime slice**: `crates/lsharp-wasm/src/wasi.rs` は object-table / free-list side metadata と GC counter export (`__lsharp_gc_live_alloc_count` / `__lsharp_gc_free_list_count` / `__lsharp_gc_collection_count` / `__lsharp_gc_freed_count`) に加え、host から明示的に叩ける `__lsharp_gc_collect` を持つ collector runtime へ拡張した。通常の `__alloc` は selfhost proxy workload を壊さない bump-compatible path を維持しつつ、`crates/lsharp-wasm/tests/e2e/support.rs` の repeated `_start` telemetry harness が run ごとに `__lsharp_gc_collect` を挟んで real collector-mode heap series を採取する。さらに collector 自体も per-entry mark state をクリアして root stack から fixed-point trace を回し、`Ref` / `Vector` / `HashMap` / `Closure` / `Record` / `ADT` payload を transitively mark できるようになった。`test_e2e_runtime_collector_reuses_unrooted_allocations_across_repeated_start_series` / `test_e2e_runtime_collector_preserves_direct_rooted_string_across_trigger` / `test_e2e_runtime_collector_preserves_string_reachable_through_rooted_ref_cell` / `test_e2e_runtime_collector_preserves_string_reachable_through_rooted_map_value` / `test_e2e_runtime_collector_skips_tombstoned_map_value_when_tracing` / `test_e2e_alloc_metrics_ci_artifact_payload` / `test_e2e_gc_lsp_actual_stdio_repeated_sequence_soak` / `test_e2e_lsp_edge_empty_document_hover_stdio` / `cargo test -q -p lsharp-wasm --lib wasi::tests:: -- --nocapture` が green で、collector-mode artifact payload の `heap_bytes_series` / `gc_*` counters と `s15_proof` / `s16_proof` も actual runtime 由来へ切り替わった。残る CP-05 blocker は legacy `0` sentinel compatibility と compiler-side GC-safe point spill / shadow stack 完全列挙側。
> - **Auto-root builtin safe-point slices**: Rust lowering (`crates/lsharp-ir/src/lower/expr.rs`) は allocating builtin に対する conservative な `root_push` / `root_pop` 自動挿入を `string-concat` / `substring` から始め、今回さらに `ref-new` と reallocate branch を取る `vector-push`、direct user-defined call (`func_indices`) と trait static dispatch (`resolve_trait_dispatch`) の heap-like 引数、closure indirect (`ctx.locals_map` / `CallIndirect`) の closure receiver 自体、`FuncCtx.local_type_names` を使った注釈付き param / 型名推定できる `let` local の heap 引数判定、`if` の同型 branch / `do` 最終式 / `match` の全 arm body が同じ既知型の case / inferable `let` 式 body に対する structural return-type 推定、`RecordUpdate` が base と同じ record 型を返す case、lambda literal が closure object を返す case、zero-arg builtin `read-stdin` の `String` 戻り値ヒント、さらに top-level scope + `Span` keyed な expr-type 結果を `Infer` から `Lower` へ渡し、top-level 多相関数や local generic closure の principal type について既知の引数型名から戻り型変数を具体化できる case（例: generic identity や `let [id (fn [x] x)] ...` の戻り値 string）、その expr-type table を merged / modular / incremental multi-file compile path にも通した slice、`map-insert` の probe loop backedge をまたぐ heap value、map probe loop 全体 (`map-insert` / `map-get` / `map-contains?` / `map-remove`) の heap-like key と string-key hash call、さらに self-TCO loop の heap-like param を loop entry 前 root slot + backedge 前 `root_set` で保持する slice まで広げた。GC-safe point 直前に source string / wrapped value / old vector / pushed value / call arguments / closure receiver / map key/value / self-TCO heap param を local spill + root stack 登録するようになり、`vector-push` realloc branch の extra `root_pop` も除去して persistent root slot を巻き込まないようにした。`test_lower_string_concat_auto_roots_arguments`, `test_lower_substring_auto_roots_source_string`, `test_lower_ref_new_auto_roots_wrapped_heap_value`, `test_lower_vector_push_auto_roots_realloc_inputs`, `test_lower_vector_push_balances_root_push_and_pop`, `test_lower_map_insert_roots_heap_value_across_loop_backedge`, `test_lower_map_insert_does_not_root_int_value_across_loop_backedge`, `test_lower_map_insert_roots_heap_key_across_loop_backedge`, `test_lower_map_get_roots_heap_key_across_loop_backedge`, `test_lower_map_contains_roots_heap_key_across_loop_backedge`, `test_lower_map_remove_roots_heap_key_across_loop_backedge`, `test_lower_map_get_does_not_root_int_key_across_loop_backedge`, `test_lower_user_call_auto_roots_string_argument`, `test_lower_user_call_auto_roots_read_stdin_argument`, `test_lower_user_call_auto_roots_record_update_argument`, `test_lower_user_call_auto_roots_lambda_argument`, `test_lower_user_call_auto_roots_vector_argument`, `test_lower_user_call_auto_roots_generic_identity_result_argument`, `test_lower_user_call_auto_roots_local_generic_closure_result_argument`, `test_lower_user_call_selectively_roots_heap_arguments`, `test_lower_self_recursive_user_call_preserves_self_tco`, `test_lower_self_recursive_heap_param_roots_loop_entry_and_updates_slot`, `test_lower_self_recursive_int_params_do_not_emit_root_updates`, `test_lower_trait_method_auto_roots_string_argument`, `test_lower_trait_method_does_not_root_int_argument`, `test_lower_trait_method_selectively_roots_heap_arguments`, `test_lower_closure_call_roots_closure_receiver`, `test_lower_closure_call_roots_receiver_before_string_arg_eval`, `test_lower_closure_call_roots_annotated_string_param_argument`, `test_lower_closure_call_roots_let_bound_string_argument`, `test_lower_closure_call_roots_let_bound_if_string_argument`, `test_lower_closure_call_roots_do_string_argument`, `test_lower_closure_call_roots_match_string_argument`, `test_lower_closure_call_roots_let_bound_match_string_argument`, `test_lower_closure_call_roots_let_expr_string_argument`, `test_lower_closure_call_roots_let_expr_if_string_argument`, `test_lower_closure_call_roots_read_stdin_argument`, `test_lower_closure_call_roots_record_update_argument`, `test_lower_closure_call_roots_lambda_argument`, `test_lower_closure_call_roots_generic_identity_result_argument`, `test_lower_closure_call_roots_local_generic_closure_result_argument`, `test_compile_multi_file_closure_call_roots_local_generic_result_argument`, `test_compile_multi_file_incremental_roots_local_generic_closure_result_argument`, `test_e2e_boot04_bootstrap_append_bytes_deep_recursion_trap_repro`, `test_e2e_boot04_bootstrap_append_bytes_recursion_depth_boundary`, `test_e2e_trait`, `test_e2e_trait_where`, `test_e2e_trait_multiple_impls`, `test_e2e_lambda_no_free_vars`, `test_e2e_closure_capture_and_call`, `test_e2e_closure_multiple_captures`, `test_e2e_closure_no_capture`, `test_e2e_map_insert_get`, `test_e2e_map_string_key_insert_get`, `test_e2e_map_string_key_contains`, `test_e2e_map_string_key_remove`, `test_e2e_bootstrap_stage1_emits_stage2_wasm_for_string_key_map_program`, `test_e2e_bootstrap_stage1_emits_stage2_wasm_for_non_literal_string_key_map_program`, `test_snapshot_wasm_closure` が green。自己再帰 direct call は `append-loop` の self-TCO を壊さない carve-out に加え、heap-like param を loop span 全体で rooted slot へ更新できるようになった。現 pipeline では generated `__lambda_*` は closure object 経由でのみ呼ばれるため `lifted_func_indices` branch は実用上未到達で、このトラックは close 扱いにできる。**ただし** principal type 上で多相のまま残る generic closure indirect heap 引数、known arg type name だけでは戻り型を具体化できない opaque 値、structural に heap 型を確定できない無注釈値、および selfhost output parity までは未拡張。
> - **BOOT-04 string literal data layout contract**: selfhost source-aware path (`selfhost/src/Backend/Wasm/Compiler.ls`) の `compile-string-literal-with-source` は string literal を raw bytes ではなく inline string object (`tag=1`, `len`, `bytes`) として data section に連結する。`crates/lsharp-wasm/tests/e2e/selfhost_bootstrap_four_layer.rs` の `*_string_literal_data_section*` はこの header-aware layout と arm / do 順序に合わせて更新し、fixed-point 系 bootstrap gate (`test_e2e_bootstrap_stage2_compiler_wasmemit_modules_deterministic`, `test_e2e_bootstrap_stage2_self_feed_fixed_input_set`, `test_e2e_bootstrap_fixed_point_stage2_stage3`, `test_e2e_bootstrap_fixed_input_set_stage_chain_match`) を再度保つ。
>   - 追補: `should_root_user_call_argument()` は unknown non-immediate fallback を持つようになり、`test_lower_user_call_conservatively_roots_opaque_call_result_argument` / `test_lower_closure_call_conservatively_roots_generic_param_argument` により Rust lowering 側の residual generic / opaque / untyped general-call surface も conservative に保護される。selfhost root runtime import parity と actual collector の transitive trace map も landed し、残る gap は sentinel compatibility と compiler-side GC-safe point spill / shadow stack 完全列挙。

- [x] `CP-01` true bootstrap fixed point を再閉鎖した（`scripts/ci/compile-phase11-inputs.sh`, `test_e2e_bootstrap_stage2_self_feed_fixed_input_set`, `test_e2e_bootstrap_fixed_input_set_stage_chain_match`, `docs/development/operations/bootstrap-diff-artifacts.md`）
  - `scripts/ci/compile-phase11-inputs.sh` と `test_e2e_bootstrap_stage2_self_feed_fixed_input_set` により、fixed input set 54 件の stage2 self-feed 決定性（single-module recovery / valid wasm / byte-identical output）を CI 経路と artifact 付きで確認した
  - さらに `test_e2e_bootstrap_fixed_input_set_stage_chain_match` により、同じ 54 件について `stage1 -> stage2` 出力と `stage2 -> stage3` 出力の full-set compare を固定し、Gate 1 を close した

- [x] `CP-04` public toolchain parity を再閉鎖した（docs/development/planning/compatibility-matrix.md, `test_e2e_gc_lsp_actual_stdio_repeated_sequence_soak`, `test_cmd_doc_site_generates_guides_and_api_site`）
  - imported module の filesystem-backed multi-file / nested LSP parity を harness + actual `lsp --stdio` representative snapshot まで固定
  - stateful `lsp --stdio` / transport / long-lived session parity を `lsp_stateful_parity.rs` broad suite と actual repeated-sequence soak まで固定
  - DocTools / HtmlDoc / DocJson / release Quick Start distribution flow を schema / HTML / archive smoke で固定
  - 2026-04-04: `test_e2e_gc_lsp_actual_stdio_repeated_sequence_soak` と `cargo test -q -p lsharp-driver test_cmd_doc_site_generates_guides_and_api_site -- --nocapture` まで green を再確認
- 追補: `didOpen` / `didChange` の parse `publishDiagnostics` refresh を transport helper + actual `lsp --stdio` snapshot で固定し、stdio body parser は legacy `source` に加えて spec 寄り `textDocument.text` / `contentChanges[0].text` も受理する。shared nav parser は hover / definition / references / completion の `position.character` を `col` として扱え、専用 rename parser も `textDocument.uri` + `position.character` + `newName` を扱えるようにし、spec 寄り open-document hover / definition / references / completion / rename を actual `lsp --stdio` で固定した。さらに hover / definition / references / completion の didChange 後 / same-URI repeated didOpen 後 latest-source fallback と、rename の didChange 後 / same-URI repeated didOpen 後 latest-source workspace edit も actual `lsp --stdio` + changed/latest-reopened snapshot で固定した。加えて type/lint diagnostics refresh も `App/Cli.ls` transport helper で `check-diagnostics-first-code` / `generate-review` から LSP diagnostics へ落とし込み、actual `lsp --stdio` snapshot (`document-sequence-type-diagnostics-refresh.json`, `document-sequence-lint-diagnostics-refresh.json`) と spec 寄り didOpen/didChange params で固定した。今回さらに stdio body parser の JSON string decode も escape-aware にし、spec 寄り `textDocument.text` / `contentChanges[0].text` に含まれる escaped quote / newline と unicode escaped quote / newline (`\u0022` / `\u000a`) が formatting / completion の最新 source に正しく渡ることを actual `lsp --stdio` で固定した。open document が `path` を持つ場合、filesystem-backed direct/nested import も hover / completion / definition / references / rename まで harness + actual `lsp --stdio` で固定し、未 open import target の definition / references / rename は path-hash virtual URI を返す narrow slice まで到達した。さらに shutdown 済み state への後続 request は `Invalid Request (-32600)` frame で拒否するようになり、transport helper と actual `lsp --stdio` snapshot (`shutdown-request-after-error.json`) で固定した。embedded default path の `review` も `--json` / `--format json`、`doc-ack` も `--trailer`、`doc-check` も `--strict` を `selfhost/src/App/EmbeddedCli.ls` と host launcher delegation gate で処理できるようにし、strict missing-trailer failure と runtime disable 下の option-aware hint 復帰まで `default_path_delegation.rs` と `scripts/ci/default-path-smoke.sh` で固定した。DocTools / HtmlDoc 側も representative doc-site HTML snapshot (`render-guide-page.html`, `render-doc-site-index.html`) を追加した。`cargo test -q -p lsharp-wasm --test lsp_stateful_parity -- --nocapture` は 42 tests green、`cargo test -q -p lsharp-wasm --test doctools_parity -- --nocapture` は 31 passed、`cargo test -q -p lsharp-wasm --test e2e selfhost_doctools_ -- --nocapture` は 45 passed、`cargo test -q -p lsharp-wasm --test e2e filesystem_document_sequence_ -- --nocapture` も 2 passed、`cargo clippy -q` も green。representative filesystem-backed snapshot は spec 寄り long-lived sequence broad gate まで拡張済みで、actual repeated-sequence soak と doc-site generation focused gate まで通過した。

- [x] `CP-05` runtime stability gate を閉じる -- G1 (`i64::MIN + heap_addr` 衝突) を documented limitation として `docs/development/planning/runtime-stability-spec.md` に確定、G2 (selfhost↔Rust output tagging parity) を vector-new/vector-push/ref-new/map-new + import-call helper の audit で確認、G3 (GC-safe point spill 棚卸し) は 4 sub-slice (非自己再帰 heap param / let heap-local / multi-arg operand stack / pattern bound heap field) すべて RED test を書いた結果 GREEN で、`should_root_user_call_argument` (`crates/lsharp-ir/src/lower/expr.rs:1870`) と `emit_root_push_local` (line 1823) と pattern bind rooting で既存実装が shadow stack 規律を満たしていることを確認。regression guard test 4 件を `crates/lsharp-wasm/tests/e2e/runtime_allocator_closures.rs` (`test_e2e_runtime_collector_preserves_non_self_recursive_heap_param` / `_let_heap_local_across_alloc` / `_first_arg_across_second_arg_alloc` / `_pattern_bound_heap_field_across_alloc`) として固定。spec doc に G3 棚卸し結果表を追記。
  - collector 有効の長寿命 REPL / LSP representative workload と stage2/stage3 fixed-point compare は `summary.json` / `collector-proof.json` に `s15_proof` / `s16_proof` として出力できるようになった
  - `docs/development/planning/runtime-stability-spec.md` S14-S16 は `bash scripts/ci/collect-gc-metrics.sh` で `s14/s15/s16 = pass` を出せるところまで artifact / CI 契約に接続された
  - selfhost compiler-mode の `root_push` / `root_pop` / `root_set` parity は actual import call まで回収でき、collector proof artifact / CI 自体も repo-controlled 範囲で閉じた。actual collector も root stack から per-entry mark state + fixed-point trace を回し、`Ref` / `Vector` / `HashMap` / `Closure` / `Record` / `ADT` payload を transitively mark できるようになった。今回さらに selfhost compiler の source-aware direct user call も spill + root auto-insertion へ広げ、加えて `test_e2e_runtime_collector_preserves_string_reachable_through_rooted_closure_capture` と `test_e2e_runtime_collector_ignores_legacy_zero_root_slot_sentinel` により runtime 側の closure capture transitive trace と root-stack legacy `0` sentinel ignore path も固定した。Phase 11 closure 上は、closure/indirect を含む broader heap 値判定は future precision work として切り分け、GC-safe point spill / shadow stack は `runtime-stability-spec.md` の G3 棚卸し GREEN により blocker 扱いしない。
  - `root_push` / `root_pop` / `root_set` の API surface に加え、Rust/WASI runtime 側の explicit root stack helper 自体は配線済みで、Rust lowering も `string-concat` / `substring` / `ref-new` / `vector-push`(再割り当て branch) の allocating boundary、map probe loop、self-TCO loop、closure receiver、注釈付き param / `let` local、さらに `if` 同型 branch / `do` 最終式 / `match` の全 arm body 同型 case / inferable `let` 式 body / `RecordUpdate` の base 同型 case / lambda literal の closure object case のような structural に heap 型を確定できる closure 引数、zero-arg `read-stdin` の monomorphic `String` 戻り値、既知の引数型名から戻り型変数を具体化できる top-level 多相関数 call、top-level scope + `Span` keyed expr-type table で concrete 化できる local generic closure call、さらにその expr-type table を merged / modular / incremental multi-file compile path に通したところに加え、unknown non-immediate fallback まで conservative な automatic root insertion を回収した。selfhost 側も source-aware `string-concat` / `substring` / `ref-new` / `vector-push` / `map-insert` / `map-get` / direct user call は IR parity まで固定できた。broader selfhost closure/indirect parity や sentinel-aware な handle discrimination は、Phase 11 gate を reopen せず future precision work として分離する。
  - 新しく顕在化した論点として、general call site へ根保護を広げるには lowering が expr-level の heap 値判定を持つ必要がある。今回 minimal slice として `crates/lsharp-types/src/infer.rs` は `ref-new` / `ref-get` / `ref-set` と `vector-*` / `map-*` builtin signature を dedicated `Ref` / `Vector` / `Map` へ揃え、`crates/lsharp-ir/src/lower/mod.rs` の type-name extraction も追随させた (`test_ref_new_builtin_returns_ref_type`, `test_vector_builtins_use_vector_type`, `test_map_builtins_use_map_type`, `test_compile_multi_file_incremental_clean_formatter_trio_cache_hit_succeeds`)。加えて `crates/lsharp-types/src/infer.rs` は top-level scope + `Span` keyed な expr-type 結果を保持し、`crates/lsharp-ir/src/lower/mod.rs` / `decl.rs` はそれを consume できるようになり、今回さらに merged / modular / incremental multi-file compile path にも同じ expr-type table を通した。これで `if` の両 branch が同型で既知型のとき、`do` の最終式が既知型のとき、`match` の全 arm body が同じ既知型のとき、inferable `let` 式が binding の local type hint を body へ引き継げるとき、`RecordUpdate` が base と同じ record 型を返すとき、lambda literal が closure object を返すとき、known arg type name から top-level 多相関数の戻り型変数を具体化できるとき、さらに local generic closure call の戻り値が expr-type table 上で concrete になるときに return-type を拾え、`read-stdin` も zero-arg `String` builtin として扱えるようになり、`test_lower_closure_call_roots_let_bound_if_string_argument` / `test_lower_closure_call_roots_do_string_argument` / `test_lower_closure_call_roots_match_string_argument` / `test_lower_closure_call_roots_let_bound_match_string_argument` / `test_lower_closure_call_roots_let_expr_string_argument` / `test_lower_closure_call_roots_let_expr_if_string_argument` / `test_lower_user_call_auto_roots_read_stdin_argument` / `test_lower_closure_call_roots_read_stdin_argument` / `test_lower_user_call_auto_roots_record_update_argument` / `test_lower_closure_call_roots_record_update_argument` / `test_lower_user_call_auto_roots_lambda_argument` / `test_lower_closure_call_roots_lambda_argument` / `test_lower_user_call_auto_roots_generic_identity_result_argument` / `test_lower_closure_call_roots_generic_identity_result_argument` / `test_lower_user_call_auto_roots_local_generic_closure_result_argument` / `test_lower_closure_call_roots_local_generic_closure_result_argument` / `test_compile_multi_file_closure_call_roots_local_generic_result_argument` / `test_compile_multi_file_incremental_roots_local_generic_closure_result_argument` を追加した。incremental path の dirty 伝播も export surface (`results` / `hidden`) だけで判定するよう分離したため、expr-type cache を持っても clean suffix module の segment reuse は壊さない。もっとも既存 selfhost/stdlib source が `0` sentinel で heap handle を表現する慣習をまだ持つため、negative immediate (`-1` など) と heap handle を runtime の high-bit だけで弁別する方針は依然 unsafe で、precise な general call rooting と handle discrimination の残 blocker は sentinel compatibility / selfhost parity 側へ細っている
  - 残りの AST 形も監査済みで、safe な structural hint slice は実質出し切った。`FieldAccess` は current parser が生成せず、`Quote` / `Unquote` / `UnquoteSplice` は macroexpand 後に残らず、`Computation` は builder `bind` / `return` semantics に依存して最終モナド型が決まるため、`if` / `do` / `match` / `let` / `RecordUpdate` / `Lambda` のような局所規則では安全に heap-like 型を確定できない。ここは Phase 11 blocker ではなく future precision work として扱う
  - `selfhost/src/Runtime/GC.ls` は fixture で回収 / root 保持 / free-list 再利用を確認できる最小 mark-sweep として維持しつつ、actual compiled Wasm runtime 側も `crates/lsharp-wasm/src/wasi.rs` の collector mode telemetry / fixed-point / workload artifact 生成に接続された。今回さらに actual collector 自体が `Ref` / `Vector` / `HashMap` / `Closure` / `Record` / `ADT` payload を transitively trace できるようになった
  - selfhost compiler / WasmEmit 側の `root_*` も actual import call まで同期済みで、残る selfhost blocker は heap 値判定と sentinel-aware な handle discrimination を selfhost output parity まで揃える作業に寄った
  - `docs/development/planning/memory-management-roadmap.md` の Phase 1-3（共通 GC header / precise root set / first collector）は runtime / workload harness / Rust allocator path まで partial に配線できたが、gate を閉じるには sentinel compatibility と collector 有効の単一セッション長寿命 workload 実測がまだ必要

- [x] stray `str` builtin と docs drift を整理した（`crates/lsharp-types/src/infer.rs`, `book/ch10-traits.md`, `test_str_is_not_treated_as_builtin`）
  - stray `str : forall a. a -> String` を type env から外し、未実装 builtin が lowering まで漏れないようにした
  - trait example は actual public builtin (`int-to-string`, `string-concat`) に合わせて更新した

- [x] `CP-06` CI / release cutover を再閉鎖した（`.github/workflows/ci.yml`, `.github/workflows/release.yml`, `docs/development/operations/CI.md`, `docs/development/operations/branch-protection-checklist.md`）
  - `gh api` で `main` の branch protection に Actions 表示名 `CI Gate v2` が required check として残っていること、および `CI` / `Release` workflow が active であることを再確認した
  - Phase 11 の mainline gate は host launcher + embedded guest component 構成の確立であり、release signing credential 投入・実 tag / GitHub Release publish・GitHub Releases / stage0 fetch 直結運用は repo 外オペレーションとして TODO blocker から切り離す

---

## Phase 11: Rust 完全撤去

> **直近反映 (2026-03-27)** — 実測・コードベース同期:
> - E2E: `cargo test -p lsharp-wasm --test e2e -- --list` の実測は **683 tests**。`#[ignore]` は `crates/lsharp-wasm/tests/e2e/selfhost_lsp_docs_ops.rs` の GC soak 2 件（`test_e2e_gc_compile_run_loop_1000`, `test_e2e_gc_repl_soak_500_eval`）。ブートストラップ検証の主経路は `try_compile_and_run_file` / `compile_and_run_file`（マルチファイル・import）。インラインソース用の `try_compile_and_run` は将来の最小再現テスト用に **残置**（`crates/lsharp-wasm/tests/e2e/support.rs`、現状 `#[allow(dead_code)]`）。
> - selfhost の正本 entrypoint は `selfhost/src/App/Main.ls`。import-only パイプライン (BOOT-01) はこの正本で維持し、マルチファイル Wasm は `ModuleGraph::topological_sort` でモジュール名・import 名をソートして出力の再現性を担保する。複数 `main` 定義がある場合は **最後**の `main` をエントリにする（`crates/lsharp-wasm/src/wasi.rs`）。
> - `selfhost/src/**` が selfhost compiler の正本 source root。検証・ドキュメント・entrypoint 参照は `selfhost/src/**` を基準に統一する。
> - 旧 stage0 stack overflow（当時 `Lower.ls` / `LowerPattern.ls` 起点で追っていた件）は `lsharp-types` の `Type::apply_subst` ループ化・Var サイクル打ち切りで解消済み。`scripts/ci/compile-phase11-inputs.sh` に含め、`KNOWN_BLOCKERS` なし。
> - `test_e2e_bootstrap_stage1_stage2_match` 等は proxy のまま。加え `test_e2e_bootstrap_stage0_oracle_chain_four_way_identity` で Rust oracle 4 連一致を固定。
> - `scripts/ci/compile-phase11-inputs.sh` は known blocker なしで通過。
> - `bash scripts/audit_docs.sh` は **error 0 / warning 0**。
> - OPS-05 は第1段の `lsharp` バイナリ経路 blocking 化に加え、`crates/lsharp-driver/src/main.rs` の `LSHARP_PATH` が executable / 配置ディレクトリ delegation を持つところまで前進。`crates/lsharp-driver/tests/default_path_delegation.rs` で executable path / directory path / invalid path error を固定。
> - 続く構造化 slice として `selfhost/src/Types/TypeInferFunctions.ls` を新設し、`infer-lambda` / `infer-defn` の param fresh / env 拡張 / curried fun 構築 / finalize を helper へ分離した。`TypeInfer.ls` から 7-arity 展開を撤去し、`test_e2e_selfhost_pipeline_macroexpand_typeinfer_integration`, `test_e2e_selfhost_module_graph_topological_sort`, `test_e2e_selfhost_type_responsibility_separation`, `test_e2e_selfhost_type_hm_core_golden`, `test_e2e_selfhost_typeinfer_float_and_unit_literals`, `test_support_selfhost_typeinfer_runtime_bundle_cached`, `test_e2e_selfhost_main_import_only_pipeline` が green。
> - `CP-02` type slice は standalone `selfhost/src/Types/TypeInfer.ls` check を回復しつつ、field access の最小 parity と quote 系 dispatch parity を更新。covered known-record slice では `Int` を返し、shape 不明時は fresh var fallback、direct record literal field access は AST 直読みで具体型を返し、quote / unquote / unquote-splice は inner expr 推論へ委譲する。`test_e2e_selfhost_typeinfer_field_access`, `test_e2e_selfhost_typeinfer_field_access_fallback_var`, `test_e2e_selfhost_typeinfer_field_access_on_record_literal`, `test_e2e_selfhost_typeinfer_quote_expr`, `test_e2e_selfhost_typeinfer_unquote_expr`, `test_e2e_selfhost_typeinfer_unquote_splice_expr`, `test_e2e_selfhost_main_import_only_pipeline`, `test_e2e_selfhost_pipeline_complete_stages`, `cargo build && cargo test && cargo clippy --quiet && bash scripts/audit_docs.sh` が再 green。
> - 続く小さい slice として direct record literal field access を 2-field case まで広げ、後続 field も AST 直読みで具体型を返せるようにした。`test_e2e_selfhost_typeinfer_field_access_on_record_literal_second_field`、`cargo run --quiet -- check selfhost/src/Types/TypeInfer.ls`、`cargo build && cargo test && cargo clippy --quiet && bash scripts/audit_docs.sh` が再 green。
> - 続く小さい slice として single-step `computation` expression は最終式の型へ委譲する special case を追加した。`test_e2e_selfhost_typeinfer_computation_single_step_bool`、`test_e2e_selfhost_typeinfer_computation_expr`、`cargo run --quiet -- check selfhost/src/Types/TypeInfer.ls`、`cargo build && cargo test && cargo clippy --quiet && bash scripts/audit_docs.sh` が再 green。
> - 続く小さい slice として `infer-lambda` を AST 実レイアウト `[8, param-count, ...]` に合わせ、0/1/2 引数 lambda を compile-safe に扱えるようにした。`test_e2e_selfhost_typeinfer_lambda_two_params_curried`、`cargo run --quiet -- check selfhost/src/Types/TypeInfer.ls`、`test_e2e_selfhost_main_import_only_pipeline`、`test_e2e_selfhost_pipeline_complete_stages`、`cargo build && cargo test && cargo clippy --quiet && bash scripts/audit_docs.sh` が再 green。
> - さらに隣接 slice として `infer-defn` も parser 実レイアウト `[20, name-hash, param-count, ...]` に追従させ、0/1/2 引数 top-level `defn` を compile-safe に扱えるようにした。`test_e2e_selfhost_typeinfer_defn_two_params_curried`、`test_e2e_selfhost_typeinfer_lambda_two_params_curried`、`cargo run --quiet -- check selfhost/src/Types/TypeInfer.ls`、`test_e2e_selfhost_main_import_only_pipeline`、`test_e2e_selfhost_pipeline_complete_stages`、`cargo build && cargo test && cargo clippy --quiet && bash scripts/audit_docs.sh` が再 green。
> - 続く隣接 slice として `infer-apply` も 3 引数までの covered slice を追加し、curried function type を 3 段目まで辿れるようにした。`test_e2e_selfhost_typeinfer_apply_three_args_curried`、`cargo run --quiet -- check selfhost/src/Types/TypeInfer.ls`、`test_e2e_selfhost_main_import_only_pipeline`、`test_e2e_selfhost_pipeline_complete_stages`、`cargo build && cargo test && cargo clippy --quiet && bash scripts/audit_docs.sh` が再 green。
> - 続く隣接 slice として `infer-lambda` / `infer-defn` は 3 引数までの covered slice へ拡張し、3 段のカリー化関数型を返せるようにした。`test_e2e_selfhost_typeinfer_lambda_three_params_curried`、`test_e2e_selfhost_typeinfer_defn_three_params_curried`、`cargo run --quiet -- check selfhost/src/Types/TypeInfer.ls`、`test_e2e_selfhost_main_import_only_pipeline`、`test_e2e_selfhost_pipeline_complete_stages`、`cargo build && cargo test && cargo clippy --quiet && bash scripts/audit_docs.sh` が再 green。
> - さらに `infer-computation` も 2-step covered slice を追加し、`let!` binder を最後の式へ渡し、`do!` は subst を進めた上で最後の式型へ委譲できるようにした。`test_e2e_selfhost_typeinfer_computation_let_bang_bool_binder`、`test_e2e_selfhost_typeinfer_computation_do_bang_bool_return`、`test_e2e_selfhost_typeinfer_computation_single_step_bool`、`cargo run --quiet -- check selfhost/src/Types/TypeInfer.ls`、`test_e2e_selfhost_main_import_only_pipeline`、`test_e2e_selfhost_pipeline_complete_stages`、`cargo build && cargo test && cargo clippy --quiet && bash scripts/audit_docs.sh` が再 green。
> - 続く隣接 slice として `infer-apply` を 4 引数までの covered slice へ拡張し、4 段の curried function type も末尾の戻り型まで辿れるようにした。`test_e2e_selfhost_typeinfer_apply_four_args_curried`、既存 `test_e2e_selfhost_typeinfer_apply_three_args_curried`、`cargo run --quiet -- check selfhost/src/Types/TypeInfer.ls`、`test_e2e_selfhost_main_import_only_pipeline`、`test_e2e_selfhost_pipeline_complete_stages`、`cargo build && cargo test && cargo clippy --quiet && bash scripts/audit_docs.sh` が再 green。
> - 続く隣接 slice として `infer-lambda` / `infer-defn` を 4 引数までの covered slice へ拡張し、4 段のカリー化関数型も返せるようにした。`test_e2e_selfhost_typeinfer_lambda_four_params_curried`、`test_e2e_selfhost_typeinfer_defn_four_params_curried`、既存 3 引数系 regression、`cargo run --quiet -- check selfhost/src/Types/TypeInfer.ls`、`test_e2e_selfhost_main_import_only_pipeline`、`test_e2e_selfhost_pipeline_complete_stages`、`cargo build && cargo test && cargo clippy --quiet && bash scripts/audit_docs.sh` が再 green。
> - 続く隣接 slice として `infer-computation` を 3-step covered slice へ広げ、`let! -> do! -> return` と `do! -> let! -> return` の両方で subst / binder env を最後の式へ渡せるようにした。`test_e2e_selfhost_typeinfer_computation_let_bang_do_bang_return_bool`、`test_e2e_selfhost_typeinfer_computation_do_bang_let_bang_return_bool`、既存 computation regression、`cargo run --quiet -- check selfhost/src/Types/TypeInfer.ls`、`test_e2e_selfhost_main_import_only_pipeline`、`test_e2e_selfhost_pipeline_complete_stages`、`cargo build && cargo test && cargo clippy --quiet && bash scripts/audit_docs.sh` が再 green。
> - 続く隣接 slice として `infer-do` に 6 式 covered slice を追加し、6 個目の式まで subst を通しつつ最後の式型を返せるようにした。`test_e2e_selfhost_typeinfer_do_six_exprs_last_bool`、`cargo run --quiet -- check selfhost/src/Types/TypeInfer.ls`、`test_e2e_selfhost_main_import_only_pipeline`、`test_e2e_selfhost_pipeline_complete_stages`、`cargo build && cargo test && cargo clippy --quiet && bash scripts/audit_docs.sh` が再 green。
> - 続く error parity slice として direct apply の `E0005` に加え、lambda / top-level `defn` / `let` init / `do` subexpression / `computation` step / `match` body・scrutinee failure でも `propagate-error-result` で nested error code を保持するようにした。`test_e2e_selfhost_typeinfer_error_infinite_type_code`, `test_e2e_selfhost_typeinfer_error_lambda_propagates_infinite_code`, `test_e2e_selfhost_typeinfer_error_defn_propagates_infinite_code`, `test_e2e_selfhost_typeinfer_error_let_propagates_infinite_init_code`, `test_e2e_selfhost_typeinfer_error_do_propagates_infinite_code`, `test_e2e_selfhost_typeinfer_error_computation_propagates_infinite_code`, `test_e2e_selfhost_typeinfer_error_match_propagates_infinite_body_code`, `cargo run --quiet -- check selfhost/src/Types/TypeInfer.ls`, `test_e2e_selfhost_main_import_only_pipeline`, `test_e2e_selfhost_pipeline_complete_stages`, `cargo build && cargo test && cargo clippy --quiet && bash scripts/audit_docs.sh` が再 green。
> - 続く slice として record 系 wrapper (`infer-record-fields` / `infer-recordlit` / `infer-fieldaccess` / `infer-recordupdate-node`) でも nested `E0005` を保持し、`match` pattern では undefined constructor と constructor/record child pattern failure が `E0001` のまま観測できるようにした。`infer-pattern-children` は child error code を subst metadata に退避する safer な shape に整理し、standalone `cargo run --quiet -- check selfhost/src/Types/TypeInfer.ls` を壊さずに `test_e2e_selfhost_typeinfer_error_record_literal_propagates_infinite_code`, `test_e2e_selfhost_typeinfer_error_field_access_propagates_infinite_code`, `test_e2e_selfhost_typeinfer_error_record_update_propagates_infinite_code`, `test_e2e_selfhost_typeinfer_error_match_undefined_constructor_pattern_code`, `test_e2e_selfhost_typeinfer_error_match_constructor_child_pattern_code`, `test_e2e_selfhost_typeinfer_error_match_record_child_pattern_code`, `test_e2e_selfhost_main_import_only_pipeline`, `test_e2e_selfhost_pipeline_complete_stages`, `cargo build && cargo test && cargo clippy --quiet && bash scripts/audit_docs.sh` を再 green に戻した。
> - 続く slice では constructor pattern が実際にコンストラクタ引数型を消費しながら subpattern を unify するようになり、arity mismatch で `E0006`、unary constructor binder では body 側へ具体型を渡せるようにした。あわせて `crates/lsharp-types/src/infer.rs` の `infer_decl_functions` で generalize 時に未確定 top-level placeholder をまとめて除外し、import-based standalone check の under-generalize を抑止した。`test_e2e_selfhost_typeinfer_error_match_constructor_arity_mismatch_code`, `test_e2e_selfhost_typeinfer_match_constructor_pattern_binder`, `test_e2e_multi_file_import_open_polymorphic_helper_stays_generalized`, `test_check_selfhost_typeinfer_standalone_import_path`, `cargo run --quiet -- check selfhost/src/Types/TypeInfer.ls`, `test_e2e_selfhost_main_import_only_pipeline`, `test_e2e_selfhost_pipeline_complete_stages`, `cargo build && cargo test && cargo clippy --quiet && bash scripts/audit_docs.sh` が再 green。
> - 途中経過ログにある「その時点のテスト数」および「当時点のテスト数」は、各マイルストーン時点のスナップショットであり、現時点の総数は冒頭の実測値を正とする。
> - 続く evidence slice として `match` の arm 同士の結果型不一致、および scrutinee/pattern 型不一致がどちらも `E0006` を返すことを固定した。`test_e2e_selfhost_typeinfer_error_match_arm_result_mismatch_code`, `test_e2e_selfhost_typeinfer_error_match_pattern_scrutinee_mismatch_code` を追加し、その時点のテスト数は **493** / **492 passed + 1 ignored** になった。
> - 続く deterministic ordering slice として `selfhost/src/Types/TypeScheme.ls` の `free-vars` / `generalize` / `instantiate` を source-order の再帰 helper へ置き換え、4 変数関数型でも束縛順と fresh 化順が崩れないようにした。`test_e2e_selfhost_typescheme_generalize_preserves_four_var_order`, `test_e2e_selfhost_typescheme_instantiate_rewrites_all_bound_vars`, `cargo run --quiet -- check selfhost/src/Types/TypeInfer.ls`, `test_check_selfhost_typeinfer_standalone_import_path`, `test_e2e_selfhost_main_import_only_pipeline`, `test_e2e_selfhost_pipeline_complete_stages`, `cargo build && cargo test && cargo clippy --quiet && bash scripts/audit_docs.sh` が再 green。
> - 続く record deterministic slice として record field 型も `TypeScheme` の `free-vars` / `instantiate` が左から辿るようにし、record field の自由変数順と fresh 化順が安定するようにした。`test_e2e_selfhost_typescheme_generalize_record_field_vars`, `test_e2e_selfhost_typescheme_instantiate_record_field_vars` を追加し、その時点のテスト数は **497** / **496 passed + 1 ignored** になった。
> - 続く syntax slice として selfhost `Parser.ls` の `match` arm pattern に最小 `parse-pattern-v3` を導入し、`_` を `ast-pat-wildcard` として返すようにした。`test_e2e_selfhost_parser_match_wildcard_pattern`, `cargo run --quiet -- check selfhost/src/Types/TypeInfer.ls`, `test_e2e_selfhost_pipeline_complete_stages`, `cargo build && cargo test && cargo clippy --quiet && bash scripts/audit_docs.sh` を再通過し、その時点のテスト数は **498** / **497 passed + 1 ignored** になった。
> - 続く syntax/type slice として同じ helper を symbol pattern にも広げ、selfhost parser が `match` arm の通常 symbol を `ast-pat-var` で返し、selfhost `TypeInfer` でも canonical `ast-pat-var` binder を body 側へ渡せるようにした。`test_e2e_selfhost_parser_match_var_pattern_tag`, `test_e2e_selfhost_typeinfer_match_pat_var_tag_binder`, `cargo run --quiet -- check selfhost/src/Types/TypeInfer.ls`, `test_e2e_selfhost_pipeline_complete_stages`, `cargo build && cargo test && cargo clippy --quiet && bash scripts/audit_docs.sh` を再通過し、その時点のテスト数は **500** / **499 passed + 1 ignored** になった。
> - 続く syntax/type slice として selfhost parser の `match` pattern で parenthesized constructor / brace record も canonical `ast-pat-constructor` / `ast-pat-recordpat` へ寄せ、selfhost `TypeInfer` でも canonical `ast-pat-constructor` / `ast-pat-recordpat` binder と error path を legacy shape と同様に扱えるようにした。`test_e2e_selfhost_parser_match_constructor_pattern_tag`, `test_e2e_selfhost_parser_match_record_pattern_tag`, `test_e2e_selfhost_typeinfer_match_pat_record_tag_binder`, `test_e2e_selfhost_typeinfer_match_pat_constructor_tag_binder`, `test_e2e_selfhost_typeinfer_error_match_pat_constructor_tag_undefined_code`, `test_e2e_selfhost_typeinfer_error_match_pat_constructor_tag_arity_code`, `test_e2e_selfhost_typeinfer_error_match_pat_record_tag_child_code`, `cargo run --quiet -- check selfhost/src/Types/TypeInfer.ls`, `test_e2e_selfhost_pipeline_complete_stages`, `cargo build && cargo test && cargo clippy --quiet && bash scripts/audit_docs.sh` を再通過し、当時点のテスト数は **507** / **506 passed + 1 ignored** になった。
> - 続く syntax/type slice として selfhost parser の int/bool literal pattern も canonical `ast-pat-lit` へ寄せ、selfhost `TypeInfer` でも `[42, lit-node]` を int/bool 型へ戻せるようにした。`selfhost/src/IR/LowerPattern.ls` も nested literal payload 前提へ同期し、`test_e2e_selfhost_parser_match_literal_pattern_tag`, `test_e2e_selfhost_typeinfer_match_pat_lit_tag`, `cargo run --quiet -- check selfhost/src/Types/TypeInfer.ls`, `test_e2e_selfhost_pipeline_complete_stages`, `cargo build && cargo test && cargo clippy --quiet && bash scripts/audit_docs.sh` を再通過し、当時点のテスト数は **509** / **508 passed + 1 ignored** になった。
> - 続く evidence slice として canonical `ast-pat-lit` が constructor / record child pattern の再帰経路でも保持されることを固定した。`test_e2e_selfhost_parser_match_nested_literal_pattern_tag`, `test_e2e_selfhost_typeinfer_match_constructor_child_pat_lit`, `test_e2e_selfhost_typeinfer_match_record_child_pat_lit` を追加し、当時点のテスト数は **512** / **511 passed + 1 ignored** になった。
> - 続く syntax/type slice として selfhost parser の `()` unit pattern も canonical `ast-pat-lit` へ寄せ、selfhost `TypeInfer` と `LowerPattern` でも unit payload を扱えるようにした。`test_e2e_selfhost_parser_match_unit_literal_pattern_tag`, `test_e2e_selfhost_typeinfer_match_pat_lit_unit_tag`, `test_e2e_selfhost_typeinfer_match_constructor_child_pat_unit_lit`, `cargo run --quiet -- check selfhost/src/Types/TypeInfer.ls`, `test_e2e_selfhost_pipeline_complete_stages` を再通過し、当時点のテスト数は **515** / **514 passed + 1 ignored** になった。
> - 続く evidence slice として nested constructor / record child の unit literal pattern も canonical `ast-pat-lit` として保持されることを固定した。`test_e2e_selfhost_parser_match_nested_unit_literal_pattern_tag`, `test_e2e_selfhost_typeinfer_match_record_child_pat_unit_lit`, `cargo run --quiet -- check selfhost/src/Types/TypeInfer.ls`, `test_e2e_selfhost_pipeline_complete_stages` を再通過し、当時点のテスト数は **517** / **516 passed + 1 ignored** になった。
> - 残る大物: true bootstrap（stage1.wasm → stage2 → stage3）、CLI/LSP/fmt/doc の公開契約 parity、stateful LSP/REPL + GC fixed-point CI。Native backend は Phase 13 (Component Model pivot) により Deferred/v2 へ移動。Rust workspace は host launcher として残存。

> 目標: L# 製 compiler/toolchain の Wasm bootstrap を完成させ、Wasmtime embedding による single binary 配布の基盤を確立する
> 配布方針: 正式配布物は Wasmtime を内包した host executable (Rust) に L# compiler の Wasm component を埋め込んだ single binary とする。native backend は将来の探求用に保持するが、Phase 11 の completion gate からは外す
> 正式完了条件:
> 1. `stageN.wasm` が selfhost compiler として `stageN+1.wasm` を生成できる -- gate: test_e2e_bootstrap_stage1_deterministic, test_e2e_bootstrap_selfhost_modules_deterministic (E2E 5件)
> 2. `stageN.wasm == stageN+1.wasm` の固定点が CI で安定する -- gate: test_e2e_bootstrap_stage1_section_stability, test_e2e_bootstrap_stage1_symbol_stability (E2E 2件), docs/development/validation/verification-spec.md P11-2d-1
> 3. Rust CLI/LSP/docs 系の公開機能が L# 側で互換提供される -- gate: docs/development/planning/compatibility-matrix.md (CLI 13コマンド/LSP 10メソッド), docs/development/planning/toolchain-parity-spec.md (AC-001~AC-608)
> 4. 長寿命プロセス (LSP/REPL/server mode) で GC 有効時にメモリが単調増加しない -- gate: docs/development/planning/runtime-stability-spec.md S14-S16, docs/development/planning/memory-management-roadmap.md M1-M3
>
> 用語定義:
> - **bootstrap oracle**: Rust 実装を stage0 として使用する参照実装 (比較検証の基準)
> - **host launcher**: Wasmtime を内包し、L# compiler component を実行する Rust binary (配布物の外殻)
> - **guest component**: L# で書かれた compiler/toolchain の Wasm component (配布物の中身)

> **Phase 11 サブフェーズ** (全て仕様固定済み。実装完了ではない):
> - P11-1 (正本監査+互換マトリクス+差分判定+受入基準) → ADR-152
> - P11-2 (ブートストラップ閉路+Native backend+ランタイム+検証+完了条件) → ADR-153
> - P11-3 (Rust parity: syntax/types/IR/backend/移行順/完了条件) → ADR-154
> - P11-4 (ツールチェイン parity: CLI/LSP/formatter/linter/docs/配布、AC-001~AC-608) → ADR-155
> - P11-5 (ランタイム安定化: GC導入/長寿命ワークロード/観測/完了条件) → ADR-156
> - P11-6 (CI切替+legacy隔離+リリース運用+最終撤去) → ADR-157
>
> 仕様固定先ドキュメント一覧:
> `docs/development/planning/compatibility-matrix.md`, `docs/development/planning/gap-classification.md`, `docs/language/backend-boundary.md`,
> `docs/language/native-backend-spec.md`, `docs/language/runtime-spec.md`, `docs/development/validation/verification-spec.md`,
> `docs/development/planning/completion-criteria.md`, `docs/development/planning/rust-parity-spec.md`, `docs/development/planning/toolchain-parity-spec.md`,
> `docs/development/planning/runtime-stability-spec.md`, `docs/development/operations/ci-gate-v2-job-graph.md`, `docs/development/operations/artifact-policy.md`,
> `docs/development/operations/default-path-migration.md`, `docs/development/operations/release-playbook.md`, `docs/development/operations/fresh-clone-spec.md`,
> `docs/development/operations/adr-rust-removal.md`, `docs/development/operations/rollback-procedure.md`,
> `scripts/audit_docs.sh`, `scripts/smoke_test_readme.sh`

*(ADR-159〜ADR-165 は Phase 11 の実装証跡ではなく、当時点の進捗記録として扱う。実装完了判定は `docs/development/planning/completion-criteria.md` / `docs/development/validation/verification-spec.md` / `docs/development/planning/compatibility-matrix.md` を優先する。)*

### Phase 11 ゴールまでの一本道

> 完了判定は以下の 6 段を依存順に閉じたときだけ行う。
> 前段が閉じていない間は、後段を完了扱いしない。

#### Step 1. `CP-01` Frontend unblock / bootstrap 入力集合を閉じる

- [x] [`BOOT-01 Main.ls import path consolidation`](docs/development/planning/phase11-implementation-plan.md#boot-01-mainls-import-path-consolidation) -- Evidence: `selfhost/src/App/Main.ls`, `test_e2e_selfhost_main_import_only_pipeline`, `test_e2e_selfhost_pipeline_complete_stages`（マルチファイル `compile_and_run_file`）。
- [x] [`IR-02 Lower split`](docs/development/planning/phase11-implementation-plan.md#ir-02-lower-split) / [`IR-04 Pattern lowering`](docs/development/planning/phase11-implementation-plan.md#ir-04-pattern-lowering) -- Evidence: `crates/lsharp-types/src/types.rs` `apply_subst` + `apply_subst_tests`、`selfhost/src/IR/Lower*.ls` が `compile-phase11-inputs.sh` で通過。
- [x] [`BOOT-03 stdlib direct compile blockers`](docs/development/planning/phase11-implementation-plan.md#boot-03-stdlib-direct-compile-blockers) の残件 -- `scripts/ci/compile-phase11-inputs.sh` に Lower/LowerPattern を含め `KNOWN_BLOCKERS` 撤去済み。
- [x] Step 1 exit gate -- `scripts/ci/compile-phase11-inputs.sh` known blocker なしで通過。

#### Step 2. `CP-01` true bootstrap fixed point を成立させる

- [x] **BOOT-04 self-TCO (自己末尾呼び出し最適化)** -- `crates/lsharp-ir/src/lower/decl.rs` に `apply_self_tco` / `find_simple_self_tail_calls` を追加し、自己末尾再帰関数 (`append-byte-vector` 等) を Wasm `loop + br` に変換するパスを実装。N=2000 関数のコードセクション (~30,000 bytes) でも stack overflow trap が発生しなくなった。境界テスト結果: N=10/50/200/500/1000 全て OK。Evidence: `test_e2e_boot04_bootstrap_append_bytes_deep_recursion_trap_repro` / `test_e2e_boot04_bootstrap_append_bytes_recursion_depth_boundary`。また `wasi_runner.rs` の `MemoryOutputPipe` バッファを 4096→4MB に拡張し大出力に対応。

- [x] [`BOOT-04 True stage1-stage2-stage3 bootstrap`](docs/development/planning/phase11-implementation-plan.md#boot-04-true-stage1-stage2-stage3-bootstrap) -- real compiler-mode / self-feed path で `Main.ls` の true bootstrap fixed point を確認済み。`selfhost/src/App/Main.ls` の compiler-mode と `selfhost/src/Syntax/Parser.ls` の body symbol/string helper fix (`parse-symbol-var-v3`, `parse-string-node-v3`) により、`test_e2e_boot04_self_hosted_stage2_compiler_runtime_resolves_param_and_user_call` / `test_e2e_boot04_self_hosted_stage2_reports_step512_progress` / `test_e2e_boot04_self_hosted_stage2_compiles_main_again` が GREEN、さらに refreshed acceptance `test_e2e_bootstrap_fixed_point_stage2_stage3` で `stage2 == stage3` (232751 bytes) を assert し、`test_e2e_bootstrap_stage1_stage2_match` / `test_e2e_bootstrap_stage1_section_stability` / `test_e2e_bootstrap_stage1_symbol_stability` / `test_e2e_bootstrap_four_layer_comparison` / `test_e2e_bootstrap_stage_chain_verification` まで exact で通過。
- [x] [`WASM-03 Deterministic LEB emit`](docs/development/planning/phase11-implementation-plan.md#wasm-03-deterministic-leb-emit) -- マルチファイル決定性 (`ModuleGraph` ソート) + E2E: `test_e2e_wasm03_token_module_compile_deterministic`, 既存 `test_e2e_bootstrap_*deterministic*`。
- [x] Step 2 exit gate -- exact gate 群で stage1→stage2 match、stage2→stage3 fixed point、section/symbol stability、4 層比較、stage chain verification、compiler-mode self-feed の minimal / step512 / `Main.ls` 実体生成がすべて GREEN。重い全体テストは回していないが、true self-bootstrap fixed point の受入条件は targeted exact で充足した。

#### ~~Step 3. `CP-03` Native parity を閉じる~~ → Deferred (Component Model pivot)

> **2026-03-30**: Wasmtime embedding + Component Model を正式配布モデルに据える方針転換により、native self-regeneration と Wasm/native differential zero は Phase 11 の completion gate から外し Deferred/v2 へ移動した。native backend コード自体は `selfhost/src/Backend/Native/` に保持する。

- [x] ~~[`NATIVE-05 Stage1-native self-regeneration`](docs/development/planning/phase11-implementation-plan.md#native-05-stage1-native-self-regeneration)~~ -- **Deferred extraction complete (→ V2-08)**。機能的等価性検証 + stage chain 構造テストに加え、`test_native_pipeline_complete_chain`, `test_native_codegen_emit_standalone_execution`, `test_native_codegen_real_execution`, `test_native_codegen_emits_full_const_instruction_bytes`, `test_native_emit_object_keeps_full_native_payload`, `test_native_emit_object_is_deterministic_across_three_targets`, `test_native_linker_response_is_deterministic_across_three_targets`, `test_e2e_selfhost_main_native_pipeline_summary` で NativeTarget→NativeCodegen→NativeEmit→Linker の実行 slice、full-width const bytecode 生成、Mach-O / ELF object への full payload 保持、same-IR repeated object emission / same-object-list repeated response generation の 3 target 決定性、`selfhost/src/App/Main.ls` から compile-source 由来 IR を x86_64-darwin / aarch64-darwin / x86_64-linux の 3 target で tiny native summary として通し、IR 長・代表 header byte・single/multi-object response byte まで確認済み。`selfhost/src/Backend/Native/NativeEmit.ls` も object 出力が先頭 4 byte で切れず payload 全体を保持するようになった。さらに `NativeTarget.ls` は 12-field descriptor (`calling convention` / `stack alignment` / `section policy` / `reloc-call policy` / `response file style` / `runtime object kind`) を持つようになり、`test_e2e_native_target_descriptor_exposes_policy_fields` で 3 target の policy field を固定した。stage-chain harness も canonical selfhost module path (`Backend/Native/*`, `IR/IR.ls`) で fixture を組むように揃え、`test_e2e_stage1_native_two_run_determinism`・`test_e2e_selfhost_main_native_summary_matches_direct_pipeline_harness`・`test_e2e_native_host_binary_link_and_execute`・zero-diff sample 群がその経路で通ることを確認した。Phase 11 mainline ではここで close し、残る true self-regeneration は `V2-08` へ移す。
- [x] ~~[`NATIVE-06 Wasm/native differential`](docs/development/planning/phase11-implementation-plan.md#native-06-wasmnative-differential)~~ -- **Deferred extraction complete (→ V2-09)**。5 観測点用 harness、空 allowlist、structural parity テストに加え、`test_wasm_native_execution_parity_double`, `test_native_codegen_processes_multiple_ir_instructions`, `test_native_emit_object_keeps_full_native_payload`, `test_native_emit_elf_object_keeps_full_native_payload`, `test_native_emit_object_headers_cover_all_three_targets`, `test_native_emit_object_is_deterministic_across_three_targets`, `test_native_linker_response_consistency_across_three_targets`, `test_native_linker_multi_object_response_consistency_across_three_targets`, `test_native_linker_response_is_deterministic_across_three_targets`, `test_e2e_selfhost_main_native_pipeline_summary` などの narrow execution/object parity slice は追加済み。`NativeCodegen.ls` は 1 命令 3 byte だけでなく、複数 IR 命令を順に full byte 列へ落とせ、`NativeEmit.ls` は Mach-O / ELF object に full payload を保持でき、同一 IR / 同一 object list に対する repeated object/response generation も 3 target で安定し、`selfhost/src/App/Main.ls` でも source 由来 IR から 3 target 分の native/object/link-response summary に加えて代表 header byte と response byte を出せる。さらに `test_e2e_zero_diff_const_0` / `test_e2e_zero_diff_const_1` / `test_e2e_zero_diff_const_42` / `test_e2e_zero_diff_const_100` / `test_e2e_zero_diff_sample_summary` で tier1 host-target const slice の Wasm stdout と native exit code 一致も確認した。Phase 11 mainline ではここで close し、残る wide differential zero は `V2-09` へ移す。
- [x] [`META-05 Differential allowlist registry`](docs/development/planning/phase11-implementation-plan.md#meta-05-differential-allowlist-registry) の完了 -- `tests/differential-allowlist.yaml` が `allowlist: []` であることを `test_e2e_meta05_differential_allowlist` で固定。
- [x] ~~Step 3 exit gate~~ (Deferred) -- Component Model pivot により native structural parity / allowlist registry / proxy differential evidence は Phase 11 close 条件から外し、残課題を `V2-08` / `V2-09` へ移管済み。NativeTarget→NativeCodegen→NativeEmit の実行 slice、Mach-O / ELF object file への full payload 保持、`selfhost/src/App/Main.ls` から source 由来 IR を通す 3 target 分の tiny native/object/link-response summary と 3 target 分の multi-object linker summary、さらに代表 header byte / response byte の観測、differential 側の 3-target object/header consistency・single/multi-object linker consistency・same-input repeated object/response generation determinism、host-target AArch64 code の実リンク・実行、const 0/1/42/100 の zero-diff sample までは partial evidence として保持する。

#### Step 4. `CP-04` Public toolchain parity を閉じる

- [x] [`CLI-02 13 command implementations`](docs/development/planning/phase11-implementation-plan.md#cli-02-13-command-implementations) -- `selfhost/src/App/Cli.ls` に 13 サブコマンド名、終了コード API (`exit-code-success`/`exit-code-compile-error`/`exit-code-runtime-error`/`exit-code-unknown-command`)、stdout/stderr 分離 (`cli-stdout`/`cli-stderr`)、`run-command`、`format-subcommand-help` と E2E 6 件は追加済み。さらに `test` handler は `TestRunner` と接続され、`:example` / `:invariant` 抽出、example 実行、invariant materialize、failing example の `runtime-error` 返却 (`test_e2e_selfhost_cli_test_source_metadata_pass`, `test_e2e_selfhost_cli_test_source_metadata_fail`) に加えて `examples:N` / `invariants:N` / `failures:N` の labeled summary も返せる。`parse` は `decls:N` / `first-decl:<name>` / `first-body:<name>` に加えて `diagnostics:0` / `diagnostics:1,P0001@1:1,first-body:unexpected token )` / `unexpected token ]` 形式の token-aware summary text、`check` は builtin 型名 text に加えて `diagnostics:0` / `diagnostics:1,T0001@1:1,first-body:if condition must be Bool` / `undefined symbol` 形式の error-code-aware summary text を返せる。`repl` も `type:Int` / `evals:1` / `input-bytes:17` の warmup session summary を返せる。`parse-diagnostics-count` は recovery 対象 `)` / `]` に対して 1 件を返し、`check-diagnostics-count` は top-level `defn` の型エラーを 1 件として返せる。`compile` / `build` は `wasm-size:<n>` text、`install` は deterministic な dry-run plan text、`lsp` は `sync:full` / `hover:true` / `completion:true` / `definition:true` / `references:true` / `rename:true` / `formatting:true` に加えて `requests:1` / `documents:0` / `source-bytes:0` の shared-state summary text も返し、completion item 自体も text label / insertText を返せる。加えて transport slice として `run-lsp-transport-request` が in-memory JSON-RPC request vector を `Content-Length` frame 付き initialize response / Method not found error に変換でき、`run-lsp-transport-sequence` は shared state 上で initialize→shutdown の複数 request を順に処理して frame 群と request-count summary を返せる。さらに raw stdio 寄りの slice として `run-lsp-stdio-frame` / `run-lsp-stdio-sequence` も追加され、`Content-Length` header 付き frame vector から request を復元して shared-state dispatch できる。加えて actual `lsp --stdio` も stdin から concatenated frame を読み、initialize / shutdown / unknown method / completion / definition / hover / references / formatting / rename に加えて `didOpen -> didChange` notification sequence と、`didOpen` 後の source-less completion / definition / hover / references / formatting / rename request、open 済み別 document からの source-less definition / hover request、`didChange` 後の source-less completion / definition / hover、same-URI repeated `didOpen` 後の latest-source completion / definition / hover を real wire text のまま処理できる (`test_e2e_selfhost_cli_main_with_lsp_stdio_initialize`, `test_e2e_selfhost_cli_main_with_lsp_stdio_initialize_shutdown_sequence`, `test_e2e_selfhost_cli_main_with_lsp_stdio_unknown_method`, `test_e2e_selfhost_cli_main_with_lsp_stdio_completion`, `test_e2e_selfhost_cli_main_with_lsp_stdio_goto_definition`, `test_e2e_selfhost_cli_main_with_lsp_stdio_hover`, `test_e2e_selfhost_cli_main_with_lsp_stdio_references`, `test_e2e_selfhost_cli_main_with_lsp_stdio_formatting`, `test_e2e_selfhost_cli_main_with_lsp_stdio_rename`, `test_e2e_selfhost_cli_main_with_lsp_stdio_document_sequence`, `test_e2e_selfhost_cli_main_with_lsp_stdio_hover_uses_open_document`, `test_e2e_selfhost_cli_main_with_lsp_stdio_definition_uses_open_document`, `test_e2e_selfhost_cli_main_with_lsp_stdio_references_uses_open_document`, `test_e2e_selfhost_cli_main_with_lsp_stdio_formatting_uses_open_document`, `test_e2e_selfhost_cli_main_with_lsp_stdio_rename_uses_open_document`, `test_e2e_selfhost_cli_main_with_lsp_stdio_completion_uses_open_document`, `test_e2e_selfhost_cli_main_with_lsp_stdio_definition_resolves_open_document`, `test_e2e_selfhost_cli_main_with_lsp_stdio_hover_resolves_open_document`, `test_e2e_selfhost_cli_main_with_lsp_stdio_completion_uses_changed_document`, `test_e2e_selfhost_cli_main_with_lsp_stdio_repeated_didopen_keeps_latest_source`, `test_e2e_selfhost_cli_main_with_lsp_stdio_hover_uses_changed_document`, `test_e2e_selfhost_cli_main_with_lsp_stdio_definition_uses_latest_reopened_document`, `test_e2e_selfhost_cli_main_with_lsp_stdio_definition_uses_changed_document`, `test_e2e_selfhost_cli_main_with_lsp_stdio_hover_uses_latest_reopened_document`)。今回さらに source-param 付き `goto-definition` / `hover` / `references` / `completion` / `formatting` / `rename` request も framed response に落とせるようになり (`test_e2e_selfhost_cli_lsp_transport_goto_definition_frame`, `test_e2e_selfhost_cli_lsp_transport_hover_frame`, `test_e2e_selfhost_cli_lsp_transport_references_frame`, `test_e2e_selfhost_cli_lsp_transport_completion_frame`, `test_e2e_selfhost_cli_lsp_transport_formatting_frame`, `test_e2e_selfhost_cli_lsp_transport_rename_frame`)、document lifecycle 側でも `lsp-render-didopen-frame` / `lsp-render-didchange-frame` が deterministic notification frame を返し、shared-state dispatch と組み合わせて didOpen→didChange の source-bytes 更新を観測できる (`test_e2e_selfhost_cli_lsp_transport_didopen_frame`, `test_e2e_selfhost_cli_lsp_transport_document_sequence`)。`main-dispatch` helper も追加され、`run-command` への委譲として parse command surface と help/version/unknown command surface を narrow E2E で固定した (`test_e2e_selfhost_cli_main_dispatch_parse_file`, `test_e2e_selfhost_cli_main_dispatch_command_surface`)。加えて actual `main` も debug stub ではなく `command-line-args` / `command-line-arg` ベースで no-args help、`--version` / `-v`、`parse input.ls`、`compile input.ls`、`build input.ls`、`compile input.ls -o out.txt`、`build input.ls --output out.txt`、`parse --help`、`-h`、`help parse`、`help compile`、`build --help` を argv 経由で処理できるようになった (`test_e2e_selfhost_cli_main_no_args_shows_help`, `test_e2e_selfhost_cli_main_with_args_version`, `test_e2e_selfhost_cli_main_with_args_short_version`, `test_e2e_selfhost_cli_main_with_args_parse_file`, `test_e2e_selfhost_cli_main_with_args_compile_file`, `test_e2e_selfhost_cli_main_with_args_build_file`, `test_e2e_selfhost_cli_main_with_args_compile_output_path`, `test_e2e_selfhost_cli_main_with_args_build_output_path`, `test_e2e_selfhost_cli_main_with_args_subcommand_help`, `test_e2e_selfhost_cli_main_with_args_short_help`, `test_e2e_selfhost_cli_main_with_help_command`, `test_e2e_selfhost_cli_main_with_help_compile_output_option`, `test_e2e_selfhost_cli_main_with_build_subcommand_help_output_option`)。WASI runner / E2E support 側も argv・dir+argv helper を持ち、actual main surface の narrow verify を回せる。さらに selfhost `Compiler.ls` も `command-line-arg` を builtin opcode `67` として lowering できるようになり、CLI source と compiler builtin table のズレを 1 つ潰した (`test_e2e_selfhost_compiler_command_line_arg_builtin_lowering`)。selfhost `WasmEmit.ls` 側でも opcode `67` が `call 3` に落ちる code section slice と、4-import stage2 module (`__alloc` / `print` / `read-file` / `command-line-arg`) を実際に生成・実行できる bootstrap slice を追加した (`test_e2e_selfhost_wasmemit_command_line_arg_instr`, `test_e2e_bootstrap_stage1_emits_stage2_wasm_for_command_line_arg_program`)。`test_e2e_selfhost_cli_lsp_transport_initialize_frame` / `test_e2e_selfhost_cli_lsp_transport_unknown_method_error` / `test_e2e_selfhost_cli_lsp_transport_sequence_summary` / `cargo run --quiet -- check selfhost/src/App/Cli.ls` / `cargo run --quiet -- check selfhost/src/Tools/Lsp/LspServer.ls` / main-dispatch 2 件 / actual main argv 17 件 / compiler builtin 1 件 / WasmEmit builtin 1 件 / bootstrap argv parity 1 件 / request transport 6 件 / raw stdio frame 2 件 / actual lsp --stdio 24 件が green。`fmt` / `review` / `doc` は deterministic な実テキスト surface を返し、`doc` body は `functions:N,types:M,first-fn:...` / `first-type:...` を含む name-aware summary を返せ、`doc-ack` は `ack:recorded` + title/body、`doc-check` は `status:ok` + title/body を返せる。`review` は unused-let / empty-do に対して count/title/body に加えて `warning` と `L0001@1:1` 形式の code/location text、さらに first diagnostic body 付き summary を stdout に出せる。`generate-review` 由来の diagnostics も severity/line/column/code slot まで持てる。加えて `run-fmt-source` / `run-fmt` は `format-program-with-source` を使うようになり、`test_e2e_selfhost_cli_fmt_source_string_literal` と `cargo run --quiet -- check selfhost/src/App/Cli.ls` が green。
- [x] [`LSP-02 10 method parity`](docs/development/planning/phase11-implementation-plan.md#lsp-02-10-method-parity) / [x] [`LSP-03 Diagnostic ordering`](docs/development/planning/phase11-implementation-plan.md#lsp-03-diagnostic-ordering-and-json-snapshots) -- `selfhost/src/Tools/Lsp/LspServer.ls` に 10 メソッド名、sort/dedup helper、JSON-RPC helper 相当の関数、diagnostics の deterministic JSON text renderer (`render-diagnostic-json` / `render-diagnostics-json`) と representative snapshot files (`tests/snapshots/lsp/diagnostics/*.json`)・関連 E2E は追加済み。加えて diagnostics は same-span / same-severity tie でも rule/message 順で安定ソートされ、parse/type overlap では parse を優先して 1 件へ潰せる。source param がある場合、top-level `defn` に対する hover/definition/references/rename/completion の source-driven subset と、`parse-program` → `format-program-with-source` を使う canonical formatting (`test_e2e_selfhost_lsp_real_shapes_*`, `test_e2e_selfhost_lsp_real_shapes_formatting_preserves_string_literal`) までは動作し、hover contents は `defn <name>` / `symbol <name>` text、completion item も label / kind / insertText の text surface を返せる。さらに repeated symbol source でも hover range は選択 occurrence に残り、definition は直近の top-level `defn` を優先できる (`test_e2e_selfhost_lsp_real_shapes_definition_prefers_nearest_defn`, `test_e2e_selfhost_lsp_real_shapes_hover_keeps_selected_occurrence_with_repeated_defns`)。今回、shared-state server state は URI→source document map に加えて open URI list を保持するようになり、同一 session 上で複数 `didOpen` 済み document を保持したまま、source param なしの definition/hover/formatting/completion 系が要求 URI に対応する open document を引けるようになった。加えて current document に定義がない場合でも、open 済み別 document 群を走査して cross-document definition/hover を返せる (`test_e2e_selfhost_lsp_runtime_multi_document_state`, `test_e2e_selfhost_lsp_real_shapes_definition_uses_uri_document`, `test_e2e_selfhost_lsp_real_shapes_hover_uses_uri_document`, `test_e2e_selfhost_lsp_real_shapes_definition_resolves_open_document`, `test_e2e_selfhost_lsp_real_shapes_hover_resolves_open_document`)。`initialize` capability summary も references/rename/formatting まで宣言でき、shared-state `server-loop-step` / `server-loop-sequence` により同一 session state 上で複数 request を順に dispatch でき、同一 URI への repeated `didOpen` でも doc-count が増殖しないよう dedup された。initialized/shutdown lifecycle flag も観測でき、bootstrap 側 `JsonRpc.ls` も initialize capability vector / shutdown sentinel に加えて deterministic な JSON-RPC response text (`{"jsonrpc":"2.0","id":...,"result":...}`) と `Content-Length: <n>\r\n\r\n<payload>` framing helper (`render-json-rpc-frame`, `parse-content-length`, `render-initialize-frame`, `render-rpc-error-response-frame`) を返せる。さらに `handle-publish-diagnostics` / `lsp-render-publish-diagnostics-frame` により `textDocument/publishDiagnostics` notification payload を deterministic diagnostics JSON と framed notification へ落とせる (`test_e2e_selfhost_cli_lsp_transport_publish_diagnostics_frame`)。今回 `lsp-render-location-frame` / `lsp-render-hover-frame` / `lsp-render-locations-frame` / `lsp-render-completion-frame` / `lsp-render-formatting-frame` / `lsp-render-rename-frame` も追加され、source-param 付き `goto-definition` / `hover` / `references` / `completion` / `formatting` / `rename` を `Content-Length` 付き response frame へ落とせるようになった (`test_e2e_selfhost_cli_lsp_transport_goto_definition_frame`, `test_e2e_selfhost_cli_lsp_transport_hover_frame`, `test_e2e_selfhost_cli_lsp_transport_references_frame`, `test_e2e_selfhost_cli_lsp_transport_completion_frame`, `test_e2e_selfhost_cli_lsp_transport_formatting_frame`, `test_e2e_selfhost_cli_lsp_transport_rename_frame`)。actual `lsp --stdio` 側の representative schema snapshot も initialize / initialize→shutdown / unknown method / didOpen→didChange / completion / hover / references / rename / open-document definition / open-document formatting / publishDiagnostics / didChange 後 hover fallback / repeated didOpen 後 definition fallback まで固定した (`test_e2e_selfhost_cli_main_with_lsp_stdio_initialize_schema_snapshot`, `test_e2e_selfhost_cli_main_with_lsp_stdio_initialize_shutdown_schema_snapshot`, `test_e2e_selfhost_cli_main_with_lsp_stdio_unknown_method_schema_snapshot`, `test_e2e_selfhost_cli_main_with_lsp_stdio_document_sequence_schema_snapshot`, `test_e2e_selfhost_cli_main_with_lsp_stdio_completion_schema_snapshot`, `test_e2e_selfhost_cli_main_with_lsp_stdio_hover_schema_snapshot`, `test_e2e_selfhost_cli_main_with_lsp_stdio_references_schema_snapshot`, `test_e2e_selfhost_cli_main_with_lsp_stdio_rename_schema_snapshot`, `test_e2e_selfhost_cli_main_with_lsp_stdio_definition_open_document_schema_snapshot`, `test_e2e_selfhost_cli_main_with_lsp_stdio_formatting_open_document_schema_snapshot`, `test_e2e_selfhost_cli_main_with_lsp_stdio_publish_diagnostics_schema_snapshot`, `test_e2e_selfhost_cli_main_with_lsp_stdio_hover_changed_document_schema_snapshot`, `test_e2e_selfhost_cli_main_with_lsp_stdio_definition_latest_reopened_schema_snapshot`)。`selfhost_jsonrpc_transport` の 3 件、`cargo run --quiet -- check selfhost/src/Tools/Lsp/JsonRpc.ls`、`cargo run --quiet -- check selfhost/src/Tools/Lsp/LspServer.ls` が green。2026-04-01 追補: `json-rpc-dispatch` / `server-loop-step` / `server-loop-sequence` / `server-loop` を `LspServer.ls` に復元、`LspServerNav.ls` を Parser/FormatterDecl/JsonRpc/Core で接続、`compile_multi_file` の Formatter トリオバッチで LSP ランタイムモジュール束の型検査を成立、`test_e2e_selfhost_lsp_hier_fixture_shape_cross_document_definition` で hier cross-document definition を固定。2026-04-02 追補: open document の `path` を起点にした filesystem-backed direct/nested import resolution を hover / completion / definition / references / rename まで広げ、未 open import target の definition / references / rename では path-hash virtual URI を返せるようにした。さらに representative actual `lsp --stdio` snapshot として `hover-filesystem-import` / `completion-filesystem-import` / `definition-filesystem-import` / `references-filesystem-import` / `rename-filesystem-import` / `filesystem-document-sequence` を追加し、spec 寄り request shape でも同じ `filesystem-document-sequence` snapshot に収束する actual regression (`test_e2e_selfhost_cli_main_with_lsp_stdio_filesystem_document_sequence_spec_style_snapshot`) を固定、`cargo test -q -p lsharp-wasm --test lsp_stateful_parity -- --nocapture` も 42 tests green。加えて `cargo test -q -p lsharp-wasm --test e2e filesystem_document_sequence_ -- --nocapture` も 2 passed となり、representative long-lived filesystem-backed sequence の broad gate まで通過した。stdio body parser の JSON string decode も escape-aware にし、spec 寄り `textDocument.text` / `contentChanges[0].text` に含まれる escaped quote / newline と unicode escaped quote / newline (`\u0022` / `\u000a`) が actual `lsp --stdio` formatting / completion でも最新 source として解釈されるよう固定した (`cargo test -q -p lsharp-wasm --test e2e escaped_ -- --nocapture` 5 passed、`cargo test -q -p lsharp-wasm --test e2e spec_params -- --nocapture` 11 passed)。さらに representative long-lived transport/session gate として `cargo test -q -p lsharp-wasm --test e2e test_e2e_gc_lsp_actual_stdio_repeated_sequence_soak -- --nocapture` も green となり、公開 LSP parity gate を再閉鎖した。
- [x] [`FMT-01 Formatter roundtrip`](docs/development/planning/phase11-implementation-plan.md#fmt-01-formatter-roundtrip) / [x] [`DOC-01 Schemas and snapshots`](docs/development/planning/phase11-implementation-plan.md#doc-01-schemas-and-snapshots) -- supported subset coverage と決定的 roundtrip/idempotency テスト、`docs/schemas/`、`generate-knowledge` / `generate-review` / `generate-doc-output` / `generate-html` と関連 E2E は追加済み。加えて Formatter は expr 側で `int/bool/string/float/unit/var/apply/if/let/fn/do/match/ann/recordlit/fieldaccess/recordupdate/computation/quote/unquote/unquote-splice`、decl 側で `defn/type/record/type-alias/type-constrained/module/import/trait/impl/computation-builder/defmacro`、pattern 側で `wildcard/var/lit/constructor/record` を canonical text または deterministic fallback なしで返せるようになり、source なしの string/float literal も `""` / `0.0` の fallback canonical text を持つ。追加の slice として `format-program-with-source` / `format-expr-with-source` が入り、source がある場合は string/float literal を span から復元できる。さらに defn trailing metadata (`:params` / `:returns` / `:doc` / `:example`) も source-aware formatting で canonical 順に再構成できるようになり、CLI `fmt` / LSP formatting まで同じ canonical text に収束する回帰 (`test_e2e_selfhost_formatter_format_program_with_source_defn_metadata`, `test_e2e_selfhost_cli_fmt_source_defn_metadata`, `test_e2e_selfhost_lsp_real_shapes_formatting_preserves_defn_metadata`, `test_e2e_selfhost_cli_main_with_lsp_stdio_formatting_preserves_defn_metadata`) を追加した。`selfhost_formatter_source_roundtrip` の 3 件、`test_e2e_selfhost_formatter_format_expr_lit_string_fallback`、`test_e2e_selfhost_formatter_format_expr_lit_float_fallback`、`test_e2e_selfhost_cli_fmt_source_string_literal`、`test_e2e_selfhost_lsp_real_shapes_formatting_preserves_string_literal`、`cargo run --quiet -- check selfhost/src/Tools/Text/Formatter.ls` が green。今回さらに representative CLI text snapshot として `test_e2e_selfhost_cli_fmt_source_string_literal_snapshot` / `tests/snapshots/cli/fmt-source-string-literal.txt`、`test_e2e_selfhost_cli_doc_source_snapshot` / `tests/snapshots/cli/doc-source-basic.txt` を追加した。CLI `run-fmt-source` / `run-fmt` はその source-aware canonical text を stdout へ返し、LSP formatting も `parse-program` / `format-program-with-source` 経由で `TextEdit.newText` に canonical text を積める。DocTools/HtmlDoc は function/type entry の deterministic sort に加えて再構成した name text を保持・描画でき、body summary も `first-fn` / `first-type` まで返せ、module decl がある場合は `generate` / `generate-doc-output` の title も `module-<name>` へ寄せられる。`generate-knowledge` は function entry に `params` (`name:type`) / `returns` / `doc` / `example` を持ち、`generate-doc-output` は `params[{name,type,doc}]` / `returns{type,doc}` / `doc` / `example` を返せる。selfhost parser も defn trailing metadata として `:doc` / `:example` / `:params` / `:returns` を保持でき、HtmlDoc は module page で parameter/return doc を含む host-truth 寄り HTML を escape/render できる。`review.schema.json` は `title` / `severity` / `message` / `line` / `column` / `code` を必須化し、`generate-review` も unused-let / empty-do に対して severity/line/column/code 付き diagnostics vector を返せ、unused-let の body は binder 名を含む。CLI `run-doc-source` / `run-doc` は name-aware な title/body を、CLI `run-doc-ack` / `run-doc-check` は status line 付きの title/body と comment-form trailer を、CLI `run-review-source` / `run-review` は count/title/body に加えて severity と `code@line:column`、さらに first diagnostic body を返せる。`doc-check-trailer-valid?` と `doc-check --strict` で trailer validation slice も追加済み。さらに `crates/lsharp-wasm/tests/doctools_parity.rs` に representative payload/HTML snapshot gate (`knowledge-payload.json`, `review-payload.json`, `doc-output-payload.json`, `render-html-rich.html`) と schema-object snapshot gate (`knowledge-schema-object.json`, `review-schema-object.json`, `doc-output-schema-object.json`) を追加し、新規 `selfhost/src/Tools/Doc/DocJson.ls` が `generate-knowledge-schema-json` / `generate-review-schema-json` / `generate-doc-output-schema-json` で `docs/schemas/*.json` と整合する deterministic JSON text を返せるようになった。`cargo test -q -p lsharp-wasm --test doctools_parity -- --nocapture` は 31 passed、`cargo test -q -p lsharp-wasm --test e2e selfhost_doctools_ -- --nocapture` は 45 passed。さらに selfhost CLI `review --json` / `review --format json` / `doc --json` / `doc --format json` も `DocJson.ls` 経由の schema-object JSON text を返せるようになり、`test_e2e_selfhost_cli_review_source_json_snapshot` / `test_e2e_selfhost_cli_doc_source_json_snapshot` / `test_e2e_selfhost_cli_main_with_args_review_json_file` / `test_e2e_selfhost_cli_main_with_args_doc_json_file` が green。release/readme/archive smoke と representative doc-site HTML snapshot、focused doc-site generation test (`cargo test -q -p lsharp-driver test_cmd_doc_site_generates_guides_and_api_site -- --nocapture`) まで green となり、public formatter/docs parity gate を再閉鎖した。
- [x] Step 4 exit gate -- CLI/LSP/FMT/DOC の selfhost source、schema/documentation、proxy tests に加え、`test` metadata 実行、top-level `defn` source-driven LSP subset、CLI `parse/check/repl/lsp` の human-readable text surface、canonical text formatter core、CLI fmt 実テキスト出力、CLI review count/title/body/severity/code-location surface、CLI doc title/body surface、CLI doc-ack/doc-check status text + comment-form trailer、`doc-ack --trailer` / `doc-check --strict` の file-handler / actual `main` slice と strict failure の non-zero process exit、LSP formatting の `newText` 実テキスト化、`initialize` capability summary の references/rename/formatting 露出、deterministic DocTools/HtmlDoc HTML slice までは完了。`docs/development/planning/compatibility-matrix.md` でも更新対象の selfhost source と関連 test を列挙済み。2026-04-03: file-based `compile` / `build` は import-aware module resolution と actual `main` の `-o` / `--output` / `--target` / nested import parity まで前進。2026-04-04: representative filesystem-backed LSP snapshots と `DocJson.ls` による payload / HTML / schema-object snapshots、selfhost actual `main` の `review/doc --json` と `review/doc --format json` argv surface、さらに invalid な `review/doc --format` value を `error: unsupported option: ...` + non-zero exit で拒否する option parity も固定した。続けて host-backed `doc` distribution ownership として `scripts/ci/release-smoke.sh` に required payload (`README.md` / `LICENSE` / `checksums.txt` / `lsharp-lsp`) 検証付きの packaged binary `doc` / `doc --json` smoke、`scripts/smoke_test_readme.sh` に inline Quick Start fixture ベースの checksum / compile / test / doc smoke を追加し、`default-path-migration.md` / `release-playbook.md` / `compatibility-matrix.md` も同期した。さらに embedded default path の `compile` / `build` は guest の `wasm-size:<n>` surface を維持したまま host launcher が output file に実 Wasm / Component bytes を書くようになり、`scripts/ci/default-path-smoke.sh` / `scripts/ci/release-smoke.sh` / `scripts/smoke_test_readme.sh` で magic bytes と実行可能性まで検証する契約に更新した。加えて actual `lsp --stdio` repeated-sequence soak (`cargo test -q -p lsharp-wasm --test e2e test_e2e_gc_lsp_actual_stdio_repeated_sequence_soak -- --nocapture`) と doc-site generation focused gate (`cargo test -q -p lsharp-driver test_cmd_doc_site_generates_guides_and_api_site -- --nocapture`) まで green を再確認し、public toolchain parity gate を再閉鎖した。

#### Step 5. `CP-05` Runtime stability gate を閉じる

- [x] [`GC-05 LSP soak and REPL GC`](docs/development/planning/phase11-implementation-plan.md#gc-05-lsp-soak-and-repl-gc) -- `test_e2e_gc_light_compile_run_loop`, `test_e2e_gc_compile_run_loop_1000`, `test_e2e_gc_repl_soak_50_eval`, `test_e2e_gc_repl_soak_500_eval` に加え、`test_e2e_gc_repl_stateful_single_session_metrics`, `test_e2e_gc_repl_session_batch_metrics`, `test_e2e_gc_repl_stateful_long_session_metrics`, `test_e2e_gc_lsp_stateful_session_sequence_metrics`, `test_e2e_gc_lsp_stateful_repeated_sequence_metrics` で単一 Wasm セッション上の REPL 継続評価メトリクス、REPL batch helper、長めの single-session REPL soak、`didOpen -> hover -> didChange -> completion -> formatting` の stateful sequence と repeated sequence までは固定済み。さらに `test_e2e_selfhost_lsp_runtime_server_loop_stateful_sequence` により shared-state `server-loop-step` dispatch も追加で固定し、同一 URI repeated `didOpen` は doc-count を増やさない。今回 `test_e2e_gc_lsp_actual_stdio_repeated_sequence_soak` により actual `lsp --stdio` の 12 反復 long-lived sequence も固定し、`emit_read_stdin_func` は 4KiB chunk を EOF まで連結する形に更新して soak-sized wire を最後まで読めるようにした。加えて `test_e2e_gc_repl_stateful_long_session_postsession_collector_telemetry` と `test_e2e_gc_lsp_actual_stdio_repeated_sequence_postsession_collector_telemetry` により、same-session workload 終了直後に actual `__lsharp_gc_collect` を叩いた post-session telemetry で `gc_collection_count > 0` / `gc_freed_count > 0` / `root_stack_top = 0` を固定した。さらに `_start` return / `proc_exit(0)` 成功終了経路で actual `__lsharp_gc_collect` を自動実行する slice を `crates/lsharp-wasm/src/wasi.rs` に追加し、`test_e2e_gc_repl_stateful_long_session_in_session_collector_telemetry` と `test_e2e_gc_lsp_actual_stdio_repeated_sequence_in_session_collector_telemetry` で session-internal telemetry (`gc_collection_count > 0` / `gc_freed_count > 0` / `root_stack_top = 0`) も固定した。さらに runtime 側でも `test_e2e_runtime_collector_preserves_string_reachable_through_rooted_closure_capture` と `test_e2e_runtime_collector_ignores_legacy_zero_root_slot_sentinel` により closure capture transitive trace と root-stack `0` sentinel ignore path を固定し、selfhost compiler source-aware path の direct user call でも `test_e2e_selfhost_compiler_user_call_*` により first-arg spill/root と call 前後 cleanup parity を固定したが、closure/indirect を含む broader selfhost GC-safe point 列挙は未完了。`gc-metrics` artifact の `proxy_workloads` には light compile+run / REPL 50 eval / stateful single-session REPL / stateful long-session REPL / actual `lsp --stdio` repeated sequence の representative result を格納し、required job 側でも機械可読に回収できる。
- [x] [`GC-06 Leak detection and metrics`](docs/development/planning/phase11-implementation-plan.md#gc-06-leak-detection-and-metrics) -- `test_e2e_alloc_metrics_peak_usage`, `test_e2e_alloc_metrics_monotonic_check`, `test_e2e_alloc_metrics_five_metric_collection`, `test_e2e_alloc_metrics_leak_suspect_detection` に加え、`test_e2e_alloc_metrics_ci_artifact_payload` と `scripts/ci/collect-gc-metrics.sh` で `ci-artifacts/gc-metrics/{sha}/summary.json` を生成し、required CI job `gc-metrics-artifact` から `gc-metrics-{sha}` artifact を保存できる。artifact payload には `gate_status` / `s14_status` / `s14_reason` / `s15_status` / `s15_reason` / `s16_status` / `s16_reason` に加えて `s15_proof` / `s16_proof` / `heap_bytes_series` / `proxy_workloads` も入り、light compile+run / REPL 50 eval / stateful single-session REPL / stateful long-session REPL / actual `lsp --stdio` repeated sequence の representative result、actual `s15_proof`（stage2/stage3 fixed-point compare）、actual `s16_proof`（canonical workload set complete declaration）を保持する。S14 monotonic-trend evaluator は Rust test / Python validator の両方で共有し、`s14_reason` / `s15_reason` / `s16_reason` と status の整合も validator で検証する。collector proof fixture 契約として `s15_proof.gc_mode` / `s16_proof.gc_mode` を collector mode (`mark-sweep` / `generational`) に制限し、`s16_status = pass` では canonical workload set の complete declaration を required にした (`test_gc_metrics_script_rejects_fixture_payload_with_noncollector_s15_gc_mode`, `test_gc_metrics_script_rejects_fixture_payload_with_incomplete_s16_workload_set`, `test_gc_metrics_script_accepts_fixture_payload_with_valid_s15_s16_proofs`)。さらに `LSHARP_GC_METRICS_INPUT=/path/to/summary.json` による validate-only fixture path と、`LSHARP_GC_PROOF_BUNDLE_INPUT=/path/to/collector-proof.json` による external S15/S16 proof merge path、sibling `collector-proof.json` の自動検出、validator 通過後の normalized sidecar 常設出力、`gc-metrics-artifact` の directory upload を追加し、fresh payload が既に持つ actual proof を stale な隣接 sidecar で上書きしない merge 規則まで固定した (`test_gc_metrics_script_merges_external_collector_proof_bundle_into_summary`, `test_gc_metrics_script_rejects_invalid_external_collector_proof_bundle`, `test_gc_metrics_script_autoloads_adjacent_collector_proof_bundle`, `test_gc_metrics_script_adjacent_proof_bundle_does_not_override_actual_payload_proofs`, `test_gc_metrics_script_writes_default_collector_proof_sidecar`, `test_gc_metrics_script_normalizes_collector_proof_sidecar_after_merge`, `test_e2e_gc06_ci_artifact_contract`, `bash scripts/ci/collect-gc-metrics.sh`)。
- [x] Step 5 exit gate -- compile+run / REPL loop、metrics API、CI artifact (`gc-metrics-artifact`) に加え、stateful LSP/REPL の単一セッション sequence soak と long-session REPL soak、actual `lsp --stdio` repeated sequence soak、さらにそれらの representative result を `proxy_workloads` として artifact 化するところまでは追加済み。今回 same-session REPL / actual stdio workload で post-session collector telemetry に加え、runtime 自身の `_start` return / `proc_exit(0)` 成功終了経路 auto collect でも `gc_collection_count > 0` / `gc_freed_count > 0` / `root_stack_top = 0` を固定したが、sentinel-aware な precise rooting と compiler-side GC-safe point spill / shadow stack 完全列挙はまだ gate 未充足。

#### Step 6. `CP-06` の運用文書と第1段 gate を整える

- [x] [`OPS-01 CI gate-v2 job graph`](docs/development/planning/phase11-implementation-plan.md#ops-01-ci-gate-v2-job-graph) / [`OPS-02 Artifact policy`](docs/development/planning/phase11-implementation-plan.md#ops-02-artifact-policy) -- `ci-gate` / `ci-gate-v2` に `default-path-smoke` / `test-fresh-clone` / `fresh-clone-smoke` / `gc-metrics-artifact` / `editor-extension-build` を required job として組み込み、`bootstrap-diff-${sha}` / `fresh-clone-archive-${sha}` / `gc-metrics-${sha}` / `ci-gate-v2-results` / `shadow-oracle-results` と release workflow-local artifact `lsharp-{version}-{target}`・GitHub Release asset `lsharp-{version}-{target}.{ext}` の命名 / retention も workflow と docs に同期した。さらに `test_e2e_ops01_ci_gate_v2` / `test_e2e_ops02_artifact_policy` が current job graph / artifact 名 drift を検知する。GitHub branch protection でも Actions 表示名 `CI Gate v2` を `main` の required check として適用済みで、`gh api` でも required check / strict / review / conversation resolution / admin enforcement を再確認した。release signing は secret-gated workflow hook まで接続済みであり、実署名 credential 投入そのものは repo 外運用として mainline blocker から外す。
- [x] [`OPS-05 Default path migration`](docs/development/planning/phase11-implementation-plan.md#ops-05-default-path-migration) -- **hybrid default path cutover 完了**: `scripts/ci/default-path-smoke.sh`, `docs/development/operations/default-path-migration.md`（公開 13 CLI サブコマンドの selfhost surface と、現行 host launcher の embedded guest subset / intentional host-backed subset / `LSHARP_PATH` delegation を明記）, `docs/development/planning/compatibility-matrix.md`（同じ `Default path / delegation` サマリ）, `crates/lsharp-driver/src/main.rs` の `LSHARP_PATH` delegation と shadow-command hint、E2E `test_e2e_ops05_default_path_migration`、driver integration `cargo test -p lsharp-driver --test default_path_delegation`。`parse` / `check` / `compile` / `build` / `test` / `review`（text + `--json` / `--format json`） / simple `doc-ack <file>` / simple `doc-check <file>` / `fmt` は embedded guest default、`install` / `repl` / `lsp` / `doc` は package/fs/network/stdio server surface のため host launcher 側の intentional subset として固定した。runtime `LSHARP_DISABLE_EMBEDDED_COMPONENT=1` では `parse` / `review` / simple `doc-ack` / simple `doc-check` が host 別契約へ暗黙 fallback せず delegation hint に戻ることも `default_path_delegation.rs` と smoke で固定済み。加えて `compile` / `build` の `-o` / `--output` は guest summary surface を維持しつつ output file に runnable Wasm / Component bytes を書くことを driver integration と `default-path-smoke.sh` の magic-bytes check で固定した。
- [x] [`OPS-06 Release playbook`](docs/development/planning/phase11-implementation-plan.md#ops-06-release-playbook) / [`OPS-07 Fresh clone without Rust`](docs/development/planning/phase11-implementation-plan.md#ops-07-fresh-clone-without-rust) -- `scripts/release-playbook.sh` は host launcher release binary を使って `compile-phase11-inputs.sh` / `default-path-smoke.sh` を再利用する形に進み、`scripts/ci/test-fresh-clone.sh` は CI 上で `test-fresh-clone`（workflow-local downloaded archive を Rust toolchain 無しで binary-only smoke）と `fresh-clone-smoke`（clean checkout 再ビルド）の 2 job を支える。さらに `.github/workflows/release.yml` と `test_e2e_ops06_release_playbook` により `v*` tag push → verify/build/release の自動 GitHub Release workflow も追加済み。今回 `scripts/release.sh` は host launcher archive に加えて companion sidecar `lsharp-{version}-{target}.component.wasm` を生成し、archive 内にも `lsharp.component.wasm` として同梱するよう更新した。`scripts/ci/release-smoke.sh` も build job で生成済み archive を展開して required payload (`README.md` / `LICENSE` / `checksums.txt` / `lsharp-lsp` / `lsharp.component.wasm`) 検証、`checksums.txt` 検証、packaged `lsharp` の `--version` / `check` / `fmt` / `compile` / `test` / `doc` / `doc --json` smoke と `lsharp-lsp --version` smoke を実行できるようにした (`test_e2e_ops06_release_smoke_contract`, `test_e2e_ops06_release_smoke_script_runs_fixture_archive`)。加えて release job は build artifact download 後に `bash scripts/checksum.sh dist > dist/checksums.txt` を実行し、archive 群と sidecar component、attached release-level checksum asset も GitHub Release へ添付する。downloaded artifact を Rust toolchain 無しで再検証する `release-smoke` job も release workflow に追加済み (`test_e2e_ops07_release_download_smoke_job`)。この middle gate は Ubuntu 上で実行可能な Linux x86_64 archive に限定し、macOS/Windows archive を同 runner で誤って実行しない current contract に揃えた。README / fresh-clone 側の `scripts/smoke_test_readme.sh` でも inline Quick Start fixture に対する checksum / compile / test / doc smoke を通すようになり、host-backed `doc` distribution ownership を二重化した。さらに `release-smoke.sh` / `default-path-smoke.sh` / `smoke_test_readme.sh` は compile output の Wasm magic bytes と実行可能性を確認する契約へ更新し、`test_e2e_ops05_default_path_migration`, `test_e2e_ops06_release_smoke_contract`, `test_e2e_ops06_release_smoke_script_runs_fixture_archive`, `test_e2e_ops07_fresh_clone_no_rust`, `test_e2e_ops07_test_fresh_clone_script_runs_binary_only_fixture_archive` を focused rerun で再確認した。今回さらに `scripts/fetch-stage0.sh` / `scripts/bootstrap.sh` / `scripts/release-bundle.sh` を追加し、release asset 取得 → `selfhost/src/App/EmbeddedCli.ls` の stage1/stage2 sidecar compare → release-style rebundle を operator path で試せるようにした。host launcher は隣接 `lsharp.component.wasm` を build-time embedded guest より優先し、`LSHARP_PATH=<*.component.wasm>` の `compile` / `build` も guest summary を保ったまま actual artifact bridge できる。`gh api` で `Release` workflow が active であることも再確認済みで、実署名・実 release publish・GitHub Releases / stage0 fetch を required gate に昇格する運用は repo 外オペレーションとして mainline blocker から外す。
- [x] [`OPS-08 Host launcher cutover and rollback`](docs/development/planning/phase11-implementation-plan.md#ops-08-final-removal-and-rollback) -- `scripts/rollback.sh`, `docs/development/operations/rollback-procedure.md`, `docs/development/operations/release-distribution-signing.md`, `docs/development/operations/release-playbook.md`, `docs/development/operations/adr-rust-removal.md` を host launcher + guest component 基準へ同期済み。GitHub Release notes の `Rollback anchor` を last-known-good release tag / host asset / guest component sidecar asset (`lsharp-{version}-{target}.component.wasm`) / checksum の正本として固定した。
- [x] Step 6 exit gate -- 文書整備に加え、`default-path-smoke` と `fresh-clone-smoke` の blocking 化、`scripts/release-playbook.sh` の release binary smoke 再利用、`LSHARP_PATH` delegation hook、`.github/workflows/release.yml` による `v*` tag push 自動 release、さらに embedded guest subset / intentional host-backed subset を含む hybrid default path ownership の明文化までは確認できる。**Component Model pivot により Rust 物理撤去 gate は撤回**。2026-04-03 に `gh api` で branch protection / active workflow を再確認し、残る実署名・実 release publish・GitHub Releases / stage0 fetch 直結運用は repo 外オペレーションとして本 TODO の blocker から切り離した。

### Phase 11 クリティカルパス現況

- [x] `CP-01 Frontend/bootstrap` -- Step 1 は完了し、WASM-03 / oracle 4 連一致 / 4 層比較 / ステージチェーン検証で stage0 proxy evidence は揃った。さらに minimal subset / extended `do` / zero-arg multi-fn call / single-param call / let local / 5 defn loop に加え、memory-only builtin (`string-char-at` / `string-length` / `vector-length` / `vector-get`) と `vector-*` / `ref-*` + `env.__alloc` import、integer-key `map-new` / `map-insert` / `map-get` / `map-size` / `map-contains?` / `map-remove`、source-aware `read-file "fixture.txt"` path literal、5 式以上の `do` / `if` branch / `match` arm body / `lambda` body string literal data section、non-literal string-key runtime hash、generalized 2-helper alloc+print pair、generalized 3-helper alloc+print+read-file triple、generalized 4-helper alloc+print+read-file+command-line-arg quad、generalized 4-helper alloc+print+read-file+__fnv1a_hash quad の real stage1→stage2 wasm 生成 slice、さらに same-source stage1→stage2 repeatability の minimal / alloc-import / read-file helper / source-aware read-file path string / command-line-arg helper / generalized alloc+print pair / generalized alloc+print+read-file triple / generalized arg quad / generalized hash quad proxy まで確認済みで、`Compiler.ls` / `WasmEmit.ls` も per-function metadata ベースの multi-function + signature/local emission、memory builtin opcode、narrow alloc-import helper、stateful if else/end 変換、`memory.copy` を含む vector growth emission、ref cell emission、fixed-capacity map emission、literal-pattern `match` 条件 lowering、`__fnv1a_hash` import 連携、helper-pair / helper-triple / helper-quad dispatch へ前進した。今回さらに `scripts/ci/compile-phase11-inputs.sh` が selfhost/stdlib/examples の fixed input set compile gate と optional fixed-point verify を持ち、`.github/workflows/ci.yml` bootstrap job は `RUN_BOOTSTRAP_FIXED_POINT: 1` と `bootstrap-diff-${{ github.sha }}` upload (`if: always()`) を接続した。続けて `test_e2e_bootstrap_cli_fixed_input_compile_gate` で `App/Cli.ls` direct compile gate を固定し、`Types.TypeInferCore` / `Tools.Text.FormatterDecl` / `Tools.Lsp.JsonRpc` / `Tools.Lsp.LspServerCore` / `Tools.Lsp.LspServerNav` import を同期した結果、historical `App/Cli.ls` memory.grow blocker は再現しなかった。`RUN_BOOTSTRAP_FIXED_POINT=1 bash scripts/ci/compile-phase11-inputs.sh` も exit 0 で `test_e2e_bootstrap_fixed_point_stage2_stage3`・`test_e2e_bootstrap_stage2_self_feed_fixed_input_set`・`test_e2e_bootstrap_fixed_input_set_stage_chain_match` を完走し、前二者が `Main.ls` fixed point と fixed input set 54 件（selfhost 40 / stdlib 11 / examples 3）の stage2 self-feed 決定性を、最後の gate が同じ 54 件の `stage1 -> stage2` 出力と `stage2 -> stage3` 出力の bit-identical compare を `fixed-input-set-self-feed-report.txt` / `fixed-input-set-self-feed.json` / `fixed-input-set-stage-chain-report.txt` / `fixed-input-set-stage-chain.json` へ保存する。これにより true stage1→stage2→stage3 self-bootstrap fixed point の full input set 実体比較を CI 経路で提示できた。Evidence: `crates/lsharp-wasm/tests/e2e/selfhost_bootstrap_four_layer.rs`, `crates/lsharp-wasm/tests/e2e/selfhost_bootstrap_acceptance.rs`, `crates/lsharp-wasm/tests/e2e/strings_patterns_compiler_integration.rs`, `scripts/ci/compile-phase11-inputs.sh`, `selfhost/src/App/Cli.ls`, `.github/workflows/ci.yml`, `docs/development/operations/bootstrap-diff-artifacts.md`
- [x] `CP-02 Syntax/types parity` -- syntax/type 系テストは 100 件超まで拡大。selfhost parser では全宣言タグ・全式ノード・全パターンタグの parse coverage を達成し、TypeInfer は 7 引数 curried lambda/defn/apply、14 式 do、3-step computation、全 match pattern 種別（wildcard/var/lit/constructor/record）、error code parity（E0001-E0006）、TypeScheme の deterministic ordering まで固定。`infer_decl_functions` の generalize 改修で standalone check / open import polymorphism を維持。E2E harness は現時点で **683 tests / 2 ignored** 構成、docs audit は **error 0 / warning 0**。Evidence: `docs/development/planning/compatibility-matrix.md`, `tests/golden/syntax/ast_node_map.json`, `selfhost/src/Types/TypeInfer.ls`, `selfhost/src/Types/TypeScheme.ls`, `selfhost/src/Syntax/Parser.ls`, `crates/lsharp-types/src/infer.rs`
- [x] `CP-03 IR/backend/native` -- **Deferred extraction complete (Component Model pivot)**。Lower/LowerPattern の stage0 compile、NATIVE-05 の structural parity、NATIVE-06 の proxy differential harness と空 allowlist に加え、`NativeCodegen.ls` は full-width const bytes と複数 IR 命令の逐次 codegen、`NativeEmit.ls` は Mach-O / ELF object file への full native payload 保持、`NativeTarget.ls` は 12-field descriptor と target policy accessor 群を持ち、`selfhost/src/App/Main.ls` は compile-source 由来 IR を x86_64-darwin / aarch64-darwin / x86_64-linux の 3 target summary へ通し、header/response bytes まで観測できるようになった。さらに stage1 が生成した host-target 向け AArch64 code を `clang -arch arm64` でリンク・実行する `test_e2e_native_host_binary_link_and_execute`、および const 0/1/42/100 の Wasm stdout と native exit code を突き合わせる zero-diff sample 群 (`test_e2e_zero_diff_const_*`, `test_e2e_zero_diff_sample_summary`) も追加済みで、canonical selfhost module path を使う stage-chain fixture も `test_e2e_native_target_descriptor_exposes_policy_fields` を含む native harness 群で固定した。Phase 11 mainline では native parity gate を reopen せず、残る true native self-regeneration / wide differential zero は `V2-08` / `V2-09` の deferred track へ移した。Evidence: `selfhost/src/Backend/Native/NativeCodegen.ls`, `selfhost/src/Backend/Native/NativeEmit.ls`, `selfhost/src/Backend/Native/NativeTarget.ls`, `selfhost/src/App/Main.ls`, `crates/lsharp-wasm/tests/e2e/selfhost_native_differential.rs`, `crates/lsharp-wasm/tests/e2e/selfhost_native_stage_chain.rs`, `tests/differential-allowlist.yaml`
- [x] `CP-04 Public toolchain` -- CLI/LSP/FMT/DOC の selfhost source、help/version・sort/dedup・schema/documentation に加え、`test` metadata 実行、top-level `defn` source-driven LSP subset、CLI check の error-code-aware summary text と REPL warmup session summary (`type:Int` / `evals:1` / `input-bytes:17`)、Formatter canonical text core、CLI fmt 実テキスト出力、CLI review count/title/body/severity/code-location surface、CLI doc title/body surface、CLI doc-ack/doc-check status text と comment-form trailer、`doc-ack --trailer` / `doc-check --strict` の file-handler / actual `main` slice と strict failure の non-zero process exit、LSP formatting `parse-program` → `format-program` integration、`initialize` capability summary の references/rename/formatting 露出、DocTools/HtmlDoc deterministic payload/HTML slice までは前進。2026-04-01: `compile_multi_file` の Formatter トリオバッチ、`LspServer` の dispatch/loop 復元、`LspServerNav` 接続、hier fixture の `test_e2e_selfhost_lsp_hier_fixture_shape_cross_document_definition`、open 済み別 document の top-level `defn` を completion candidate に merge する stateful parity (`test_e2e_lsp_stateful_completion_resolves_hier_fixture_shape_open_document`, `test_e2e_lsp_actual_stdio_completion_resolves_hier_fixture_shape_open_document`)、および HtmlDoc の function/type/module title escape regression (`test_e2e_selfhost_htmldoc_render_function_signature_escapes_html`, `test_e2e_selfhost_htmldoc_render_type_definition_escapes_html`, `test_e2e_selfhost_htmldoc_render_module_page_escapes_title`) も追加。2026-04-02: Doc trailer slice として `DocTools.ls` が `; Doc-Review-Status:` / `; Doc-Reviewed-By:` comment trailer と `doc-check-trailer-valid?` を持ち、`App/Cli.ls` も `doc-ack --trailer` / `doc-check --strict` を actual `main` と strict failure の exit parity まで処理できるようになった。続けて DOC-01 schema fidelity として `review.schema.json` が `title` / `severity` / `message` / `line` / `column` / `code` を必須化し、`knowledge.schema.json` が function entry の `params` / `returns` / `doc` / `example` を、`doc-output.schema.json` が function/type entry arrays と `html.sections` の `id/count` metadata` に加えて function `params[{name,type,doc}]` / `returns{type,doc}` / `doc` / `example` property を持てるようになった。selfhost `Parser.ls` は defn trailing metadata として `:doc` / `:example` / `:params` / `:returns` を保持し、typed `generate-knowledge` は function entry へ `params` (`name:type`) / `returns` / `doc` / `example` を返せる。typed `generate-doc-output` は parameter/return doc を含む object shape を返し、`HtmlDoc` も module page で function signature / params / returns / doc / example を host-truth 寄り HTML として escape/render できる。2026-04-03: `App/Cli.ls` が `App.ModuleResolver` / `App.CompilerMode` を介する import-aware compile/build file path を持ち、nested import fixture に対する `run-compile` / `run-build` / actual `main` の `compile/build <file>` / `-o` / `--output` / `--target` parity も固定した (`test_e2e_selfhost_cli_compile_file_handler_multifile_nested_imports`, `test_e2e_selfhost_cli_build_file_handler_multifile_nested_imports`, `test_e2e_selfhost_cli_main_with_args_compile_file_multifile_nested_imports`, `test_e2e_selfhost_cli_main_with_args_build_file_multifile_nested_imports`, `test_e2e_selfhost_cli_main_with_args_compile_target_and_output_path`, `test_e2e_selfhost_cli_main_with_args_compile_target_changes_wasm_size`, `test_e2e_selfhost_cli_main_with_args_build_output_path_and_target_alias`)。2026-04-04: representative filesystem-backed actual `lsp --stdio` snapshot、`DocJson.ls` による payload / HTML / schema-object snapshot、release/readme/fresh-clone smoke、actual `lsp --stdio` repeated-sequence soak、driver 側 doc-site generation focused gate まで green を再確認し、公開 toolchain parity gate を再閉鎖した。Evidence: `selfhost/src/App/Cli.ls`, `selfhost/src/Tools/Lsp/LspServer.ls`, `selfhost/src/Tools/Lsp/LspServerNav.ls`, `crates/lsharp-wasm/tests/lsp_stateful_parity.rs`, `crates/lsharp-ir/src/lib.rs` (`try_infer_formatter_trio_batch`), `selfhost/src/Tools/Text/Formatter.ls`, `selfhost/src/Tools/Doc/DocTools.ls`, `selfhost/src/Tools/Doc/HtmlDoc.ls`, `selfhost/src/Tools/Test/TestRunner.ls`, `selfhost/src/Syntax/Parser.ls`, `crates/lsharp-wasm/tests/doctools_parity.rs`, `crates/lsharp-wasm/tests/e2e/selfhost_cli_core.rs`, `crates/lsharp-wasm/tests/e2e/selfhost_cli_actual_main_args.rs`, `crates/lsharp-wasm/tests/e2e/selfhost_doctools_cli_diagnostics.rs`, `crates/lsharp-wasm/tests/e2e/selfhost_lsp_docs_ops.rs`, `crates/lsharp-wasm/tests/e2e/selfhost_parser_metadata_forms.rs`, `crates/lsharp-wasm/tests/e2e/selfhost_gc_stateful_soak.rs`, `crates/lsharp-driver/src/doc_site.rs`
- [x] `CP-05 Runtime stability` -- GC-05/06 の proxy soak、metrics API、CI gate 文書は追加済み。加えて REPL single-session / long-session soak と repeated LSP stateful sequence、actual `lsp --stdio` repeated sequence soak までは固定し、same-URI `didOpen` の doc-count 増殖も止めた。`gc-metrics-artifact` required job と artifact payload の `gate_status` / `s14_status` / `s14_reason` / `s15_status` / `s15_reason` / `s16_status` / `s16_reason`、さらに actual `s15_proof` / `s16_proof` により collector proof artifact / CI 自体は自己記述化された。actual collector は root stack から per-entry mark state + fixed-point trace を回し、`Ref` / `Vector` / `HashMap` / `Closure` / `Record` / `ADT` payload を transitively mark できる。さらに `_start` return / `proc_exit(0)` 成功終了経路で `__lsharp_gc_collect` を自動実行する slice を追加し、`test_e2e_gc_repl_stateful_long_session_in_session_collector_telemetry` / `test_e2e_gc_lsp_actual_stdio_repeated_sequence_in_session_collector_telemetry` で representative workload の session-internal collector telemetry まで固定した。selfhost compiler-mode の root runtime import parity も `root_push` / `root_pop` / `root_set` の actual call まで回収し、source-aware `string-concat` / `substring` / `ref-new` / `vector-push` / `map-insert` / `map-get` / direct user call の spill + root auto-insertion IR parity も `test_e2e_selfhost_compiler_*` 群で固定済み。`test_e2e_alloc_metrics_ci_artifact_payload` と `bash scripts/ci/collect-gc-metrics.sh` の current required path は `allocator_mode = "collector"`、`s14_status = "pass"`、`s15_status = "pass"`、`s16_status = "pass"`、actual `heap_bytes_series`、`gc_collection_count > 0`、`gc_freed_count > 0` を持つ `summary.json` / `collector-proof.json` を出力し、`runtime-stability-spec.md` の G1 は documented limitation、G3 の 4 slice 棚卸しは GREEN として整理済み。これで Phase 11 Gate 2 の machine-readable evidence が揃った。Evidence: `crates/lsharp-wasm/tests/e2e.rs`, `crates/lsharp-wasm/tests/gc_metrics_contract.rs`, `crates/lsharp-wasm/tests/e2e/support.rs`, `crates/lsharp-wasm/tests/e2e/runtime_allocator_closures.rs`, `crates/lsharp-wasm/tests/e2e/strings_patterns_compiler_integration.rs`, `crates/lsharp-wasm/tests/e2e/selfhost_rooting_parity.rs`, `crates/lsharp-wasm/tests/e2e/selfhost_bootstrap_four_layer.rs`, `crates/lsharp-wasm/tests/e2e/selfhost_cli_core.rs`, `crates/lsharp-wasm/src/wasi.rs`, `crates/lsharp-ir/src/lower/decl.rs`, `crates/lsharp-ir/src/lower/expr.rs`, `crates/lsharp-ir/src/lower/mod.rs`, `crates/lsharp-ir/src/lower/tests.rs`, `crates/lsharp-types/src/infer.rs`, `crates/lsharp-syntax/src/span.rs`, `selfhost/src/Backend/Wasm/Compiler.ls`, `selfhost/src/Backend/Wasm/WasmEmit.ls`, `selfhost/src/App/CompilerMode.ls`, `selfhost/src/App/Cli.ls`, `docs/development/planning/gc-ci-gate-spec.md`, `scripts/ci/collect-gc-metrics.sh`
- [x] `CP-06 CI cutover` -- `ci-gate` / `ci-gate-v2` に compile gate + audit-docs + default-path-smoke + `test-fresh-clone` + fresh-clone-smoke + gc-metrics-artifact + editor-extension-build は構成済みで、release playbook も release binary で共通 smoke を再利用する段階まで前進し、driver 側 `LSHARP_PATH` delegation で external compiler への差し替え hook も入った。さらに embedded guest default path は `parse` / `check` / `compile` / `build` / `test` / `review`（text + `--json` / `--format json`） / simple `doc-ack <file>` / simple `doc-check <file>` / `fmt` まで拡張され、`test_test_without_lsharp_path_uses_embedded_component_default_path` / `test_review_without_lsharp_path_uses_embedded_component_default_path` / `test_review_json_without_lsharp_path_uses_embedded_component_default_path` / `test_review_format_json_without_lsharp_path_uses_embedded_component_default_path` / `test_doc_ack_without_lsharp_path_uses_embedded_component_default_path` / `test_doc_check_without_lsharp_path_uses_embedded_component_default_path` に加えて `test_review_without_lsharp_path_respects_embedded_disable_flag` / `test_doc_ack_without_lsharp_path_respects_embedded_disable_flag` / `test_doc_check_without_lsharp_path_respects_embedded_disable_flag` と `default-path-smoke.sh` で metadata summary (`examples:N` / `invariants:N` / `failures:N`)、review diagnostics summary (`unused-let` / `warning` / `L0001@1:1`)、review schema-object JSON、doc ack/check payload (`ack:recorded` / `status:ok`)、runtime disable 時の delegation hint 復帰も固定済み。さらに `.github/workflows/release.yml` により `v*` tag push 自動 release も追加済み。2026-04-02: `docs/development/operations/branch-protection-checklist.md` / `CI.md` / `ci-gate-v2-job-graph.md` と current workflow graph を同期し、GitHub branch protection でも Actions 表示名 `CI Gate v2` を `main` の required check として適用した。2026-04-03: `gh api` で branch protection と `CI` / `Release` workflow の active state を再確認した。**Component Model pivot により Rust workspace は host launcher として残存する方針に転換**。default path ownership も全 13 CLI コマンドについて embedded guest subset / intentional host-backed subset として整理済みで、実署名・実 release publish・GitHub Releases / stage0 fetch 直結運用は repo 外オペレーションとして TODO blocker から切り離す。Evidence: `.github/workflows/ci.yml`, `.github/workflows/release.yml`, `scripts/ci/default-path-smoke.sh`, `scripts/ci/test-fresh-clone.sh`, `scripts/release-playbook.sh`, `docs/development/operations/CI.md`, `docs/development/operations/ci-gate-v2-job-graph.md`, `docs/development/operations/artifact-policy.md`, `docs/development/operations/branch-protection-checklist.md`, `docs/development/operations/adr-rust-removal.md`, `docs/development/operations/rollback-procedure.md`, `crates/lsharp-driver/tests/default_path_delegation.rs`, `test_e2e_ops01_ci_gate_v2`, `test_e2e_ops05_default_path_migration`, `test_e2e_ops06_release_playbook`, `test_e2e_ops07_fresh_clone_no_rust`, `test_e2e_ops08_final_removal_rollback`
- [x] `CP-06` embedded review/doc default-path parity 回帰 -- 根因は `crates/lsharp-ir/src/lower/mod.rs` の `FuncCtx::alloc_local` が `_strcat_lhs` / `_strcat_rhs` / `_..._root_slot` など compiler temp local を name 再利用していたことで、`crates/lsharp-ir/src/lower/expr.rs` の rooted lowering が入れ子の `string-concat` / `int-to-string` chain で外側一時値を内側 lowering に上書きしていた点だった。`_` prefix の compiler temp local を常に fresh allocate する修正を入れ、focused regression `test_e2e_string_concat_nested_summary_chain` / `test_e2e_string_concat_nested_code_location_chain`、selfhost `test_e2e_selfhost_cli_review_source_core` / `test_e2e_selfhost_cli_doc_source_core` / `test_e2e_selfhost_doctools_doc_ack_trailer_payload`、driver default-path `review` / `review --json` / `doc-ack` / `doc-check` 群まで再 green に戻した。追補: `cargo clippy -q -p lsharp-wasm -p lsharp-driver --tests -- -D warnings` の focused rerun では今回差分外の既存 lint debt（`crates/lsharp-wasm/src/codegen.rs` の doc comment blank line、`crates/lsharp-wasm/src/wasi_runner.rs` の `clone_on_copy`、`crates/lsharp-wasm/tests/snapshot.rs` の `useless_format`、`selfhost_bootstrap_four_layer.rs` / `selfhost_cli_core.rs` の test-only style warnings）が surfaced しており、この tranche では記録のみに留める。加えて broad `cargo test -q` rerun では今回差分外の stale test 契約も surfaced し、`e2e_selfhost_tokens` は canonical `Token.ls` の `demo-main` wrapper に揃え、`e2e_selfhost_typeinfer_pattern_match` は heap pointer 値ではなく `string-eq` を介した意味論確認へ寄せて brittle expectation を除去し、`meta_validation` は Deferred / v2 項目の actual `[DEFERRED]` 表記に追随させた。

### Phase 11 実装状態

- [x] [META-02 Completion marker sync](docs/development/planning/phase11-implementation-plan.md#meta-02-completion-marker-sync) -- `TODO.md`, `docs/development/planning/compatibility-matrix.md`, `docs/development/planning/completion-criteria.md` を実装実態へ同期。2026-04-01: Formatter トリオバッチ・LSP hier・branch-protection チェックリストの証跡を `TODO.md` 正本メモへ反映。
- [x] [META-03 Audit-docs gate](docs/development/planning/phase11-implementation-plan.md#meta-03-audit-docs-gate) -- `scripts/audit_docs.sh`, `.github/workflows/ci.yml` で Phase 11 完了矛盾とエビデンス欠落を fail-fast 化。
- [x] [BOOT-03 stdlib direct compile blockers](docs/development/planning/phase11-implementation-plan.md#boot-03-stdlib-direct-compile-blockers) -- `scripts/ci/compile-phase11-inputs.sh` を追加し、bootstrap job で selfhost/stdlib/examples の fixed input set を blocking 化。
- [x] [BOOT-01 Main.ls import path consolidation](docs/development/planning/phase11-implementation-plan.md#boot-01-mainls-import-path-consolidation) -- Evidence: `selfhost/src/App/Main.ls` import-only コメント・パイプライン、`crates/lsharp-wasm/tests/e2e.rs`（`compile_and_run_file` / `selfhost_main_path`）。
- [x] [BOOT-04 True stage1-stage2-stage3 bootstrap](docs/development/planning/phase11-implementation-plan.md#boot-04-true-stage1-stage2-stage3-bootstrap) -- 4 層比較 + ステージチェーン検証 + proxy / oracle evidence に加え、minimal subset / extended `do` block / zero-arg multi-fn call / single-param call / let local / 5 defn loop / memory-only builtin (`string-char-at` / `string-length` / `vector-length` / `vector-get`) に加えて integer-key `map-contains?` / `map-remove` の real stage1→stage2 生成 slice までは確認済み。追加の slice として direct / nested `do` / 5 式以上の `do` / `if` branch / `match` arm body / `lambda` body string literal を source-aware に lowering できるようになり、さらに non-literal string-key map も opcode 68 + `alloc+print+read+__fnv1a_hash` helper combo で stage2 実行まで通った。今回さらに generalized 2-helper alloc+print pair が `emit-type-section-helper-pair-main` / `emit-import-section-helper-pair` 経由で、generalized 3-helper alloc+print+read-file triple が `emit-type-section-helper-triple-main` / `emit-import-section-helper-triple` 経由で、generalized 4-helper alloc+print+read-file+command-line-arg quad と generalized 4-helper alloc+print+read-file+__fnv1a_hash quad が `emit-type-section-helper-quad-main` / `emit-import-section-helper-quad` 経由で stage2 実行まで通り、同一 tiny source に対する stage1→stage2 repeatability も minimal / alloc-import / read-file helper / source-aware read-file path string / command-line-arg helper / generalized alloc+print pair / generalized alloc+print+read-file triple / generalized arg quad / generalized hash quad path で固定できた。`test_e2e_selfhost_compiler_string_literal_source_data_lowering` / `test_e2e_selfhost_compiler_nested_string_literal_source_data_lowering` / `test_e2e_selfhost_compiler_extended_do_string_literal_source_data_lowering` / `test_e2e_selfhost_compiler_if_string_literal_source_data_lowering` / `test_e2e_selfhost_compiler_match_string_literal_source_data_lowering` / `test_e2e_selfhost_compiler_lambda_string_literal_source_data_lowering` / `test_e2e_selfhost_compiler_map_non_literal_string_key_runtime_hash_lowering` と `test_e2e_bootstrap_stage1_emits_stage2_wasm_for_string_literal_data_section` / `test_e2e_bootstrap_stage1_emits_stage2_wasm_for_nested_string_literal_data_section` / `test_e2e_bootstrap_stage1_emits_stage2_wasm_for_extended_do_string_literal_data_section` / `test_e2e_bootstrap_stage1_emits_stage2_wasm_for_if_string_literal_data_section` / `test_e2e_bootstrap_stage1_emits_stage2_wasm_for_match_string_literal_data_section` / `test_e2e_bootstrap_stage1_emits_stage2_wasm_for_lambda_string_literal_data_section` / `test_e2e_bootstrap_stage1_emits_stage2_wasm_for_non_literal_string_key_map_program` / `test_e2e_bootstrap_stage1_emits_stage2_wasm_for_generalized_alloc_print_helper_pair` / `test_e2e_bootstrap_stage1_emits_stage2_wasm_for_generalized_alloc_print_read_helper_triple` / `test_e2e_bootstrap_stage1_emits_stage2_wasm_for_generalized_alloc_print_read_arg_helper_quad` / `test_e2e_bootstrap_stage1_emits_stage2_wasm_for_generalized_alloc_print_read_hash_helper_quad` / `test_e2e_bootstrap_stage1_emits_identical_stage2_wasm_for_same_tiny_source` / `test_e2e_bootstrap_stage1_emits_identical_alloc_stage2_wasm_for_same_source` / `test_e2e_bootstrap_stage1_emits_identical_read_helper_stage2_wasm_for_same_source` / `test_e2e_bootstrap_stage1_emits_identical_read_file_path_stage2_wasm_for_same_source` / `test_e2e_bootstrap_stage1_emits_identical_arg_helper_stage2_wasm_for_same_source` / `test_e2e_bootstrap_stage1_emits_identical_alloc_print_pair_stage2_wasm_for_same_source` / `test_e2e_bootstrap_stage1_emits_identical_read_helper_triple_stage2_wasm_for_same_source` / `test_e2e_bootstrap_stage1_emits_identical_arg_helper_quad_stage2_wasm_for_same_source` / `test_e2e_bootstrap_stage1_emits_identical_hash_helper_quad_stage2_wasm_for_same_source`、さらに `test_e2e_bootstrap_stage1_emits_stage2_wasm_for_map_contains_program` / `test_e2e_bootstrap_stage1_emits_stage2_wasm_for_map_remove_program` / `test_e2e_bootstrap_stage1_emits_stage2_wasm_for_read_file_path_string_program` / `test_e2e_bootstrap_stage1_emits_stage2_wasm_for_command_line_arg_program` が green。追加で `test_e2e_bootstrap_fixed_input_set_stage_chain_match` が fixed input set 54 件（selfhost 40 / stdlib 11 / examples 3）について `stage1 -> stage2` 出力と `stage2 -> stage3` 出力の bit-identical compare を固定し、`fixed-input-set-stage-chain-report.txt` / `fixed-input-set-stage-chain.json` に mismatch 情報（fingerprint / export_match / data_match / first_diff）を保存できるようになった。これにより true stage1→stage2→stage3 self-bootstrap fixed point は full input set 実体比較まで閉じた。Evidence: `crates/lsharp-wasm/tests/e2e/selfhost_bootstrap_four_layer.rs`, `crates/lsharp-wasm/tests/e2e/selfhost_bootstrap_acceptance.rs`, `crates/lsharp-wasm/tests/e2e/strings_patterns_compiler_integration.rs`, `scripts/ci/compile-phase11-inputs.sh`, `docs/development/operations/bootstrap-diff-artifacts.md`
- [x] ~~[NATIVE-05 Stage1-native self-regeneration](docs/development/planning/phase11-implementation-plan.md#native-05-stage1-native-self-regeneration)~~ -- **Deferred extraction complete (→ V2-08)**。structural parity / stage chain テストに加え、`test_native_pipeline_complete_chain` / `test_native_codegen_emit_standalone_execution` / `test_native_codegen_real_execution` / `test_native_codegen_emits_full_const_instruction_bytes` / `test_native_emit_object_keeps_full_native_payload` / `test_e2e_selfhost_main_native_pipeline_summary` で NativeTarget→NativeCodegen→NativeEmit→Linker の実行 slice、full-width const bytecode、Mach-O / ELF object への full payload 保持、compile-source 由来 IR を 3 target summary として通した header/response byte 観測まで固定した。さらに `test_e2e_selfhost_main_native_summary_matches_direct_pipeline_harness` で `selfhost/src/App/Main.ls` の integrated native summary が direct harness の `emit-native` / `emit-object` / `build-linker-args` / `generate-response-file` 結果と、3 target の native/object/linker/response/multi-response 観測点で一致することを固定した。さらに `test_e2e_stage1_native_two_run_determinism` で `selfhost/src/App/Main.ls` を二度独立に実行し、全 71+ 出力行 (darwin/linux/aarch64 native\_len を含む全 native 観測点) が完全一致することを確認した。これにより「stage0 が生成する stage1 は決定的であり stage2 と等価になる」という stage1→stage2 等価性の前提を証明した。Phase 11 mainline ではここで close し、実 native バイナリをリンク・実行して stage2 output を取得する true self-regeneration 比較は `V2-08` の残件とする。`test_e2e_native_self_regeneration_functional_equivalence`, `test_e2e_native_stage_chain_structure`, `test_native_pipeline_complete_chain`, `test_native_codegen_emit_standalone_execution`, `test_native_codegen_real_execution`, `test_native_codegen_emits_full_const_instruction_bytes`, `test_native_emit_object_keeps_full_native_payload`, `test_e2e_selfhost_main_native_pipeline_summary`, `test_e2e_selfhost_main_native_summary_matches_direct_pipeline_harness`, `test_e2e_stage1_native_two_run_determinism`。
- [x] ~~[NATIVE-06 Wasm/native differential](docs/development/planning/phase11-implementation-plan.md#native-06-wasmnative-differential)~~ -- **Deferred extraction complete (→ V2-09)**。5観測点 harness、空 allowlist、structural parity テストに加え、`test_native_codegen_processes_multiple_ir_instructions` で multi-instr native bytecode slice、`test_native_emit_object_keeps_full_native_payload` / `test_native_emit_elf_object_keeps_full_native_payload` で Mach-O / ELF object payload retention slice、`test_native_emit_object_headers_cover_all_three_targets` / `test_native_linker_response_consistency_across_three_targets` / `test_native_linker_multi_object_response_consistency_across_three_targets` で 3-target object header と single/multi-object response consistency も追加済み。さらに `test_e2e_selfhost_main_native_summary_matches_direct_pipeline_harness` で integrated summary と direct harness の target 別 native/object/header/response/multi-response 観測点が一致することを固定した。Phase 11 mainline ではここで close し、実 native 生成物の wide zero diff 証明は `V2-09` の残件とする。`test_e2e_wasm_native_differential_five_observation_points`, `test_e2e_differential_allowlist_empty`, `test_e2e_wasm_native_differential_structural_parity`, `test_native_codegen_processes_multiple_ir_instructions`, `test_native_emit_object_keeps_full_native_payload`, `test_native_emit_elf_object_keeps_full_native_payload`, `test_native_emit_object_headers_cover_all_three_targets`, `test_native_linker_response_consistency_across_three_targets`, `test_native_linker_multi_object_response_consistency_across_three_targets`, `test_e2e_selfhost_main_native_summary_matches_direct_pipeline_harness`。
- [x] [CLI-02 13 command implementations](docs/development/planning/phase11-implementation-plan.md#cli-02-13-command-implementations) -- `selfhost/src/App/Cli.ls` に 13 サブコマンド名、終了コード API、stdout/stderr 分離、`run-command`、`format-subcommand-help` と E2E 6 件は追加済み。さらに `parse` は `decls:N` / `first-decl:<name>` / `first-body:<name>` の deterministic text と `diagnostics:1,P0001@1:1,first-body:unexpected token )` / `unexpected token ]` 形式まで返せ、`parse-diagnostics-count` は recovery 対象の `)` / `]` を 1 件として数え、`test` handler は metadata suite を実行し、failing example を `runtime-error` にでき、さらに `examples:N` / `invariants:N` / `failures:N` の labeled summary も返せる。`check` は builtin 型名 text surface と `diagnostics:1,T0001@1:1,first-body:if condition must be Bool` / `undefined symbol` 形式まで返せ、`repl` は `type:Int` / `evals:1` / `input-bytes:17` の warmup session summary を返せ、`check-diagnostics-count` は top-level `defn` の型エラーを 1 件として返せる。`lsp` は `sync:full` / `hover:true` / `completion:true` / `definition:true` / `references:true` / `rename:true` / `formatting:true` の capability summary text に加えて `requests:1` / `documents:0` / `source-bytes:0` の shared-state summary も返せ、`run-lsp-transport-request` / `run-lsp-transport-sequence` により framed JSON-RPC transport を扱え、`run-lsp-stdio-frame` / `run-lsp-stdio-sequence` と actual `lsp --stdio` により initialize / shutdown / unknown method の stdin wire loop まで narrow E2E で固定された。`main-dispatch` helper により parse/help/version/unknown の entrypoint surface も `run-command` 委譲として narrow E2E で固定された。`doc` / `doc-ack` / `doc-check` は `first-fn` / `first-type` 付きの name-aware title/body summary に加えて comment-form trailer (`; Doc-Review-Status:` / `; Doc-Reviewed-By:`) も出力でき、`doc-ack --trailer` / `doc-check --strict` の narrow argv surface まで actual `main` で固定した。`review` は unused-let / empty-do に対する deterministic な count/title/body/severity/code-location surface を返せる。`fmt` も `format-program-with-source` 接続により string literal を保持でき、`test_e2e_selfhost_cli_fmt_source_string_literal`、`test_e2e_selfhost_cli_main_dispatch_parse_file`、`test_e2e_selfhost_cli_main_dispatch_command_surface`、actual `main` argv coverage、actual `lsp --stdio` 3 件と `cargo run --quiet -- check selfhost/src/App/Cli.ls` が green。
- [x] [LSP-02 10 method parity](docs/development/planning/phase11-implementation-plan.md#lsp-02-10-method-parity) / [x] [LSP-03 Diagnostic ordering](docs/development/planning/phase11-implementation-plan.md#lsp-03-diagnostic-ordering-and-json-snapshots) -- 10 メソッド名、sort/dedup helper、JSON-RPC helper 相当の関数に加え、diagnostics を固定順 JSON text へ落とす `render-diagnostic-json` / `render-diagnostics-json`、および `tests/snapshots/lsp/diagnostics/` の representative snapshot files と targeted E2E も追加済み。加えて same-span / same-severity tie でも rule/message 順の deterministic sort が効き、parse/type overlap では parse diagnostic を優先して 1 件へ集約できる。source param 付きの hover/definition/references/rename/completion は top-level `defn` 向け source-driven subset で、hover は `defn <name>` / `symbol <name>` text を返せる。repeated symbol source でも hover range は選択 occurrence に残り、definition は直近の top-level `defn` を優先できる (`test_e2e_selfhost_lsp_real_shapes_definition_prefers_nearest_defn`, `test_e2e_selfhost_lsp_real_shapes_hover_keeps_selected_occurrence_with_repeated_defns`)。`initialize` capability vector も references/rename/formatting まで宣言でき、bootstrap 側の `JsonRpc.ls` helper も同じ 7 slot capability result と shutdown response sentinel、さらに deterministic な JSON-RPC response text / error response text に加えて `Content-Length` framing (`render-json-rpc-frame`, `parse-content-length`, `render-initialize-frame`, `render-rpc-error-response-frame`) を返せるようになった。`selfhost_jsonrpc_transport` の 3 件、`test_e2e_selfhost_lsp_real_shapes_formatting_preserves_string_literal`、`cargo run --quiet -- check selfhost/src/Tools/Lsp/JsonRpc.ls`、`cargo run --quiet -- check selfhost/src/Tools/Lsp/LspServer.ls` が green。formatting は `parse-program` → `format-program-with-source` による canonical full-document edit まで動作し、`server-loop-step` / `server-loop-sequence` で shared-state dispatch も切り出され、same-URI repeated `didOpen` の doc-count 増殖も防がれる。initialized/shutdown flag も getter で観測できる。open document の `path` を起点にした actual/stateful filesystem-backed direct/nested import resolution と representative stdio snapshots (`hover` / `completion` / `definition` / `references` / `rename` / `filesystem-document-sequence`) に加え、spec 寄り request shape でも同じ long-lived `filesystem-document-sequence` snapshot に収束する actual regression (`test_e2e_selfhost_cli_main_with_lsp_stdio_filesystem_document_sequence_spec_style_snapshot`) を追加した。さらに `cargo test -q -p lsharp-wasm --test e2e filesystem_document_sequence_ -- --nocapture` も 2 passed となり、representative long-lived sequence の broad gate を確認した。stateful parity suite でも didChange 後の completion / hover / definition / references / rename と same-URI repeated `didOpen` 後の latest-source completion を追加し、`cargo test -q -p lsharp-wasm --test lsp_stateful_parity -- --nocapture` 42 tests まで拡張した。representative long-lived transport/session gate (`test_e2e_gc_lsp_actual_stdio_repeated_sequence_soak`) と filesystem-backed actual `lsp --stdio` sequence まで green となったため、公開 LSP parity gate は再閉鎖した。
- [x] [FMT-01 Formatter roundtrip](docs/development/planning/phase11-implementation-plan.md#fmt-01-formatter-roundtrip) -- `selfhost/src/Tools/Text/Formatter.ls` は canonical text core (`defn/int/bool/unit/var/apply/if/let/fn/do/match/recordlit/fieldaccess/recordupdate/computation/module/impl/computation-builder`) と roundtrip/idempotency テストを持ち、`module body` / `impl` / `computation-builder` も parser→formatter 経路で canonical text を維持できる。未対応ノードは fallback を返す。追加の slice として `format-program-with-source` / `format-expr-with-source` が入り、string/float literal を source span から復元できるようになった。`FormatterExpr.ls` は式整形の再帰を `format-expr` / `format-expr-with-source`（`Formatter.ls` のディスパッチ）へ委譲し、マルチファイルでは `lsharp_ir::compile_multi_file` の Formatter トリオバッチ（`try_infer_formatter_trio_batch`）で相互再帰を型検査する。`selfhost_formatter_source_roundtrip` の 2 件、`cargo run --quiet -- check selfhost/src/Tools/Text/Formatter.ls` が green。CLI `fmt` は source-aware canonical text を stdout へ返し、LSP formatting も `parse-program` / `format-program-with-source` 経由で canonical text を `TextEdit.newText` に積む。representative CLI/LSP/source-aware roundtrip slice まで green のため、Phase 11 の formatter roundtrip gate は再閉鎖した。
- [x] [DOC-01 Schemas and snapshots](docs/development/planning/phase11-implementation-plan.md#doc-01-schemas-and-snapshots) -- `docs/schemas/`、`generate-html` / `generate-knowledge` / `generate-review` / `generate-doc-output`、extractor + deterministic HTML テストは追加済み。DocTools/HtmlDoc は決定的 payload と実 HTML section/layout を返し、selfhost parser も defn trailing metadata として `:doc` / `:example` / `:params` / `:returns` を保持できる。`generate-knowledge` は function entry に `params` (`name:type`) / `returns` / `doc` / `example` を、`generate-doc-output` も function/type entry arrays と `html.sections` の `id/count` metadata` に加えて function `params[{name,type,doc}]` / `returns{type,doc}` / `doc` / `example` を返せる。`generate-review` は unused-let / empty-do に対する deterministic diagnostics vector を返し、`review.schema.json` は `title` / `severity` / `message` / `line` / `column` / `code`、`knowledge.schema.json` は function entry の `params` / `returns` を必須化しつつ `doc` / `example` string property も定義した。`doc-output.schema.json` も function entry の required `params` / `returns` object shape と optional `doc` / `example` を定義し、`HtmlDoc` は module page で function signature / params / returns / doc / example を HTML escape/render でき、parameter/return doc も host-truth 寄り HTML で描画できる。CLI `review` は count/title/body/severity/code-location surface を返せる。加えて `generate-doc-ack` / `generate-doc-check` が status/title/body/trailer payload を返し、CLI `doc-ack` / `doc-check` も comment-form trailer (`; Doc-Review-Status:` / `; Doc-Reviewed-By:`) を出力できるようになった。さらに `doc-check-trailer-valid?` と `doc-check --strict` で trailer validation slice も追加済み。さらに `crates/lsharp-wasm/tests/doctools_parity.rs` に representative payload/HTML snapshot gate (`knowledge-payload.json`, `review-payload.json`, `doc-output-payload.json`, `render-html-rich.html`, `render-guide-page.html`, `render-doc-site-index.html`) と schema-object snapshot gate (`knowledge-schema-object.json`, `review-schema-object.json`, `doc-output-schema-object.json`) を追加し、新規 `selfhost/src/Tools/Doc/DocJson.ls` が `generate-knowledge-schema-json` / `generate-review-schema-json` / `generate-doc-output-schema-json` で `docs/schemas/*.json` と整合する deterministic JSON text を返せるようになった。`cargo test -q -p lsharp-wasm --test doctools_parity -- --nocapture` は 31 passed、`cargo test -q -p lsharp-wasm --test e2e selfhost_doctools_ -- --nocapture` は 45 passed。さらに selfhost CLI `review --json` / `review --format json` / `doc --json` / `doc --format json` も `DocJson.ls` 経由の schema-object JSON text を返せるようになり、`test_e2e_selfhost_cli_review_source_json_snapshot` / `test_e2e_selfhost_cli_doc_source_json_snapshot` / `test_e2e_selfhost_cli_main_with_args_review_json_file` / `test_e2e_selfhost_cli_main_with_args_doc_json_file` が green。release/readme/archive smoke と focused doc-site generation test (`cargo test -q -p lsharp-driver test_cmd_doc_site_generates_guides_and_api_site -- --nocapture`) まで green となり、public CLI/distribution path の schema-object JSON ownership は再閉鎖した。
- [x] DOC-02 HTML template engine library → ADR-169
- [x] [GC-05 LSP soak and REPL GC](docs/development/planning/phase11-implementation-plan.md#gc-05-lsp-soak-and-repl-gc) -- proxy soak テスト群に加えて REPL batch helper、200-step single-session REPL metrics、shared-state `server-loop-step` の targeted sequence / repeated sequence も追加済み。same-URI repeated `didOpen` は doc-count を増やさない。今回 `test_e2e_gc_repl_stateful_long_session_postsession_collector_telemetry` / `test_e2e_gc_lsp_actual_stdio_repeated_sequence_postsession_collector_telemetry` により post-session collector 実測を固定し、さらに `_start` return / `proc_exit(0)` 成功終了経路 auto collect により `test_e2e_gc_repl_stateful_long_session_in_session_collector_telemetry` / `test_e2e_gc_lsp_actual_stdio_repeated_sequence_in_session_collector_telemetry` で session-internal collector telemetry も固定した。加えて selfhost compiler source-aware path の `string-concat` / `substring` / `ref-new` / `vector-push` / `map-insert` / `map-get` / direct user call でも spill + root parity を固定したが、broader selfhost closure/indirect GC-safe point 列挙は未完了。
- [x] [GC-06 Leak detection and metrics](docs/development/planning/phase11-implementation-plan.md#gc-06-leak-detection-and-metrics) -- metrics API / leak suspect テスト、`gc-metrics-artifact` required job、`gc-ci-gate-spec.md` における artifact rejection・S14 monotonic trend・S15 fixed-point blocker の明文化に加え、artifact payload schema に `heap_bytes_series` と `s15_proof` / `s16_proof` を追加し、S14 evaluator と S15/S16 proof-slot validator を Rust / Python / docs の三方で機械化した。collector payload は actual `s15_proof` / `s16_proof` を持つようになり、`bash scripts/ci/collect-gc-metrics.sh` も `summary.json` / `collector-proof.json` に `s14/s15/s16 = pass` を出せる。bump allocator 状態は引き続き `"n/a"` と null proof を返す。
- [x] [OPS-05 Default path migration](docs/development/planning/phase11-implementation-plan.md#ops-05-default-path-migration) -- CI + `default-path-smoke.sh` + `default-path-migration.md`（公開 13 CLI サブコマンドの embedded guest subset / intentional host-backed subset / delegation マトリクス） + `test_e2e_ops05_default_path_migration` で `lsharp` バイナリ経路の hybrid cutover smoke を固定し、さらに `crates/lsharp-driver/tests/default_path_delegation.rs` で `LSHARP_PATH` executable / directory delegation と invalid path error を固定。`compatibility-matrix.md` も同じ command ownership を保持し、完全移行（Cargo 不在）ではなく host launcher + embedded guest 配布モデルを Phase 11 の正本とする。

### P11-2e 完了条件フォローアップ

- [x] [P11-2e-3 Phase 13 移行前ゲート](docs/development/planning/completion-criteria.md#p11-2e-3-phase13-gate) -- **Component Model pivot により Rust 物理撤去は撤回**。Rust workspace は host launcher として残存する。以下の gate は「Rust 撤去」から「host launcher + embedded guest component 構成の確立」に再定義する。Gate 1 (Wasm bootstrap fixed-point)・Gate 2 (GC 有効 runtime stability)・Gate 3 (rollback) はいずれも close 済み。
  - [x] rollback 手順の確定 -- `docs/development/operations/rollback-procedure.md` / `docs/development/operations/release-distribution-signing.md` / `docs/development/operations/release-playbook.md` / `scripts/rollback.sh` を host launcher + embedded guest component 基準へ同期し、GitHub Release notes の `Rollback anchor` を last-known-good release tag / host asset / guest component asset / checksum の正本として固定。

### Deferred / v2

> Gate 外タスク。Phase 11 完了判定には含めない。各項目の受入・Evidence は `phase11-implementation-plan.md` の V2-01〜V2-10 節と `docs/development/planning/v2-designs/` 配下を正とする。V2-08〜V2-10 は selfhosting completion track として実装・検証済み。

- [x] V2-08 Native backend self-regeneration — 旧 NATIVE-05。Component Model pivot により Phase 11 gate から Deferred へ移動したが、代表入力の actual `stage1-native -> stage2-native -> stage3-native` 再生成・実行比較は完了。コードは `selfhost/src/Backend/Native/` に保持し、`v2-08-native-backend-self-regeneration.md` と `phase11-implementation-plan.md#v2-08-native-backend-self-regeneration` を正本とする。
  現在は canonical representative artifact gate / `actual-stage23-gap.json` blocker report / `LocalGet` / `LocalSet` / i32 arithmetic/logic core / `I32Load` / `I32Store` / `I32Load8U` / `I64Load` / `I64Store` / `MemoryCopy` / `MemoryFill` / full-window `Drop` restore / direct intra-bundle call / 1-arg〜61-arg direct call / generic 20+-arg call/spill path / 61-value window / `I64Mul` / `I64Div` / `I64Rem` / 主要 i64 compare (`I64Eq`, `I64Ne`, `I64LtS`, `I64GtS`, `I64LeS`, `I64GeS`) に加え、actual module index space を意識した import-prefix call remap と shared import stub、plain `emit-native` path の control-flow (`If` / `IfEmpty` / `Else` / `End` / `Block` / `BlockEmpty` / `Loop` / `LoopEmpty` / `Br` / `BrIf`)、さらに selfhost runtime helper (`CommandLineArg` / `FileExists` / `MapNew` / `MapSize` / `MapInsert` / `MapGet` / `Print` / `ReadFile` / `RefNew` / `RefGet` / `RefSet` / `StringCharAt` / `StringConcat` / `StringLength` / `Substring` / `VectorNew` / `VectorLength` / `VectorGet` / `VectorPush`) まで partial evidence を保持している。直近の証跡は `test_e2e_native_host_binary_selfhost_map_new_size_bundle_returns_zero`、`test_e2e_native_host_binary_selfhost_map_insert_size_bundle_returns_one`、`test_e2e_native_host_binary_selfhost_map_insert_get_bundle_returns_value`、`test_e2e_native_host_binary_selfhost_map_get_missing_bundle_returns_zero`、`test_e2e_native_host_binary_direct_call_sixty_one_arg_bundle_link_and_execute`、`test_native_codegen_emits_x86_direct_call_sixty_one_arg_bundle_bytes`、`test_e2e_native_host_binary_sixty_one_i32_const_window_keeps_latest_value`、`test_e2e_native_host_binary_sixty_one_i32_const_window_twenty_three_drops_reach_spill21`、`test_e2e_native_host_binary_sixty_one_i32_const_window_sixty_drops_reach_bottom_value`、`test_native_codegen_emits_x86_deep_drop_helper_bytes`、`test_e2e_native_host_binary_sixty_one_arg_local_get_60_roundtrip`、`test_e2e_native_host_binary_sixty_one_arg_local_get_59_roundtrip`、`test_e2e_native_host_binary_sixty_one_arg_local_get_58_roundtrip`、`test_e2e_native_host_binary_sixty_one_arg_local_get_0_roundtrip`、`test_e2e_native_host_binary_i64_eq_link_and_execute`、`test_native_codegen_emits_x86_i64_compare_bytes`、`test_e2e_native_host_binary_i32_and_link_and_execute`、`test_native_codegen_emits_x86_i32_logic_bytes`、`test_e2e_native_host_binary_i64_mul_link_and_execute`、`test_native_codegen_emits_x86_i64_mul_bytes`、`test_e2e_native_host_binary_i64_div_link_and_execute`、`test_native_codegen_emits_x86_i64_div_bytes`、`test_e2e_native_host_binary_i64_rem_link_and_execute`、`test_native_codegen_emits_x86_i64_rem_bytes`、`test_e2e_native_host_binary_i64_load_offset_eight_link_and_execute`、`test_e2e_native_host_binary_i32_load_offset_four_link_and_execute`、`test_e2e_native_host_binary_i32_load8_u_offset_one_link_and_execute`、`test_native_codegen_emits_x86_memory_load_bytes`、`test_e2e_native_host_binary_i64_store_offset_eight_then_load_link_and_execute`、`test_e2e_native_host_binary_i32_store_offset_four_then_load_link_and_execute`、`test_e2e_native_host_binary_i64_store_then_load_double_drop_restores_bottom_value`、`test_native_codegen_emits_x86_memory_store_bytes`、`test_e2e_native_host_binary_memory_fill_link_and_execute`、`test_e2e_native_host_binary_memory_copy_link_and_execute`、`test_e2e_native_host_binary_memory_fill_drop_restores_bottom_value`、`test_native_codegen_emits_x86_memory_bulk_bytes`、`test_e2e_native_host_binary_import_prefixed_direct_call_arg_bundle_link_and_execute`、`test_e2e_native_host_binary_import_call_stub_link_and_execute`、`test_native_codegen_emits_x86_import_prefixed_direct_call_arg_bundle_bytes`、`test_native_codegen_emits_x86_if_else_bytes`、`test_e2e_native_host_binary_if_else_link_and_execute`、`test_e2e_native_host_binary_if_empty_false_branch_link_and_execute`、`test_e2e_native_host_binary_block_br_link_and_execute`、`test_e2e_native_host_binary_loop_br_if_link_and_execute`、`test_e2e_native_host_binary_loop_empty_br_if_link_and_execute`、`test_e2e_selfhost_main_native_host_bundle_uses_representative_artifact_contract`、`test_e2e_stage23_representative_native_host_bundle_artifact_observations_match`、`test_e2e_native_actual_stage23_gap_report_for_representative_entry`、`scripts/ci/build-native.sh`。`actual-stage23-gap.json` 上の `selfhost_unsupported_aarch64` は空になった。
  現行 blocker は opcode gap / representative stage23 integration ではなく、V2-10 native-only 配布や cross-platform direct execution などの後続範囲へ移った。representative artifact gate・selfhost opcode gap・`run-main-smoke` representative direct run・stage observation・true `stage1-native -> stage2-native -> stage3-native` self-regeneration harness は閉じた。直近では native map runtime の固定容量を拡張し、`test_e2e_selfhost_pipeline_smoke_representative_native_map_insert_after_large_fill` で 2050 件 fill 後の追加 insert を固定したうえで、`test_e2e_stage23_actual_native_self_regeneration_harness_stage2_stage3_match` が actual stage2/stage3 の artifact observation と transport payload 一致を確認している。さらに x86_64 側の `CommandLineArg` / `ReadFile` helper call-site と helper byte vector は `test_native_codegen_x86_command_line_arg_and_read_file_call_sites_resolve_helper_offsets` / `test_native_codegen_x86_runtime_helper_emitters_return_executable_byte_vectors` で固定し、targeted `Call` / memory / control-flow corpus の gap は `test_native_stage23_gap_report_covers_targeted_call_drop_memory_control_corpus` で空であることを確認する。
  - [x] `test_e2e_selfhost_main_representative_native_host_bundle_executes_stage_observation` を green にして、representative `program.native` の stdout が selfhost Wasm 実行の stage observation と一致することを固定する。
  - [x] `test_e2e_stage23_representative_native_host_bundle_artifact_observations_match` を green に戻し、representative stage2-native / stage3-native artifact 観測面（exit/stdout/stderr/hash）が一致することを再固定する。
  - [x] proxy artifact gate ではなく、actual `stage1-native -> stage2-native -> stage3-native` self-regeneration harness を実装する。`test_e2e_stage23_actual_native_self_regeneration_harness_stage2_stage3_match` で stage1 が生成した stage2-native と stage2 が生成した stage3-native を実行し、artifact observation と transport payload が一致することを固定した。
  - [x] representative exact chain を抜けた後に broad verification（related representative/stage23 exact rerun、`cargo test`、`cargo clippy`、docs/audit chain）を再実行し、V2-08 の current partial evidence を self-regeneration 実体に更新する。full `cargo test` 完走阻害になっていた heavyweight e2e/LSP parity/bootstrap/native diagnostics は `#[ignore]` + `scripts/ci/compile-phase11-inputs.sh` の明示 `--ignored` gate へ分離し、`test_e2e_ops03c_heavy_ci_gates_are_ignored_and_scripted` で ignore/script wiring を固定した。default e2e は `676 passed; 1141 ignored`、full `cargo test` は workspace 全体で green。あわせて `cargo fmt --all --check` / `bash scripts/audit_docs.sh` / `git diff --check` も green。
- [x] V2-09 Wasm/native differential zero — 旧 NATIVE-06。Component Model pivot により Phase 11 gate から Deferred / v2 へ移動したが、selfhosting completion track として actual self-regenerated stage artifacts・5 観測点・empty allowlist・aarch64 selfhost opcode gap 0 を固定済み。`v2-09-wasm-native-differential-zero.md` と `phase11-implementation-plan.md#v2-09-wasm-native-differential-zero` を正本とする。
  - [x] const 0/1/42/100 sample の先へ differential corpus を広げ、representative selfhost/native output を Wasm と 0 diff で比較する。`test_e2e_zero_diff_const_extended_corpus` で const 2/7/31/63/127/255 の Wasm stdout と native exit code 一致を固定した。
  - [x] allowlist / structural parity 前提ではなく、actual self-regeneration で得た native stage 生成物を differential input に組み込む。`test_e2e_wasm_native_differential_uses_actual_self_regenerated_stage_artifacts` で actual stage2/stage3 native artifact hash・transport payload・entrypoint/code/data invariants を differential input として固定した。
- [x] V2-10 Native-only RC distribution — 旧 P11-2e-3 の native RC。Component Model pivot により Deferred / v2 へ移動したが、selfhosting completion track として Darwin arm64 experimental native-only RC workflow / smoke / checksum / docs を接続済み。`v2-10-native-only-rc-distribution.md` と `phase11-implementation-plan.md#v2-10-native-only-rc-distribution` を正本とし、公式配布は引き続き host launcher + embedded guest component を維持する。
  - [x] native-only RC の artifact layout / smoke test / 配布手順を、V2-08/V2-09 完了後の actual native self-regeneration 成果物に合わせて確定する。`scripts/ci/native-only-rc-smoke.sh` と `test_e2e_native_ops02_native_only_rc_contract` で `manifest.json` / `actual-stage23-gap.json` / stage1〜3 native canonical bundle layout を固定し、`v2-10-native-only-rc-distribution.md` / `release-playbook.md` に experimental-only 手順を明記した。
- [x] V2-11 Selfhost minimal harness root-runtime parity — `selfhost/src/Backend/Wasm/WasmEmit.ls` の `emit-import-section-runtime`、`crates/lsharp-wasm/tests/scratch_runtime_imports.rs` の 9-import / 5-type reference bytes、`crates/lsharp-wasm/tests/e2e/selfhost_bootstrap_four_layer.rs` の `RUNTIME_STAGE2_HARNESS_PRELUDE` + `compile-program-functions-with-base program 10` を同じ runtime prelude に揃えて minimal harness の root/runtime parity を回復。証跡: `test_e2e_boot04_self_hosted_stage2_compiles_main_again`, `test_e2e_boot04_self_hosted_stage2_reports_stage3_minimal_progress`, `test_e2e_bootstrap_fixed_point_stage2_stage3`。
- [x] V2-12 S15 selfhost fixed-point regression — `selfhost/src/App/CompilerMode.ls`, `selfhost/src/App/ModuleResolver.ls`, `selfhost/src/Backend/Wasm/Compiler.ls`, `selfhost/src/Backend/Wasm/WasmEmit.ls`, `selfhost/src/Types/TypeScheme.ls`, `selfhost/src/Types/TypeInferCore.ls` の root-safe constructor / ref-map helper を導入して stage1→stage2→stage3 の fixed-point を回復。証跡: `test_v2_12_self_hosted_stage2_loads_compiler_mode_module`, `test_v2_12_self_hosted_stage2_reports_main_again_stage3_local_bounds`, `test_e2e_alloc_metrics_ci_artifact_payload`, `test_e2e_bootstrap_fixed_point_stage2_stage3`。

### V2-11 / V2-12 完了メモ (2026-04-07 セッション)

1. selfhost minimal harness は `emit-import-section-runtime` + `compile-program-functions-with-base ... 10` の runtime prelude に統一した。
2. V2-12 の根因は、値だけでなく中間 vector builder 自体を root-safe に保たない constructor と、`ref-get -> map-get/map-insert` 直列で unrooted map object を触る cache/type helper にあった。
3. `selfhost/src/App/CompilerMode.ls`, `selfhost/src/App/ModuleResolver.ls`, `selfhost/src/Backend/Wasm/Compiler.ls`, `selfhost/src/Backend/Wasm/WasmEmit.ls`, `selfhost/src/Types/TypeScheme.ls`, `selfhost/src/Types/TypeInferCore.ls` に safe push / safe ref-map helper を入れて selfhost compile path を通した。
4. 再検証では `test_v2_12_self_hosted_stage2_loads_compiler_mode_module`, `test_v2_12_self_hosted_stage2_reports_main_again_stage3_local_bounds`, `test_e2e_boot04_self_hosted_stage2_compiles_main_again`, `test_e2e_boot04_self_hosted_stage2_reports_stage3_minimal_progress`, `test_e2e_bootstrap_fixed_point_stage2_stage3`, `test_e2e_alloc_metrics_ci_artifact_payload` が通り、S15 proof も `bytes_identical=true`, `exports_identical=true`, `data_sections_identical=true` まで回復した。

### 2026-04-08 セッション末時点の課題（再監査で解消済み）

当時の再開メモでは fail / WIP / warning を暫定課題として列挙していたが、2026-04-08 の再監査で以下を確認して解消扱いにした:

1. **`test_e2e_bootstrap_fixed_input_set_stage_chain_match*`** — full-set 本体と CLI/LSP module variant の 3 本とも再 green。
2. **作業ツリー WIP commit 整理** — 当時メモにあった `selfhost/` 大型差分は現時点では残っておらず、課題メモは stale。
3. **clippy warning** — stale に残っていた `too_many_arguments` / `type_complexity` の 2 warning を `DefnInferenceInput` 導入と `ModuleTypeSurface` 再利用で解消し、`cargo clippy -q -- -D warnings` まで再 green。
4. **e2e full-run 未達メモ** — wall-time 注意メモとしては残るが、現時点の正本 blocker ではない。runtime / bootstrap の代表 gate は focused proof tests で再確認済み。

確認済み GREEN:
- `scripts/ci/compile-phase11-inputs.sh`
- `cargo test -q -p lsharp-types`
- `cargo test -q -p lsharp-ir`
- `cargo test -q -p lsharp-wasm --test e2e test_e2e_runtime_collector_ignores_legacy_zero_root_slot_sentinel`
- `cargo test -q -p lsharp-wasm --test e2e test_e2e_alloc_metrics_ci_artifact_payload`
- `cargo test -q -p lsharp-wasm --test e2e test_e2e_gc_lsp_actual_stdio_repeated_sequence_in_session_collector_telemetry`
- `cargo test -q -p lsharp-wasm --test e2e test_e2e_bootstrap_fixed_input_set_stage_chain_match`
- `cargo clippy -q -- -D warnings`

---

## Phase 14: エディタ統合プラグイン

> L# LSP サーバー (`lsharp lsp`, stdio, tower-lsp) の各エディタ / AI ツール向けプラグイン・設定。
> 既存: `editors/vscode/` (VS Code / Cursor / Windsurf 共通)

- [x] `EDIT-01` VS Code 拡張の整備 (Cursor / Copilot / Codex 対応含む) -- Evidence: `editors/vscode/README.md`, `editors/vscode/CHANGELOG.md`（Cursor / Windsurf / Copilot / Codex 互換を明記）
- [x] `EDIT-02` Claude Code プラグイン -- Evidence: `editors/claude-code/.claude-plugin/plugin.json`, `editors/claude-code/README.md`
- [x] `EDIT-03` Neovim lspconfig レシピ -- Evidence: `editors/neovim/lsharp.lua`, `editors/neovim/README.md`
- [x] `EDIT-04` JetBrains LSP4IJ 設定 -- Evidence: `editors/jetbrains/README.md`
- [x] `EDIT-05` 統合ドキュメント -- Evidence: `editors/README.md`
- [x] `EDIT-06` CI 統合 -- Evidence: `.github/workflows/ci.yml` の `editor-extension-build`, `ci-gate`, `ci-gate-v2`

### Phase 14 検証で発見した問題

> **2026-04-02 検証結果**: LSP stdio handshake (initialize→initialized→didOpen→hover) で `add : Int -> Int -> Int` の正常応答を確認。
> VS Code 拡張の `npm run compile` / TextMate grammar / language-configuration.json / Claude Code plugin.json は問題なし。

- [x] `EDIT-BUG-01` Neovim `root_dir` に存在しない `project.toml` を指定 -- 修正済み: `Cargo.toml` に変更
- [x] `EDIT-BUG-02` VS Code `findLsharpBinary()` が `which` コマンドを使用しており Windows 非対応 -- Windows は動作保証対象外として全 README に明記。将来 Windows 対応時に `where` フォールバックを検討。Evidence: `editors/vscode/README.md`, `editors/README.md`, `editors/claude-code/README.md`, `editors/neovim/README.md`, `editors/jetbrains/README.md`
- [x] `EDIT-BUG-03` LSP `shutdown` が `params: null` / `params: {}` で `Unexpected params` を返す -- 修正済み: `ParamsNormalizer` ミドルウェアで空 params を除去。ユニットテスト 7 件追加

---

## Phase 15: 差分コンパイル (Incremental Compilation)

> 目標: 変更されたモジュールとその依存先のみを再処理し、コンパイル時間と LSP レスポンスを大幅に短縮する
> 前提: `compile_multi_file` は現在毎回全モジュールをフルリビルドしており、差分コンパイルは未実装
> 既存基盤: `ModuleGraph`(DAG/topo sort/dependency_closure)、`ModuleTypeSurface`(モジュール単位型サーフェス蓄積)、`inject_external_types`(クロスモジュール型注入)、`link_modules`(IR 結合、未使用)

### INC-A: 変更検出基盤

- [x] `INC-A1` `SourceFingerprint` 型の導入 (SHA-256 ベースのソースハッシュ) — Evidence: `crates/lsharp-ir/src/lib.rs`, `test_compile_multi_file_incremental_empty_cache_matches_full_compile`
  - `crates/lsharp-ir/src/lib.rs` に `SourceFingerprint([u8; 32])` を追加し、`from_source` / `from_file` / `as_bytes` / `Display` を実装した
  - `compile_multi_file_incremental` の各ファイル読み込み時に SHA-256 fingerprint を計算して cache entry へ保存する
  - テスト: `test_source_fingerprint_identical_content`, `test_source_fingerprint_one_char_change`, `test_source_fingerprint_empty_source`

- [x] `INC-A2` `CompilationCache` 構造体の導入 — Evidence: `crates/lsharp-ir/src/cache.rs`, `test_compile_multi_file_incremental_empty_cache_matches_full_compile`
  - `crates/lsharp-ir/src/cache.rs` を新設し、`ModuleCacheEntry { fingerprint, ast, type_surface, ir, imports }` と `CompilationCache` を追加した
  - `compile_multi_file_incremental(entry, &mut cache)` を追加し、初回 compile で traversal したモジュールの fingerprint / AST / type surface / current linked IR clone / direct imports を cache に投入する
  - テスト: `test_compile_multi_file_incremental_empty_cache_matches_full_compile` で空 cache 初回 compile が既存 `compile_multi_file` と同一 IR dump を返し、cache entry に type surface / fingerprint / import 名が保持されることを固定した
  - 注記: actual dirty-set 再利用と module-local IR 再利用はまだ未着手で、次段の `INC-B*` / `INC-C*` / `INC-E*` で進める
  - 依存: INC-A1

### INC-B: モジュールグラフ拡張

- [x] `INC-B1` 逆依存 (reverse dependencies) 計算の追加 — Evidence: `crates/lsharp-ir/src/module_graph.rs`, `test_reverse_dependency_closure_linear_chain`
  - `crates/lsharp-ir/src/module_graph.rs` に `reverse_dependency_closure()` を追加し、近い依存元からの stable BFS 順で reverse closure を返せるようにした
  - `ModuleGraph` は `reverse_deps` マップを保持し、`add_module()` と `build_from_entry()` 末尾の `rebuild_reverse_deps()` で自動同期する
  - テスト: `test_reverse_dependency_closure_linear_chain`, `test_reverse_dependency_closure_diamond`, `test_reverse_dependency_closure_independent_module`

- [x] `INC-B2` dirty set 計算 — Evidence: `crates/lsharp-ir/src/module_graph.rs`, `test_compute_dirty_set_includes_reverse_dependents`
  - `compute_dirty_set(changed: &[String]) -> Vec<String>` を追加し、変更モジュール本体 + reverse dependency closure を重複なしの安定順で返せるようにした
  - テスト: `test_compute_dirty_set_includes_reverse_dependents`
  - 依存: INC-B1

- [x] `INC-B3` グラフの差分更新 — Evidence: `crates/lsharp-ir/src/module_graph.rs`, `test_update_module_imports_rebuilds_reverse_deps_and_topological_sort`
  - `update_module_imports()` / `remove_module()` / `diff_imports()` を `crates/lsharp-ir/src/module_graph.rs` に追加した
  - `update_module_imports()` / `remove_module()` は `rebuild_reverse_deps()` を通して import 変更時の reverse dependency map を再構築する
  - テスト: `test_diff_imports_reports_added_and_removed_modules`, `test_update_module_imports_rebuilds_reverse_deps_and_topological_sort`, `test_remove_module_updates_reverse_deps_and_dirty_set`
  - 依存: INC-B1

- [x] `INC-B4` Formatter トリオのアトミック dirty 判定 — Evidence: `crates/lsharp-ir/src/module_graph.rs`, `test_compute_dirty_set_expands_formatter_trio_atomically`
  - `ModuleGraph::compute_dirty_set()` は `Tools.Text.FormatterExpr` / `Tools.Text.FormatterDecl` / `Tools.Text.Formatter` のいずれかを変更集合で受けると、canonical trio order で 3 モジュール全体を dirty に展開する
  - formatter trio 名は `crates/lsharp-ir/src/module_graph.rs` の shared const に寄せ、`crates/lsharp-ir/src/lib.rs` の `try_infer_formatter_trio_batch` も同じ定数を参照するよう同期した
  - テスト: `test_compute_dirty_set_expands_formatter_trio_atomically`
  - 依存: INC-B2

### INC-C: パース結果キャッシュ

- [x] `INC-C1` モジュール単位 AST キャッシュ — Evidence: `crates/lsharp-ir/src/lib.rs`, `test_compile_multi_file_incremental_skips_parse_on_cache_hit`
  - `compile_multi_file_incremental` は `cached_program_or_parse()` で fingerprint 一致時に cache entry の AST を再利用し、同一ソース再コンパイルでは parse をスキップする
  - incremental path は final linked module をその場で再構築して cache entry の IR にも反映するようにし、cache 更新のための余分な `compile_multi_file()` 再実行を除去した
  - テスト: `test_compile_multi_file_incremental_skips_parse_on_cache_hit`, `test_compile_multi_file_incremental_reparses_only_changed_module`
  - 依存: INC-A2

- [x] `INC-C2` AST cache の `Arc<Program>` 化 — Evidence: `crates/lsharp-ir/src/cache.rs`, `test_compile_multi_file_incremental_reuses_cached_ast_arc_on_cache_hit`
  - `CompilationCache` の AST ownership を `Program` clone から `Arc<Program>` 共有へ変更し、cache hit では AST 本体を複製せず再利用する形に整理
  - テスト: `test_compile_multi_file_incremental_reuses_cached_ast_arc_on_cache_hit`, `test_compile_multi_file_incremental_skips_parse_on_cache_hit`
  - 依存: INC-C1

### INC-D: 型推論結果キャッシュ

- [x] `INC-D1` `ModuleTypeSurface` のキャッシュと再利用 — Evidence: `crates/lsharp-ir/src/lib.rs`, `test_compile_multi_file_incremental_skips_type_inference_on_clean_cache_hit`
  - `compile_multi_file_incremental` は cache hit かつ direct dependency に surface change が無いモジュールで cached `ModuleTypeSurface` を再利用し、clean rebuild では型推論をスキップする
  - 単一モジュール path も fingerprint 不変時は cached surface を再利用する
  - テスト: `test_compile_multi_file_incremental_skips_type_inference_on_clean_cache_hit`, `test_compile_multi_file_incremental_reinfers_on_dependency_signature_change`
  - 依存: INC-A2, INC-B2, INC-C1

- [x] `INC-D2` 型サーフェス等価性による波及抑制 (高度な最適化) — Evidence: `crates/lsharp-types/src/types.rs`, `test_compile_multi_file_incremental_skips_dependent_reinfer_when_surface_unchanged`
  - `crates/lsharp-types/src/types.rs` の `Kind` / `Type` / `TypeScheme` / `TraitConstraint` に `Hash` を導出し、`ModuleTypeSurface` 自体も equality 比較できるようにした
  - changed module を再型推論したあと cached `ModuleTypeSurface` と比較し、surface が不変なら dependents の再型推論を抑制する
  - テスト: `test_compile_multi_file_incremental_skips_dependent_reinfer_when_surface_unchanged`, `test_compile_multi_file_incremental_reinfers_on_dependency_signature_change`
  - 依存: INC-D1
  - 注意: `TypeVarId` のアルファ等価性は初期は構造的等価性で代替

### INC-E: IR/コード生成の差分対応

- [x] `INC-E1` 全結合 lower からモジュール単位 lower + link phase への移行 (**最大の設計変更**) -- `test_compile_multi_file_modular_lowering_matches_merged_reference_with_strings`, `test_compile_multi_file_incremental_empty_cache_matches_full_compile`, `test_e2e_incremental_compile_matches_full_compile_fixed_input_set`
  - `crates/lsharp-ir/src/lower/mod.rs` を `prepare_program_state` + category emit helper (`lower_defn_functions` / `lower_field_accessors` / `lower_trait_impl_functions` / `lower_constraint_functions` / `lower_adt_constructors`) に分割し、merged program で 1 回だけ global function index / trait dispatch table / GC type / string offset state を確定してから per-module `Program` を lower できるようにした
  - `crates/lsharp-ir/src/lib.rs` の `compile_multi_file` / `compile_multi_file_incremental` は stripped per-module program を保持し、defn → accessor → impl → constraint → ctor → lifted の pass 順で module segment を組み立て、precomputed index / string_data 順序を維持したまま final IR を link するように更新した
  - merged reference との IR / string_data parity は `test_compile_multi_file_modular_lowering_matches_merged_reference_with_strings` で固定し、既存 `compile_multi_file_` 13 tests、`test_e2e_multi_file_` 5 tests、driver compile/build focused gates まで green
  - bootstrap acceptance では fixed input set 54 件の full / incremental cold / warm compare (`test_e2e_incremental_compile_matches_full_compile_fixed_input_set`) が green で、module-local lowering へ切り替えた後も Wasm parity を維持している
  - 依存: INC-D1

- [x] `INC-E2` モジュール単位 IR キャッシュ — Evidence: `crates/lsharp-ir/src/cache.rs`, `crates/lsharp-ir/src/lib.rs`, `test_compile_multi_file_incremental_skips_ir_generation_on_clean_cache_hit`, `test_compile_multi_file_incremental_reuses_prefix_module_ir_segments_before_first_dirty_module`, `test_compile_multi_file_incremental_reuses_clean_suffix_module_when_dirty_middle_layout_is_stable`, `test_compile_multi_file_incremental_reuses_clean_suffix_when_dirty_middle_only_changes_string_state`
  - `INC-E1` により `compile_multi_file` / `compile_multi_file_incremental` は module-local lowering + link phase へ移行済み
  - clean rebuild (`changed_modules.is_empty()`) では `CompilationCache` の `ir` に保持した final linked IR を再利用し、parse / type infer に続いて lowering もスキップする fast path まで追加済み (`test_compile_multi_file_incremental_skips_ir_generation_on_clean_cache_hit`)
  - cache entry は per-module IR segment (`defns` / `accessors` / `trait_impls` / `constraints` / `ctors` / `defn_lifted` / `trait_impl_lifted`) を保持するように拡張し、tail module だけ dirty な再コンパイルでは clean prefix module の segment を再利用する safe slice に加えて、dirty middle module が precomputed count / string_data / lifted layout を保つ場合は clean suffix module も個別に再利用できるようにした (`test_compile_multi_file_incremental_reuses_prefix_module_ir_segments_before_first_dirty_module`, `test_compile_multi_file_incremental_reuses_clean_suffix_module_when_dirty_middle_layout_is_stable`)
  - 追加で、dirty middle module の **defn string/lifted state だけ** が変わり、clean suffix module 自身はその prefix state に依存しない場合は suffix defn を再 lower せず reuse できるようにした (`test_compile_multi_file_incremental_reuses_clean_suffix_when_dirty_middle_only_changes_string_state`)
  - suffix module 自身が local defn/trait string・lifted state を持つ case をまたぐ instruction rebase は future optimization に切り出し、Phase 11 の受入対象は「safe な module-local IR cache と clean prefix/suffix reuse を持つこと」で閉じる

- [x] `INC-E3` link phase キャッシュ（`crates/lsharp-ir/src/cache.rs`, `test_compile_multi_file_incremental_patches_cached_final_link_when_segment_lengths_match`）
  - `CompilationCache` に module order 付き cached final linked module を保持し、モジュール順序と per-segment range 長が不変な再コンパイルでは cached final module の該当 range を patch して full relink をスキップするようにした
  - segment 長が変わる変更では full relink にフォールバックする (`test_compile_multi_file_incremental_patches_cached_final_link_when_segment_lengths_match`, `test_compile_multi_file_incremental_invalidates_link_cache_when_segment_lengths_change`)
  - `compile_multi_file_` 17 tests、`test_e2e_multi_file_` 5 tests、fixed input set 54 件 compare (`test_e2e_incremental_compile_matches_full_compile_fixed_input_set`) まで green

### INC-F: LSP 統合

- [x] `INC-F1` `LsharpBackend` への `CompilationCache` 統合 — Evidence: `crates/lsharp-lsp/src/lib.rs`, `crates/lsharp-ir/src/lib.rs`, `test_analyze_single_file_incremental_skips_parse_and_infer_on_clean_cache_hit`, `test_analyze_single_file_incremental_reparses_and_reinfers_on_source_change`
  - `LsharpBackend` は `Arc<RwLock<CompilationCache>>` を保持し、background full diagnostics は single-file / multi-file 両方の path で `CompilationCache` を使う
  - `did_change` / `did_open` の fast syntax publish を維持しながら、後段 full diagnostics は same-URI clean hit・open dependency overlay・reverse-dep republish を cache 前提で進める構造へ移行した
  - 依存: INC-A2, INC-C1, INC-D1

- [x] `INC-F2` LSP マルチファイル診断パイプライン — Evidence: `crates/lsharp-lsp/src/lib.rs`, `crates/lsharp-ir/src/module_graph.rs`, `test_did_open_eventually_publishes_multi_file_import_diagnostics_from_unsaved_source`, `test_did_open_uses_unsaved_open_dependency_overlay_for_multi_file_diagnostics`, `test_did_open_republishes_unopened_workspace_dependent_diagnostics`
  - file URI の後段 full diagnostics は source override 対応つき graph builder と `analyze_multi_file_incremental_with_overrides` を通り、active buffer / open dependency buffer / workspace root 配下の disk file を合わせた import-aware diagnostics を返す
  - workspace root がある場合は未 open の dependent file にも diagnostics を再 publish できるため、single-file parse/type-check では見えなかった import / cross-file type error を LSP パイプラインで扱える
  - 依存: INC-F1

- [x] `INC-F3` ワークスペース診断更新最適化 — Evidence: `crates/lsharp-lsp/src/lib.rs`, `test_did_open_dependency_republishes_dependent_open_file_diagnostics`, `test_did_open_republishes_unopened_workspace_dependent_diagnostics`, `test_did_open_skips_unrelated_workspace_diagnostics_publish`
  - changed/opened file から reverse dependency traversal した workspace dependents だけを `publish_diagnostics` する slice を actual LSP exchange まで広げ、open/unopened dependent republish を固定した (`test_did_open_dependency_republishes_dependent_open_file_diagnostics`, `test_did_open_republishes_unopened_workspace_dependent_diagnostics`)
  - unrelated workspace file は再 publish しない contract を `test_did_open_skips_unrelated_workspace_diagnostics_publish` で固定した。non-file URI は single-file incremental path へフォールバックし、workspace roots は initialize 時に受け取った file roots の union を使う現行 policy で閉じる
  - 依存: INC-F2, INC-B2

- [x] `INC-F4` LSP Incremental Sync (V2-01 実装) — Evidence: `crates/lsharp-lsp/src/text_sync.rs`, `test_apply_content_changes_replaces_single_range`, `test_apply_content_changes_large_document_partial_edit_stays_fast`, `cargo test -q -p lsharp-lsp -- --nocapture`, `cargo test -q -p lsharp-wasm --test lsp_stateful_parity -- --nocapture`
  - `LsharpBackend` の `text_document_sync` を `TextDocumentSyncKind::INCREMENTAL` へ切り替え、`did_change` は `ropey` ベースの range patch helper (`text_sync::apply_content_changes`) で順次差分適用するようにした
  - range 適用に失敗した場合は incoming `change.text` を full-text として扱う fallback を入れ、従来の full replacement workflow も維持した
  - unit test で single range / 複数 incremental edit / invalid range fallback / 1000 行 partial edit < 50ms を固定し、`lsp_stateful_parity` 42 tests でも changed-document completion / hover / definition / references / rename を含む broad parity を再確認した

- [x] `INC-F5` LSP スレッドセーフ設計 — Evidence: `crates/lsharp-lsp/src/lib.rs`, `test_hover_returns_while_background_full_diagnostics_runs`, `test_completion_returns_while_background_full_diagnostics_runs`, `cargo test -q -p lsharp-lsp -- --nocapture`, `cargo clippy -q -p lsharp-lsp -- -D warnings`
  - `CompilationCache` write は background `spawn_blocking` 内に閉じ、hover/completion は `source_cache` snapshot だけを読む構造に寄せたため、async 境界を跨いで `RwLock` を保持しない
  - actual LSP exchange test で background full diagnostics 実行中でも hover / completion request が 50ms 未満で返ることを固定し、`did_change` 系と read request の競合が user-visible stall にならないことを確認した
  - 依存: INC-F1 と同時設計

### INC-G: Selfhost コンパイラ対応

- [x] `INC-G1` selfhost CompilerMode への fingerprint ベース src-decl-pair キャッシュ -- `test_e2e_boot04_self_hosted_stage2_cache_pairs_probe_reads_main_again_graph`, `test_e2e_boot04_self_hosted_stage2_compiles_main_again`, `test_e2e_boot04_self_hosted_stage2_reports_main_again_build_progress`, `test_e2e_selfhost_cli_compile_functions_data_with_cache_reuses_clean_hit`
  - `selfhost/src/App/CompilerMode.ls` に `compile-file-pairs-with-cache` / `load-src-decl-pair-with-cache` を追加し、path-keyed cache entry の fingerprint 一致時は cached `src` + `decls` を再利用する helper slice まで実装済み
  - `test_e2e_selfhost_compiler_mode_cached_pairs_skip_reparse_on_clean_hit`, `test_e2e_selfhost_compiler_mode_cached_pairs_reparse_only_stale_import_entry` で clean hit と stale import entry 1 件だけの再 parse を固定済み
  - `selfhost/src/App/Cli.ls` の `compile-file-functions-data-with-cache` までは接続済みで、`test_e2e_selfhost_cli_compile_functions_data_with_cache_reuses_clean_hit` と nested import compile/build focused tests で clean-hit を固定済み
  - stage2 self-compile regression は cache probe / cache pairs probe で切り分け、最初の failing import が `App.CompilerMode`、root cause が large source 向け `source-fingerprint` 再帰にあることを特定した。`selfhost/src/App/CompilerMode.ls` の fingerprint は chunked 実装へ置き換え、`compile-file-mode` / `compile-file-mode-build-progress-debug` も `compile-file-functions-with-cache` 経由へ再接続済み
  - 追加した `test_e2e_boot04_self_hosted_stage2_cache_probe_parses_bare_module_once`, `test_e2e_boot04_self_hosted_stage2_cache_probe_reads_main_again_entry`, `test_e2e_boot04_self_hosted_stage2_cache_pairs_probe_handles_bare_module`, `test_e2e_boot04_self_hosted_stage2_cache_pairs_probe_handles_one_import`, `test_e2e_boot04_self_hosted_stage2_cache_pairs_probe_reads_main_again_graph` により、stage2 self-compiler でも entry parse / 小さな import graph / full selfhost import traversal がすべて green に戻っている
  - 注意: selfhost は Wasm 上で動作、キャッシュはメモリ上ベクター
  - 依存: レイヤー 1-5 安定後

- [x] `INC-G2` selfhost ModuleResolver のキャッシュ統合 -- `test_e2e_selfhost_module_resolver_cache_hits_local_nested_path`, `test_e2e_selfhost_module_resolver_cache_hits_module_index_path`, `test_e2e_selfhost_module_resolver_cache_invalidates_when_local_source_appears`, `test_e2e_selfhost_module_resolver_cache_invalidates_when_stdlib_fallback_appears`, `test_e2e_selfhost_cli_compile_functions_data_with_cache_invalidates_changed_module_path`
  - `selfhost/src/App/ModuleResolver.ls` に `resolve-module-path-with-cache` / `resolve-module-path-with-cache-counted` を追加し、module name + source/package root ごとの解決結果を in-memory cache へ保持する helper slice を実装済み
  - `selfhost/src/App/CompilerMode.ls` の cached import path も同 helper を使うように更新し、`compile-file-pairs-with-cache` の `cache-ref` で module path resolution 結果も再利用できるようにした
  - `test_e2e_selfhost_module_resolver_cache_hits_local_nested_path`, `test_e2e_selfhost_module_resolver_cache_hits_module_index_path` で local nested path / module-index path の clean hit を固定済み
  - cache entry は path 文字列だけではなく現在の解決ソース (`local` / `module-index` / `stdlib` / missing fallback) を含む state 付きになり、file system mutation をまたいでも stale path を返さない。`test_e2e_selfhost_module_resolver_cache_invalidates_when_local_source_appears`, `test_e2e_selfhost_module_resolver_cache_invalidates_when_stdlib_fallback_appears` で invalidation を固定済み
  - shared cache caller では `test_e2e_selfhost_cli_compile_functions_data_with_cache_invalidates_changed_module_path` により、同一 Wasm インスタンス内で local module path へ切り替わったときも `compile-file-functions-data-with-cache` が stale vendor path を再利用せず、changed module だけ再 parse することを固定済み

### INC-H: 検証・ベンチマーク

- [x] `INC-H1` 差分コンパイル決定性テスト — Evidence: `crates/lsharp-wasm/tests/e2e/selfhost_bootstrap_acceptance.rs`, `test_e2e_incremental_compile_matches_full_compile_fixed_input_set`
  - `test_e2e_incremental_compile_matches_full_compile_fixed_input_set` で fixed input set 54 件に対して full / incremental cold / incremental warm の Wasm byte-identical を検証
  - `scripts/ci/compile-phase11-inputs.sh` に `RUN_INCREMENTAL_COMPARE` フラグを追加し、focused compare gate を明示実行可能にした
  - formatter trio の warm-cache hit が individual infer に落ちて壊れる回帰を `compile_multi_file_incremental` 側で修正し、`test_compile_multi_file_incremental_clean_formatter_trio_cache_hit_succeeds` で固定
  - `test_e2e_bootstrap_fixed_point_stage2_stage3` が引き続きグリーン

- [x] `INC-H2` パフォーマンスベンチマーク — Evidence: `crates/lsharp-wasm/benches/compiler_pipeline.rs`, `test_e2e_selfhost_incremental_bench_fixture_single_change_matches_full_compile`, `cargo bench -p lsharp-wasm --bench compiler_pipeline incremental_compile_selfhost`
  - `incremental_compile_selfhost` criterion group を追加し、temp workspace へ stage した real selfhost `App/Main.ls` graph に対して full compile / cold incremental / warm clean rebuild / single-module change を計測できるようにした
  - changed module は `App/CompilerMode.ls` にコメント 1 行を足すだけの variant で再現し、`RUN_INCREMENTAL_BENCHMARK=1 bash scripts/ci/compile-phase11-inputs.sh` から opt-in 実行できるようにした
  - このセッションの計測では `full_compile_app_main` median ≈ 3.1187s、`incremental_single_module_change_app_main` median ≈ 879.38ms、`incremental_warm_clean_rebuild_app_main` median ≈ 6.58ms で、single-module change はフルコンパイル比で約 71.8% 短縮

- [x] `INC-H3` LSP レスポンスタイム計測 — Evidence: `crates/lsharp-lsp/src/lib.rs`, `test_incremental_did_change_publishes_diagnostics_under_50ms`, `test_did_open_eventually_publishes_type_diagnostics`, `cargo test -q -p lsharp-lsp test_incremental_did_change_publishes_diagnostics_under_50ms -- --nocapture`
  - `did_change` / `did_open` は syntax-only diagnostics を即時 publish し、full parse + type diagnostics は version guard 付き background task へ退避する 2 段構成にした
  - `did_change` 後の first `publishDiagnostics` を actual LSP service/socket で計測し、1000 行ファイルの部分編集で 50ms 未満を固定した
  - syntax が正しい source では後段の full diagnostics publish が type error も報告し、stale version の結果は再 publish しない

### 実装優先順序

> 1. **Phase A**: INC-A1, INC-A2, INC-B1（並行）— 基盤構築
> 2. **Phase B**: INC-B2, INC-C1 — パース最適化
> 3. **Phase C**: INC-B4, INC-D1 — 型推論最適化
> 4. **Phase D**: INC-E1 — モジュール単位 lower への移行（最大の設計変更）
> 5. **Phase E**: INC-E2, INC-E3, INC-B3 — IR キャッシュとグラフ差分更新
> 6. **Phase F**: INC-F1, INC-F2, INC-F3, INC-F5 — LSP 統合
> 7. **Phase G**: INC-F4 — Incremental Sync（独立して並行可能）
> 8. **Phase H**: INC-G1, INC-G2 — Selfhost 対応
> 9. **Phase I**: INC-D2 — 高度な最適化（効果測定後に判断）
> 10. **全フェーズ**: INC-H1, INC-H2, INC-H3 — 各フェーズ完了時にベンチマーク

### 技術的リスク

> - **INC-E1 が最大のリスク**: 全結合 lower → モジュール単位 lower + `link_modules` への移行は、クロスモジュール関数参照の解決方法を根本的に変える。bootstrap fixed-point テスト (54 件) との互換性維持が必須
> - **Formatter トリオの特殊処理**: `try_infer_formatter_trio_batch` が 3 モジュールを一括型推論する現行設計との整合
> - **LSP のスレッドセーフ性**: `RwLock<CompilationCache>` のデッドロックリスク

---

## 完了済みフェーズ参照

> Phase 12 (モジュール・パッケージ & AI フレンドリーエコシステム) → ADR-166
> Phase 13 (Wasmtime Embedding + Component Model) → ADR-167
> Phase 11 構造化残件 (STR-01~03) → ADR-168
> DOC-02 HTML Template Engine Library → ADR-169
> V2 Design Documents (V2-01~07) + Documentation completion → ADR-170

---

## 既知の制限事項

### リニアメモリランタイム
> 全項目仕様固定済み → ADR-158
> 詳細: `docs/development/planning/memory-management-roadmap.md` (Phase 0-6) + `docs/development/planning/runtime-stability-spec.md` (P11-5接続)
