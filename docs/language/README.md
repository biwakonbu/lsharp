# language

実装済み、または v1 契約として扱う言語 / runtime / backend 仕様をまとめる。

- `backend-boundary.md` -- frontend / lowering / codegen の境界契約
- `native-backend-spec.md` -- Native backend の ABI / object emitter / deterministic codegen
- `runtime-spec.md` -- runtime API、値表現、GC root 管理
