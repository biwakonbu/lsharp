# L# v0.2 Milestone 3 — Validation parity and artifact closure

## 目的

M2 で追加した intent/evidence graph と `lsharp validate` を、Rust の検証済み slice
から selfhost/native の日常開発経路へ移し、Mac Apple Silicon と Linux x86_64 で
同じ observable contract を閉じる。M3 は validation model を作り直す milestone では
なく、既存の version 1 manifest、report、exit code、provenance を全 surface に接続
するための parity / artifact milestone とする。

## 現状（origin/main, 2026-07-27）

- Rust `lsharp validate --source` と version 1 JSON manifest input は、parser → graph →
  report まで接続済み。
- Rust CLI の `--emit-manifest` は、report stdout と分離した atomic/durable file output
  として接続済み。parse/adapter/write failure の fail-closed evidence もある。
- Rust MCP `lsharp_validate` は `source` / `file` / `manifest` / `manifest_file` を受理し、
  `include_manifest: true` で同じ `IntentGraph::to_manifest_json_value()` を inline output
  として返す。MCP は filesystem write を行わない。
- selfhost `App.Cli` には source validation と manifest output の Rust-host actual Wasm
  evidence があるが、current native stage0 の parity は未取得。
- `EmbeddedCli` の source/report/pass-fail-unknown/exit と `--emit-manifest` の report/manifest
  file 分離出力、および missing-parent write failure 時の no-report/no-file + exit `1`
  fail-closed は Rust-host actual Wasm の verified slice がある。native stage0 の
  atomic/durable writer と write/provenance failure は未完了である。
- EC-M3-01 の再利用 fixture（source と Rust canonical manifest bytes）と、native source-file
  smoke が同じ fixtureで `unknown` report、exit `2`、manifest bytesを比較する harness contract
  を追加した。Linux wrapper は fixture を VM workdir へ明示コピーし、成功経路の `cargo` /
  `rustc` / host `lsharp` を blocked tool で遮断する。
- 2026-07-27 に native source-file smoke の inner runnerへ `--format text` の同じ canonical
  fixtureを追加し、`status: unknown`、open question / review / contradiction / stale facts の
  固定順6行と exit `2` を要求する contract test を追加した。これは fake Lima/provenance harness
  の script contract であり、current source-commit に一致する実 stage0 artifact/runtime の
  GREEN ではない。
- 同じ fixtureについて、`--emit-manifest` の親ディレクトリ欠落を stable write failure、exit `1`、
  no-report/no-file として要求する native smoke contract も追加した。これは Rust-host actual Wasm
  の fail-closed semantics と境界を揃える partial contract であり、native atomic/durable writer
  や実 stage0 artifact/runtime の evidence ではない。
- independent review を含む complete graph fixtureの `validate --source` も native smokeへ追加し、
  JSON `status: pass` / exit `0` と text の固定順6行（review count `1`、他 metrics `0`）を要求する
  contract にした。これも fake Lima/provenance harness の script evidenceであり、実 stage0
  artifact/runtime の GREEN ではない。
- contradiction fixture の `validate --source` も native smokeへ追加し、JSON `status: fail` / exit `1`
  と text の固定順6行（independent review `1`、contradicting observation `1`、他 metrics `0`）を
  要求する contract にした。report を返す判定 failure と report を返さない parse/graph/write
  failure の境界を固定したが、これも fake Lima/provenance harness の script evidenceであり、実
  stage0 artifact/runtime の GREEN ではない。
- invalidation された review と、その review が `evaluates` する evidence の stale propagation
  fixture も native smokeへ追加し、JSON/text の `status: unknown` / exit `2`、
  `stale_reviews: 1`、`stale_evidence: 1`、stderr 空を要求する contract にした。これは
  Rust-host/selfhost source projection と native report policy を揃える partial contractであり、
  fake Lima/provenance harness の script evidenceに留まる。
- orphan edge が存在しない node を参照する source fixture も native smokeへ追加し、
  `source validation error:5`、exit `1`、report/manifestなしの fail-closed を要求する contract
  にした。これは既存 Rust `MissingNodeReference` の error code と source adapter span boundary
  を native surfaceへ接続する partial contractであり、fake Lima/provenance harness の script
  evidenceに留まる。
- edge payload が不足する malformed source fixture も native smokeへ追加し、
  `source validation error:1`、exit `1`、report/manifestなしの fail-closed を要求する contract
  にした。これは既存 Rust source adapter の malformed edge/span 診断を native surfaceへ接続する
  partial contractであり、fake Lima/provenance harness の script evidenceに留まる。
- wire prefix が不正な edge endpoint の source fixture も native smokeへ追加し、
  `source validation error:2`、exit `1`、report/manifestなしの fail-closed を要求する contract
  にした。これは既存 Rust source adapter の malformed ID 診断を native surfaceへ接続する partial
  contractであり、fake Lima/provenance harness の script evidenceに留まる。
- metadata kind と wire prefix が不一致な node fixture も native smokeへ追加し、
  `source validation error:3`、exit `1`、report/manifestなしの fail-closed を要求する contract
  にした。これは既存 Rust source adapter の typed kind mismatch 診断を native surfaceへ接続する
  partial contractであり、fake Lima/provenance harness の script evidenceに留まる。
- 未登録 evidence を `supports` する source fixtureも native smokeへ追加し、
  `source validation error:6`、exit `1`、report/manifestなしの fail-closed を要求する contract
  にした。これは既存 Rust source adapter の `EvidenceRegistryRequired` 診断を native surfaceへ
  接続する partial contractであり、fake Lima/provenance harness の script evidenceに留まる。
- review registry が存在する状態で未登録 review を `evaluates` する source fixtureも native smokeへ
  追加し、`source validation error:10`、exit `1`、report/manifestなしの fail-closed を要求する
  contractにした。これは既存 Rust source adapter の `MissingReview` 診断を native surfaceへ接続する
  partial contractであり、fake Lima/provenance harness の script evidenceに留まる。
- 同じ review ID を二度宣言する source fixtureも native smokeへ追加し、`source validation error:7`、
  exit `1`、report/manifestなしの fail-closed を要求する contractにした。これは既存 Rust source
  adapter の `DuplicateReview` 診断を native surfaceへ接続する partial contractであり、fake
  Lima/provenance harness の script evidenceに留まる。
- review の visibility が未知値となる source fixtureも native smokeへ追加し、
  `source validation error:8`、exit `1`、report/manifestなしの fail-closed を要求する contract
  にした。これは既存 Rust source adapter の invalid review field 診断を native surfaceへ接続する
  partial contractであり、fake Lima/provenance harness の script evidenceに留まる。
  selfhost/native の全 validation surface、canonical manifest parity、Mac/Linux の
  artifact/runtime matrix、fallback を通らない provenance gate は未完了。したがって M2
  aggregate は `[~]` のまま維持する。

## タスク分解

### EC-M3-01 — canonical manifest parity

同じ source fixture と同じ version 1 manifest fixtureを Rust oracle、selfhost stage0、
native program で実行する。

- source node / evidence / edge の順序、stable ID、sampling、provenance の JSON bytes を
  canonical serializer と比較する。
- manifest input → report と source input → report の status、trace gap、diagnostic、exit
  codeを比較する。
- malformed schema、unknown field、duplicate node、orphan endpoint、逆順 spanを各入口で
  fail-closed にする。
- source の `:motivates` が未登録の intent node を参照する場合は stable code `5`、exit `1`、
  report/manifestなしで拒否することを native source-file smoke の contract に含める。
- source edge の endpoint が不足する malformed metadata は stable code `1`、exit `1`、
  report/manifestなしで拒否することを native source-file smoke の contract に含める。
- source edge の wire prefix が不正な endpoint は stable code `2`、exit `1`、report/manifestなしで
  拒否することを native source-file smoke の contract に含める。
- source metadata の kind と wire prefix が不一致な node は stable code `3`、exit `1`、
  report/manifestなしで拒否することを native source-file smoke の contract に含める。
- `supports` / `contradicts` が未登録 evidence を参照する source edge は stable code `6`、exit `1`、
  report/manifestなしで拒否することを native source-file smoke の contract に含める。
- review registry が存在する状態で未登録 review を `evaluates` / `invalidates` する source edge は
  stable code `10`、exit `1`、report/manifestなしで拒否することを native source-file smoke の
  contract に含める。
- source review registry が同じ ID を複数回宣言する場合は stable code `7`、exit `1`、
  report/manifestなしで拒否することを native source-file smoke の contract に含める。
- review metadata の visibility が `public` / `redacted` 以外の場合は stable code `8`、exit `1`、
  report/manifestなしで拒否することを native source-file smoke の contract に含める。
- `evaluates` / `invalidates` の review edge subject が review など許可されない kind の場合は
  stable code `9`、exit `1`、report/manifestなしで拒否することを native source-file smoke の
  contract に含める。
- source metadata の kind と wire prefix が不一致な node は stable code `3`、exit `1`、
  report/manifestなしで拒否することを native source-file smoke の contract に含める。
- RED は `crates/lsharp-wasm/tests/e2e/` に fixture 単位で追加し、Rust oracle と native
  stage0 の両方で同じ失敗値を固定する。

完了条件: bytes / report / exit code の差分がなく、fallback 呼び出しがないことを
evidence envelope で確認する。

### EC-M3-02 — selfhost `App.Cli` validation surface

`App.Cli` の `validate --source <file> --format json [--emit-manifest <path>]` を native
stage0 の正式入口にする。

- success (`unknown` を含む) では report stdout と manifest file を混ぜない。
- `--format text` は JSON と同じ facts を固定順で出力し、canonical fixture の unknown report は
  exit `2` であることを native source-file smoke の contract に含める。
- `--emit-manifest` の親ディレクトリが存在しない場合は、stable write diagnostic、exit `1`、
  report/artifact なしで fail-closed になることを native source-file smoke の contract に含める。
- complete graph（independent review あり）は JSON `status: pass` / exit `0` と同じ text facts を
  返すことを native source-file smoke の contract に含める。
- contradiction（independent review の `:outcome "contradicted"` と `:contradicts` edge）は
  JSON `status: fail` / exit `1` と同じ text facts を返し、stdout の report を保持して stderr は
  空にすることを native source-file smoke の contract に含める。
- invalidation された review と `evaluates` edge で stale になった evidence は、JSON/text とも
  `status: unknown` / exit `2`、`stale_reviews: 1`、`stale_evidence: 1` の report を stdout に
  返し、stderr を空にすることを native source-file smoke の contract に含める。
- parse/graph error では report/status/artifact を残さず exit `2`、manifest write error では
  stable diagnostic と exit `1` を返し report/artifact を残さない。
- output path の親ディレクトリ、symlink、既存 file、source fingerprint を明示的に検査する。
- `cargo`、`rustc`、host `lsharp`、暗黙の Rust fallback が成功経路に入らない negative gate
  を追加する。

### EC-M3-03 — `EmbeddedCli` parity / external boundary

`EmbeddedCli` が公開する command surface を `App.Cli` と同じ fixture で検査する。

- `validate` の source/report/manifest boundary と `pass=0` / `fail=1` / `unknown=2` を
  実 artifact から確認する。EmbeddedCli の Rust-host actual Wasm fixture では、source
  report の 3 status/exit boundary と、`--emit-manifest` の version 1 manifest file 出力を
  検証済み。manifest write failure では no-report/no-file と exit `1` の fail-closed も
  検証済み。
- native の atomic/durable filesystem、native write/provenance、component、MCP host
  integration は曖昧な成功にせず、未実装なら stable な external-boundary として返す。
- `tools/list` / `tools/call` の MCP inline manifest は Rust host contract として保持し、
  EmbeddedCli の filesystem writer は version 1 serializer を再利用する。atomic/durable と
  native provenance はこの task の完了条件に含めず、EC-M3-01/02 の後続境界として残す。

### EC-M3-04 — two-target artifact/runtime matrix

Mac Apple Silicon (`aarch64-apple-darwin`) と Linux x86_64 (`x86_64-unknown-linux-gnu`)
を別々の evidence lane として閉じる。

- Mac: `scripts/ci/native-selfhost-dev-source-file-smoke.sh` と release-stage0 package。
- Linux: `scripts/ci/native-linux-x86-native-stage0-source-file-smoke.sh` と Lima VM gate。
- 各 target で `check` / `test` / `compile` / `build`、manifest bytes、Wasm validate、
  standalone runtime の結果を記録する。
- stage0 producer commit、source fingerprint、artifact digest、runtime target を同じ
  evidence envelope に保存する。stale artifact は採用しない。

### EC-M3-05 — provenance and rollback gate

日常開発の成功と release artifact の再現性を分離して検証する。

- stage0 の取得、再生成、source commit mismatch、rollback を fixture と script の contract
  に固定する。
- current source と stage0 source の fingerprint mismatch は実行前に拒否する。
- VM lock、artifact reuse、temporary workdir の cleanup を gate 後に確認する。
- rollback は emergency path として残し、Rust oracle の成功を native GREEN と数えない。

## 依存関係と実行順

```text
EC-M3-01 canonical parity
        ↓
EC-M3-02 App.Cli ──┐
                    ├─ EC-M3-04 two-target matrix
EC-M3-03 EmbeddedCli┘          ↓
                    EC-M3-05 provenance / rollback
```

各タスクは `RED → selfhost/native 実装 → focused GREEN → Rust differential → artifact /
runtime → docs/ADR/TODO` の順で進める。一度に一つの observable contract だけを扱い、
Linux VM の heavy replay と同じ仮説の重複実行を禁止する。

## 完了ゲート

M3 の完了は単一の unit test、Rust driver の成功、summary/header、または stale artifact
では宣言しない。次の全項目を current checkout の証拠で監査する。

1. Rust oracle と native stage0 の source/manifest/report/exit code が同一 fixtureで一致する。
2. `App.Cli` と `EmbeddedCli` の対応 command、拒否境界、artifact bytes/runtime が確認できる。
3. Mac Apple Silicon と Linux x86_64 の必要 lane がそれぞれ pass する。
4. 成功経路に `cargo`、`rustc`、host `lsharp`、Rust fallback がないことを negative gate で確認する。
5. producer commit、source fingerprint、manifest/artifact digest、target、runtime の provenance
   が追跡でき、rollback と temporary artifact cleanup が検証済みである。
6. 検証済み evidence を ADR に移し、aggregate が未完了なら `TODO.md` の `[~]` を維持する。

## 最初の着手点

最初は EC-M3-01 の小さな RED として、source fixture と同じ graph を Rust
`IntentGraph::to_manifest_json_value()` と selfhost serializer に通し、canonical JSON の
最初の差分（bytes、node count、edge count、status、exit code）を固定する。selfhost/native
implementation や Linux VM gate を先に広げず、差分の failure boundary が一つに絞れた後に
EC-M3-02 へ進む。

2026-07-25 に Rust-host actual Wasm の verified slice として、全 node kind、主要 edge、
span、sampling、provenance を含む `serde_json::Value` equality と canonical JSON bytes equality を
`test_e2e_selfhost_evidence_manifest_matches_rust_canonical_value` で固定した。さらに Rust CLI
では、source 入力から生成した canonical manifest を再入力した場合の report JSON と exit code
の一致を `validate_source_and_emitted_manifest_have_same_report_and_exit_code` で固定した。
これは native stage0、Linux x86_64、release artifact/runtime の証拠ではないため、M3 aggregate
と M2 の `[~]` 判定は変更しない。Evidence:
`docs/adr/decisions-v0.2-m3-manifest-parity.md`。

2026-07-25 に EC-M3-05 の provenance/path boundary として、native runner が stage0 manifest
の `compiler` / `transport_driver` / `materializer` に package 外の相対 pathや package 外へ解決する
symlink を受け入れない fixture contract を追加した。`compiler: ../outside/compiler` と
`bin/compiler-link -> /tmp/.../outside-compiler` は source commit が一致していても実行前に拒否され、
transport/materializer/program は起動しない。これは fake stage0 の fail-closed evidence であり、
実 target の artifact/runtime、public acquisition、rollback archive の完了証拠ではない。Evidence:
`docs/adr/decisions-v0.2-stage0-manifest-path-provenance.md`。

2026-07-27 に EC-M3-03 の EmbeddedCli slice として、実 argv の `validate --source` を source
fixture で実行し、complete graph + independent review の `pass=0`、trace gap / open question /
review 不足の `unknown=2`、contradiction の `fail=1` を実 artifact で固定した。また
`--emit-manifest` は `external-boundary:embedded-cli-manifest-output` を返し、report/manifest
file を曖昧に成功させないことを確認した。Rust canonical serializer の adapter parity は
既存 `selfhost_evidence_registry` sliceであり、今回の EmbeddedCli filesystem 境界とは分離する。
これは Rust-host actual Wasm evidence であり、native stage0、atomic/durable filesystem writer、
Mac/Linux target matrix の完了証拠ではない。Evidence:
`docs/adr/decisions-ec-m3-embedded-validation-status.md`。

2026-07-27 の follow-up で、`EmbeddedCli` の `--emit-manifest` を `App.Cli` と同じ
`validation-source-manifest-json` serializer と `write-file` 経路へ接続した。実 argv の
`validate --source ... --format json --emit-manifest ...` は report を stdout 1 行に保ち、
version 1 manifest file（nodes/evidence/edges）を生成し、trace gap の `unknown=2` を維持する。
旧 external-boundary/no-write の判断はこの sliceで superseded された。これは Rust-host actual
Wasm の output wiring であり、native stage0 の atomic/durable writer、write failure、source
provenance、Mac/Linux runtime matrix の完了証拠ではない。Evidence:
`docs/adr/decisions-ec-m3-embedded-manifest-emission.md`。

2026-07-27 に EC-M3-01 の fixture/harness slice として、同一 source fixtureから生成した
Rust canonical manifest bytesを `tests/fixtures/validation/ec-m3-canonical-manifest.json` に固定し、
Rust oracle value/bytes testと native source-file smoke contractへ接続した。smokeは
`validate --source ... --format json --emit-manifest ...` の `unknown` report、exit `2`、
trace/open-question/review metrics、manifest bytesを比較し、duplicate node sourceは exit `1`、
diagnosticのみ、report/manifestなしで fail-closed にする。Linux VM wrapperもfixtureを明示的に
コピーする。`cargo` / `rustc` / host `lsharp` は blocked toolとして成功経路から除外する。
これは実 stage0 artifactを使った native runtimeの GREEN ではなく、current source-commitに一致する
stage0でMac/Linux actual executionを行うための再現可能な RED/contractである。Evidence:
`docs/adr/decisions-ec-m3-native-manifest-parity-fixture.md`。

2026-07-27 に EC-M3-02 の status failure boundary として、`:outcome "contradicted"` と
`:contradicts` edge を持つ fixtureを native source-file smokeへ追加した。JSON は
`status: fail` / exit `1` / `contradicting_observations: 1` を返し、text は status と6件の
metricsを固定順で返す。parse/graph/write の diagnostic-only failureとは異なり report stdoutを
保持する契約である。これは fake Lima/provenance harness の verified sliceで、実 stage0
artifact/runtime、Mac/Linux matrix、native fallback exclusion の完了証拠ではない。Evidence:
`docs/adr/decisions-v0.2-native-validation-fail-report.md`。

2026-07-27 に EC-M3-02 の stale report boundary として、invalidation された review と、その
review が `evaluates` する evidence の stale propagation fixtureを native source-file smokeへ
追加した。JSON/text は `status: unknown` / exit `2`、`stale_reviews: 1`、`stale_evidence: 1`、
stderr 空を返す。判定 failure/report保持、parse/graph/write の diagnostic-only failure と並ぶ
stale policy の observable contractである。これは fake Lima/provenance harness の verified
sliceで、実 stage0 artifact/runtime、Mac/Linux matrix、native fallback exclusion の完了証拠では
ない。Evidence: `docs/adr/decisions-v0.2-native-validation-stale-report.md`。

2026-07-27 に EC-M3-01 の source graph fail-closed boundary として、`:motivates` が未登録の
`intent:checkout/missing` を参照する orphan fixtureを native source-file smokeへ追加した。
`source validation error:5`、exit `1`、stdout reportなし、manifest fileなしを要求する。これは
Rust `SourceGraphError::MissingNodeReference` と同じ error code の native contractであり、fake
Lima/provenance harness の verified sliceに留まる。実 stage0 artifact/runtime、Mac/Linux matrix、
native fallback exclusion の完了証拠ではない。Evidence:
`docs/adr/decisions-v0.2-native-validation-orphan-edge.md`。

2026-07-27 に EC-M3-01 の malformed source graph boundary として、endpoint が一つしかない
`:motivates "intent:checkout/safe-cancel"` fixtureを native source-file smokeへ追加した。
`source validation error:1`、exit `1`、stdout reportなし、manifest fileなしを要求する。これは
Rust source adapter の malformed edge/span diagnostic と同じ code 1 を native contractへ接続した
partial sliceであり、fake Lima/provenance harness の verified evidenceに留まる。実 stage0
artifact/runtime、Mac/Linux matrix、native fallback exclusion の完了証拠ではない。Evidence:
`docs/adr/decisions-v0.2-native-validation-malformed-edge.md`。

2026-07-27 に EC-M3-01 の source edge ID boundary として、`claim:checkout` のように `/` が
ない endpoint を持つ invalid-ID fixtureを native source-file smokeへ追加した。`source validation
error:2`、exit `1`、stdout reportなし、manifest fileなしを要求する。これは Rust source adapter
の malformed ID diagnostic と同じ code 2 を native contractへ接続した partial sliceであり、fake
Lima/provenance harness の verified evidenceに留まる。実 stage0 artifact/runtime、Mac/Linux matrix、
native fallback exclusion の完了証拠ではない。Evidence:
`docs/adr/decisions-v0.2-native-validation-invalid-id.md`。

2026-07-27 に EC-M3-01 の source node kind boundary として、`:claim "intent:checkout/wrong-kind"`
fixtureを native source-file smokeへ追加した。`source validation error:3`、exit `1`、stdout
reportなし、manifest fileなしを要求する。これは Rust source adapter の `KindMismatch` と同じ
code 3 を native contractへ接続した partial sliceであり、fake Lima/provenance harness の verified
evidenceに留まる。実 stage0 artifact/runtime、Mac/Linux matrix、native fallback exclusion の完了
証拠ではない。Evidence: `docs/adr/decisions-v0.2-native-validation-kind-mismatch.md`。

2026-07-27 に EC-M2-02/M3-01 の evidence registry closure boundary として、未登録の
`evidence:checkout/missing` を `:supports` する fixtureを native source-file smokeへ追加した。
`source validation error:6`、exit `1`、stdout reportなし、manifest fileなしを要求する。これは
Rust source adapter の `EvidenceRegistryRequired` と同じ code 6 を native contractへ接続した
partial sliceであり、fake Lima/provenance harness の verified evidenceに留まる。実 stage0
artifact/runtime、Mac/Linux matrix、native fallback exclusion の完了証拠ではない。Evidence:
`docs/adr/decisions-v0.2-native-validation-evidence-registry.md`。

2026-07-27 に EC-M2-02 の review registry closure boundary として、登録済み review と未登録
`review:checkout/missing` を同じ source に置き、未登録 reviewを `:evaluates` する fixtureを native
source-file smokeへ追加した。`source validation error:10`、exit `1`、stdout reportなし、manifest
fileなしを要求する。これは Rust source adapter の `MissingReview` と同じ code 10 を native
contractへ接続した partial sliceであり、fake Lima/provenance harness の verified evidenceに留まる。
実 stage0 artifact/runtime、Mac/Linux matrix、native fallback exclusion の完了証拠ではない。Evidence:
`docs/adr/decisions-v0.2-native-validation-missing-review.md`。

2026-07-27 に EC-M2-02 の review registry uniqueness boundary として、同じ
`review:checkout/duplicate` を異なる provenance digestで二度宣言する fixtureを native
source-file smokeへ追加した。`source validation error:7`、exit `1`、stdout reportなし、manifest
fileなしを要求する。これは Rust source adapter の `DuplicateReview` と同じ code 7 を native
contractへ接続した partial sliceであり、fake Lima/provenance harness の verified evidenceに留まる。
実 stage0 artifact/runtime、Mac/Linux matrix、native fallback exclusion の完了証拠ではない。Evidence:
`docs/adr/decisions-v0.2-native-validation-duplicate-review.md`。

2026-07-27 に EC-M2-02 の review metadata boundary として、visibility が `private` の
`review:checkout/invalid` fixtureを native source-file smokeへ追加した。`source validation error:8`、
exit `1`、stdout reportなし、manifest fileなしを要求する。これは Rust source adapter の
invalid review field diagnostic と同じ code 8 を native contractへ接続した partial sliceであり、
fake Lima/provenance harness の verified evidenceに留まる。実 stage0 artifact/runtime、
Mac/Linux matrix、native fallback exclusion の完了証拠ではない。Evidence:
`docs/adr/decisions-v0.2-native-validation-invalid-review.md`。

2026-07-27 に EC-M2-02 の review edge subject-kind boundary として、登録済み review を別の
review の `evaluates` subject に指定する `review:checkout/registered` fixtureを native source-file
smokeへ追加した。`source validation error:9`、exit `1`、stdout reportなし、manifest fileなしを
要求する。これは Rust source adapter の `EdgeSubjectKindMismatch` と同じ code 9 を native contract
へ接続した partial sliceであり、fake Lima/provenance harness の verified evidenceに留まる。実
stage0 artifact/runtime、Mac/Linux matrix、native fallback exclusion の完了証拠ではない。Evidence:
`docs/adr/decisions-v0.2-native-validation-review-subject-kind.md`。

2026-07-27 に同じ EC-M2-02 typed review-edge subject boundary の `invalidates` 経路として、
review の `invalidates` subject に `claim:checkout/rejects` を指定する fixtureを native source-file
smokeへ追加した。`source validation error:9`、exit `1`、stdout reportなし、manifest fileなしを要求する。
`evaluates` 経路と同じ Rust source adapter の `EdgeSubjectKindMismatch` code 9 を native contract
へ接続した partial sliceであり、fake Lima/provenance harness の verified evidenceに留まる。実
stage0 artifact/runtime、Mac/Linux matrix、native fallback exclusion の完了証拠ではない。Evidence:
`docs/adr/decisions-v0.2-native-validation-invalidation-subject-kind.md`。

## 非目標

- M2 の stable ID、validation report、manifest schema を再設計しない。
- MCP に filesystem write や hidden `verified` shortcut を追加しない。
- 未検証 target を supported matrix に混ぜない。
- Rust を bootstrap/oracle/rollback 用途から削除しない。
