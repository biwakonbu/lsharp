# L# v0.3 Milestone 1 — source/native provenance closure

- Status: Active planning
- Date: 2026-08-01
- Parent design: [`v0.3-review-provenance-lifecycle.md`](v0.3-review-provenance-lifecycle.md)
- Scope: `EC-M3-04` / `EC-M3-05`

## 目的

Rust canonical model と Rust-host の selfhost Wasm で検証済みの review attestation / evidence
identity を、current-source の native stage0、packaged artifact、release evidence へ同じ observable
contract のまま接続する。既存の partial slice を完了扱いへ拡大解釈せず、source、selfhost、native、
release の境界ごとに実行可能な task と validation gate を固定する。

## 現在の証拠（2026-07-31）

| 境界 | 検証済みの範囲 | 残る境界 |
| --- | --- | --- |
| `EC-M3-01` | Rust の canonical bytes、strict UTC、Ed25519、binding、explicit clock、wire schema/parser の exact duplicate trust-store boundary、retired/active trust-key rotation と active-key 一意選択、verified signature receipt の Rust/native canonical handoff | selfhost/native producer と target artifact parity、live provider/auth、native 暗号検証の接続 |
| `EC-M3-02` | Rust の append-only lifecycle reducer、ordering、rollback、clock state | provider snapshot、native/report parity |
| `EC-M3-03` | Rust CLI/MCP の trust/lifecycle/context、fail-closed projection | selfhost/native CLI/MCP parity |
| `EC-M3-04` | Rust source adapter、selfhost `IntentSource` / `Evidence` の named fields、canonical bytes、`unverified`、span、source error の Rust-host E2E。native source-file smoke の report/manifest fixture contract と Mac current-source producer/runtime | Linux x86_64 current-source runtime、packaged provenance、Mac/Linux matrix |
| `EC-M3-05` | Rust CLI/MCP/manifest と selfhost の evidence identity field order、nullable digest、conflict boundary。native source-file smoke の explicit identity JSON/text/manifest、identity options なしの JSON/manifest no-implicit boundary、all-or-none input contract、positional manifest の同値 identity reattach / conflict fail-closed、通常の selfhost `App.Cli` の JSON/text/manifest parity、native MCP subset の provider/output/input、manifest nested schema、canonical `review_now` lexical schema、explicit identity report/manifest postflight一致検証、source/file routeの no-implicit identity boundary、Mac current-source identity runtime、offline release identity verifier と optional packaged manifest projection、非 object manifest root の traceback-free fail-closed runtime validation、native MCP `lsharp_validate` の inline/file manifest root preflight（malformed JSON と配列・null・数値を native 実行前に拒否）、native MCP emitted manifest の root/schema/required-array preflight（malformed JSON、非 object、version不一致、欠落・未知・型不一致を traceback なしで拒否）、native `lsharp_validate` report の root/required/status/count/collection preflight（array/null/malformed/missing/unknown/invalid status/boolean count を traceback なしで拒否）、native `lsharp_check` output の root/required/boolean/array preflight（array/null/malformed/missing/unknown/type mismatch を traceback なしで拒否）、native `lsharp_errors` canonical table projection、native offline `lsharp_search` installed-package projection、native offline `lsharp_project_context` TOML/dependency/package projection、native offline `lsharp_package_api` existing `docs/api.json` projection と missing-file native `doc --json` source projection、Rust-canonical 生成済み `stdlib/api.json` の artifact projection と missing-artifact native `doc --json` source projection、native `lsharp_hover`、`lsharp_definition`、`lsharp_references`、`lsharp_completion` の source/file/position native `lsp --stdio` projection、native `lsharp_compile_run` の source/file → native compile → 外部 `wasmtime` 実行境界を追加検証 | native MCP の package-install/provider semantics、manifest runtime validation の残る全 surface、native stage0 report/runtime、Linux/current-source packaged target artifact runtime |

根拠となる ADR は [`decisions-v0.3-source-attestation-producer.md`](../../adr/decisions-v0.3-source-attestation-producer.md)、
[`decisions-v0.3-selfhost-source-attestation-graph.md`](../../adr/decisions-v0.3-selfhost-source-attestation-graph.md)、
[`decisions-v0.3-native-source-attestation-smoke.md`](../../adr/decisions-v0.3-native-source-attestation-smoke.md)、
[`decisions-v0.3-native-evidence-identity-smoke.md`](../../adr/decisions-v0.3-native-evidence-identity-smoke.md)、
[`decisions-v0.3-review-evidence-identity.md`](../../adr/decisions-v0.3-review-evidence-identity.md)、
[`decisions-v0.3-review-evidence-manifest-identity.md`](../../adr/decisions-v0.3-review-evidence-manifest-identity.md)、
[`decisions-v0.3-release-identity-gate.md`](../../adr/decisions-v0.3-release-identity-gate.md) である。

## 次の task

### M3-04-N1 — current-source native source-file parity

同一の `:review-attestation` fixtureを Rust oracle、selfhost producer、Mac Apple Silicon native stage0、
Linux x86_64 native stage0 へ渡し、次の値を機械比較する。

- named fields と field order
- length-prefixed UTF-8 canonical bytes（`expires-at` 有無を含む）
- producer state (`unverified`)
- directive span (`start` / `end`)
- valid / invalid の source error code と exit code
- report/manifest の有無、stdout/stderr の境界

成功経路は `cargo`、`rustc`、host `lsharp`、network、暗黙の trust store/lifecycle を呼ばない。
native stage0 の source commit と fixture producer fingerprint が current checkout に一致しない場合は
失敗として扱う。

2026-07-31 の verified partial slice は、Rust/selfhost の JSON report に named fields、field order、
期限付き/期限なしの `expires_at`、canonical bytes、`unverified`、directive span を追加し、Rust
focused test、selfhost `EmbeddedCli` actual Wasm E2E、Linux source-file static/provenance harness
で同じ fixtureを検証した。2026-07-31 に current `f6a6da30` の Mac Apple Silicon stage0 producer/package と
`native-selfhost-dev-source-file-smoke.sh` を実行し、actual App.Cli E2E（542.31秒）と source-file smoke が
passした。Linux x86_64 current-source runtime、packaged provenance と両 target matrixは未検証のため、
M3-04-N1 は `[~]` のまま残す。
なお、既定 macOS `TMPDIR` の task-owned artifact path が内側 producer の安全検査で拒否される RED も
再現し、`TMPDIR_ROOT/lsharp-*` だけを許可する fail-closed boundaryを追加した。その後、末尾 `/` を含む
既定 `TMPDIR` が `T//lsharp-*` を生成する外側 wrapperの連結不備を RED として固定し、
`TMPDIR_ROOT="${TMPDIR_ROOT%/}"` で正規化した。既定 `TMPDIR=/var/folders/.../T/` の actual
Mac producer/package（484.89秒）と生成 stage0の source-file smoke は GREEN である。

さらに current `3e1b26901aef8191a47382b06bf87fe62c9fb9ad` の Mac Apple Silicon producer/package を実行し、
actual `App.Cli` E2E（510.15秒）、`aarch64-apple-darwin` stage0 manifest の source commit/target 一致、
および `native-selfhost-dev-source-file-smoke.sh` を通過した。fresh evidence manifestには期限付き/期限なし
`expires_at`、`unverified`、directive span、invalid source error code `8`、stdout/stderr、exit code、
`compile.wasm` / `build.wasm` の同一 digest/sizeを保存した。これは Mac current-source の追加実証であり、
Linux x86_64 current-source runtime、fetch後の packaged artifact/runtime、Mac/Linux target matrixは未検証のため、
M3-04-N1 は引き続き `[~]` である。

同日、fixtureを [`tests/fixtures/validation/ec-m3-review-attestation-source.ls`](../../../tests/fixtures/validation/ec-m3-review-attestation-source.ls)
へ canonicalizeし、Rust parser、Rust-host selfhost、Mac source-file smoke、Linux stage0 wrapperが
同じ source bytesを読むようにした。Linux fake Lima/provenance harnessで必須検査、VM copy、期限付き/
期限なし canonical bytes と span の static contract を再確認した。これは fixture provenance の
verified partial sliceであり、Mac current-source runtime は上記の direct producer/package/smoke で検証済み、
Linux x86_64 runtime、fetch後の packaged artifact/runtime、Mac/Linux target matrix は未検証のままである。

2026-08-01 に source-file evidence writer の provenance input boundary を強化し、stage0 manifest の
`source_commit` を小文字40桁 hexadecimal に限定した。uppercase input は evidence directory の staging 前に
fail-closed で拒否し、canonical lowercase input の保存、既存 evidence の上書き拒否、Python/shell syntax gate を
contract test で確認した。これは writer boundary の verified partial sliceであり、Linux current-source runtime、
packaged provenance、Mac/Linux target matrix は引き続き未検証である。

同じ writer の work directory に symlink が含まれる場合も、初期入力と staging 結果の両方で検出して
evidence directory を作成しない fail-closed 契約を追加した。外部 path を参照する symlink を証跡へ残さない
task-owned evidence boundaryの verified partial sliceであり、Linux current-source runtime、packaged provenance、
Mac/Linux target matrixは未検証のままである。

2026-08-02 に Linux x86_64 source-file smoke の stage0 入力を、非 symlink の regular directory かつ配下に
symlink がない provenance-safe inputへ限定した。root symlink と nested symlink を fake Lima harness の RED→GREEN
で固定し、拒否前に VM startup/copy が呼ばれないことを確認した。既存の replay-lock と evidence-copy harness も
同じ focused lane で再確認する。これは stage0 input preflight の verified partial sliceであり、current-source
Linux runtime、fetch後の packaged provenance、provider/auth、Mac/Linux runtime matrix は未検証のため、
M3-04-N1 は `[~]` のまま残す。ADR: [`decisions-v0.3-native-linux-stage0-source-directory-provenance.md`](../../adr/decisions-v0.3-native-linux-stage0-source-directory-provenance.md)。

2026-08-02 に rollback compatibility archive の manifest payload declaration parity を追加した。`entry_binary` /
`lsp_binary` / `component` が実際に release smoke が消費する固定 payload 名と一致しない archive を、checksum、
rollback anchor、target/version/source commit が整っていても拒否する RED→GREEN を provider snapshot harness
で確認した。これは local packaged rollback manifest の verified partial sliceであり、live provider/auth、
current-source Linux runtime、両 target の packaged provenance/rollback bytes parity は未検証のため、M3-04-N1 は
`[~]` のまま残す。ADR: [`decisions-v0.3-rollback-archive-manifest-payload-parity.md`](../../adr/decisions-v0.3-rollback-archive-manifest-payload-parity.md)。

続けて同日、native-only primary manifest の `rollback_anchor.kind` を supplied rollback archive の
`archive_kind` と binding する boundary を追加した。primary checksum、asset/SHA、nested rollback identity が
正しくても kind が異なる fixture を RED→GREEN で拒否することを provider snapshot harness で確認した。
これは local primary-to-rollback declaration の verified partial sliceであり、live provider/auth、current-source
Linux runtime、両 target の packaged provenance/rollback bytes parity は未検証のため、M3-04-N1 は `[~]` のまま残す。
current-source replay blocker の再現は `git rev-parse --verify HEAD` と
`find /tmp /Users/biwakonbu/github/tmp -maxdepth 5 -type f -name manifest.json -path '*lsharp*'` で行う。ADR:
[`decisions-v0.3-rollback-anchor-kind-parity.md`](../../adr/decisions-v0.3-rollback-anchor-kind-parity.md)。

2026-08-02 に official two-target orchestrator の explicit `review-attestation-report` handoffを追加した。
reportは non-empty regular fileかつ JSON object / `review_attestations` listとして target invocation前に検査し、
source smoke evidence rootと組み合わせない入力を拒否する。Mac hostとLinux/Lima wrapperへ同一 reportを一度だけ
渡し、copy-out後の target別 evidence manifestの `review_attestations` を入力 reportと exact compareするため、
missing report、片側の伝播欠落、Linux側の copy-out/mismatch は downstream smokeの成功扱いにならない。
report-free routeは `review_attestations` を暗黙生成せず、fake two-target harnessで両target exact projection、
report-free、missing/preflight、Linux mismatchの RED→GREENを固定した。これは orchestrator handoff の verified
partial sliceであり、current-source Linux runtime、実 Mac/Lima runtime、provider/auth取得、packaged provenance と
両 target packaged/rollback parityは未検証のため、M3-04-N1 / M3-05-N9 は `[~]` のまま残す。ADR:
[`decisions-v0.3-native-official-two-target-attestation-report.md`](../../adr/decisions-v0.3-native-official-two-target-attestation-report.md)。

### M3-05-N1 — native evidence identity projection

M3-04-N1 の fixtureへ explicit `subject/source/artifact/now` と trust/lifecycle digest を渡し、
Rust/selfhost/native の JSON、text、MCP projection が次を共有することを固定する。

- identity field order と nullable digest (`null`)
- `verified` / `unverified` / `stale` / `revoked` / `invalid` の順序と report policy
- 既存 manifest identity との byte-equivalent 再 attach、conflict の fail-closed
- identity がない場合の後方互換と、暗黙値を補わない境界

通常の selfhost `App.Cli` については、Rust-host actual Wasm の JSON/text/manifest projection と
未検証 review の exit `2` を ADR [`decisions-v0.3-selfhost-cli-review-evidence-identity.md`](../../adr/decisions-v0.3-selfhost-cli-review-evidence-identity.md)
へ記録した。native stage0、MCP、provider、両 target の runtime は引き続き未完である。

2026-07-31 に native source-file smoke の identity options なし routeについて、JSON report と
emitted manifest の両方に `review_evidence_identity` を暗黙追加しない回帰契約を追加した。
explicit identity route、nullable digest、partial input拒否と同じ fake Linux provenance harness で
確認した verified partial sliceであり、native MCP、provider、current-source/packaged target runtimeは
未検証のままである。

同日、positional version 1 manifest input の既存 `review_evidence_identity` を fixed-wire fields
から scoped に再構成し、caller の明示 identity が同値なら report へ再 attach、異なる場合は
`source validation error:14`、exit `1`、stdout/manifest なしで停止する source contract を追加した。
Rust-host actual Wasm の parser/runtime test と Linux fake Lima/provenance harness で確認した
verified partial sliceであり、Mac current-source source-file smoke まで検証済みである。Linux x86_64
runtime、native MCP、provider、fetch後の packaged artifact と両 target runtime evidence は未検証のままである。

### M3-05-N2 — release identity gate

keyset digest、lifecycle snapshot digest、source commit、artifact digest を evidence identity に束ね、
Mac/Linux の packaged stage0 と release smoke が同じ identity を出すことを検証する。provider adapter は
外部入力境界に留め、helper 不在や digest mismatch を `verified` として扱わない。

既存 release surface には [`verify-native-release-identity.py`](../../scripts/ci/verify-native-release-identity.py)
を接続した。explicit identity を受け取った native-only archive と packaged stage0 は同じ canonical
objectを manifest/fileへ投影し、release smoke は artifact bytes、source commit、manifest/file conflictを
offlineで再検証する。identityが未指定の旧 archive は後方互換の unverified boundaryとして残し、
`NATIVE_ONLY_REQUIRE_REVIEW_EVIDENCE_IDENTITY=1` で strict missing-input failureへ切り替えられる。
provider helperの実取得、current-source native replay、両 target runtimeは次の gateで閉じる。

2026-07-31 の offline input slice では [`prepare-review-evidence-identity.py`](../../scripts/ci/prepare-review-evidence-identity.py)
を追加した。subject digest、source commit、artifact、clock を明示入力とし、trust store と lifecycle
snapshot は両方指定された場合だけ raw bytes の SHA-256 を計算する。出力は verifier と同じ field order
の JSON で、parent directory が存在しない場合は atomic output を開始せず失敗する。これは provider
取得や native runtime の証拠ではなく、caller/provider adapter と release verifier の間の offline
producer boundary を固定する verified partial slice である。`now` は単なる形状チェックではなく、
閏年を含む暦日・時分秒の範囲を Rust の canonical timestamp parser と共有し、preparer と verifier
の両方で不正な日時を fail-closed にする。
さらに verifier は明示された trust-store/lifecycle snapshot を任意に raw bytes から再計算し、
identity digest と一致しない場合を fail-closed にする。provider の取得・認証は外部境界のまま残す。
native-only `scripts/release.sh` には `NATIVE_ONLY_REVIEW_TRUST_STORE` /
`NATIVE_ONLY_REVIEW_LIFECYCLE` の all-or-none wiring を接続し、snapshot 指定時の digest mismatch を
packaging 前に拒否する。

2026-08-02 に verifier の provider snapshot path boundary を regular non-symlink file に限定した。実体と同じ
bytesを持つ trust-store / lifecycle の symlink fixture を RED→GREEN で拒否し、native packaging、release smoke、
stage0 package caller が共有する offline identity gate の provenance-safe input を固定した。これは provider
snapshot path の verified partial sliceであり、live provider/auth acquisition、current-source Linux runtime、
両 target の packaged provenance/rollback bytes parity は未検証のため、M3-05-N2 は `[~]` のまま残す。
current-source blocker の再現は `git rev-parse --verify HEAD` と
`find /tmp /Users/biwakonbu/github/tmp -maxdepth 5 -type f -name manifest.json -path '*lsharp*'`。ADR:
[`decisions-v0.3-provider-snapshot-regular-file.md`](../../adr/decisions-v0.3-provider-snapshot-regular-file.md)。

続けて同日、release smoke の `checksums.txt` target path を archive root 内の POSIX relative path に限定した。
`../../../outside-checksum-target.txt` の checksum-valid fixture を RED→GREEN で拒否し、checksum evidence が
extracted package 外の file を参照しない boundary を固定した。これは packaged release-smoke の verified partial
sliceであり、live provider/auth acquisition、current-source Linux runtime、両 target の packaged provenance/
rollback bytes parity は未検証のため、M3-05-N2 / M3-05-N7 は `[~]` のまま残す。current-source blocker の再現は
`git rev-parse --verify HEAD` と `find /tmp /Users/biwakonbu/github/tmp -maxdepth 5 -type f -name manifest.json
-path '*lsharp*'`。ADR: [`decisions-v0.3-release-smoke-checksum-path.md`](../../adr/decisions-v0.3-release-smoke-checksum-path.md)。

続けて同日、rollback archive の `checksums.txt` に README/LICENSE、launcher、LSP、component、manifest の
checksum entry を必須化した。`lsharp` の entry だけを欠落させた checksum-valid rollback fixture を RED→GREEN
で拒否し、smoke が実行・検証する payload 全てに checksum evidence がある boundary を固定した。これは
rollback package checksum coverage の verified partial sliceであり、live provider/auth acquisition、current-source
Linux runtime、両 target の packaged provenance/rollback bytes parity は未検証のため、M3-05-N2 / M3-05-N7 は `[~]`
のまま残す。current-source blocker の再現は `git rev-parse --verify HEAD` と
`find /tmp /Users/biwakonbu/github/tmp -maxdepth 5 -type f -name manifest.json -path '*lsharp*'`。ADR:
[`decisions-v0.3-rollback-checksum-coverage.md`](../../adr/decisions-v0.3-rollback-checksum-coverage.md)。

2026-08-01 に verifier の `--manifest` root shape を harden した。配列・null・数値の manifest を
release identity input として渡した場合、Python の `AttributeError` traceback を出さず、return code `1`
と `review_evidence_identity` を示す安定した入力エラーで停止する。`NativeReleaseIdentityTest` の
8 tests（新規 manifest root 3 variantsを含む）で確認した。これは manifest runtime validation の
verified partial sliceであり、native MCP full-tool/provider semantics、current-source target runtime と
packaged bytes parity は未完了のままである。

### M3-05-N7 — stage0 package / release-smoke snapshot propagation

stage0 package と release smoke が、native-only release と同じ explicit provider snapshot bytesを
同じ verifier へ渡す境界を固定する。

- `package-native-stage0-release.sh` の `--review-trust-store` / `--review-lifecycle` は identity file
  と all-or-none で受け取り、raw snapshot mismatch を package 前に拒否する。
- `release-smoke.sh` の `RELEASE_REVIEW_TRUST_STORE` / `RELEASE_REVIEW_LIFECYCLE` は native-only archive
  の `review-evidence-identity.json` と同じ verifierへ渡し、片側指定・空 snapshot・identity 不在を拒否する。
- rollback compatibility archive の recursive smoke へ provider snapshot env を持ち込まない。
- snapshot bytes は公開 archiveへコピーせず、caller/provider の offline input境界に留める。

2026-07-31 の focused evidence は stage0 package test の正しい snapshot 成功と改ざん trust snapshot
拒否、native-only/rollback fixtureを使う release-smoke recursive test、identity verifier/release surface
の 7 tests、shell syntax、Python compile、diff audit である。
これは offline propagation の verified partial sliceであり、multi-target orchestrator、provider
adapter、current-source stage0 provenance、Mac Apple Silicon / Linux x86_64 の実 release runtime は残る。
ADR: [`decisions-v0.3-stage0-release-smoke-snapshot-wiring.md`](../../adr/decisions-v0.3-stage0-release-smoke-snapshot-wiring.md)。

2026-08-02 に packaged stage0 releaseの `artifact_digest` を、stage0 manifestの compiler bytesへ
bindingした。別 artifact digestの identityを provider snapshot/source commitが正しくても archiveへ
格納しない RED→GREENを `test-native-stage0-release-package.sh` で確認した。これは packaged stage0
provenanceの verified partial sliceであり、provider API/auth取得・意味検証、current-source Linux runtime、
Mac/Linux両 targetの packaged bytes parity、rollback parityは未検証である。ADR:
[`decisions-v0.3-native-stage0-release-artifact-binding.md`](../../adr/decisions-v0.3-native-stage0-release-artifact-binding.md)。

続けて同日、stage0 directoryに embedded `review-evidence-identity.json` がある場合の明示 identityを
embedded payloadと比較する conflict boundaryを追加した。provider snapshot/source commit/artifact digestが
正しくても subject digestの異なる入力を silently overwrite せず、archive作成前に拒否する RED→GREENを同じ
stage0 release package harnessで確認した。これは packaged stage0 provenanceの verified partial sliceであり、
provider API/auth取得・意味検証、current-source Linux runtime、Mac/Linux両 targetの packaged bytes parity、
rollback parityは未検証である。ADR:
[`decisions-v0.3-native-stage0-release-embedded-identity-conflict.md`](../../adr/decisions-v0.3-native-stage0-release-embedded-identity-conflict.md)。

### M3-05-N8 — native-official multi-target snapshot propagation

`native-official-release-local.sh` と target-native smoke の入口から、同じ trust-store / lifecycle
snapshot pathを Mac Apple Silicon と Linux x86_64 の stage0 package、native release、release-smokeへ
明示的に渡す。各 targetで source commit、artifact bytes、identity digest、packaged runtime、rollback
anchorを比較し、snapshot pathの片側指定・欠落・改ざんを両 targetで fail-closed にする。provider API/auth
の実取得は外部境界として残し、実行時に network/helper を暗黙に呼ばないことを negative gate で確認する。

2026-07-31 の verified partial slice は、`NATIVE_OFFICIAL_REVIEW_TRUST_STORE` /
`NATIVE_OFFICIAL_REVIEW_LIFECYCLE` の all-or-none inputを、両 targetの `release.sh`、stage0 package、
Mac host smoke、Linux Lima smokeへ伝播する orchestrator wiringとして固定した。Linux側では snapshot
bytesを VM内へコピーし、verifierの `review_identity_timestamp.py` も同じ work directoryへ渡す。
fake two-target gate で env/option/copy と片側拒否を確認したが、current-source の実 Mac/Linux
stage0再生成、packaged runtime、provider API/auth は未完了である。ADR:
[`decisions-v0.3-native-official-multitarget-snapshot-wiring.md`](../../adr/decisions-v0.3-native-official-multitarget-snapshot-wiring.md)。

### M3-05-N9 — current-source two-target runtime evidence

同一 source commit の current-source native stage0 と packaged artifactを Mac Apple Silicon / Linux
x86_64 で実行し、provider snapshot digest、source/artifact identity、stage0 fetch、`--version` /
`--help`、rollback anchor、Wasm/runtime boundaryを実 bytes・stdout/stderr・exit codeで比較する。
stale stage0、implicit Rust/host fallback、network/helper呼び出し、片側だけの snapshot入力は negative
gateで拒否し、実行後に VM・temporary archive・processを回収する。

2026-07-31 の verified partial slice では、`native-official-release-local.sh` が各 target の stage0
archiveを `fetch-stage0.sh` で展開した直後、Mac host または Linux/Lima の既存 source-file smokeへ
fetch済み directoryを渡す wiringを追加した。fake two-target harnessで、元の入力ではなく
`${SMOKE_ROOT}/stage0-${target}` が両 wrapperへ渡ること、既存 snapshot propagation と片側入力拒否を
確認した。これは orchestrator contract の evidenceであり、実 Mac/Linux stage0 runtime、provider
取得、packaged App.Cli と rollback/Wasm parity の証拠ではない。ADR:
[`decisions-v0.3-native-official-stage0-runtime-smoke.md`](../../adr/decisions-v0.3-native-official-stage0-runtime-smoke.md)。

同日、`NATIVE_OFFICIAL_SOURCE_SMOKE_EVIDENCE_ROOT` を指定した official gate が target別の fresh leafへ
source-file smoke evidenceを渡す wiringを追加した。Mac hostはそのまま evidence writerへ渡し、Linux/Limaは
writerをVMへコピーしたうえで、成功・失敗の stdout/stderr、Wasm digest/size、exit code、stage0 manifestを
hostへ再帰コピーする。absolute/non-root、cleaned smoke root外、既存 leaf、symlinkを拒否する fake two-target
harness、Linux static contract、fake `limactl` による成功/失敗 copy-out（失敗 exit code保持）を通過した。これは証跡伝播の verified partial sliceであり、current-source
Mac/Linux stage0 runtime、provider取得、packaged App.Cliとrollback/Wasm parityは未検証のため、M3-05-N9は
`[~]` のまま残す。ADR:
[`decisions-v0.3-native-official-stage0-runtime-smoke.md`](../../adr/decisions-v0.3-native-official-stage0-runtime-smoke.md)。

同日、fresh current `e44ca72746e2c970588ac357979dc2b0bc8a67cc` の Mac Apple Silicon stage0 producerを
actual App.Cli E2E（491.75s）で完走させ、stage0 manifest/source commit/target/payloadを検証した。
stage0 archiveを作成して local HTTPの `fetch-stage0.sh` で release/package checksumと provenanceを
再検証し、producer/fetched package双方の source-file smokeを通過した。両 smokeは exit `0`、
`compile.wasm` / `build.wasm` digest `afd1638e444a7e8c371dc1d17550479fcc5e4efbbb9e9dbdffa8551933d71a00`
（2,559 bytes）、stage0 manifest digest `103566c49e1d16074e7cda46ff42ca59ea74c08210a41541d9b882adaa43586b`
を記録し、App.Cli producerの `--version` / `--help` も exit `0` / stderr空を確認した。これは Mac
current-source/fetch runtimeの verified partial sliceであり、Linux runtime、provider取得、両 target
packaged App.Cliとrollback/Wasm parityは未検証のため、M3-05-N9は `[~]` のまま残す。

2026-08-01 に `origin/main` の current `be17567f35f2f688a51652efc0bb6ba31ed12582` を clean worktreeで
Mac Apple Silicon stage0 producerへ渡し、actual App.Cli E2E（489.04s）と stage0 packageを完走した。
生成 manifest digestは `574f4fedbf3a0bd7d034c9443b99fa2137eed3199ad06320f581c583606a4f48`（259 bytes）で、
source commit/target/payloadを検証した。stage0 release packageの relative `--output-dir` が archive作成時に
相対パスを二重解決して失敗する RED を再現し、output directoryを canonical absolute pathへ解決する修正と
regression testを追加した。修正後、local file release inputの package→`fetch-stage0.sh` checksum/provenance
検証→fetched stage0 source-file smokeを完走し、producer/fetched smokeとも exit `0`、
`compile.wasm` / `build.wasm` の digest `afd1638e444a7e8c371dc1d17550479fcc5e4efbbb9e9dbdffa8551933d71a00`
（2,559 bytes）で一致した。これは Mac current-source/fetch runtimeとrelease package pathの verified
partial sliceであり、Linux runtime、provider取得、両 target packaged App.Cli/rollback parityは残るため、
M3-05-N9は `[~]` のままとする。

同日、別セッションの Linux hostgen VM replay と公式 release gate の競合を避けるため、
`native-official-release-local.sh` に `LSHARP_NATIVE_LINUX_X86_HOST_REPLAY_LOCK_DIR` の fail-closed
preflightを追加した。live PIDを持つ lockを検出すると dist/archive作成や `limactl` 呼び出し前に exit `90`
で停止し、artifact/vm workdir/PIDを operator logへ残す。fake lock contractと実 parser replayの実 lockで
確認し、stale/不正形状 lockは自動削除しない。さらに直接の
`native-linux-x86-native-stage0-source-file-smoke.sh` も同じ lockを `ensure_vm_running` 前に検査し、
`limactl` 未呼び出しを fake contractで確認した。これは他タスクを邪魔しない operator safety の
verified partial sliceであり、Linux runtime/provider/packaged parityの completion evidenceではない。

続く producer slice では、`native-macos-aarch64-stage0-release.sh` を追加し、current-source の
Mac `App.Cli` producer、Mac transport driver、Mac materializer、stage0 package builderを一つの
入口へ接続した。fake harnessで source commit、target payload、既存 output の fail-closed を確認したが、
実 Mac producerとsource-file smokeの完走、およびLinux targetの実 runtimeは未検証である。ADR:
[`decisions-v0.3-native-macos-stage0-producer.md`](../../adr/decisions-v0.3-native-macos-stage0-producer.md)。

source-file smoke の一時 work directory を後から監査できるよう、
`NATIVE_SELFHOST_SOURCE_SMOKE_EVIDENCE_DIR` を指定した任意の evidence export と、Wasm/stdout/stderr/exit
code の digest/size manifest を追加した。これは N9 operator gate の保存契約を固定する verified partial slice
であり、実 target runtime の成功を意味しない。ADR:
[`decisions-v0.3-native-source-smoke-evidence.md`](../../adr/decisions-v0.3-native-source-smoke-evidence.md)。

EC-M3-01 の native manifest preflight では、node/evidence/subject の `namespace`/`key` が空になる
serializer parity 差分を検出した。wire ID を一度だけ分解する共通 helperへ統合し、Mac stage0 の
working-tree preflightで Rust canonical manifest bytes と一致することを確認した。これは focused
serializer sliceであり、fresh producer、packaged runtime、provider、rollback、Linux evidence は未完了である。
ADR:
[`decisions-v0.3-ec-m3-native-id-serializer.md`](../../adr/decisions-v0.3-ec-m3-native-id-serializer.md)。

続く native validation boundary follow-up では、review-attestation span の終端を named field
読了後へ移し、evidence の required field 欠落と review/source pair/triple の引数過不足を
malformed sentinel として fail-closed にした。`supports` / `contradicts` の未登録 evidence は
wire ID より先に registry-required を返し、positional version 1 manifest input、manifest write
failure の file-exists 検査、native `error:` の stderr routing と exit code 保持、failed review の
independent-review count parity も追加した。既存 stage0 に working-tree source を渡す Mac
source-file smoke は全 validation/report/manifest/negative cases を通過したが、stage0 manifest
は旧 `64b207ed` のままである。fresh current-source producer、packaged artifact/runtime、Linux
x86_64/provider/rollback evidence は未完了であり、ADR:
[`decisions-v0.3-native-validation-boundary-followups.md`](../../adr/decisions-v0.3-native-validation-boundary-followups.md)。

さらに N9 の operator preflight として、`native-official-release-local.sh` は開始時に
`SOURCE_COMMIT` が current checkout の HEAD と一致する 40 桁 lowercase commit であることを
検査するようにした。不一致の場合は packaging、provider input、release smoke、Lima VM を起動せず
fail-closed に停止する。fake two-target snapshot gate で stale commit の拒否と invocation log
不変を確認した。これは stale input の早期排除だけを閉じる partial sliceであり、actual Mac/Linux
stage0 runtime、provider取得/auth、packaged artifact、rollback、Wasm parity は未完了である。ADR:
[`decisions-v0.3-native-official-source-commit-preflight.md`](../../adr/decisions-v0.3-native-official-source-commit-preflight.md)。

provider snapshot を指定する N9 operator gate では、Mac/Linux の App.Cli と stage0 の
`review-evidence-identity.json` 4入力も packaging 前に揃っている必要があるようにした。どれかが
欠ける場合は release、provider helper、release smoke、Lima VM を起動せず fail-closed に停止し、
fake two-target gate で invocation log 不変を確認する。snapshot 未指定の旧 archive は unverified
compatibility boundary として残す。これは provider input closure の partial sliceであり、actual
provider/auth、両 target runtime、rollback、Wasm parity は未完了である。ADR:
[`decisions-v0.3-native-official-provider-identity-preflight.md`](../../adr/decisions-v0.3-native-official-provider-identity-preflight.md)。

続けて、provider snapshot が指定された場合の4つの `review-evidence-identity.json` について、JSON objectの
`source_commit` が current checkout の HEAD と一致することも packaging 前に検査するようにした。不正 JSON、
非 object、欠落または別 source commit は `review evidence identity source_commit mismatch` として拒否し、release、
provider helper、stage0 fetch、source smoke、release smoke、Lima VMを開始しない。fake two-target snapshot
gateで identity source commit mismatch の RED→GREEN と invocation log 不変を確認した。これは provider
identity/current-source binding の verified partial sliceであり、provider API/auth の実取得・意味検証、
actual Mac/Linux runtime、packaged bytes parity、rollback/Wasm parity は未検証であるため、M3-04-N1 / M3-05-N2 /
M3-05-N7 / M3-05-N9 は `[~]` のまま残す。ADR:
[`decisions-v0.3-native-official-provider-source-commit-binding.md`](../../adr/decisions-v0.3-native-official-provider-source-commit-binding.md)。

さらに、provider snapshot 指定時の4つの identityについて、既存の
`verify-native-release-identity.py` を `--source-commit`、trust-store、lifecycle、
`--require-provider-input` 付きで packaging 前に呼び出すようにした。current `source_commit` だけを持つ
不完全 JSON の RED→GREEN、canonical schema error、provider digestの早期検査、downstream invocation log 不変を
fake two-target snapshot gateで確認した。これは provider identity schema/digest preflight の verified partial
sliceであり、provider API/auth の実取得・意味検証、actual Mac/Linux runtime、packaged bytes parity、
rollback/Wasm parity は未検証であるため、M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は `[~]` のまま残す。ADR:
[`decisions-v0.3-native-official-provider-identity-schema-preflight.md`](../../adr/decisions-v0.3-native-official-provider-identity-schema-preflight.md)。

さらに、stage0 directoryに `review-evidence-identity.json` が埋め込まれている場合、
`package-native-stage0-release.sh` が explicit identity、trust-store、review-lifecycle snapshotなしに
archiveを作らない preflightを追加した。provider snapshotなしで埋込み identityを包装する RED→GREENと、
packaging前の archive不生成を direct release-package harnessで確認した。これは既存 verifierへ委譲する
packaged identity inputの verified partial sliceであり、live provider/auth取得・意味検証、current-source
Linux runtime、両 target packaged bytes、rollback/Wasm parityは未検証のため、M3-04-N1 / M3-05-N2 /
M3-05-N7 / M3-05-N9は`[~]`のまま維持する。ADR:
[`decisions-v0.3-native-stage0-release-embedded-identity-provider-preflight.md`](../../adr/decisions-v0.3-native-stage0-release-embedded-identity-provider-preflight.md)。

同日の最終監査で current `origin/main` / `HEAD` `c20ef6b0a24a6c02de1d504d20e072aebbed6a80` に一致する
Linux stage0 manifestと expected replay lockは見つからなかった。別セッション所有の Lima hostagentを
変更せず、Linux runtime replay / stage regenerationは起動していない。再現 commandは
`current_head="$(git rev-parse --verify HEAD)"; find /tmp /Users/biwakonbu/github/tmp -type f -name manifest.json -path '*lsharp*'`
と `find /tmp -maxdepth 3 \( -name 'lsharp-native-linux-x86-hostgen-vm-*' -o -name '*.lock' \)` であり、
current-source Linux runtime、provider/auth取得、両 target packaged/rollback parityは未検証のまま
`[~]` を維持する。

さらに、offline identity producer と verifier が trust-store / review-lifecycle の zero-byte snapshotを
provider provenanceとしてdigest化できる穴を閉じた。provider fieldだけを `must be non-empty` として
RED→GREENにし、producer output未生成と verifier fail-closedを focused testsで確認した。これは空の
provider inputを拒否する verified partial sliceであり、live provider/auth取得・署名意味検証、current-source
Linux runtime、両 target packaged/rollback bytes parityは未検証のため、M3-04-N1 / M3-05-N2 / M3-05-N7 /
M3-05-N9は`[~]`のまま維持する。ADR:
[`decisions-v0.3-provider-snapshot-nonempty-preflight.md`](../../adr/decisions-v0.3-provider-snapshot-nonempty-preflight.md)。

さらに、trust-store と review-lifecycle に同じ lexical-normalized pathを渡す provider adapterの誤配線を
producer / verifierの両方で `must be different files` として拒否した。same-path RED→GREENと producer output
未生成を focused testsで確認した。これは provider input role bindingの verified partial sliceであり、live
provider/auth取得・署名意味検証、current-source Linux runtime、両 target packaged/rollback bytes parityは未検証のため、
M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9は`[~]`のまま維持する。ADR:
[`decisions-v0.3-provider-snapshot-role-binding.md`](../../adr/decisions-v0.3-provider-snapshot-role-binding.md)。

さらに、native MCP が明示 provider snapshotを raw bytes の digestへ変換するだけで、署名・lifecycleの
semantic verificationを実行しない境界を固定した。provider snapshotを指定した native report の
`verified` / `stale` / `revoked` stateを受理せず、`unverified` 以外は `provider semantic verification is
unavailable` で fail-closed にする RED→GREEN と focused suite を確認した。これは provider semantic
boundary の verified partial sliceであり、live provider/auth取得、実 semantic verifier、current-source
Linux runtime、両 target packaged/rollback bytes parityは未検証のため、M3-04-N1 / M3-05-N2 / M3-05-N7 /
M3-05-N9は`[~]`のまま維持する。ADR:
[`decisions-v0.3-native-mcp-provider-semantic-boundary.md`](../../adr/decisions-v0.3-native-mcp-provider-semantic-boundary.md)。

同じ N9 operator preflight の cleanup path は、`/tmp/lsharp-*` prefix だけでなく `.` / `..` の
traversal component も拒否するようにした。`/tmp/lsharp-owner/../outside` を fake two-target gate
へ渡す RED を追加し、packaging、provider input、release smoke、Lima VM より前に
`unsafe cleanup path` として停止し、prefix 外の sentinel を保持する GREEN を確認した。これは
task-owned temporary cleanup の lexical safety boundary に限る verified partial sliceであり、
実 target runtime、provider/auth、packaged artifact、rollback、Wasm parity は未完了である。ADR:
[`decisions-v0.3-native-official-cleanup-path.md`](../../adr/decisions-v0.3-native-official-cleanup-path.md)。

さらに Linux x86_64 release smoke の Lima VM work directory は、archive/helper/snapshot copy や
smoke setup が失敗しても専用 `EXIT` trap で回収するようにした。fake `limactl` の copy failure
で、失敗後にも同じ work directory の cleanup invocation が残ることを確認した。これは task-owned
VM temporary cleanup の partial sliceであり、実 Lima VM、current-source stage0、provider/auth、
packaged artifact、rollback、Wasm parity は未完了である。ADR:
[`decisions-v0.3-native-official-vm-smoke-cleanup.md`](../../adr/decisions-v0.3-native-official-vm-smoke-cleanup.md)。

同じ cleanup boundary で、gate 自身が停止状態から起動した Lima VM だけを終了時に stop し、
既に Running の共有 VM は停止しない lifecycle ownership も固定した。fake two-target gate の
Stopped/Running 両状態と copy failure の non-zero 保持を確認している。さらに owned VM の stop
failure は smoke success に隠さず non-zero として返す。これは VM lifecycle の operator partial
sliceであり、実 Lima VM、current-source stage0、provider/auth、packaged artifact、rollback、Wasm
parity は未完了である。ADR:
[`decisions-v0.3-native-official-vm-lifecycle-cleanup.md`](../../adr/decisions-v0.3-native-official-vm-lifecycle-cleanup.md)。

下流の `native-linux-x86-native-stage0-source-file-smoke.sh` にも同じ ownership/cleanup/status 契約を
接続した。停止中 VM の start→stop、既存 Running VM の no-stop、owned VM stop failure の non-zero を
fake stage0 harness で固定している。これは stage0 smoke operator の partial sliceであり、実 Linux
stage0 runtime、current-source provenance、provider/auth、packaged artifact、rollback、Wasm parity は
未完了である。ADR:
[`decisions-v0.3-native-stage0-vm-lifecycle-cleanup.md`](../../adr/decisions-v0.3-native-stage0-vm-lifecycle-cleanup.md)。

2026-08-01 の current `origin/main` `1cdbe555f63c909fbfb3940c8462cf4b08ba442d` では、Mac Apple
Silicon と Linux x86_64 の App.Cli producer、Linux hostgen stage2/stage3 fixed point、protocol
stage0 compiler、provider snapshot identity、rollback compatibility archive、stage0 package/fetch、
official release smoke の evidence を同一 source commit で揃えた。Mac の producer/fetched stage0
source-file smoke は exit `0`、`compile.wasm` / `build.wasm` は SHA-256
`afd1638e444a7e8c371dc1d17550479fcc5e4efbbb9e9dbdffa8551933d71a00`（2,559 bytes）で一致した。
Linux の current-source source-file smoke は別タスクが保持する hostgen replay lock（exit `90`）で
実行待ちのため、Linux runtime と native MCP/provider adapter は未完了の `[~]` とする。詳細な
artifact digest、protocol/compiler 境界、失敗した App.Cli stage0 の訂正、cleanup ownership は
[`decisions-v0.3-current-source-two-target-runtime.md`](../../adr/decisions-v0.3-current-source-two-target-runtime.md)
へ記録した。

2026-08-02 の current Cloud checkout `e7c7e8644429acae2823af6695c7b4a842761588` について、Linux
stage0 artifact、provenance、replay lock を read-only 監査した。current source commit と一致する
stage0 manifest は見つからず、確認できた manifest は別 commit (`ec8d9de4` など) のため、実 Linux
runtime replay は起動しなかった。代わりに `fetch-stage0.sh` の archive preflight が directory または
regular file 以外を拒否する契約を追加し、valid local package と unknown tar entry の RED→GREEN を fake
release input で確認した。これは packaged archive input safety の verified partial sliceであり、current-source
Linux runtime、packaged stage0 runtime、provider/auth、rollback、Mac/Linux runtime matrix は未検証のため、
M3-05-N9 は `[~]` のまま残す。ADR: [`decisions-v0.3-stage0-fetch-archive-entry-provenance.md`](../../adr/decisions-v0.3-stage0-fetch-archive-entry-provenance.md)。
再現 command は `git rev-parse --verify HEAD` と
`find /tmp /Users/biwakonbu/github/tmp -maxdepth 4 -type f -name manifest.json -path '*lsharp*'` であり、
current source commit に一致する入力がないため `LSHARP_NATIVE_LINUX_X86_STAGE0_DIR` を指定した Linux
source-file smoke の実行条件を満たさない。

同日、`STAGE0_RELEASE_BASE_URL` の provider URL preflight を追加し、HTTPS host または local `file://`
だけを許可する境界を `curl` 前に固定した。HTTP、埋め込み credentials、query/fragment の RED→GREEN と
fake `curl` 未呼び出しを確認し、既存 local fetch/install harness も継続成功した。これは provider URL
input の verified partial sliceであり、live provider API/auth、current-source Linux runtime、packaged
target parity、rollback parity の証拠ではないため、M3-05-N9 は `[~]` を維持する。ADR:
[`decisions-v0.3-stage0-fetch-provider-url-boundary.md`](../../adr/decisions-v0.3-stage0-fetch-provider-url-boundary.md)。

同日、既存 atomic install harness に final install move failure と rollback restore move failure の二重失敗を
追加した。restore move が失敗しても旧 stage0 を copy-recover し、成功後に hidden backup を回収する RED→GREEN
を確認した。これは local rollback restore failure の verified partial sliceであり、実 I/O 障害下の recovery、
rollback archive parity、live provider/auth、current-source Linux runtime、packaged target parity の証拠ではないため、
M3-05-N9 は `[~]` を維持する。ADR:
[`decisions-v0.3-stage0-fetch-rollback-restore-failure.md`](../../adr/decisions-v0.3-stage0-fetch-rollback-restore-failure.md)。

同日、native selfhost runner の `mcp-server` を無条件拒否から、newline-delimited JSON-RPC の
`initialize` / `ping` / `tools/list` / `tools/call` と `lsharp_check`・`lsharp_validate`・
`lsharp_format` の deterministic subsetへ移行した。shim は native `program.native` だけを呼び、
provider snapshot pathは明示した trust store / lifecycle の両ファイルを offline で raw bytes の
SHA-256 digestへ変換して `validate` へ渡す。片側指定、欠損、非 regular、空、明示 digestとの不一致は
native実行前に fail-closed とし、ネットワーク・provider helper・署名/lifecycle意味検証は呼ばない。
tools/list の JSON Schema にも provider path と review identity 4-field の all-or-none
`dependentRequired` を公開し、`additionalProperties: false` と shim自身の partial/unknown
input preflightでも同じ境界を使う。
focused testで identity digest forwarding、manifest projection、provider digest、mismatch、欠損/
empty、malformed input、missing executable、check migrationDiagnostics schemaを固定し、既存 runner
contract testも通過した。さらに validate report の trace gap、review identity、review verification
schemaを Rust MCP と揃え、manifest top-level の version/nodes/evidence/edges 契約も固定した。これは
native MCP/provider snapshot adapter の verified partial sliceであり、Rust MCP 全tool parity、
provider取得/auth、署名/lifecycle検証、Linux runtimeは残る。ADR:
[`decisions-v0.3-native-mcp-subset-shim.md`](../../adr/decisions-v0.3-native-mcp-subset-shim.md)、
[`decisions-v0.3-native-mcp-provider-snapshot-adapter.md`](../../adr/decisions-v0.3-native-mcp-provider-snapshot-adapter.md)、
[`decisions-v0.3-native-mcp-review-identity-contract.md`](../../adr/decisions-v0.3-native-mcp-review-identity-contract.md)、
[`decisions-v0.3-native-mcp-validate-input-closed-world.md`](../../adr/decisions-v0.3-native-mcp-validate-input-closed-world.md)、
[`decisions-v0.3-native-mcp-check-output-schema.md`](../../adr/decisions-v0.3-native-mcp-check-output-schema.md)、
[`decisions-v0.3-native-mcp-validate-output-schema.md`](../../adr/decisions-v0.3-native-mcp-validate-output-schema.md)、
[`decisions-v0.3-native-mcp-manifest-output-schema.md`](../../adr/decisions-v0.3-native-mcp-manifest-output-schema.md)。

2026-08-01 に lsharp_errors、lsharp_search、lsharp_project_context、lsharp_package_api、lsharp_stdlib_api を追加した。lsharp_errors は canonical
crates/lsharp-driver/src/error_codes.rs を read-only lookup して LS#### と E0001-E0005
（E0003 は LS1002 へ統合）の alias、未知コード、docs link を返す契約を固定した。Rust/native
schema は error_code 必須・空文字拒否・additionalProperties: false で揃え、fake native
program の実行が発生しないことも確認した。lsharp_search は `.lsharp/packages` の
`lsharp.toml` を読む offline installed-package projection とし、query、決定的順序、closed-world
schema、fake native program 非実行を同じ boundary で固定した。lsharp_project_context は
`lsharp.toml` の project/exports/dependencies と `.lsharp/packages` を offline projection し、
依存名の決定的順序、closed-world schema、引数 fail-closed、fake native program 非実行を固定した。
native MCP 47 tests、native LSP relay 5 tests、Rust `mcp_server::tests` 87 tests（package API filter 6 tests）、driver unit 214 tests、Python compile、runner contract を通過した
verified partial である。`.lsharp/packages` の file/dangling symlink を除外し、directory symlink を path dependency
互換のため保持する Rust/native discovery contract も追加した。ADR:
[`decisions-v0.3-native-mcp-package-entry-boundary.md`](../../adr/decisions-v0.3-native-mcp-package-entry-boundary.md)。lsharp_package_api は installed package の既存 `docs/api.json` を読む offline projection に加え、
artifact が無い場合は sorted `src/**/*.ls` ごとの native `doc --json` を in-memory で統合する。full
closed-world shape validation、malformed native doc の fail-closed、docs/api.json 非作成を Rust/native で揃えた。
lsharp_stdlib_api は Rust canonical `doc --json` から生成した `stdlib/api.json` を読む offline projection に加え、
artifact が無い場合の sorted direct `stdlib/*.ls` ごとの native `doc --json` in-memory projection を追加した。
native `lsharp_hover` は source/file と line/character（`col` alias）を native `lsp --stdio` へ渡し、
signature/doc の scalar projection と malformed/child failure の fail-closed を追加検証した。
続いて `lsharp_definition` の同じ source/file/position relay、単一 location の closed `{start,end}`
projection、invalid range/ambiguous location の fail-closed を追加検証した。native MCP 37 tests、native LSP
relay 5 tests、Rust `mcp_server::tests` 87 tests（package API filter 6 tests）、driver unit 214 tests、Python compile、runner contract を通過した
verified partial である。続いて `lsharp_references` の `includeDeclaration: true` relay、複数 location の
closed `{count,ranges}` projection、native `null` の空集合化、invalid range の fail-closed を追加し、native MCP 42 tests を通過した。
さらに `lsharp_completion` の array/CompletionList relay、numeric completion kind の Rust 名称投影、native
`null` の空候補化、invalid item の fail-closed を追加し、native MCP 47 tests を通過した。package-install
semantics、compile-run/provider semantics と target runtime は残る。さらに `lsharp_compile_run` の
source/file route、native `compile` と外部 `wasmtime` の明示境界、compile/runtime failure・欠落 artifact・
runtime 未設定の fail-closed、stdout 分離、task-owned temporary cleanup を追加し、native MCP 52 tests を通過した。
これは local external-runtime の verified partial であり、supported-target stage0/packaged runtime parity ではない。
package-install/provider semantics と target runtime は残る。ADR:
[decisions-v0.3-native-mcp-subset-shim.md](../../adr/decisions-v0.3-native-mcp-subset-shim.md)、
[decisions-v0.3-native-mcp-compile-run-boundary.md](../../adr/decisions-v0.3-native-mcp-compile-run-boundary.md)。
さらに native `lsharp_format` の `formatted` closed-world output schema と、stdout を伴う native
non-zero `fmt` の stderr/status 付き fail-closed error を追加検証した。native MCP 60 tests と
Python compile を通過した verified partial である。これは opaque source-text relay の境界に限られ、
target runtime、provider semantics、Rust/native full parity は残る。ADR:
[`decisions-v0.3-native-mcp-format-output.md`](../../adr/decisions-v0.3-native-mcp-format-output.md)。
さらに native MCP LSP 4 tools の `character` / `col` position alias を oneOf schema と runtime
preflight で揃え、両方指定した入力を native 実行前に fail-closed とした。native MCP 62 tests と
Python compile を通過した verified partial である。単独 `col` の互換 relay は維持し、target runtime、
provider semantics、Rust/native full parity は残る。ADR:
[`decisions-v0.3-native-mcp-lsp-position-alias.md`](../../adr/decisions-v0.3-native-mcp-lsp-position-alias.md)。
さらに native MCP `lsharp_check` / `lsharp_format` の `source`/`file` input schema と runtime
preflight を closed-world に揃え、未知引数・source/file 同時指定を native 実行前に拒否した。native
MCP 65 tests と Python compile を通過した verified partial である。target runtime、provider semantics、
Rust/native full parity は残る。ADR:
[`decisions-v0.3-native-mcp-check-format-input.md`](../../adr/decisions-v0.3-native-mcp-check-format-input.md)。
さらに共通 `source` input schema に `minLength: 1` を設定し、native LSP 4 tools、`check`、`validate`、
`format`、`compile_run` の `tools/list` と runtime preflight の非空契約を揃えた。空文字・空白のみの
`source` を native 実行前に拒否し、native MCP 68 tests と Python compile を通過した verified partial
である。target runtime、provider semantics、Rust/native full parity は残る。ADR:
[`decisions-v0.3-native-mcp-source-input-schema.md`](../../adr/decisions-v0.3-native-mcp-source-input-schema.md)。
さらに native MCP `lsharp_check` の `migrationDiagnostics` item を postflight 検証し、required field、
code/semantics/disposition enum、range、非負 position、optional message の不正値を native 成功として
返さない contract を追加した。完全な item はそのまま通し、native MCP 69 tests と Python compile を
通過した verified partial である。target runtime、provider semantics、Rust/native full parity は残る。ADR:
[`decisions-v0.3-native-mcp-check-output-items.md`](../../adr/decisions-v0.3-native-mcp-check-output-items.md)。
さらに native MCP `lsharp_validate` の `trace_gaps` / `review_verifications` item を postflight 検証し、
required field、closed field、trace-gap code、review ID pattern、lifecycle state の不正値を native 成功
として返さない contract を追加した。完全な nested report はそのまま通し、native MCP 70 tests と
Python compile を通過した verified partial である。identity、manifest runtime、target runtime、provider
semantics、Rust/native full parity は残る。ADR:
[`decisions-v0.3-native-mcp-validate-output-items.md`](../../adr/decisions-v0.3-native-mcp-validate-output-items.md)。
さらに native MCP `lsharp_validate` の `review_evidence_identity` report object を postflight 検証し、
required field、unknown field、非 nullable string、nullable provider digest の型境界を揃えた。manifest
input 経由の malformed identity を native 成功として返さず、完全な identity は保持することを native
MCP 72 tests と Python compile で確認した verified partial である。manifest nested runtime、target runtime、
provider semantics、Rust/native full parity は残る。ADR:
[`decisions-v0.3-native-mcp-review-identity-output.md`](../../adr/decisions-v0.3-native-mcp-review-identity-output.md)。
さらに native MCP emitted manifest の `nodes` / `reviews` item を postflight 検証し、closed field、
required identifier、kind/visibility/state enum、non-empty text、span offset、provenance digest の境界を
揃えた。valid node/review manifest は保持し、malformed item は native 成功として返さないことを native
MCP 73 tests と Python compile で確認した verified partial である。evidence/edges nested runtime、target
runtime、provider semantics、Rust/native full parity は残る。ADR:
[`decisions-v0.3-native-mcp-manifest-items.md`](../../adr/decisions-v0.3-native-mcp-manifest-items.md)。
さらに native MCP emitted manifest の `edges` を全 relation variant（`motivates`、`constrained-by`、
`tested-by`、`supports`、`contradicts`、`evaluates`、`invalidates`）ごとに postflight 検証し、relation
固有 field、ID、subject kind、unknown field の境界を揃えた。valid edge manifest は保持し、malformed edge
は native 成功として返さないことを native MCP 74 tests と Python compile で確認した verified partial
である。evidence nested、referential integrity、target runtime、provider semantics、Rust/native full parity
は残る。ADR: [`decisions-v0.3-native-mcp-manifest-edges.md`](../../adr/decisions-v0.3-native-mcp-manifest-edges.md)。

さらに native MCP emitted manifest の `evidence` item を postflight 検証し、closed field、required
identifier、subject kind、method/outcome/independence enum、execution/sampling、provenance の境界を揃えた。
valid evidence record は保持し、malformed record は native 成功として返さないことを native MCP 75 tests
と Python compile で確認した verified partial である。referential integrity、target runtime、provider
semantics、Rust/native full parity は残る。ADR:
[`decisions-v0.3-native-mcp-manifest-evidence.md`](../../adr/decisions-v0.3-native-mcp-manifest-evidence.md)。

さらに native MCP emitted manifest の referential integrity を postflight 検証し、node/evidence/explicit
review の duplicate ID、evidence subject、全 edge relation の graph-owned endpoint、node kind mismatch を
fail-closed にした。explicit `reviews` registry がある場合だけ review endpoint を閉じ、contract/change と
省略された review registry は opaque external boundary として保持する。全7 relation variant と opaque
boundary の valid fixture を保持し、malformed closure を拒否することを native MCP 76 tests と Python
compile で確認した verified partial である。target runtime、provider semantics、Rust/native full parity は
残る。ADR:
[`decisions-v0.3-native-mcp-manifest-referential-closure.md`](../../adr/decisions-v0.3-native-mcp-manifest-referential-closure.md)。

さらに native MCP emitted manifest の `execution.sampling.coverage` を Rust `SamplingPlan` と同じ invariant
へ揃え、非空 bucket 合計の `cases` 一致、checked u64 overflow 拒否、空/省略 coverage の後方互換を固定した。
canonical、mismatch、overflow、empty coverage の fixture を native MCP 77 tests と Python compile で確認した
verified partial である。provider semantics、target runtime、Rust/native full parity は残る。ADR:
[`decisions-v0.3-native-mcp-manifest-sampling.md`](../../adr/decisions-v0.3-native-mcp-manifest-sampling.md)。

さらに native MCP `lsharp_validate` report の5つの counter を shared `U64_MAX` まで postflight 検証し、
`u64::MAX + 1` を native success として返さない boundary を固定した。schema maximum assertion と
overflow fixture を native MCP 77 tests、Python compile、docs audit、diff checkで確認した verified partial
である。provider semantics、target runtime、Rust/native full parity は残る。ADR:
[`decisions-v0.3-native-mcp-validate-report-counters.md`](../../adr/decisions-v0.3-native-mcp-validate-report-counters.md)。

さらに native MCP shim が直接読む JSON-RPC request、native report、direct/file manifest input、emitted
manifest を strict object-pairs parserで読み、nested duplicate keyを最後の値へ黙って上書きしない boundaryを
固定した。duplicate fixtureを native MCP 78 tests、Python compile、docs audit、diff checkで確認した verified
partialである。provider semantics、target runtime、Rust/native full parity は残る。ADR:
[`decisions-v0.3-native-mcp-json-duplicate-keys.md`](../../adr/decisions-v0.3-native-mcp-json-duplicate-keys.md)。

さらに native MCP の LSP response frame と package/stdlib `api.json` artifact を shared strict JSON
decoderへ統合し、duplicate `result` / `package` key を last-value 化せず relay-specific error として拒否する
boundaryを固定した。LSP、package、stdlib の duplicate fixtureを native MCP 79 tests、Python compile、docs
audit、diff checkで確認した verified partialである。provider semantics、target runtime、Rust/native full parity
は残る。ADR:
[`decisions-v0.3-native-mcp-json-relays.md`](../../adr/decisions-v0.3-native-mcp-json-relays.md)。

さらに shared strict JSON decoder の `parse_constant` で `NaN` / `Infinity` / `-Infinity` を拒否し、
JSON-RPC、native report、LSP、package/stdlib artifact の非標準 JSON constant を成功値として返さない
boundaryを固定した。native MCP 79 tests、Python compile、docs audit、diff checkで確認した verified partial
である。provider semantics、target runtime、Rust/native full parity は残る。ADR:
[`decisions-v0.3-native-mcp-json-constants.md`](../../adr/decisions-v0.3-native-mcp-json-constants.md)。

2026-08-02 に native official release gate の Linux hostgen replay lock preflight を provider snapshot / identity /
source smoke evidence 検証より前へ固定した。別セッションが live replay を所有中で provider identity が欠ける場合も、
provider入力へ進まず exit `90` と owner/artifact/VM path を返し、release outputを作成しない RED→GREEN を
`test-native-official-release-replay-lock.sh` で確認した verified partial である。これは operator ownership の
failure-order boundary に限られ、provider API/auth acquisition・意味検証、current-source Linux runtime、Mac/Linux
両 targetの packaged provenance/rollback bytes parity の証拠ではない。current-source manifest/expected replay lockが
現HEADに一致せず、別セッションのLima/cargo/replay processも所有中のため Linux replay・stage regeneration・full
buildは未実行である。ADR: [`decisions-v0.3-native-official-release-replay-lock-precedence.md`](../../adr/decisions-v0.3-native-official-release-replay-lock-precedence.md)。

2026-08-02 に release smoke の provider snapshot preflight を archive path確認・展開より前へ固定した。欠落 archiveと
trust-storeのみを指定した場合も、archive errorへ進まず all-or-none provider診断を返し、work directoryを作成しない
RED→GREENを `test-release-smoke-provider-snapshots.sh` で確認した verified partial である。これは provider inputの
failure-order boundaryに限られ、live provider API/auth acquisition・意味検証、current-source Linux runtime、Mac/Linux両
targetの packaged provenance/rollback bytes parity の証拠ではない。current-source manifest/expected replay lockが現HEADに
一致せず、別セッションのLima/cargo/replay processも所有中のため Linux replay・stage regeneration・full buildは未実行で
ある。ADR: [`decisions-v0.3-release-smoke-provider-preflight-order.md`](../../adr/decisions-v0.3-release-smoke-provider-preflight-order.md)。

2026-08-02 に stage0 release archive の stagingから root-level raw `review-trust-store.snapshot` と
`review-lifecycle.snapshot` を除外した。stage0 inputへ private provider bytesを混入したときに archive listingへ漏れる
REDを確認し、identity projectionを保持したまま両 raw snapshotを archiveへ含めない GREENを
`test-native-stage0-release-package.sh` で確認した verified partial である。これは公開 packaged archiveへの raw provider
material exclusionに限られ、live provider API/auth acquisition・意味検証、current-source Linux runtime、Mac/Linux両 targetの
packaged provenance/rollback bytes parity の証拠ではない。current-source manifest/expected replay lockが現HEADに一致せず、
別セッションのLima/cargo/replay processも所有中のため Linux replay・stage regeneration・full buildは未実行である。ADR:
[`decisions-v0.3-native-stage0-release-provider-snapshot-exclusion.md`](../../adr/decisions-v0.3-native-stage0-release-provider-snapshot-exclusion.md)。

2026-08-02 に stage0 release package の input/output ownership boundary を追加した。output directory が stage0 input
directory 自身またはその配下にある場合、staging directoryを入力 package内へ作成せず、archive生成前に安定診断で拒否する
RED→GREENを `test-native-stage0-release-package.sh` で確認した。relative output directoryを stage0外へ解決して受理する
既存経路も同じ harnessで維持した。これは packaged stage0 の path ownership に限る verified partial sliceであり、live
provider API/auth acquisition・意味検証、current-source Linux runtime、Mac/Linux両 targetの packaged provenance/rollback
bytes parity の証拠ではない。current-source manifest/expected replay lockが現HEADに一致せず、別セッションのLima/cargo/replay
processも所有中のため Linux replay・stage regeneration・full buildは未実行である。ADR:
[`decisions-v0.3-native-stage0-release-output-boundary.md`](../../adr/decisions-v0.3-native-stage0-release-output-boundary.md)。

2026-08-02 に release smoke の cleanup work directory safetyを追加した。`WORK_DIR` が `/`、`/tmp`、repository
root、repositoryの `target` または `target/ci` に解決される場合、archive/provider workへ進まず
`unsafe release smoke work directory` で拒否する RED→GREENを `test-release-smoke-provider-snapshots.sh` で確認した。
task-owned leaf directoryを使う既存の provider snapshot / rollback smoke は同じ harnessで維持した。これは cleanup ownership
boundaryに限る verified partial sliceであり、live provider API/auth acquisition・意味検証、current-source Linux runtime、
Mac/Linux両 targetの packaged provenance/rollback bytes parity の証拠ではない。current-source manifest/expected replay lockが
現HEADに一致せず、別セッションのLima/cargo/replay processも所有中のため Linux replay・stage regeneration・full buildは
未実行である。ADR: [`decisions-v0.3-release-smoke-workdir-safety.md`](../../adr/decisions-v0.3-release-smoke-workdir-safety.md)。

2026-08-02 に `fetch-stage0.sh` の install destination preflightを追加した。`STAGE0_DIR` が `/`、`/tmp`、
`/private/tmp`、repository root、repositoryの `target` または `target/ci` に canonicalizeされる場合、release URL
validation・temporary workspace・curlより前に `ERROR: unsafe stage0 install directory` で拒否する RED→GREENを
`test-fetch-stage0-provider-url.sh` で確認した。既存の package checksum、manifest/source commit、install/restore
contractは再実装していない。これは fetch install destination ownershipの verified partial sliceであり、live provider
API/auth取得・意味検証、current-source Linux runtime、Mac/Linux両 targetの packaged provenance/rollback bytes parityは
未検証のため、M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は `[~]` のまま残す。current-source manifest/expected replay lockが
現HEADに一致せず、別セッション所有のLima/cargo/replay processも変更していないため Linux replay・stage regeneration・full buildは
未実行である。blockerの再現 commandは `current_head="$(git rev-parse --verify HEAD)"; find /tmp
/Users/biwakonbu/github/tmp -maxdepth 5 -type f -name manifest.json -path '*lsharp*'` と `find /tmp
/Users/biwakonbu/github/tmp -maxdepth 4 \( -name 'lsharp-native-linux-x86-hostgen-vm-*' -o -name '*.lock' \)`。Evidence:
[`decisions-v0.3-fetch-stage0-install-directory-safety.md`](../../adr/decisions-v0.3-fetch-stage0-install-directory-safety.md)。

2026-08-02 に `scripts/release.sh` の release version preflightを追加した。`VERSION` を archive directory、archive
filename、manifest metadataへ流す前に ASCII letters、digits、dot、underscore、hyphenだけへ限定し、`v1/unsafe` を
release output作成前に拒否する RED→GREENを `test-native-release-identity.py` で確認した。これは release version
namespaceの verified partial sliceであり、archive entry、manifest schema、checksum、rollback anchor、provider identity、
atomic install、stage0 package input/outputの契約は変更していない。live provider API/auth取得・意味検証、current-source
Linux runtime、Mac/Linux両 targetの packaged provenance/rollback bytes parityは未検証のため、M3-04-N1 / M3-05-N2 / M3-05-N7 /
M3-05-N9 は `[~]` のまま残す。current-source manifest/expected replay lockが現HEADに一致せず、別セッション所有の
Lima/cargo/replay processも変更していないため Linux replay・stage regeneration・full buildは未実行である。blockerの
再現 commandは `current_head="$(git rev-parse --verify HEAD)"; find /tmp /Users/biwakonbu/github/tmp -maxdepth 5 -type f
-name manifest.json -path '*lsharp*'` と `find /tmp /Users/biwakonbu/github/tmp -maxdepth 4 \( -name
'lsharp-native-linux-x86-hostgen-vm-*' -o -name '*.lock' \)`。Evidence:
[`decisions-v0.3-release-version-path-safety.md`](../../adr/decisions-v0.3-release-version-path-safety.md)。

2026-08-02 に native official release gate の source-smoke evidence output root preflightを追加した。`NATIVE_OFFICIAL_SOURCE_SMOKE_EVIDENCE_ROOT` を
canonicalizeし、repository root、`target`、`target/ci`、`ci-artifacts`、`dist`、`stage0`、system temporary root、または cleaned release smoke root配下を
package/output/runtime workより前に拒否する RED→GREENを `test-native-official-release-snapshots.sh` で確認した。既存の Mac/Linux source-smoke evidence、provider snapshot、
Lima lifecycle propagationは維持した。これは source-smoke evidence output ownershipの verified partial sliceであり、package input/output、cleanup、provider snapshot、archive、
manifest、identity、runtime semantics、atomic installの契約は変更していない。live provider API/auth取得・意味検証、current-source Linux runtime、Mac/Linux両 targetの packaged
provenance/rollback bytes parityは未検証のため、M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は `[~]` のまま残す。current-source manifest/expected replay lockが現HEADに一致せず、
別セッション所有のLima/cargo/replay processも変更していないため Linux replay・stage regeneration・full buildは未実行である。blockerの再現 commandは
`current_head="$(git rev-parse --verify HEAD)"; find /tmp /Users/biwakonbu/github/tmp -maxdepth 5 -type f -name manifest.json -path '*lsharp*'` と `find /tmp
/Users/biwakonbu/github/tmp -maxdepth 4 \( -name 'lsharp-native-linux-x86-hostgen-vm-*' -o -name '*.lock' \)`。Evidence:
[`decisions-v0.3-native-official-source-smoke-evidence-root-safety.md`](../../adr/decisions-v0.3-native-official-source-smoke-evidence-root-safety.md)。

2026-08-02 に明示 review-lifecycle snapshot の semantic state preflightを追加した。`verify-native-release-identity.py` は
UTF-8のJSON object、object array、またはJSONL recordを読み、`state` を `proposed` / `active` / `superseded` / `revoked` に限定し、
未知state・壊れたJSON・非object recordを digest一致後も fail-closed に拒否する RED→GREENを
`test-native-release-identity.py` で確認した。これは lifecycle state allowlist の verified partial sliceであり、既存の
provider snapshot regular-file/nonempty/digest/role binding、MCP semantic boundary、署名/authentication、sequence reducerを再実装していない。
live provider API/auth取得、current-source Linux runtime、Mac/Linux両 targetの packaged provenance/rollback bytes parityは未検証のため、
M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は `[~]` のまま残す。current-source manifest/expected replay lockが現HEADに一致せず、別セッション所有の
Lima/cargo/replay processも変更していないため Linux replay・stage regeneration・full buildは未実行である。blockerの再現 commandは
`current_head="$(git rev-parse --verify HEAD)"; find /tmp /Users/biwakonbu/github/tmp -maxdepth 5 -type f -name manifest.json -path '*lsharp*'` と
`find /tmp /Users/biwakonbu/github/tmp -maxdepth 4 \( -name 'lsharp-native-linux-x86-hostgen-vm-*' -o -name '*.lock' \)`。Evidence:
[`decisions-v0.3-provider-lifecycle-state-preflight.md`](../../adr/decisions-v0.3-provider-lifecycle-state-preflight.md)。

2026-08-02 に明示 review-lifecycle snapshot の duplicate sequence preflightを追加した。recordが integer `sequence` を持つ場合、
同じ `(review_id, sequence)` が複数回現れる snapshotを `duplicate review lifecycle sequence` で fail-closed に拒否する RED→GREENを
`test-native-release-identity.py` で確認した。これは前段の state allowlistに続く duplicate sequence の verified partial sliceであり、
sequence必須化、rollback ordering/reducer、既存 provider snapshot regular-file/nonempty/digest/role binding、MCP semantic、署名/authenticationを
再実装していない。live provider API/auth取得、current-source Linux runtime、Mac/Linux両 targetの packaged provenance/rollback bytes parityは未検証のため、
M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は `[~]` のまま残す。current-source manifest/expected replay lockが現HEADに一致せず、別セッション所有の
Lima/cargo/replay processも変更していないため Linux replay・stage regeneration・full buildは未実行である。blockerの再現 commandは
`current_head="$(git rev-parse --verify HEAD)"; find /tmp /Users/biwakonbu/github/tmp -maxdepth 5 -type f -name manifest.json -path '*lsharp*'` と
`find /tmp /Users/biwakonbu/github/tmp -maxdepth 4 \( -name 'lsharp-native-linux-x86-hostgen-vm-*' -o -name '*.lock' \)`。Evidence:
[`decisions-v0.3-provider-lifecycle-sequence-duplicate-preflight.md`](../../adr/decisions-v0.3-provider-lifecycle-sequence-duplicate-preflight.md)。

2026-08-02 に明示 review-lifecycle snapshot の sequence rollback preflightを追加した。同じ `review_id` の integer sequenceが入力順で減少する
`2 → 1` eventを `review lifecycle sequence rollback` で fail-closed に拒否する RED→GREENを `test-native-release-identity.py` で確認した。これは
既存の state allowlist / duplicate sequence に続く sequence rollback の verified partial sliceであり、sequence必須化、effective time ordering、state transition、
payload reducer、provider snapshot regular-file/nonempty/digest/role binding、MCP semantic、署名/authenticationを再実装していない。live provider API/auth取得、
current-source Linux runtime、Mac/Linux両 targetの packaged provenance/rollback bytes parityは未検証のため、M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は `[~]` のまま残す。
current-source manifest/expected replay lockが現HEADに一致せず、別セッション所有のLima/cargo/replay processも変更していないため Linux replay・stage regeneration・full buildは未実行である。
blockerの再現 commandは `current_head="$(git rev-parse --verify HEAD)"; find /tmp /Users/biwakonbu/github/tmp -maxdepth 5 -type f -name manifest.json -path '*lsharp*'` と
`find /tmp /Users/biwakonbu/github/tmp -maxdepth 4 \( -name 'lsharp-native-linux-x86-hostgen-vm-*' -o -name '*.lock' \)`。Evidence:
[`decisions-v0.3-provider-lifecycle-sequence-rollback-preflight.md`](../../adr/decisions-v0.3-provider-lifecycle-sequence-rollback-preflight.md)。

2026-08-02 に明示 review-lifecycle snapshotの `effective_at` preflightを追加した。存在する `effective_at` fieldが strict UTC/calendar timestampでない
`2024-02-30T00:00:00Z` eventを `review lifecycle effective_at must be a strict UTC timestamp` で fail-closedに拒否する RED→GREENを
`test-native-release-identity.py` で確認した。これは native provider inputの timestamp shape/calendar verified partial sliceであり、field必須化、sequence reducer・state transition・
effective-time ordering、provider snapshot regular-file/nonempty/digest/role binding、MCP semantic、署名/authenticationを再実装していない。live provider API/auth取得・意味検証、
current-source Linux runtime、Mac/Linux両 targetの packaged provenance/rollback bytes parityは未検証のため、M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は `[~]` のまま残す。
current-source manifest/expected replay lockが現HEADに一致せず、別セッション所有のLima/cargo/replay processも変更していないため Linux replay・stage regeneration・full buildは未実行である。blockerの再現 commandは
`current_head="$(git rev-parse --verify HEAD)"; find /tmp /Users/biwakonbu/github/tmp -maxdepth 5 -type f -name manifest.json -path '*lsharp*'` と
`find /tmp /Users/biwakonbu/github/tmp -maxdepth 4 \( -name 'lsharp-native-linux-x86-hostgen-vm-*' -o -name '*.lock' \)`。Evidence:
[`decisions-v0.3-provider-lifecycle-effective-at-preflight.md`](../../adr/decisions-v0.3-provider-lifecycle-effective-at-preflight.md)。

2026-08-02 に明示 review-lifecycle snapshotの terminal state preflightを追加した。同じ review の `revoked` / `superseded` 後に続く
`active` event（sequence `1 → 2`）を `review lifecycle terminal state reactivation` で fail-closedに拒否する RED→GREENを
`test-native-release-identity.py` で確認した。これは sequenced provider inputの terminal-state reactivation verified partial sliceであり、initial state、完全な transition matrix・
payload reducer・effective-time ordering、provider snapshot regular-file/nonempty/digest/role binding、MCP semantic、署名/authenticationを再実装していない。live provider API/auth取得・意味検証、
current-source Linux runtime、Mac/Linux両 targetの packaged provenance/rollback bytes parityは未検証のため、M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は `[~]` のまま残す。
current-source manifest/expected replay lockが現HEADに一致せず、別セッション所有のLima/cargo/replay processも変更していないため Linux replay・stage regeneration・full buildは未実行である。blockerの再現 commandは
`current_head="$(git rev-parse --verify HEAD)"; find /tmp /Users/biwakonbu/github/tmp -maxdepth 5 -type f -name manifest.json -path '*lsharp*'` と
`find /tmp /Users/biwakonbu/github/tmp -maxdepth 4 \( -name 'lsharp-native-linux-x86-hostgen-vm-*' -o -name '*.lock' \)`。Evidence:
[`decisions-v0.3-provider-lifecycle-terminal-state-preflight.md`](../../adr/decisions-v0.3-provider-lifecycle-terminal-state-preflight.md)。

2026-08-02 に明示 review-lifecycle snapshotの initial state preflightを追加した。同じ reviewの最初の sequenced eventが `revoked` / `superseded` の場合を
`review lifecycle initial state must be one of active, proposed` で fail-closedに拒否する RED→GREENを `test-native-release-identity.py` で確認した。これは sequenced provider inputの
initial-state rule verified partial sliceであり、既存のstate allowlist・terminal reactivation・sequence rollback/duplicate・effective_at以外の完全な transition matrix、payload reducer、
provider snapshot regular-file/nonempty/digest/role binding、MCP semantic、署名/authenticationを再実装していない。live provider API/auth取得・意味検証、current-source Linux runtime、
Mac/Linux両 targetの packaged provenance/rollback bytes parityは未検証のため、M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は `[~]` のまま残す。current-source manifest/expected replay lockが
現HEADに一致せず、別セッション所有のLima/cargo/replay processも変更していないため Linux replay・stage regeneration・full buildは未実行である。blockerの再現 commandは
`current_head="$(git rev-parse --verify HEAD)"; find /tmp /Users/biwakonbu/github/tmp -maxdepth 5 -type f -name manifest.json -path '*lsharp*'` と
`find /tmp /Users/biwakonbu/github/tmp -maxdepth 4 \( -name 'lsharp-native-linux-x86-hostgen-vm-*' -o -name '*.lock' \)`。Evidence:
[`decisions-v0.3-provider-lifecycle-initial-state-preflight.md`](../../adr/decisions-v0.3-provider-lifecycle-initial-state-preflight.md)。

2026-08-02 に明示 review-lifecycle snapshotの proposed-to-terminal transition preflightを追加した。`proposed sequence: 1` の後に `revoked` / `superseded sequence: 2` を置く eventを
`review lifecycle terminal transition requires active` で fail-closedに拒否する RED→GREENを `test-native-release-identity.py` で確認した。これは sequenced provider inputの proposed-to-terminal transition verified partial sliceであり、
既存の initial state・terminal reactivation・effective_at・sequence rollback/duplicate以外の完全な transition matrix、payload reducer、provider snapshot regular-file/nonempty/digest/role binding、
MCP semantic、署名/authenticationを再実装していない。live provider API/auth取得・意味検証、current-source Linux runtime、Mac/Linux両 targetの packaged provenance/rollback bytes parityは未検証のため、
M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は `[~]` のまま残す。current-source manifest/expected replay lockが現HEADに一致せず、別セッション所有のLima/cargo/replay processも変更していないため Linux replay・stage regeneration・full buildは未実行である。blockerの再現 commandは
`current_head="$(git rev-parse --verify HEAD)"; find /tmp /Users/biwakonbu/github/tmp -maxdepth 5 -type f -name manifest.json -path '*lsharp*'` と
`find /tmp /Users/biwakonbu/github/tmp -maxdepth 4 \( -name 'lsharp-native-linux-x86-hostgen-vm-*' -o -name '*.lock' \)`。Evidence:
[`decisions-v0.3-provider-lifecycle-terminal-transition-preflight.md`](../../adr/decisions-v0.3-provider-lifecycle-terminal-transition-preflight.md)。

2026-08-02 に明示 review-lifecycle snapshotの active-state regression preflightを追加した。`proposed → active → proposed`（sequence `1 → 2 → 3`）eventを
`review lifecycle active state regression` で fail-closedに拒否する RED→GREENを `test-native-release-identity.py` で確認した。これは sequenced provider inputの active-to-proposed regression verified partial sliceであり、
既存の initial state・proposed terminal・terminal reactivation・effective_at・sequence rollback/duplicate以外の完全な transition matrix、payload reducer、provider snapshot regular-file/nonempty/digest/role binding、
MCP semantic、署名/authenticationを再実装していない。live provider API/auth取得・意味検証、current-source Linux runtime、Mac/Linux両 targetの packaged provenance/rollback bytes parityは未検証のため、
M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は `[~]` のまま残す。current-source manifest/expected replay lockが現HEADに一致せず、別セッション所有のLima/cargo/replay processも変更していないため Linux replay・stage regeneration・full buildは未実行である。blockerの再現 commandは
`current_head="$(git rev-parse --verify HEAD)"; find /tmp /Users/biwakonbu/github/tmp -maxdepth 5 -type f -name manifest.json -path '*lsharp*'` と
`find /tmp /Users/biwakonbu/github/tmp -maxdepth 4 \( -name 'lsharp-native-linux-x86-hostgen-vm-*' -o -name '*.lock' \)`。Evidence:
[`decisions-v0.3-provider-lifecycle-active-state-regression-preflight.md`](../../adr/decisions-v0.3-provider-lifecycle-active-state-regression-preflight.md)。

2026-08-02 に明示 review-lifecycle snapshotの active self-transition preflightを追加した。`proposed → active → active`（sequence `1 → 2 → 3`）eventを
`review lifecycle active state self-transition` で fail-closedに拒否する RED→GREENを `test-native-release-identity.py` で確認した。これは sequenced provider inputの active self-transition verified partial sliceであり、
既存の active regression・proposed terminal・initial state・terminal reactivation・effective_at・sequence rollback/duplicate以外の完全な transition matrix、payload reducer、provider snapshot regular-file/nonempty/digest/role binding、
MCP semantic、署名/authenticationを再実装していない。live provider API/auth取得・意味検証、current-source Linux runtime、Mac/Linux両 targetの packaged provenance/rollback bytes parityは未検証のため、
M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は `[~]` のまま残す。current-source manifest/expected replay lockが現HEADに一致せず、別セッション所有のLima/cargo/replay processも変更していないため Linux replay・stage regeneration・full buildは未実行である。blockerの再現 commandは
`current_head="$(git rev-parse --verify HEAD)"; find /tmp /Users/biwakonbu/github/tmp -maxdepth 5 -type f -name manifest.json -path '*lsharp*'` と
`find /tmp /Users/biwakonbu/github/tmp -maxdepth 4 \( -name 'lsharp-native-linux-x86-hostgen-vm-*' -o -name '*.lock' \)`。Evidence:
[`decisions-v0.3-provider-lifecycle-active-state-self-transition-preflight.md`](../../adr/decisions-v0.3-provider-lifecycle-active-state-self-transition-preflight.md)。

2026-08-02 に明示 review-lifecycle snapshotの proposed self-transition preflightを追加した。`proposed → proposed`（sequence `1 → 2`）eventを
`review lifecycle proposed state self-transition` で fail-closedに拒否する RED→GREENを `test-native-release-identity.py` で確認した。これは sequenced provider inputの proposed self-transition verified partial sliceであり、
既存の active self-transition/regression・proposed terminal・initial state・terminal reactivation・effective_at・sequence rollback/duplicate以外の完全な transition matrix、payload reducer、provider snapshot regular-file/nonempty/digest/role binding、
MCP semantic、署名/authenticationを再実装していない。live provider API/auth取得・意味検証、current-source Linux runtime、Mac/Linux両 targetの packaged provenance/rollback bytes parityは未検証のため、
M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は `[~]` のまま残す。current-source manifest/expected replay lockが現HEADに一致せず、別セッション所有のLima/cargo/replay processも変更していないため Linux replay・stage regeneration・full buildは未実行である。blockerの再現 commandは
`current_head="$(git rev-parse --verify HEAD)"; find /tmp /Users/biwakonbu/github/tmp -maxdepth 5 -type f -name manifest.json -path '*lsharp*'` と
`find /tmp /Users/biwakonbu/github/tmp -maxdepth 4 \( -name 'lsharp-native-linux-x86-hostgen-vm-*' -o -name '*.lock' \)`。Evidence:
[`decisions-v0.3-provider-lifecycle-proposed-state-self-transition-preflight.md`](../../adr/decisions-v0.3-provider-lifecycle-proposed-state-self-transition-preflight.md)。

2026-08-02 に明示 review-lifecycle snapshot の sequence lower-bound preflightを追加した。`sequence: 0` の初期 `proposed` eventを
`review lifecycle sequence must be a positive integer` で拒否する RED→GREENを `test-native-release-identity.py` で確認した。これは native provider inputの
sequence positive-integer verified partial sliceであり、sequence field必須化、既存の sequence rollback/duplicate、完全な transition matrix、payload reducer、
effective-time ordering、provider snapshot regular-file/nonempty/digest/role binding、MCP semantic、署名/authenticationの完了証拠ではない。live provider API/auth取得・意味検証、
current-source Linux runtime、Mac/Linux両 targetの packaged provenance/rollback bytes parityは未検証のため、M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は `[~]` のまま残す。
current-source manifest/expected replay lockが現HEADに一致せず、別セッション所有のLima/cargo/replay processも変更していないため Linux replay・stage regeneration・full buildは未実行である。Evidence:
[`decisions-v0.3-provider-lifecycle-sequence-positive-preflight.md`](../../adr/decisions-v0.3-provider-lifecycle-sequence-positive-preflight.md)。

2026-08-02 に明示 review-lifecycle snapshot の sequence presence preflightを追加した。`sequence` fieldを欠落させた初期 `proposed` eventを
`review lifecycle sequence is required` で拒否する RED→GREENを `test-native-release-identity.py` で確認した。これは native provider inputの
sequence presence verified partial sliceであり、provider snapshotの取得・認証、完全な transition matrix/reducer、state semantics、MCP semantic、署名/authenticationの完了証拠ではない。
関連する identity preparation、official snapshot、replay-lock、release-smoke、stage0 package の fixtureにも `sequence: 1` を明示した。live provider API/auth取得・意味検証、
current-source Linux runtime、Mac/Linux両 targetの packaged provenance/rollback bytes parityは未検証のため、M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は `[~]` のまま残す。
current-source manifest/expected replay lockが現HEADに一致せず、別セッション所有のLima/QEMU/replay processも変更していないため Linux replay・stage regeneration・full buildは未実行である。Evidence:
[`decisions-v0.3-provider-lifecycle-sequence-required-preflight.md`](../../adr/decisions-v0.3-provider-lifecycle-sequence-required-preflight.md)。

2026-08-02 に明示 review-lifecycle snapshot の sequence continuity preflightを追加した。同じ `review_id` の `sequence: 1` の次に `sequence: 3` が現れる
eventを `review lifecycle sequence gap` で拒否する RED→GREENを `test-native-release-identity.py` で確認した。これは native provider inputの
sequence continuity verified partial sliceであり、既存の sequence required/positive/duplicate/rollback、完全な transition matrix/reducer、Rust/native reducer parity（後続 parity ADRで検証）、
provider snapshot semantics、MCP semantic、署名/authenticationの完了証拠ではない。live provider API/auth取得・意味検証、current-source Linux runtime、Mac/Linux両 targetの
packaged provenance/rollback bytes parityは未検証のため、M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は `[~]` のまま残す。current-source manifest/expected replay lockが現HEADに一致せず、
別セッション所有のLima/QEMU/replay processも変更していないため Linux replay・stage regeneration・full buildは未実行である。Evidence:
[`decisions-v0.3-provider-lifecycle-sequence-gap-preflight.md`](../../adr/decisions-v0.3-provider-lifecycle-sequence-gap-preflight.md)。

2026-08-02 に native provider lifecycle snapshot の `review_id` required preflightを追加した。`review_id` を欠落させた
`sequence: 1` / `proposed` recordを `review lifecycle review_id is required` で拒否する RED→GREENを `test-native-release-identity.py` で確認し、
Rust `ReviewLifecycleEvent::new` の空 `review_id` 拒否も focused testで対応付けた。これは provider inputの required-field verified partial sliceであり、
stable IDの selfhost/MCP全入力 route parity、完全な transition matrix/reducer、live provider API/auth、current-source Linux runtime、Mac/Linux両 targetの packaged
provenance/rollback bytes parityは未検証のため、M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は `[~]` のまま残す。current-source manifest/expected replay lockが
現HEADに一致せず、別セッション所有のLima/QEMU/replaydも稼働中のため Linux replay・stage regeneration・full buildは未実行である。Evidence:
[`decisions-v0.3-provider-lifecycle-review-id-required.md`](../../adr/decisions-v0.3-provider-lifecycle-review-id-required.md)。

2026-08-02 に native provider lifecycle snapshot の stable `review_id` wire-format preflightを追加した。非空だが key segmentが欠けた
`review:checkout` を Rust canonical と native providerの双方で拒否する RED→GREENを確認した。Rust lifecycle 6件、review wire 8件、native
identity 27件と関連 offline harness、Python/Bash syntaxを通過した。これは native provider routeとRust canonical `ReviewId::parse` の
形式 parity verified partial sliceであり、selfhost/MCPの全入力 route、完全な transition matrix/reducer、live provider API/auth、current-source
Linux runtime、Mac/Linux両 targetの packaged provenance/rollback bytes parityは未検証のため、M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は
`[~]` のまま残す。current-source manifest/expected replay lockが現HEADに一致せず、別セッション所有のLima/QEMU/replaydも稼働中のため Linux
replay・stage regeneration・full buildは未実行である。Evidence:
[`decisions-v0.3-provider-lifecycle-review-id-wire-format.md`](../../adr/decisions-v0.3-provider-lifecycle-review-id-wire-format.md)。

2026-08-02 に provider-required release identityの auth-context binding preflightを追加した。identityに trust-store / review-lifecycle の
digestが埋め込まれていても、`--require-provider-input` に実ファイル pathが無い呼び出しを
`provider auth context is required` で fail-closed に拒否する RED→GREENを `test-native-release-identity.py` で確認した。
これは provider-required identityを明示 snapshot inputへ結び付ける verified partial sliceであり、既存の snapshot regular-file / nonempty /
digest、lifecycle shape、MCP semantic、署名/authenticationを再実装していない。live provider API/auth取得・意味検証、current-source Linux runtime、
Mac/Linux両 targetの packaged provenance/rollback bytes parityは未検証のため、M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は `[~]` のまま残す。
current-source manifest/expected replay lockが現HEADに一致せず、別セッション所有のLima/QEMU/replaydも稼働中のため Linux replay・stage regeneration・full buildは
未実行である。blockerの再現 commandは `current_head="$(git rev-parse --verify HEAD)"; find /tmp /Users/biwakonbu/github/tmp -maxdepth 5 -type f -name manifest.json -path '*lsharp*'`
と `find /tmp /Users/biwakonbu/github/tmp -maxdepth 4 \( -name 'lsharp-native-linux-x86-hostgen-vm-*' -o -name '*.lock' \)`。Evidence:
[`decisions-v0.3-native-provider-auth-context-binding.md`](../../adr/decisions-v0.3-native-provider-auth-context-binding.md)。

2026-08-02 に native provider lifecycle snapshot の `effective_at` ordering parityを追加した。native preflightは strict UTC/calendar shapeを検証していたが、
同じ reviewの `proposed sequence: 1` at `2026-08-02T00:00:00Z` に続く `active sequence: 2` at `2026-08-01T23:59:59Z` を受理していた。
Rust canonical reducerの `EffectiveTimeRollback` と同じ `review lifecycle effective_at rollback` fail-closed boundaryを nativeへ追加する RED→GREENを、
`test-native-release-identity.py` と Rust `review_lifecycle` focused testで対応付けた。これは effective-at ordering parityの verified partial sliceであり、
既存の strict shape、sequence、state transition、snapshot digest、provider API/auth取得・意味検証、current-source Linux runtime、Mac/Linux両 targetの packaged
provenance/rollback bytes parityは未検証のため、M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は `[~]` のまま残す。current-source manifest/expected replay lockが
現HEADに一致せず、別セッション所有のLima/QEMU/replaydも稼働中のため Linux replay・stage regeneration・full buildは未実行である。blockerの再現 commandは
`current_head="$(git rev-parse --verify HEAD)"; find /tmp /Users/biwakonbu/github/tmp -maxdepth 5 -type f -name manifest.json -path '*lsharp*'` と
`find /tmp /Users/biwakonbu/github/tmp -maxdepth 4 \( -name 'lsharp-native-linux-x86-hostgen-vm-*' -o -name '*.lock' \)`。Evidence:
[`decisions-v0.3-provider-lifecycle-effective-at-ordering-parity.md`](../../adr/decisions-v0.3-provider-lifecycle-effective-at-ordering-parity.md)。

2026-08-02 に native selfhost MCP `initialize` の server identity を Rust canonical route と揃えた。
native shim が返していた `serverInfo.name: "lsharp-native"` を `"lsharp"` へ変更し、Cargo package version
`0.1.0` とともに固定する RED→GREEN を `python3 scripts/ci/test-native-selfhost-mcp.py -k
initialize_tools_and_supported_calls_stay_native_only` で確認した。これは MCP JSON-RPC envelope metadata の
verified partial parity slice であり、既存の ReviewId/sequence/effective_at/provider identity 契約を再実装しない。
Rust MCP 全tool parity、package-install semantics、live provider API/auth acquisition・意味検証、current-source
Linux runtime、Mac/Linux 両 target の packaged provenance/rollback bytes parity は未検証のため、関連項目は `[~]`
のまま残す。Evidence: [`decisions-v0.3-native-mcp-initialize-server-identity-parity.md`](../../adr/decisions-v0.3-native-mcp-initialize-server-identity-parity.md)。

同日、native selfhost MCP `tools/list` の descriptor order を Rust canonical `list_tools()` と揃えた。native は
同じ13 routeを LSP-first で返していたが、Rust の check→validate→LSP→offline/package→compile/run→errors/search
順へ explicit canonical tupleで並べ、全配列順を focused test の RED→GREEN で固定した。これは MCP route envelope の
deterministic order verified partial parity slice であり、`initialize` identity、ReviewId/sequence/effective_at、provider/auth、
artifact provenance の契約を再実装しない。Rust MCP 全 semantic parity、package-install semantics、live provider API/auth
acquisition・意味検証、current-source Linux runtime、Mac/Linux 両 target の packaged provenance/rollback bytes parity は
未検証のため、関連項目は `[~]` のまま残す。Evidence: [`decisions-v0.3-native-mcp-tools-list-order-parity.md`](../../adr/decisions-v0.3-native-mcp-tools-list-order-parity.md)。

同日、native selfhost MCP の JSON-RPC request id envelope を Rust canonical transport と揃えた。id field が欠落した
notificationだけを無応答とし、明示 `id: null` は `jsonrpc: "2.0"` response の `id: null` として保持する RED→GREENを
focused route testで確認した。これは MCP envelope の null/missing distinction verified partial parity sliceであり、
initialize identity、tools/list order、ReviewId/sequence/effective_at、provider/auth、artifact provenance の契約を再実装しない。
MCP 全 error envelope/semantic parity、package-install semantics、live provider API/auth acquisition・意味検証、current-source
Linux runtime、Mac/Linux 両 target の packaged provenance/rollback bytes parity は未検証のため、関連項目は `[~]` のまま残す。
Evidence: [`decisions-v0.3-native-mcp-null-request-id-envelope.md`](../../adr/decisions-v0.3-native-mcp-null-request-id-envelope.md)。

同日、native selfhost MCP `tools/call` の missing-name error envelope を Rust canonical transport と揃えた。`params.name` が欠落または
非文字列の場合も、Rust と同じ result-level `isError: true`・`content[0].text: "tool not found"` を返す RED→GREENを native 82 tests と
Rust MCP 89 testsで確認した。明示的な unknown tool name の既存契約は再実装せず、missing-name boundaryだけを固定した verified partial
sliceである。これは initialize identity、tools/list order、id:null、ReviewId/sequence/effective_at、provider/auth、artifact provenance
の契約を再実装しない。MCP 全 error/semantic parity、package-install semantics、live provider API/auth acquisition・意味検証、current-source
Linux runtime、Mac/Linux 両 target の packaged provenance/rollback bytes parity は未検証のため、関連項目は `[~]` のまま残す。Evidence:
[`decisions-v0.3-native-mcp-tools-call-missing-name-envelope.md`](../../adr/decisions-v0.3-native-mcp-tools-call-missing-name-envelope.md)。

同日、native selfhost MCP `tools/call` の non-object `params` envelope を Rust canonical transport と揃えた。`params: []` などの非 object
入力を Rust と同じ empty params として扱い、result-level `isError: true`・`content[0].text: "tool not found"` を返す RED→GREENを native
83 tests と Rust MCP 90 testsで確認した。これは non-object params boundaryの verified partial sliceであり、initialize identity、tools/list
order、id:null、missing-name、ReviewId/sequence/effective_at、provider/auth、artifact provenance の契約を再実装しない。MCP 全
error/semantic parity、package-install semantics、live provider API/auth acquisition・意味検証、current-source Linux runtime、Mac/Linux 両
target の packaged provenance/rollback bytes parity は未検証のため、関連項目は `[~]` のまま残す。Evidence:
[`decisions-v0.3-native-mcp-tools-call-non-object-params-envelope.md`](../../adr/decisions-v0.3-native-mcp-tools-call-non-object-params-envelope.md)。

2026-08-02 に明示 provider lifecycle snapshot の freshness preflightを追加した。lifecycle recordの
`effective_at` が release identity の caller-provided `now` より未来の場合、raw snapshot digestが一致していても
`review lifecycle effective_at is after identity now` で fail-closed に拒否する RED→GREENを
`test-native-release-identity.py` で確認した。これは同一 review内の effective_at ordering、strict timestamp shape、
attestation expiry、snapshot digestとは別の caller-clock freshness boundaryであり、provider API/auth取得・署名意味検証の
証拠ではない。current-source Linux runtime、Mac/Linux両 targetの packaged provenance/rollback bytes parityも未検証のため、
M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は `[~]` のまま残す。current-source manifest/expected replay lockが現HEADに
一致せず、別セッション所有のLima/QEMU/replaydも稼働中のため Linux replay・stage regeneration・full buildは未実行である。
Evidence: [`decisions-v0.3-provider-lifecycle-future-effective-at.md`](../../adr/decisions-v0.3-provider-lifecycle-future-effective-at.md)。

同日、packaged release-smoke の release archive と rollback compatibility archive の入力を symlink でない
regular file に限定した。anchor checksum と同じ bytes を指す rollback archive symlink を現行 harness の RED
で受け入れることを確認し、`regular file without symlink` で fail-closed に拒否する GREEN を
`test-release-smoke-provider-snapshots.sh` で固定した。これは archive input path の provenance-safe boundary
だけを検証する partial slice であり、既存の manifest/anchor/checksum/rollback payload 検証を再実装しない。
live provider API/auth acquisition・意味検証、current-source Linux runtime、Mac/Linux 両 target の packaged
provenance/rollback bytes parity は未検証のため、M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は `[~]` のまま残す。
current-source manifest/expected replay lockが現HEADに一致せず、別セッション所有のLima/QEMU/replaydも稼働中のため
Linux replay・stage regeneration・full buildは未実行である。Evidence:
[`decisions-v0.3-packaged-archive-input-regular-file.md`](../../adr/decisions-v0.3-packaged-archive-input-regular-file.md)。

同日、rollback compatibility archive の再帰 smoke に manifest の `version` を nested `VERSION` として渡し、
rollback executable の `--version` output と manifest version の parity を固定した。manifest/anchor/checksum を
再生成しても `lsharp 9.9.9` を返す rollback fixture を従来実装が受け入れる RED と、
`packaged CLI version mismatch` で fail-closed にする GREEN を `test-release-smoke-provider-snapshots.sh` で確認した。
これは rollback executable version の offline parityだけを検証する partial sliceであり、既存の
manifest/anchor/checksum/payload/archive input 契約を再実装しない。live provider API/auth acquisition・意味検証、
current-source Linux runtime、Mac/Linux 両 target の packaged provenance/rollback bytes parity は未検証のため、
M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は `[~]` のまま残す。current-source manifest/expected replay lockが
現HEADに一致せず、別セッション所有のLima/QEMU/replaydも稼働中のため Linux replay・stage regeneration・full buildは
未実行である。Evidence:
[`decisions-v0.3-packaged-rollback-version-output-parity.md`](../../adr/decisions-v0.3-packaged-rollback-version-output-parity.md)。

同日、非native packaged archiveの `lsharp-lsp --version` 出力を caller-provided `VERSION` と比較する境界を追加した。
`lsharp-lsp` だけが `9.9.9` を返す checksum-valid fixtureを従来 smokeが受け入れる RED と、
`packaged LSP version mismatch` で fail-closed にする GREENを `test-release-smoke-provider-snapshots.sh` で確認した。
これは packaged LSP version output parityだけを検証する partial sliceであり、既存の native CLI version、rollback
executable version、manifest/anchor/checksum/archive input 契約を再実装しない。live provider API/auth acquisition・意味検証、
current-source Linux runtime、Mac/Linux両 targetの packaged provenance/rollback bytes parityは未検証のため、
M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は `[~]` のまま残す。current-source manifest/expected replay lockが現HEADに
一致せず、別セッション所有のLima/QEMU/replaydも稼働中のため Linux replay・stage regeneration・full buildは未実行である。
Evidence: [`decisions-v0.3-packaged-lsp-version-output-parity.md`](../../adr/decisions-v0.3-packaged-lsp-version-output-parity.md)。

同日、offline release identity verifierに caller-provided `--verification-now` UTC clockを追加し、identityの
`now` が検証時計より未来の場合を `identity now is after verification now` で fail-closed にした。identity
`now=2026-08-15T00:00:00Z` と検証時計 `2026-08-14T23:59:59Z` の RED→GREENを
`test-native-release-identity.py` で確認した。これは lifecycle `effective_at` ordering、snapshot digest/regular-file、
strict timestamp shapeとは別の provider identity caller-clock freshness boundaryであり、live provider API/auth取得や
署名意味検証の証拠ではない。current-source Linux runtime、Mac/Linux両 targetの packaged provenance/rollback bytes parityも
未検証のため、M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は `[~]` のまま残す。current-source manifest/expected replay lockが
現HEADに一致せず、別セッション所有のLima/QEMU/replaydも稼働中のため Linux replay・stage regeneration・full buildは未実行である。
Evidence: [`decisions-v0.3-provider-identity-verification-clock.md`](../../adr/decisions-v0.3-provider-identity-verification-clock.md)。

同日、native-only packaged App.Cli の `--help` を stdout/stderr 別々に収集し、成功・`Usage: lsharp` の stdout・空の stderrを
要求する packaged output boundaryを追加した。checksum-valid な fake packageが help warningを stderrへ漏らす REDを、
`native-only App.Cli help must keep stderr empty` で fail-closedにする GREENと、valid native/rollback fixtureの継続成功を
`test-release-smoke-provider-snapshots.sh` で確認した。これは native/LSP version、archive/rollback manifest・checksum、provider
snapshotとは別の packaged help output verified partial sliceであり、live provider API/auth取得・意味検証、current-source Linux
runtime、Mac/Linux両 targetの packaged provenance/rollback bytes parityは未検証である。M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は
`[~]` のまま残す。current-source manifest/expected replay lockが現HEADに一致せず、別セッション所有のLima/QEMU/replaydも稼働中の
ため Linux replay・stage regeneration・full buildは未実行である。Evidence:
[`decisions-v0.3-packaged-native-help-output.md`](../../adr/decisions-v0.3-packaged-native-help-output.md)。

同日、official release gate の provider input preflightに明示 `NATIVE_OFFICIAL_REVIEW_VERIFICATION_NOW` を接続し、4つの
target/stage0 identityへ caller clockを渡すようにした。App.Cli artifact identityには sibling `program.native` の bytesも渡し、
future identity `now` または artifact digest mismatchを release/package/fetch/smoke/Lima開始前に fail-closed とする RED→GREENを
`test-native-official-release-snapshots.sh` で確認した。これは provider snapshot input→identity freshness→source/artifact bindingを
一つの official gate boundaryへ集約する verified partial sliceであり、live provider API/auth取得・署名意味検証、current-source Linux
runtime、Mac/Linux両 targetの packaged provenance/rollback bytes parityは未検証である。M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は
`[~]` のまま残す。current-source manifest/expected replay lockが現HEADに一致せず、別セッション所有のLima/QEMU/replaydも稼働中のため
Linux replay・stage regeneration・full buildは未実行である。Evidence:
[`decisions-v0.3-native-official-provider-freshness-binding.md`](../../adr/decisions-v0.3-native-official-provider-freshness-binding.md)。

同日、offline release identity verifierの明示 `--artifact` 入力を regular fileかつ非symlinkに限定した。
期待 digestと同じ bytesを持つ外部 artifact symlinkを従来 verifierが受け入れる RED と、
`artifact must be a regular non-symlink file` で fail-closed にする GREENを
`test-native-release-identity.py` で確認した。これは artifact path provenance の入力境界だけを検証する
partial sliceであり、既存の artifact digest/source binding、provider snapshot、archive/rollback、MCP/lifecycle
契約を再実装しない。provider API/auth取得・意味検証、current-source Linux runtime、Mac/Linux両 targetの
packaged provenance/rollback bytes parityは未検証のため、M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は
`[~]` のまま残す。current-source manifest/expected replay lockが現HEADに一致せず、別セッション所有の
Lima/QEMU/replaydも稼働中のため Linux replay・stage regeneration・full buildは未実行である。Evidence:
[`decisions-v0.3-release-identity-artifact-regular-file.md`](../../adr/decisions-v0.3-release-identity-artifact-regular-file.md)。

2026-08-02 に残件を細かな packaged edge ではなく、M3-04-N1 / M3-05-N9 の current-source two-target runtime gate
（compile/build → source-bound artifact/manifest → packaged/stage0 → Mac/Linux runtime）として再監査した。監査時の
`HEAD` `f36b51539c1d903d89005f02d4bd9a9fe11770f0` に source commit が一致する manifest と task-owned expected replay lockは
見つからず、Lima hostagent/QEMU/replayd は別セッション所有だったため、`native-official-release-local.sh`、stage regeneration、
Linux replay、full buildは起動していない。これは gate 未実行の blocker evidenceであり、live provider API/auth取得・意味検証、
current-source Linux runtime、Mac/Linux両 targetの packaged provenance/rollback bytes parityは未検証のまま、M3-04-N1 / M3-05-N2 /
M3-05-N7 / M3-05-N9 を `[~]` に維持する。source-bound manifest、expected replay lock、VM/process ownership が揃った後に、
Mac/Linux stdout/stderr、exit status、artifact digest、runtime bytesを一つの batchで記録する。再現 command と再開条件は
[`decisions-v0.3-current-source-two-target-runtime-gate-blocker.md`](../../adr/decisions-v0.3-current-source-two-target-runtime-gate-blocker.md) に固定した。

2026-08-02 に native release identity verifier の lifecycle snapshotを、Rust `ReviewLifecycleRegistry::from_events` と
selfhost reducerと同じ `(review_id, sequence)` canonical orderで reduceする parityを追加した。`revoked sequence: 2` →
`active sequence: 1` の逆順でも、canonical orderでは有効な `active` → `revoked` になる fixtureを RED→GREENで固定し、
既存の rollback/gap/duplicate/transition/effective_at 診断は維持した。native identity 33件、Rust wire declaration-order 1件、
selfhost lifecycle reducer 1件がGREENである。これは lifecycle producer declaration-order parityの verified partial sliceであり、
live provider/auth取得・署名検証、current-source Linux runtime、Mac/Linux両 targetの packaged provenance/rollback bytes parityは
未検証のため、EC-M3-01〜05 と M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は `[~]` のまま残す。Evidence:
[`decisions-v0.3-native-lifecycle-declaration-order-parity.md`](../../adr/decisions-v0.3-native-lifecycle-declaration-order-parity.md)。

2026-08-02 に trust-store の key rotation boundaryを追加した。Rust wireは `active`（省略時は `true`）を
保持し、同一 provider/algorithm で retired key と active key を共存させる一方、active key が複数になる入力を
`MultipleActiveKeys` で拒否する。signature verification は retired key を `unverified` とし、nativeの
`verify-native-review-trust-store.py` も同じ provider/algorithm の active key ambiguityを fail-closed にする。
Rust trust-store/signature/schema focused suites、native rotation/ambiguity 2件がGREENである。これは trust-store
selection と Rust/native preflight の verified partial sliceであり、暗号署名の native semantic verification、
live provider/auth取得、current-source Linux runtime、Mac/Linux両 targetの packaged provenance/rollback bytes parityは
未検証のため、EC-M3-01〜05 と M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は `[~]` のまま残す。Evidence:
[`decisions-v0.3-review-trust-store-active-key-rotation.md`](../../adr/decisions-v0.3-review-trust-store-active-key-rotation.md)。

2026-08-02 に Rust の実署名検証結果を native/provider 境界へ渡す verification receipt を追加した。
`ReviewVerificationReceipt::from_verified_signature` は明示 trust store で `verified` にならない attestationから
receiptを生成せず、canonical attestation digest、trust-store digest、provider/key/algorithm、明示
`verification_now`を固定する。native `verify-native-review-verification-receipt.py` は同じ closed field
shape、`verified` state、digest、実在 UTC 日付、length-prefixed canonical bytesを検証し、receiptを暗黙の
暗号検証として扱わない。Rust receipt 4件、native receipt 3件がGREENである。これは署名検証結果の
Rust/native handoff verified partial sliceであり、native暗号署名検証、live provider/auth取得、current-source Linux
runtime、Mac/Linux両 targetの packaged provenance/rollback bytes parityは未検証のため、EC-M3-01〜05 と
M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は `[~]` のまま残す。Evidence:
[`decisions-v0.3-review-verification-receipt.md`](../../adr/decisions-v0.3-review-verification-receipt.md)。

2026-08-02 に Rust verified receipt を native MCP の既存 `lsharp_validate` reportへ接続した。
明示 `review_verification_receipt` regular fileを native commandへ渡し、返却
`review_verifications[].receipt` が receiptの `review_id`、`verified` state、closed field payloadと
exact一致しない場合は MCP error とする RED→GREENを native MCP focused suiteで確認した。不正 receiptは
native実行前に拒否し、receiptなしの provider snapshotは従来どおり raw/unverified boundaryを維持する。
これは standalone validatorの追加ではなく、既存 report surfaceへの verified handoff projectionである。
native暗号署名検証、live provider/auth取得、current-source Linux runtime、Mac/Linux両 targetの packaged
provenance/rollback bytes parityは未検証のため、EC-M3-01〜05 と M3-04-N1 / M3-05-N2 / M3-05-N7 /
M3-05-N9 は `[~]` のまま残す。current-source manifest/expected replay lockは現HEADに一致せず、別セッション
所有のLima/QEMU/replaydも稼働中のため Linux replay、stage regeneration、full buildは未実行である。Evidence:
[`decisions-v0.3-native-mcp-review-receipt-projection.md`](../../adr/decisions-v0.3-native-mcp-review-receipt-projection.md)。

2026-08-02 に native MCP `lsharp_validate --emit-manifest` の既存 `reviews[]` projectionへ
Rust verified receiptを接続した。receipt `review_id` の namespace/keyを manifest reviewへ束ね、
`verification_state: verified` と closed `verification_receipt` payloadを reportと同値に保つ。
receiptなしの manifestに暗黙の verified stateを生成せず、明示 receipt使用時の欠落・曖昧・改変は
manifest postflightで拒否する RED→GREENを native MCP focused suiteとRust receipt/selfhost fixtureで確認した。
これは既存 manifest review surfaceへの handoff projectionであり、MCP root/clock/schema、receipt closed shape、
provider identity、active-key、lifecycle、archive/version/symlink契約を再実装しない。native暗号署名検証、live
provider/auth取得、current-source Linux runtime、Mac/Linux両 target packaged provenance/rollback bytes parityは
未検証のため EC-M3-01〜05 と M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は `[~]` のまま残す。Evidence:
[`decisions-v0.3-native-mcp-review-receipt-manifest.md`](../../adr/decisions-v0.3-native-mcp-review-receipt-manifest.md)。

2026-08-02 に native source-file smoke の既存 evidence writerへ、selfhost producerが出力した
`validation-attestation-json.stdout` を明示入力として接続した。`review_attestations` 配列を
証跡 `manifest.json` へ値として保存し、入力 reportが欠落・空・JSON objectでない・配列を持たない場合は
evidence directoryを作らず fail-closed にする RED→GREENを offline harness で確認した。Rust parser と
selfhost producer の同一 fixtureによる canonical bytes/state/span parityは既存 focused evidenceを再利用し、
writerはそれらの意味論を再実装せず producer outputの handoff ownershipだけを閉じる。MCP receipt
projection、MCP root/clock/schema、provider identity、active-key、lifecycle、archive/version/symlink契約とは
重複しない。native source smoke実行は既存 current-source manifest/expected lock と VM ownership blockerの
ため未実行であり、Linux runtime、live provider/auth、Mac/Linux両 target packaged provenance/rollback bytes
parityは未検証のため `[~]` のまま残す。Evidence:
[`decisions-v0.3-native-source-attestation-evidence-handoff.md`](../../adr/decisions-v0.3-native-source-attestation-evidence-handoff.md)。

2026-08-02 に native MCP `lsharp_validate --include-manifest` の report/manifest projection parity を
追加した。Rust canonical route は一つの intent graph から report と manifest を投影するため、native report に
embedded `manifest` がある場合、別途 `--emit-manifest` された JSON と構造的に一致しない valid object を
`native validate report manifest projection mismatch` で downstream projection 前に拒否する。embedded manifest
を返さない既存 native producerは互換維持し、receipt/provider identity/schemaの個別契約は再実装しない。
fake native harness の mismatch RED→GREEN と Rust canonical focused test を確認したが、current-source manifest/
expected replay lock が現HEADに一致せず、Lima/QEMU/replayd は別セッション所有のため Linux replay、stage regeneration、
full buildは未実行である。native MCP report/manifest semantic parity のこの slice は verified partial であり、
live provider/auth、current-source Linux runtime、Mac/Linux packaged/rollback bytes parity は未検証のまま、EC-M3-01〜05 と
M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 を `[~]` に維持する。Evidence:
[`decisions-v0.3-native-mcp-report-manifest-projection-parity.md`](../../adr/decisions-v0.3-native-mcp-report-manifest-projection-parity.md)。

2026-08-02 に native MCP `lsharp_validate` report の source-owned `review_attestations[]` projection parity を
追加した。Rust canonical source routeが返す review ID、subject/source/provenance identity、provider/key/algorithm、signature、
issued/expiry、positive sequence、state、canonical bytes、spanの14フィールドを native output schema と runtime postflightへ
接続した。valid reportを受理し、missing/extra/state/byte/spanの異常を tracebackなしで拒否する fake native RED→GREEN、Rust
source-attestation report test、source-file evidence、official two-target fake harnessを確認した。これは source/native MCP report wire
parityの verified partial sliceであり、暗号署名・live provider/auth、current-source Linux runtime、Mac/Linux packaged/rollback bytes
parityは未検証のまま、EC-M3-01〜05 と M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 を `[~]` に維持する。current-source
manifest/expected replay lockが現HEADに一致せず、Lima/QEMU/replaydは別セッション所有のため Linux replay、stage regeneration、full buildは
未実行である。Evidence: [`decisions-v0.3-native-mcp-review-attestation-report.md`](../../adr/decisions-v0.3-native-mcp-review-attestation-report.md)。

2026-08-02 に explicit verification receipt と native MCP source-owned `review_attestations[]` の binding parity を追加した。
receipt がある report で同じ `review_id` の attestation projection が欠落・曖昧、`verified` でない、provider/key/algorithm が不一致、または
canonical bytes の SHA-256 が receipt の `attestation_digest` と一致しない場合を、MCP downstream projection 前に fail-closed にする
RED→GREENを固定した。native shimは暗号署名を再検証せず、Rust verified receiptの identity/material handoffだけを検査する。
native MCP 95 tests、Rust receipt 4 tests、Rust source-attestation focused test、source-file evidence、official two-target fake gateを確認した。
current-source manifest/expected replay lockは現HEADに一致せず、別セッション所有のLima/QEMU/replaydも稼働中のため Linux replay・stage regeneration・full buildは未実行である。
live provider/auth取得・署名意味検証、current-source Linux runtime、Mac/Linux両 targetの packaged provenance/rollback bytes parityは未検証のため、
EC-M3-01〜05 と M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は `[~]` を維持する。Evidence:
[`decisions-v0.3-native-mcp-review-attestation-receipt-binding.md`](../../adr/decisions-v0.3-native-mcp-review-attestation-receipt-binding.md)。

2026-08-02 に provider snapshot semantic fail-closed boundaryを native MCP source-attestation projectionまで拡張した。
trust-store/review-lifecycle snapshotを指定した場合、native verifierが意味検証していないため、`review_attestations[]` の
`verified` / `stale` / `revoked` stateも receiptで明示的に束ねられた対象以外は `provider semantic verification is unavailable` で拒否する
RED→GREENを固定した。既存 snapshot/receipt/Rust semantic verifierを再実装せず、native postflightの暗黙 trustだけを閉じた。
native MCP 96 tests、native receipt 3 tests、Rust receipt 4 tests、Rust source-attestation focused test、source-file evidence、official two-target fake gateを確認した。
current-source manifest/expected replay lockは現HEADに一致せず、別セッション所有のLima/QEMU/replaydも稼働中のため Linux replay・stage regeneration・full buildは未実行である。
native cryptographic/live provider/auth semantic verification、current-source Linux runtime、Mac/Linux両 targetの packaged provenance/rollback bytes parityは未検証のため、
EC-M3-01〜05 と M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は `[~]` を維持する。Evidence:
[`decisions-v0.3-native-mcp-provider-attestation-semantic-boundary.md`](../../adr/decisions-v0.3-native-mcp-provider-attestation-semantic-boundary.md)。

2026-08-02 に explicit verification receipt と provider snapshot context の coherency boundaryを追加した。receiptと
trust-store/review-lifecycle snapshotを同時に渡す場合、receiptの `trust_store_digest` が今回の raw trust-store bytesから計算した digest、
または明示された provider digestと一致しなければ native起動前に fail-closed にする RED→GREENを固定した。receipt schemaに lifecycle digestはないため、
このsliceはtrust-store identityの照合だけを行い、lifecycle意味検証・live provider/auth取得・native暗号検証は追加しない。
native MCP 97 tests、native receipt 3 tests、Rust receipt 4 tests、Rust source-attestation focused test、source-file evidence、official two-target fake gateを確認した。
current-source manifest/expected replay lockは現HEADに一致せず、別セッション所有のLima/QEMU/replaydも稼働中のため Linux replay・stage regeneration・full buildは未実行である。
native cryptographic/live provider/auth semantic verification、current-source Linux runtime、Mac/Linux両 targetの packaged provenance/rollback bytes parityは未検証のため、
EC-M3-01〜05 と M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は `[~]` を維持する。Evidence:
[`decisions-v0.3-native-mcp-review-receipt-provider-context.md`](../../adr/decisions-v0.3-native-mcp-review-receipt-provider-context.md)。

2026-08-02 に receipt schemaへ lifecycle digestを追加せず、receiptと lifecycle snapshot/digest の組み合わせを native MCPが暗黙の
lifecycle semantic verification として扱わない fail-closed boundaryを追加した。receiptが指定され、`review_lifecycle` pathまたは
`review_lifecycle_digest`が同時に渡された場合は、canonical receiptがsignature/trust-store factだけを持ち lifecycle identityを持たないため、
native起動前に `native MCP receipt cannot establish lifecycle semantic binding without lifecycle-bound receipt` で拒否する。explicit trust-store digestだけの
receipt pathは従来どおり受理する。native MCP 98 tests、native receipt 3 tests、Rust receipt 4 tests、Rust source-attestation focused test、source-file evidence、
official two-target fake gateを確認した。これは receipt/provider trust-store coherency、receipt→attestation projection、provider semantic state拒否とは別の
lifecycle binding contractである。current-source manifest/expected replay lockは現HEADに一致せず、別セッション所有のLima/QEMU/replaydも稼働中のため
Linux replay・stage regeneration・full buildは未実行である。native cryptographic verification、live provider/auth acquisition、current-source Linux runtime、
Mac/Linux両 target packaged provenance/rollback bytes parityは未検証のため、M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は `[~]` のまま残す。ADR:
[`decisions-v0.3-native-mcp-review-receipt-lifecycle-boundary.md`](../../adr/decisions-v0.3-native-mcp-review-receipt-lifecycle-boundary.md)。

2026-08-02 に explicit `review_now` context と `ReviewVerificationReceipt.verification_now` の coherency boundaryを追加した。receiptが指定され、
caller identityが明示される場合は、Rust外部 verifierが作った verified factの検証時刻と現在の review contextが完全一致しなければ native起動前に
`native MCP receipt verification clock mismatch with review context` で拒否する。一致するreceipt＋identityは従来どおり受理する。
receipt schemaへ lifecycle digestを追加せず、lifecycle semantic verification、trust-store digest coherency、receipt projectionとは別の external verification
context contractである。native MCP 99 tests、native receipt 3 tests、Rust receipt 4 tests、Rust source-attestation focused test、release identity 33 tests、
source-file evidence、official two-target fake gateを確認した。current-source manifest/expected replay lockは現HEADに一致せず、別セッション所有のLima/QEMU/replaydも
稼働中のため Linux replay・stage regeneration・full buildは未実行である。native cryptographic verification、live provider/auth acquisition、current-source Linux runtime、
Mac/Linux両 target packaged provenance/rollback bytes parityは未検証のため、M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は `[~]` のまま残す。ADR:
[`decisions-v0.3-native-mcp-review-receipt-verification-clock.md`](../../adr/decisions-v0.3-native-mcp-review-receipt-verification-clock.md)。

2026-08-02 に native MCP `lsharp_validate` の live provider/auth acquisition external boundaryを明示した。`provider_url`、`provider_api_url`、
provider/auth token、`auth_context` のような network/auth入力は native実行前に
`live provider/auth acquisition is an external boundary; use explicit offline snapshots` で拒否し、provider snapshotの regular-file/nonempty/digest入力だけを受理する。
これは receipt verification clock、trust-store coherency、provider semantic verificationとは別の acquisition boundaryであり、network helperや暗号検証をnativeへ追加しない。
native MCP 100 tests、native receipt 3 tests、Rust receipt 4 tests、Rust source-attestation focused test、release identity 33 tests、source-file evidence、official two-target fake gateを確認した。
current-source manifest/expected replay lockは現HEADに一致せず、別セッション所有のLima/QEMU/replaydも稼働中のため Linux replay・stage regeneration・full buildは未実行である。
native cryptographic verification、live provider/auth実取得、current-source Linux runtime、Mac/Linux両 target packaged provenance/rollback bytes parityは未検証のため、
M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は `[~]` のまま残す。ADR:
[`decisions-v0.3-native-mcp-live-provider-auth-boundary.md`](../../adr/decisions-v0.3-native-mcp-live-provider-auth-boundary.md)。

2026-08-02 に packaged stage0 release archive の offline round-trip provenance binding を追加した。生成前 packageの
target/source commit/identity/checksum検証に加えて、公開直前に archiveを展開し、regular/symlink-free package、
manifest binding、payload checksum、生成前 treeとの完全一致を再検証する。fake `tar` による archive manifest
source commit改変の RED→GREEN と invalid output cleanupを `bash scripts/ci/test-native-stage0-release-package.sh` で確認した。
これは packaged provenanceの verified partial sliceであり、live provider/auth、native cryptographic verification、
current-source Linux runtime、Mac/Linux両 targetの packaged provenance/rollback bytes parityを完了扱いにしない。
current-source manifest/expected replay lockは現HEADに一致せず、別セッション所有のLima/QEMU/replaydも稼働中のため
Linux replay・stage regeneration・full buildは未実行である。関連項目は `[~]` のまま残す。ADR:
[`decisions-v0.3-stage0-archive-round-trip-provenance.md`](../../adr/decisions-v0.3-stage0-archive-round-trip-provenance.md)。

2026-08-02 に rollback compatibility archive の offline payload checksum closure を追加した。required checksum entryの
存在だけでなく、展開後の全 regular file（`checksums.txt`を除く）と安全・一意な checksum target の集合が一致することを
manifest preflight後に検証する。`unlisted-payload` の RED→GREEN、既存 missing-required diagnostic維持、release-smoke/fetch/archive/
atomic/package/official fake focused batchを確認した。これは rollback payload provenanceの verified partial sliceであり、
current-source Linux runtime、live provider/auth、native cryptographic verification、Mac/Linux両 targetの packaged/rollback runtime parityを
完了扱いにしない。current-source manifest/expected replay lockは現HEADに一致せず、別セッション所有のLima/QEMU/replaydも稼働中のため
Linux replay・stage regeneration・full buildは未実行である。関連項目は `[~]` のまま残す。ADR:
[`decisions-v0.3-rollback-archive-payload-closure.md`](../../adr/decisions-v0.3-rollback-archive-payload-closure.md)。

## 進行順と所有範囲

2026-08-02 に source-file smoke evidence の fetched stage0 payload binding を追加した。既存の archive/checksum
検証後に、evidence writerへ `--stage0-dir` を明示入力し、regular/symlink-free directory 内の全 regular fileを
POSIX-relative path、byte size、file SHA-256の sorted recordsとして deterministic に束ね、
`stage0_payload_sha256` を `stage0_manifest_sha256` と併記する。source-file smoke callerも同じ fetched directoryを
渡し、payload digestをテスト側で独立再計算した RED→GREEN、root/nested symlink、manifest path mismatch、missing report、
overwrite拒否を offline harnessで確認した。これは fetched package bytes と source-file evidence の bindingに限定した
verified partial sliceであり、fetch/archive checksum、provider/auth取得・意味検証、native crypto、current-source Linux runtime、
Mac/Linux両 target packaged provenance/rollback bytes parityは未検証のため、M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は
`[~]` のまま残す。current-source manifest/expected replay lockは現HEADに一致せず、別セッション所有のLima/QEMU/replaydが
稼働中のため Linux replay・stage regeneration・full buildは未実行である。Evidence:
[`decisions-v0.3-source-smoke-fetched-payload-binding.md`](../../adr/decisions-v0.3-source-smoke-fetched-payload-binding.md)。

同日、official two-target orchestratorの source-smoke postflightへ fetched stage0 payload digest cross-checkを追加した。
各 targetの `${SMOKE_ROOT}/stage0-${target}` を同じ sorted path/size/file SHA-256 recordsで再計算し、evidence manifestの
`stage0_payload_sha256` と exact一致しない場合、target smoke evidenceを受理しない。fake two-target harnessで matching Mac/Linux
projectionの GREEN と、Linuxだけを改変した digestの RED→fail-closedを確認した。これは既存 source-smoke digestの orchestrator
bindingに限定した verified partial sliceであり、fetch/archive checksum、live provider/auth取得・意味検証、native crypto、
current-source Linux runtime、Mac/Linux両 target packaged provenance/rollback bytes parityは未検証のため、M3-04-N1 / M3-05-N2 /
M3-05-N7 / M3-05-N9 は `[~]` のまま残す。current-source manifest/expected replay lockは現HEADに一致せず、別セッション所有の
Lima/QEMU/replaydが稼働中のため Linux replay・stage regeneration・full buildは未実行である。Evidence:
[`decisions-v0.3-native-official-source-smoke-payload-binding.md`](../../adr/decisions-v0.3-native-official-source-smoke-payload-binding.md)。

続けて、official two-target source-smoke postflightへ fetched stage0 manifest/source identity bindingを追加した。
各 targetの fetched `manifest.json` を再読込し、target、current `SOURCE_COMMIT`、manifest bytesの SHA-256、既存の
`stage0_payload_sha256` を evidence manifestと exact比較する。fetched manifest自体の target/source_commit も current target/sourceへ
束ね、fake two-target harnessで Mac/Linux matching projection、Linux payload digest mismatch、Linux manifest digest mismatchの
RED→GREEN fail-closedを確認した。これは source-smoke payload bindingを上流の manifest/source identityへ拡張した verified partial
sliceであり、fetch/archive checksum、live provider/auth取得・意味検証、native crypto、current-source Linux runtime、Mac/Linux両 target
packaged provenance/rollback bytes parityは未検証のため、M3-04-N1 / M3-05-N2 / M3-05-N7 / M3-05-N9 は `[~]` のまま残す。
current-source manifest/expected replay lockは現HEADに一致せず、別セッション所有のLima/QEMU/replaydが稼働中のため Linux replay・stage
regeneration・full buildは未実行である。Evidence:
[`decisions-v0.3-native-official-stage0-identity-binding.md`](../../adr/decisions-v0.3-native-official-stage0-identity-binding.md)。

1. M3-04-N1 の RED と同一 fixture を先に作る。
2. Mac native stage0 の focused gate を通し、重い Linux VM gate は既存 lock/artifact を再利用して一つだけ実行する。
3. M3-05-N1 の text/JSON/MCP parity を同じ fixtureへ追加する。
4. M3-05-N2 の packaged/release gate と provenance evidence を追加する。
5. M3-05-N7 の stage0 package / release-smoke offline propagation を維持する。
6. M3-05-N8 の multi-target orchestrator propagation を維持し、current-source runtime は N9 で閉じる。
7. M3-05-N9 の Mac Apple Silicon / Linux x86_64 runtime evidenceを一つの target jobずつ閉じる。
8. 各 verified slice を ADR へ記録し、aggregate completion boundary を満たすまで `TODO.md` は `[~]` に留める。

parser/type-inference の bounded batch を進める active worktreeとはファイル ownershipを共有しない。
heavy native replay 中は、非共有の fixture、diagnostic、docs、contract testだけを進め、同じ VM job を重複起動しない。

2026-08-02 の verified partial: offline path-package installの destination boundaryを Rust/nativeで揃えた。
同一 fixtureで、既存の非 symlink package destinationを削除せず fail-closedにし、既存 symlinkの更新は temporary
symlinkから atomic renameで確定する。Rust install focused 5 testsとnative installer 8 testsを通過した。
MCP package projectionには新規 install routeを追加していない。Git/registry provider acquisition、native MCP全 package-install
semantics、current-source Linux runtime、Mac/Linux packaged/rollback parityは未検証であり、M3-05-N9は `[~]` のまま残す。
current-source manifest/expected replay lockが現HEADに一致せず、別セッション所有のLima/QEMU/replaydも稼働中のため、
Linux replay・stage regeneration・full buildは未実行である。次の再現 command:
`cargo test -p lsharp-driver test_cmd_install_path_dependency -- --nocapture` と
`python3 scripts/ci/test-native-selfhost-install.py`。

2026-08-02 の verified partial: Git dependency installを Rust/native 同一 local fixtureで揃えた。既存 destinationは regular
file、manifestなし directory、valid/dangling symlinkを拒否し、fresh cloneは `.tmp-*` で行い、manifest確認後だけ promoteする。
clone/manifest/promotion failureでは lock/module-indexを更新せず、temporary cloneを残さない。Rust Git focused 4 testsと native
Git focused 3 testsを通過した。これは package-install mutation/failure boundaryだけであり、registry/cache、MCP package-install API、
複数依存 transactionality、provider/auth、current-source Linux runtime、Mac/Linux packaged/rollback parityは未検証である。
M3-05-N9は `[~]` のまま維持する。current-source manifest/expected replay lockは現HEADに一致せず、別セッションのLima/QEMU/replaydが
稼働中のため Linux replay・stage regeneration・full buildは未実行である。再現 command:
`cargo test -p lsharp-driver test_cmd_install_git_dependency -- --nocapture` と
`python3 -m unittest scripts/ci/test-native-selfhost-install.py -k 'git_'`。

2026-08-02 の verified partial: cached-version dependencyの Rust/native semver lexical parityを揃えた。
同じ offline cache fixtureの `math-core = "+1.0.0"` を Rust `cmd_install` と native selfhost installerへ渡し、
nativeが拒否していた signed componentを Rustも ASCII digit-only preflightで fail-closed にした。これは既存の
cached-version compatibility selectionを再実装せず、registry/networkのない入力字句境界だけを閉じた sliceである。
複数依存の atomic promotion/rollback transactionality、registry/provider/auth取得、native MCP package-install semantics、
current-source Linux runtime、Mac/Linux packaged/rollback parityは未検証である。current-source manifest/expected replay lockが現HEADに一致せず、
別セッション所有のLima/QEMU/replaydが稼働中のため Linux replay・stage regeneration・full buildは未実行である。
Evidence: [`decisions-v0.3-native-package-install-cached-semver-parity.md`](../../adr/decisions-v0.3-native-package-install-cached-semver-parity.md)。
次の RED は同一 Rust/native offline fixtureで複数依存を一つの task-owned staging/promotion boundaryへ束ね、後続依存の失敗時に
先行 final package、lock.toml、module-indexを残さない transactionalityを固定する。

2026-08-02 の verified partial: mixed path + cached-version dependencyの Rust/native dependency-resolution transaction boundaryを揃えた。
Rustは依存名をnativeと同じ順序で処理し、pathとfresh Gitの成果物を `.install-txn-*` へ置き、cached-versionは既存 cacheのみを解決する。
すべての dependency resolution後に final promotion、lock.toml、module-indexを実行し、後続 cached missで先行 path packageや
lock/index、staging residueを残さない RED→GREENを同一 fixtureで確認した。これは resolution failure boundaryに限定され、final rename・lock/index
I/O failure時の完全 rollback、registry/provider/auth、native MCP package-install semantics、current-source Linux runtime、Mac/Linux packaged/rollback parityは未検証である。
Evidence: [`decisions-v0.3-native-package-install-mixed-transaction.md`](../../adr/decisions-v0.3-native-package-install-mixed-transaction.md)。
次の RED は final promotionまたは lock/index I/O failure時の rollback/atomic commit boundaryとする。

2026-08-02 の verified partial: final promotion loopの Rust/native rollback boundaryを揃えた。同じ local fixtureの既存 path package symlinkと
fresh Git packageを stagingし、promotion index `1` の test-only failpointで先行 promotionを rollback、旧 destinationと sentinel lock/indexを
保持し、fresh Git destinationと `.install-txn-*` residueを残さない RED→GREENを確認した。Rustは `cfg(test)` failpoint、nativeは
`LSHARP_TEST_INSTALL_FAILPOINT=promotion:1` を使い、通常のCLI/APIや外部 networkを変更しない。これは final rename loopの rollbackに限定され、
lock.toml/module-index I/O failure時の完全 rollback、registry/provider/auth、native MCP package-install semantics、current-source Linux runtime、
Mac/Linux packaged/rollback parityは未検証である。Evidence: [`decisions-v0.3-native-package-install-promotion-rollback.md`](../../adr/decisions-v0.3-native-package-install-promotion-rollback.md)。
次の RED は lock/index I/O failureでも package destinationと旧 metadataを復元する commit boundaryとする。

2026-08-02 の verified partial: lock.toml/module-index metadata commitの Rust/native rollback boundaryを揃えた。同じ local path + fresh Git
fixtureで final promotion後に `lock` と `index` の test-only failpointを注入し、既存 path destination symlink、sentinel lock/indexを復元し、
fresh Git destination、partial metadata、`.install-txn-*` residueを残さない RED→GREENを確認した。Rustは `cfg(test)` failpoint、nativeは
`LSHARP_TEST_INSTALL_FAILPOINT=lock|index` を使い、通常の CLI/API や外部 networkを変更しない。これは final promotion + metadata rollbackに限定され、
完全な installer transactionality、filesystem durability、registry/provider/auth、native MCP package-install semantics、current-source Linux runtime、
Mac/Linux packaged/rollback parityは未検証である。Evidence: [`decisions-v0.3-native-package-install-promotion-rollback.md`](../../adr/decisions-v0.3-native-package-install-promotion-rollback.md)。
次の RED は metadata rollback後の filesystem durability/fsync境界、または外部 registry/provider取得を含む完全 transactionalityとする。

2026-08-02 の verified partial: package installer の filesystem durability orderingを Rust/nativeで揃えた。Rustの durable-file helperを lock.tomlへ接続し、
module-indexは temporary file/directory sync→rename→parent directory sync、package promotionは staged path sync→rename→final path/parent syncを行う。
同じ local path + fresh Git fixtureへ `promotion-before-sync`、`promotion-after-sync`、`lock-sync`、`index-sync` の test-only failpointを注入し、
既存 package symlink、sentinel lock/indexを復元し、fresh Git destinationと `.install-txn-*` residueを残さない RED→GREENを Rust/nativeで確認した。
これは offline ordering/fail-closed boundaryに限定され、crash consistency、filesystem journaling、power-loss durability、cross-device rename、完全な
installer transactionality、registry/provider/auth、current-source Linux runtime、Mac/Linux packaged/rollback parityは未検証である。Evidence:
[`decisions-v0.3-native-package-install-promotion-rollback.md`](../../adr/decisions-v0.3-native-package-install-promotion-rollback.md)。
次の RED は実 filesystem crash/power-loss durability または外部 registry/provider取得を含む完全 transactionalityとする。

## 完了条件

- Rust oracle と selfhost/native の同一 fixtureで bytes、state、span、exit code、report/manifest boundary が一致する。
- Mac Apple Silicon と Linux x86_64 の current-source native stage0、および packaged artifact/runtime の evidence がある。
- 成功経路に Rust fallback、host compiler、network、implicit trust root がないことを negative gate で確認する。
- source、identity、release の各判断を ADR に残し、未完了の target/ABI/provider boundary を `[~]` と残リスクへ記録する。

2026-08-02 の verified partial: cached registry candidateを Rust/nativeで同じ provenance boundaryへ揃えた。matching候補の rootは regular directory、
tree全体は symlink-free、`lsharp.toml` は regular/parseableで dependency nameと valid semverを持つことを要求し、invalid candidateは
success扱いせず fail-closed にする。同version候補は cache directory nameの辞書順最大を tie-breakerにして deterministic に選ぶ。同一 offline
fixtureで valid tie、root/nested symlink、malformed manifestを確認し、Rust/nativeの選択結果と invalid時の lock/index保持、
`.install-txn-*` residue不在を GREENにした。registry/network/provider取得、crash/power-loss semantics、完全 transactionality、current-source Linux runtime、
Mac/Linux packaged/rollback parityは未検証である。Evidence: [`decisions-v0.3-native-package-install-promotion-rollback.md`](../../adr/decisions-v0.3-native-package-install-promotion-rollback.md)。
次の RED は外部 registry/provider取得を含む完全 transactionalityまたは実 filesystem durabilityとする。

2026-08-02 の verified partial: 宣言済み local `path` dependency の input validationを Rust/nativeで揃えた。missing path、通常 file、
`lsharp.toml` 欠落を managed `.lsharp` directory作成前に同じ診断 familyで拒否し、lock/indexや transaction stagingを成功状態として残さない。
同一 fixtureの Rust installer 22 tests と native installer 18 tests、Rust format、Python syntaxを通過した。これは cached registry candidate
provenance、destination/promotion/metadata/durability rollbackとは別の local provider input boundaryであり、registry/network clientは追加していない。
live registry/provider取得、crash/power-loss semantics、完全 transactionality、native MCP package-install semantics、current-source Linux runtime、
Mac/Linux packaged/rollback parityは未検証のため、EC-M3-05 / M3-05-N9 は `[~]` のまま残す。current-source manifest/expected replay lockが現HEADに一致せず、
別セッション所有のLima/QEMU/replaydが稼働中のため、Linux replay・stage regeneration・full buildは未実行である。Evidence:
[`decisions-v0.3-native-package-install-path-input-fail-closed.md`](../../adr/decisions-v0.3-native-package-install-path-input-fail-closed.md)。

2026-08-02 の verified partial: version dependency の registry external boundaryを Rust/nativeで揃えた。version specは明示された offline
`.lsharp/packages` cacheからのみ解決し、cache missを live registry取得へ暗黙に委譲せず、
`registry provider acquisition is an external boundary` として managed `.lsharp`、lock/index、transaction stagingの作成前に fail-closed にする。
同一 no-cache fixture と既存 installer batchで Rust 23 tests、native 19 testsがGREENである。これは local path-provider input validation、cached
candidate provenance、promotion/metadata/sync rollbackとは別の acquisition boundaryであり、registry/network/auth clientは追加していない。
live registry/provider retrieval/auth、完全 transactionality、crash/power-loss semantics、native MCP package-install semantics、current-source Linux runtime、
Mac/Linux packaged/rollback parityは未検証のため、EC-M3-05 / M3-05-N9 は `[~]` のまま残す。current-source manifest/expected replay lockが現HEADに一致せず、
別セッション所有のLima/QEMU/replaydが稼働中のため、Linux replay・stage regeneration・full buildは未実行である。Evidence:
[`decisions-v0.3-native-package-install-registry-external-boundary.md`](../../adr/decisions-v0.3-native-package-install-registry-external-boundary.md)。
