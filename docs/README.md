# docs

`docs/` 配下の文書は、役割ごとに次の 3 系統へ整理している。

- `language/` -- 実装済み、または v1 契約として扱う言語 / runtime / backend の仕様
- `development/` -- Phase 11 の計画、検証、運用、移行、詳細設計
- `guides/` -- L# 利用者向けのチュートリアル / マニュアル置き場

なお、`book/` は独立した導線として維持し、この再編には含めていない。
また、`adr/` と `schemas/` は Markdown 再配置の対象外としてそのまま残している。
