# examples

この directory は L# の小さなサンプルソースを置く場所です。正本は tracked な `.ls` ファイルです。

各サンプルが示す機能、実行状態、関連ドキュメントは [`docs/guides/examples.md`](../docs/guides/examples.md) を参照してください。

`*.wasm` は生成物であり、`.gitignore` 対象です。必要になったら `lsharp compile` で再生成します。
